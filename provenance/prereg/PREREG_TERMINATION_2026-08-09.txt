PRE-REGISTRATION -- THE TERMINATION CONDITION FOR IN-HOUSE GRUT PHYSICS.
BLIND-SAFE: yes

STATUS: *** UNSIGNED = NOT IN FORCE. *** This file becomes the program's stop condition only when
the human owner signs the block at the bottom. A stop condition the author did not commit to is
not one. Sealed (hashed in prereg/MANIFEST.txt) so the outcomes cannot be renegotiated after the
results arrive; per the blind-safe rule, this file contains no result-adjacent numerics -- every
threshold it references is one ALREADY RECORDED in the register or its frozen instruments, cited
by name.

WHY OUTCOMES-FIRST. This program's own history: every wave that pre-registered its outcomes was
honest under pressure, and every number that traveled without a pre-registration eventually needed
a retraction. The stop condition is the largest single claim the program will ever make about
itself; it gets the same discipline.

================================ THE CHANNELS AND THEIR OUTCOMES ================================

C1. THE SPECIALIST DISPATCH (the pole-vs-cut question; DISPATCH_ONE_PAGE.md).
    The clock starts at the SEND DATE (sending is the owner's act), recorded by appending a dated
    line to this file's companion result file -- never to this file.
    (a) POLE-CLASS ANSWER: the core structural assumption is earned at one loop. The program
        continues PAST the termination date under a NEW signed condition -- continuation is a new
        decision, not momentum.
    (b) CUT-CLASS ANSWER: rung3_single_pole is refuted as stated. The armed tier trigger recorded
        in the mu_linear ruling EXECUTES (no re-litigation); the program moves to deposit within
        one wave. The deposit says plainly that the framework's one decisive external question was
        answered against it.
    (c) NO RESPONSE within SIX MONTHS of the send date, after at least one reminder: the question
        is recorded as posed-and-unclaimed. The program deposits WITHOUT the answer, stating
        exactly that: the one decisive question was asked, nobody took it, and the register is
        left in a state where whoever eventually computes it can adjudicate the deposit in an
        afternoon. Non-response is NOT evidence for either horn and must not be spun as either.

C2. DESI DR3 / THE w(z) CHANNEL.
    The register's kill-channel commitments (rung7's no-phantom-crossing; the two-scale IR mode)
    stand as banked; the decision thresholds are the ones already frozen in the register's own
    overturning clauses -- this file adds NO new numbers.
    (a) A crossing established at the register's own kill grade: the w(z) leg dies, executed per
        its overturning clause. This kills a differentiator, not the framing; the deposit records
        the leg as data-closed.
    (b) Evolving-w strengthens short of kill grade: the leg's window narrows; NO new dial may be
        added to rescue it (the booked dials are the booked dials).
    (c) Consistency with a constant: the leg survives unused; the deposit carries it as an
        untriggered instrument.

C3. THE TT-AUTO RIGOROUS CHANNEL (Part-7 calc, time-boxed; A8 discipline applies -- every number
    it produces is family-conditional unless the activation-scale frontier is resolved from
    outside).
    (a) The window closes to unobservable at every declared family member: the empirical surface
        is gone; the deposit is ontology + derived boundaries + method (the D3 terminal already
        defined in X_FLOOR_MAP), and says so without euphemism.
    (b) A percent-level window survives: it ships in the deposit as an ALLOWANCE, never a
        prediction -- the register's own ruling (x has no floor) forbids the stronger word.

C4. THE METHOD (claimledger + the register discipline).
    (a) The "different team" half of method_novelty's falsifier discharges (an independent team,
        a real error caught, confirmed by someone who is not us): method_novelty graduates per its
        own node text, and the method is a stand-alone deliverable INDEPENDENT of every physics
        outcome above.
    (b) No external uptake by the termination date: the method ships as-is, bounded exactly as the
        node states -- a competent synthesis, in-house evidence only. The claim does not inflate
        at deposit time.

================================ THE DATE ================================
IN-HOUSE PHYSICS CALCULATION STOPS on 2026-12-31, or upon completion of the two Part-7 time-boxed
calcs, WHICHEVER IS EARLIER -- except that a C1(a) pole-class answer arriving before that date
re-opens continuation under a new signed condition. After the stop: banking waves may only CONSUME
external results (specialist answers, DESI releases, external claimledger reports) against the
frozen instruments; no new in-house physics calculations may be started. The deposit is written in
the first wave after the date, whatever state the channels are in, and reports each channel in
whichever of its pre-registered outcomes obtains -- including "still open", stated as such.

WHAT THIS FILE MAY NEVER BE USED FOR: cherry-picking. The channels above are ALL of the program's
live empirical fronts as of sealing. Reporting the deposit on a subset because the subset looks
better is the laundering shape, applied to an ending.

================================ SIGNATURE ================================
Owner signature (types name + date to sign; the seal covers the file WITH this block EMPTY, and
the companion result file records the signing -- the sealed file itself is never edited):

    SIGNED: ______________________  DATE: ____________
