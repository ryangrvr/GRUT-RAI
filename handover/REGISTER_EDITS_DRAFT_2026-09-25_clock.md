# REGISTER EDITS DRAFT — 2026-09-25 (clock-mismatch ruling) — NOT APPLIED

**Status: DRAFTED FOR THE BANK GATE. `claims.json` untouched by this file.**
Source: `CLOCK_MISMATCH_VERDICT_01.md` (verdict (C), 23/23) and
`CLOCK_MISMATCH_OWNER_RULING_01.md`. Pattern follows
`handover/REGISTER_EDITS_DRAFT_2026-08-20.md` (drafted, never silently applied).

## EDIT 1 — `rung3_single_pole`, boundary_condition append

> CLOCK/TRANSPORT (2026-09-25, verdict C, sha e3c757dd): the "Ht ≈ 1 vs
> Ht ≳ 4.3" comparison filed against this node is RE-TYPED. The two filed
> clocks coincide on the shared comoving worldline (T = t, exact); the
> comparison instead mixes a constant-H dS kernel with an FRW quantity —
> different solutions (X2). It is a CONDITIONAL calculation ("if the
> constant-H₀ dS kernel is naively applied to FRW, a shortfall follows"), not
> a contradiction, and may not be re-filed until a dS→FRW kernel transport
> rule is banked and priced. The within-toy result (6.12% / 4.32 / 6.68)
> stands.

## EDIT 2 — `rung7_wz`, boundary_condition append

> CLOCK/TRANSPORT (2026-09-25, verdict C): τ₂ ~ 1/H₀ is an FRW cosmic-time
> quantity on the matter+Λ solution; comparisons against constant-H dS kernel
> structures require the unbanked transport rule (see
> background_time_translation_flow). Computed rider: the lag 1/H₀ exceeds the
> age of the universe (H₀t₀ = 0.951, Ω_m = 0.315), so "probing the kernel at
> lag τ₂" is not realizable on the actual background history.

## EDIT 3 — `background_time_translation_flow`, statement/sub_status note

> UPSTREAM ROLE (2026-09-25): the clock-mismatch instrument identified this
> node's omission as the precise missing object of the rung3/rung7
> comparison: the transport prescription K_dS(t−t′;H) → K_FRW(t,t′;H(t),…).
> The transport instrument (KERNEL_TRANSPORT_CHARTER_01.md) is chartered on
> exactly this question; its verdict should be attached here when recorded.

*All three edits are additive annotations (no tier moves, no ledger deltas).
They await the both-directions firewall + owner relay before acceptance.*

## EDIT 4 — `background_time_translation_flow`, outcome annotation (added post-transport-verdict)

> TRANSPORT INSTRUMENT OUTCOME (2026-09-25, O-NONUNIQUE with O-MISSING
> co-occurring, 11/11, sha 95f0f2a7): at free level the dS->FRW transport
> exists as RECOMPUTATION from admitted inputs (C1 tensor primitive + declared
> background); no LOCAL substitution rule is derivable (best member 34% worst-
> case; eps = -Hdot/H^2 >= 0.4725 at every epoch -- no small parameter); the
> local family is derived-with-domain (<= 0.3% at lag 0.241/H0, O(1) wrong by
> lag 0.8/H0) and its nonuniqueness is measured (spread 0.003 -> 1.64 across
> the grid). KMS/ladder side requires three relocation-class inputs (FRW state
> choice; temperature/KMS structure absent a timelike Killing field; a proved
> stationary reduction). The rung3/rung7 conditional comparison's premises are
> disjoint: dominance needs lag >= 4.33/H_*, the licensed domain ends ~0.25/H0.

*Additive annotation; no tier move, no ledger delta. Awaits firewall + relay.*
