# RIDERS — 2026-09-25 (directive Step 4b/4c; own-file riders, sealed ledger untouched)

## RIDER 1 — non-stationarity rider on the T3 confrontation mapping

The comparison of J(ω) exponent classes inside the certificate window
ω ≳ 3.4H, as specified in the sealed ledger
(`T2_THEOREM_GATE_AND_PREREGISTRATION_01.md`, seal `d2da3a5`,
2026-09-24 18:37:06 −05:00, immutable), **must state the stationarity
regime it assumes and whether that window sits in the adiabatic regime
relative to the measured lags** of `NONSTATIONARITY_RESULT_01.md`
(pooled best-stationary residual R = 0.516 on the frozen FRW grid,
CORRECTION 01 wording governing). This is a **required-statement rider,
not an invalidation claim** — R = 0.516 does not by itself impugn the
window comparison.

Timeline-citation asymmetry (stated per the standing rule whenever the
dual pre-registration is cited): the adjudicator side is commit-sealed
(d2da3a5, 18:37:06 −05:00); the builder side's first rebuilt run is
mtime/timestamp-attested (18:49 local, recorded in
U3_COUPLING_ADJUDICATION_02), not commit-sealed.

## RIDER 2 — realization-dimension citation (informational)

The committed primary for realization-dimension/mode-count claims is the
repaired P4 (`calc/u3_origin_persistence.py`, `origin/adjudicator-track`).
Symbol collision flagged: D-1's N = the noise kernel of the (K, N) pair;
P4's N = the realization dimension. Nothing frozen is modified.
