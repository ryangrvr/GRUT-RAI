# CLASS-C dispatch decisions — manifest v1.0 → v1.1

> **STATUS: EXECUTION RECORD OF THE OWNER'S GREEN LIGHT (2026-08-22).**
> Resolving an `UNDECIDED-DISPATCH` field is a scientific event, not a coding
> convenience (owner rule). Each resolution below carries justification, scope,
> and consequences, recorded BEFORE any result exists to interpret. Fields that
> remain genuinely open stay `UNDECIDED-DISPATCH` — they are owner/dispatch
> decisions, not code convenience. The frozen certificate
> (`CLASS_C_DISPATCH_FROZEN.md`) hashes the v1 manifest and remains untouched;
> this file + the v1.1 manifest constitute the versioned supersession.

## Adjudication context

Owner ruling same day: **Class A closed/exhausted** for parameter-free rung-3
discharge; **Class C open, decisive**; green light to execute the frozen
dispatch. First result to be treated as a DISCOVERY RESULT.

## Decision D1 — clock (the formulation, not a number)

**v1.0:** `UNDECIDED-DISPATCH`.
**v1.1:** `DECLARED — the primary class-C object is the two-time response
G_R(x,x′); no global ρ(ω) is presumed; ANY one-clock spectral reduction
(static-Killing or worldline) requires its own proof for the assembled object;
candidate clocks are catalogued (static-Killing T: only global candidate,
keystone-map D3b; comoving proper τ: D3a-scoped, worldline kernels only).`

*Justification:* banked keystone-map results D3a/D3b — stationarity exists
along geodesics for invariant fields (numerically verified) and globally only
in static-Killing time; the graviton channel was exhibited NON-stationary
(`calc/tt_worldline_spectrum.py`, >130% shape drift). Declaring a single clock
now would be manufacturing a premise.
*Scope/consequence:* outcome classes 4 (secular/nonstationary) and 6
(ill-posed even after assembly) become structurally likely paths; the
pole/cut question may be unreachable without the epoch-window input priced at
W* < 0.25 e-folds.

## Decision D2 — boundary conditions

**v1.0:** `UNDECIDED-DISPATCH`.
**v1.1:** `DECLARED — Bunch-Davies analyticity (Euclidean regularity at the
horizon; the iε prescription of the booked state).`

*Justification:* identical to the declared state (`rung2`-booked BD); a
different choice would be a NEW physical input requiring its own pricing — none
is proposed.
*Scope:* consistent with every calculation already executed in-tree.

## Decision D3 — approximation order

**v1.0:** `UNDECIDED-DISPATCH`.
**v1.1:** `DECLARED — leading nonzero order in the graviton coupling for the
assembled TT dissipation (the O(G²) candidate fixed by rung3's own coherence
argument), with exact free kinematics.`

*Justification:* banked rung3 coherence result — the pole-vs-cut distinction
lives AT O(G²), below the current O(G) resolution; no other order contains the
question.
*Consequence:* if the assembled O(G²) object carries no low-frequency
singularity, that IS the answer for this order; higher orders are new physics
inputs requiring their own pricing.

## Remaining UNDECIDED-DISPATCH (deliberately)

| field | why still open |
|---|---|
| `gauge` | the gauge-invariant assembly is wall (A): no graviton-probe construction exists; choosing a gauge before the assembly scheme exists would be selecting the answer's frame. **Resolution procedure now frozen: `CLASS_C_WALL_CONTRACTS.md` §D4 (five questions + dual-gauge acceptance test).** |
| `renormalization` | TTW Table 8 is unrenormalized; the prescription is coupled to the RG half of wall (B), still re-deferred in the literature. **Resolution procedure now frozen: `CLASS_C_WALL_CONTRACTS.md` §D5 (divergence set, permitted counterterms, scheme-independence requirement on classification).** |

Both are owner/dispatch decisions. The solver continues to REFUSE while they
are undecided — demonstrated immediately after this file was recorded.
