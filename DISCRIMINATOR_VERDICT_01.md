# RELATIONAL-vs-MEDIUM DISCRIMINATOR — D-1 VERDICT

**Date:** 2026-09-25 · **Charter:** `DISCRIMINATOR_CHARTER_01.md`
(pre-registration frozen at `53ab11f` before the run) · **Instrument:**
`calc/discriminator_d1.py` · **Artifact:** `DISCRIMINATOR_D1_RESULT.json`
(sha `d579ecb03e771c75…`) · **Battery 13/13**, sensitivity control detecting
(a 1% influence-data perturbation registers at Δ₁ = 7.8e-3 ≫ threshold).
Scope travels with every claim: the exactly solvable Gaussian class,
factorized initial state. Register untouched; fenced routes untouched.

## THE OWNER'S QUESTION, ANSWERED

> *Can two theories with the same K(t,t′), same effective observables, and
> different microscopic interpretations of that kernel be experimentally or
> mathematically distinguished at all?*

**Mathematically: YES. Experimentally (at S-limited access): NO. And the
two halves are load-bearing together:**

- **E1 — underdetermination, established at machine precision.** W-A (star
  bath, 9 dimensions) and W-B′ (chain bath plus two decoupled hidden modes,
  11 dimensions) are **genuinely non-isomorphic theories** — different
  dimension, different full spectrum — with identical influence data
  (Δ_K = 6.3e-15, Δ_N < 1e-8). Every S-local observable agrees to
  Δ₁ = 4.5e-15 over the full time grid, and adding a probe coupled only
  through S changes nothing (Δ₂ = 6.3e-15). **Distinct microscopic
  interpretations of the same kernel are S-observationally equivalent,
  forever, by theorem-grade demonstration in this class.**
- **E4 — the representation exemplar.** W-A and its exact Lanczos chain
  W-B are one theory twice (same dimension, same spectrum, all access
  levels equal): "star medium" vs "chain of relations" is literally a
  coordinate choice here.
- **E2 — where the physics actually lives.** W-D has the SAME Hamiltonian
  as W-A and the SAME kernel **exactly** (Δ_K = 0.0 — the kernel's
  state-independence, the same fact the T2 chain froze), but a thermal
  instead of ground bath state: the noise kernel differs by 1.09 and the
  S-local trajectories split at Δ₁ = 0.91. **Kernel-equal theories are
  distinguishable the moment their fluctuation data differ.** The
  equivalence-class label is the pair (K, N) — in non-Gaussian worlds, the
  full influence functional — never K alone.

## THE CONSEQUENCE, PER THE OWNER'S OWN DECISION RULE

**At fixed influence data (K, N) and S-limited access, the
medium-vs-relational dispute is REPRESENTATIONAL** — the two vocabularies
describe unobservable surplus structure, and per the owner's rule both are
**discarded as fundamental at that level**. What survives as physical, and
where any genuine ontology-level discriminator must live, is **what
constrains the realizable (K, N) pairs**: a framework earns physical
distinctness exactly by forbidding or forcing dissipation–fluctuation
relations (KMS/FDT-class structure, positivity/passivity cones, sector
couplings) that its rival does not.

**Restated for the program:** the physical content of the candidate
ℛ = (𝒜, 𝒞, ρ, 𝔖) is not its vocabulary but **the constraint set it induces
on influence data across sectors and states.** The same holds for any
medium picture. Two proposals that induce the same constraint set are one
theory.

**The pointer to the gravitational sector (pointer, not computation; the
noise fork stays owner-held):** the record already contains exactly the
E2-shaped structure — G_R state-independent while the noise kernel is
state-dependent (T2, frozen), and the O(H²) FDT/KMS obstruction (the
dissipation–fluctuation relation that *cannot* close on the non-stationary
dS state at that order). If the frameworks ever part ways observably, the
recorded evidence says it will be **in the fluctuation sector, through a
K–N relation**, not through the kernel.

## SCOPE AND WHAT THIS DOES NOT ESTABLISH

Gaussian class, factorized initial state, |S| = 1, finite baths, access
levels as declared. It does not prove that no exotic access channel exists
in field theory; it does not touch type-III subtleties (the recorded III₁
obstruction stands as the boundary of the toy's validity); and the
"discard both vocabularies" consequence is conditional on fixed (K, N) —
a framework that *constrains* (K, N) differently is not discarded, it is
exactly what a real theory would be.

## HARD STOP

Verdict recorded. The waiting decision: **the owner rules on the
vocabularies** (per their pre-stated rule, the expected ruling at this
scope is: neither "medium" nor "relational structure" is fundamental; the
fundamental object is the influence-data constraint structure) **and
selects the next instrument** — P-2 (criterion unification) remains
chartered-next; P-3 waits per the standing order. Λ_R, Matsubara, Π₀, U5
remain fenced.
