# RELATIONAL ONTOLOGY — OWNER RULING 01

**Date:** 2026-09-25 · **Rules on:** `RELATIONAL_ONTOLOGY_MAP_01.md` + P-1
(sha `8220a4f5…`). Recorded from the owner's own words; corrected by owner
edit if misstated.

## 1. THE P-1 RESULT IS ACCEPTED, AT ITS SHARPENED READING

The regime trichotomy stands as computed: structured → intrinsic selection
(the impurity existence proof); symmetric → equivalence classes only;
generic → no intrinsic selection (CPR-consistent). The criterion-plurality
finding (𝔖_rank ≠ 𝔖_MI ≠ 𝔖_cohesion) is ruled a genuine discovery, not a
defect, and the disclosed taxonomy incompleteness is accepted as recorded.

## 2. THE FORMULATION UPDATE (candidate, not banked)

The candidate fundamental structure is now written

> **ℛ = (𝒜, 𝒞, ρ, 𝔖)** with the crucial qualification
> **𝔖 = 𝔖[𝒜, 𝒞, ρ] may itself be contextual rather than unique.**

Reality may not contain a universally privileged system/environment
decomposition; a decomposition becomes physically meaningful when the
underlying correlations/dynamics make some sector robust enough to support
an effective description. The working hierarchy:

> primitive relational structure → correlations + dynamics + state →
> stable sector / subsystem *when one exists* → K(t,t′) → effective fields
> → geometry, matter, quantum/classical behavior.

**What disappears from the bottom layer: the kernel is no longer a
candidate fundamental law.** (Consistent with ROOT-1's KERNEL-STANDARD and
the map's downstream placement.)

## 3. THE DISTINCTION THIS RULING FIXES

"**A subsystem exists**" (P-1 supports it, in some regimes) is not
"**there is a unique law of what counts as a subsystem**" (NOT
established). P-2's three outcomes are recorded for when it runs: a master
functional ℱ exists and is unique → a universal principle of
subsystemhood; several invariant functionals exist and no master → subsystemhood
is intrinsically plural; no invariant definition → system/environment is
fundamentally representational or observer-relative.

## 4. SEQUENCE CHANGE — THE DISCRIMINATOR COMES FIRST

The relational-vs-medium discriminator is ordered **before P-3** (and
before any construction that presumes one vocabulary). The owner's next
question, verbatim:

> **"Can two theories with the same K(t,t′), same effective observables,
> and different microscopic interpretations of that kernel be
> experimentally or mathematically distinguished at all?"**
> If NO — the dispute is representational and both vocabularies are
> discarded as fundamental. If YES — a genuine ontology-level discriminator
> exists.

Chartered as instrument **D-1** (`DISCRIMINATOR_CHARTER_01.md`). P-2
remains chartered-next-after; P-3 waits. Λ_R, Matsubara, Π₀, U5 stay
fenced; no prediction campaign.
