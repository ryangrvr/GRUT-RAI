# GRUT Minimum Core

**Date:** 2026-08-23 · Emitted from `claims.json` + reality-audit V1/V2 verdicts.
**The core may be empty or wholly borrowed — pre-registered as legitimate (ADDED-F).**

## The core: three nodes

| node | tier | nature |
|---|---|---|
| `mu_linear` | derived-pending | computation (positive result) |
| `x_no_pin_theorem` | derived-pending | **negative/open result** |
| `kk_static_transfer` | derived-pending | computation |

**Two of the three are negative or open results.** A theory whose minimum specific core is mostly
statements of what does *not* follow is a different object from one with three positive predictions.

## Exclusions, stated on this artifact's face ([A-4])

1. **`vc_rho_lambda`, `vc_v_ew`, `vc_m_planck` are EXCLUDED as measured data every candidate
   theory must reproduce, not GRUT-specific content.** This is the same category error the charter
   already fixed once for Λ ("an observed value is a datum every candidate theory must reproduce,
   not a discretionary posit") and it recurred inside the V2 core count. Written here so it cannot
   drift back.

2. **`rung3_single_pole` does NOT survive UN-ASSUME of the R1 ontology — and is not demoted.**
   It is **conditional on the ontology**, which is exactly what the Class-C calculation tests.
   The audit and the physics converge on one target rather than competing: rung3 carries GRUT's
   distinctive novelty AND is conditional on an un-derived stance AND is the decisive physics test.
   Its inputs survive as mathematics (DOS~ω², J~ω³); its conclusion *is* the ontology.

## The core scientific question (unchanged)

> Does the gravitational vacuum actually generate the finite-memory / single-pole structure that
> GRUT assumes?

## Competitor-framework screen (third cut)

Not computable: no instrument exists. **Blocker logged; joins build queue.**

