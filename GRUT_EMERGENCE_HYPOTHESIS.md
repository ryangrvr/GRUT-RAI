# GRUT Emergence Hypothesis

> **STATUS: CANDIDATE HYPOTHESIS -- NOT YET A DERIVED THEORY.**

*GENERATED by `provenance/emergence_hypothesis.py` from `claims.json`. The layer->claims mapping is authored (it is the construction); **every status is generated from the register** and a layer with no derived support is printed TO-DERIVE mechanically. `--check` diffs against this file. This artifact asserts no new physics and upgrades nothing.*

**The first rung is `u3_split_origin`, not the responsive vacuum.** The disputed transition is nothing/fundamental ontology -> distinction -> system/bath; the responsive-medium machinery only begins once that decomposition is warranted, and it currently is not.


## Part 1 -- The ontological hypothesis

GRUT proposes, in one sentence: **the universe behaves as a finite-memory responsive medium** -- perturbations relax through a kernel that remembers, and the familiar structures (dissipation, thermality, the gravitational stiff limit, the quantum reduced dynamics) are limits of that response rather than independent posits. The hypothesis is CONDITIONAL and its ontology is STIPULATED, not derived: the medium response is the entry price (+4 founding node, +1 ontology stance, +1 time-translation flow), the first two rungs (distinction, system/bath) are open fences, and quantum mechanics is imported (+2) rather than emergent. What is GRUT's own is the middle: the memory kernel and its consequences. What is imported is priced and listed in Part 3. Nothing here is claimed to follow from nothing.


## Part 2 -- The derivation chain (layered)

Each layer's status is generated. A layer is only as derived as its register support; a layer with zero derived covering claims prints TO-DERIVE.

| layer | transition | status (generated) |
|---|---|---|
| `L0` | Can GRUT say why anything can be distinguished from anything else? | TO-DERIVE (open) |
| `L1` | Why is an open-system description physically warranted (rather than chosen)? | TO-DERIVE (open) |
| `L2` | The constitutive-response machinery (current entry price). | derived-pending + priced-import + borrowed |
| `L3` | The central GRUT conjecture: the vacuum's response has a finite-memory kernel. | derived-pending + open |
| `L4` | Does the memory kernel reduce to a single pole? The decisive bath question. | derived-pending + open |
| `L5` | An open medium HAS an arrow; WHICH direction is imported. | original-result + derived-pending + priced-import + borrowed |
| `L6` | Memory time to zero collapses the kernel to the GR limit -- RECOVERED-WITH-IMPORTS. | original-result + priced-import + adopted-stance + borrowed |
| `L7` | Reduced-density-matrix dynamics; quantum mechanics USED, not explained. | ASSUMED-ONLY (priced-import + borrowed) |
| `L8` | Why definite outcomes with Born weights? NOT DERIVED. | MAPPED, NOT BANKED -- prose artifacts only; zero register nodes; NOT DERIVED |
| `L9` | LCDM-shaped by a no-go export, with a computed but non-predictive mu allowance. | derived-pending + open |
| `L10` | The relaxing vacuum as the dark-energy story; DESI's w(z) is the live kill-channel. | derived-pending + open |
| `L11` | The Standard Model appears NOWHERE in the register. SILENT. | SILENT -- no register node covers this layer |
| `L12` | How observers arise has never been POSED. UNPOSED. | UNPOSED -- the question has never been asked in the register |


### L0. Fundamental origin -> distinction

**Transition:** Can GRUT say why anything can be distinguished from anything else?

This is the rung the whole hypothesis rests on, and it is NOT DERIVED. If the framework cannot ground the first distinction, then every later 'the vacuum responds' statement is a choice of vocabulary rather than a consequence. Recorded FIRST and honestly: nothing below this rung is derivation until it moves.

**Layer status (generated):** TO-DERIVE (open)

| register claim | status (generated) |
|---|---|
| `u3_split_origin` | to-derive |


### L1. Distinction -> system/bath decomposition

**Transition:** Why is an open-system description physically warranted (rather than chosen)?

The deepest open question at the medium link, now promoted to its own layer. The register carries the question as a fence (u3_split_origin, tier to-derive). If the answer is negative, the responsive-medium posit is exposed as the decomposition that makes the mathematics possible -- the exact hidden-assumption shape this artifact exists to expose.

**Layer status (generated):** TO-DERIVE (open)

| register claim | status (generated) |
|---|---|
| `u3_split_origin` | to-derive |


### L2. System/bath -> response

**Transition:** The constitutive-response machinery (current entry price).

The program's entry price: the INI-in open-system formalism (+4), the finite-memory ontology stance (+1), and the background TIME-TRANSLATION FLOW (+1) -- the presupposition that lets a kernel be written at a single frequency at all. The standard open-system toolkit is borrowed scaffolding. All of it is assumed, priced, and marked; none of it is derived.

**Layer status (generated):** derived-pending + priced-import + borrowed

| register claim | status (generated) |
|---|---|
| `rung1_inin_formalism` | derived-pending +4 |
| `rung1_ontology_finite_memory` | assumed +1 |
| `background_time_translation_flow` | assumed +1 |
| `linear_response_viscoelastic` | assumed (borrowed) |
| `relativistic_hydro_israel_stewart` | assumed (borrowed) |
| `superfluid_bec_media` | assumed (borrowed) |


### L3. Response -> finite memory

**Transition:** The central GRUT conjecture: the vacuum's response has a finite-memory kernel.

The load-bearing structural posit of the framework. Its status is DERIVED-PENDING at best: the pole-vs-cut question (below) is the one decisive external test, and a cut-class answer kills this layer as stated.

**Layer status (generated):** derived-pending + open

| register claim | status (generated) |
|---|---|
| `rung3_single_pole` | derived-pending |
| `u2_kernel_universality` | to-derive |
| `u4_constitutive_origin` | to-derive |
| `eft_operator_basis` | to-derive |


### L4. Finite memory -> single-pole structure

**Transition:** Does the memory kernel reduce to a single pole? The decisive bath question.

The one place the framework makes a structural bet it did not feed in by construction. The contract-level retarded kernel is computed and banked (delta 0); the transfer question is answered only conditionally (chi_inf >= 0 at class level, never unconditional); the classification and order-parameter branches are open. The KK/Love relations are the derived-pending export of the kernel's form. A cut answer refutes this layer, and with it L3 and everything above built on the kernel's form.

**Layer status (generated):** derived-pending + open

| register claim | status (generated) |
|---|---|
| `kr_contract_retarded_tier4` | derived-pending |
| `kk_static_transfer` | derived-pending |
| `rung4_love_kk` | derived-pending |
| `u5_constitutive_phases` | to-derive |
| `u6_constitutive_order` | to-derive |


### L5. Memory -> arrow (existence intrinsic, direction imported)

**Transition:** An open medium HAS an arrow; WHICH direction is imported.

The register's strongest original joint: existence of the arrow is intrinsic to dissipation (the KMS gate is also the ledger's only -1, discharging the noise kernel as an independent input). The DIRECTION is state-dependent and rides the low-entropy boundary -- a priced import the framework sharpened and then declined to answer. Thermality is the concurrent half of the same node: the KMS condition is one fact doing two jobs. The horizon temperature enters as an IMPORT (Unruh, priced), and founding_h1's two-levels-of-description claim is GENERIC -- true of any medium with a response, not uniquely GRUT.

**Layer status (generated):** original-result + derived-pending + priced-import + borrowed

| register claim | status (generated) |
|---|---|
| `arrow_of_time` | assumed +1 |
| `rung2_kms_gate` | derived-pending -1 |
| `fluctuation_theorems` | assumed (borrowed) |
| `past_hypothesis` | assumed (borrowed) |
| `entropy_foundations` | assumed (borrowed) |
| `second_law_h_theorem` | assumed (borrowed) |
| `entropy_area_unruh` | assumed (borrowed) |
| `founding_h1_zeta_casimir` | shown |


### L6. Memory -> gravity (the stiff limit)

**Transition:** Memory time to zero collapses the kernel to the GR limit -- RECOVERED-WITH-IMPORTS.

GR is recovered only with imports (+2: area entropy, Unruh temperature) and is never derived; the coupling's normalization resisted derivation on named obstructions (settled-negative, frozen); the response's tensor structure is a priced choice (+1 Lorentz-covariance licensing fence, discharged or not by the microscopic Sigma_R^TT at Wall A).

**Layer status (generated):** original-result + priced-import + adopted-stance + borrowed

| register claim | status (generated) |
|---|---|
| `rung5_gr_limit` | assumed +2 |
| `rung9a_value` | shown |
| `rung9b_bridge` | assumed [settled-negative] |
| `p_tt_ansatz` | assumed +1 |
| `response_lorentz_covariance` | shown |
| `analogue_gravity_acoustic` | assumed (borrowed) |


### L7. Gravity -> quantum limit

**Transition:** Reduced-density-matrix dynamics; quantum mechanics USED, not explained.

The quantum limit is an ASSUMED node priced +2 (the quantization condition and the Born measure are both imported). This is where the emergence chain stops being a theory of emergence and starts importing structure -- the register's own accounting, printed mechanically.

**Layer status (generated):** ASSUMED-ONLY (priced-import + borrowed)

| register claim | status (generated) |
|---|---|
| `rung6_qm_limit` | assumed +2 |
| `born_rule` | assumed (borrowed) |


### L8. Quantum -> Born outcomes

**Transition:** Why definite outcomes with Born weights? NOT DERIVED.

Mapped in the quantum-foundations cluster but never banked: einselection dissociates from the Born measure only asymmetrically, and 'decoherence explains classicality independently of probability' is folklore. Zero register nodes. An honest NOT-DERIVED layer, kept as a layer rather than tidied away.

**Layer status (generated):** MAPPED, NOT BANKED -- prose artifacts only; zero register nodes; NOT DERIVED


### L9. Classicality -> structure (linear cosmology)

**Transition:** LCDM-shaped by a no-go export, with a computed but non-predictive mu allowance.

The trace-only endpoint is excluded (retraction history on record); the interior family opens a computed allowance (mu-1 up to ~20% at the loose edge, no floor) that predicts nothing; the framework's own no-pin lemma closes route R3 as classifier. The two owed-or-retired calcs (TT-auto rigorous; xi_ij) live at this layer's edge.

**Layer status (generated):** derived-pending + open

| register claim | status (generated) |
|---|---|
| `mu_linear` | derived-pending [no_go_export] |
| `x_no_pin_theorem` | derived-pending |
| `zeta_interior_family` | to-derive |
| `founding_h2_R_zeta_bridge` | to-derive [disfavored] |
| `l0_r2_exact_unique_breaker` | to-derive [screened-dissolves] |
| `l0_r3_payoff_mu_linear` | to-derive [screened-dissolves] |


### L10. Structure -> cosmology (dark energy)

**Transition:** The relaxing vacuum as the dark-energy story; DESI's w(z) is the live kill-channel.

Out of equilibrium the medium's slow relaxation is the candidate story (+3: amplitude, two-scale commitment, single-departure shape -- all priced). The value of Lambda is UNDETERMINED by every framework in the program, booked as such. The sign computations (wz sign, w0-wa sign, no-crossing export) are the genuinely non-constructed consequences: they are independent of the kernel's detailed form, which is what makes them tests.

**Layer status (generated):** derived-pending + open

| register claim | status (generated) |
|---|---|
| `rung7_wz` | to-derive +3 |
| `rung7_w1_wz_map` | derived-pending |
| `rung7_w2_wa_sign` | to-derive |
| `rung7_w3_nocrossing_export` | to-derive |
| `lambda_undetermined` | to-derive |


### L11. Cosmology -> matter

**Transition:** The Standard Model appears NOWHERE in the register. SILENT.

The constitution's assumed-list names 'the SM spectrum' in prose, but no node books it. A silent layer visible in the hypothesis is worth more than one tidied away.

**Layer status (generated):** SILENT -- no register node covers this layer


### L12. Matter -> observers

**Transition:** How observers arise has never been POSED. UNPOSED.

No node, no map, no artifact. Recorded as the hypothesis's honest end; posing the question well would itself be a node.

**Layer status (generated):** UNPOSED -- the question has never been asked in the register


### The decisive test

The hypothesis's non-constructed consequence under test is the SINGLE-POLE structure (L4): the kernel's pole-vs-cut question, executed as rung8_falsifier / the Class-C calculation. The sign exports (wz sign, w0-wa sign, no-crossing, L10) are the second family of genuine tests: they are independent of the kernel's detailed form. A cut-class answer, or a measured sign against these exports, refutes layers -- this is what 'could be wrong' concretely means here.


## Part 3 -- External inputs (what GRUT imports)

*Generated mechanically: every GRUT-scope claim with tier `assumed` or `grut_standing == borrowed`, with its price. This is where the emergence stops and structure is imported; the largest single import is the quantum limit (+2) and the low-entropy past (+1).*

| node | status (generated) |
|---|---|
| `rung5_gr_limit` | assumed +2 |
| `rung6_qm_limit` | assumed +2 |
| `rung1_ontology_finite_memory` | assumed +1 |
| `arrow_of_time` | assumed +1 |
| `p_tt_ansatz` | assumed +1 |
| `background_time_translation_flow` | assumed +1 |
| `rung9b_bridge` | assumed [settled-negative] |
| `born_rule` | assumed (borrowed) |
| `entropy_area_unruh` | assumed (borrowed) |
| `past_hypothesis` | assumed (borrowed) |
| `linear_response_viscoelastic` | assumed (borrowed) |
| `relativistic_hydro_israel_stewart` | assumed (borrowed) |
| `analogue_gravity_acoustic` | assumed (borrowed) |
| `superfluid_bec_media` | assumed (borrowed) |
| `entropy_foundations` | assumed (borrowed) |
| `fluctuation_theorems` | assumed (borrowed) |
| `second_law_h_theorem` | assumed (borrowed) |


## Off-layer nodes (accounted, not layers)

*Every GRUT node is either in a layer above or listed here -- the generator errors otherwise.*

| node | why off-layer |
|---|---|
| `emergence_chain` | the chain artifact itself -- tripped this same coverage guard before, which is the guard working |
| `founding_h3_doubleslit_anchor` | deferred anchor (disposition: deferred) |
| `info_i1_renorm_as_information` | information-principle triplet: generic half |
| `info_i2_beyond_standard_bridge` | information-principle triplet: screened-refuted |
| `info_i3_distinct_consequence` | information-principle triplet: moot |
| `l0_r1_redundancy_exists` | frontier-3 generic half (its crux nodes sit at L9) |
| `method_novelty` | about the program's method, not the universe |
| `passivity_channel_diagonal` | frame-free linear-response mathematics, not a stage of the story |
| `rung8_falsifier` | the tabletop discriminator -- an instrument pointed at the hypothesis, not a layer |
| `u1_form_universality` | about the FRAMEWORK's form, not a layer |


## The finding this artifact is built to state

**GRUT presently has a candidate hypothesis, not a derived hypothesis.** The register's own accounting says where the distinction occurs: layers L0-L1 are open fences (nothing/fundamental ontology -> distinction -> system/bath), L2 is assumed and priced, L3-L4 are derived-pending on one decisive bath question, and the quantum limit (L7) is an import (+2) -- the exact place the framework stops being a theory of emergence and starts importing structure. The research program this defines is not sector-filling; it is establishing whether a minimal generative mechanism exists underneath the sectors, beginning at `u3_split_origin`.
