# TT-1 — THE PHYSICAL D = 4 TT CHANNEL AND ξ: CHARTER + PRE-REGISTRATION (frozen before evaluation)

**Date:** 2026-09-25 · **Authority:** owner ruling given in-session on
SX-1. *"SX-1: ACCEPTED … The next authorized fork is therefore … D = 4
TT/ξ, now with the spatial coupling premise explicitly classified rather
than silently assumed."*

**Binding record (from the SX-1 ruling):**
- Sel-4x is IRREDUCIBLE, reduced to the co-stretch declaration.
- Co-stretch ≡ unit change is a classification identity.
- "Any constant length rescaling = unit change" is false in the tested
  class.
- Decidability is class-split by access.
- "One intrinsic scale ⇒ every uniform rescaling is a unit change" is a
  statement about that model class, not a theorem about nature.

**Target:** the question CP-1 left open as fork (b):
- CP-1 showed that in D = 4 **strain** kinematics (zero net spatial
  momentum) IR Weyl selects the improved tensor, which is off-locus.
- It also showed that for **TT probes** the coupling form within the
  ≤2-derivative family is forced up to amplitude (rank 1), with the
  exponent "kinematic, v3 territory".

TT-1 asks, for the **physical, on-shell TT channel**:
- **(Q1)** Does ξ, including the higher improvement tower, matter?
- **(Q2)** Is the coupling *form* forced once higher-derivative
  couplings are allowed?
- **(Q3)** Under what conditions on the retained sector does the TT
  pair channel **exist** at all?

**Outcomes:** DERIVED/SELECTED (in class) · CONSTRAINED-NONUNIQUE ·
CLASS-SPLIT · IRREDUCIBLE INPUT · FAILS/NULL, reported per question.

**Fences (binding):**
- **No absolute exponent is computed or compared to 7.** ω⁷ is
  occupancy evidence only; the v3 record and its convention are not
  reopened.
- Channel **existence and structure** only.
- Sel-4x (constant limit) is inherited as classified, not re-decided.
  The TT probe is dynamic and on-shell.
- Not used as selectors: CARRIER, GeoInv, the supplied probe as a
  selector, or a preselected coupling.
- ℏ located; GR-1 3D red; operator ordering fenced.

**Kinematics (frozen):**
- The graviton is on-shell: k = p₁ + p₂ (spatial) with Ω = ω₁ + ω₂ =
  |k| (c = 1).
- The matter pair has momenta p₁ and p₂ with dispersion ω(p). The
  opening angle is solved exactly from
  cos θ = [(ω(a) + ω(b))² − a² − b²]/(2ab), where a = |p₁| and
  b = |p₂|.
- TT projector about k̂: Λ(X) = PXP − ½P tr(PX), with P = 1 − k̂k̂.

## 1. THE LEGS (frozen, predictions written before any number)

- **L-ξ (Q1: is ξ, and its tower, visible in the physical TT
  channel?).**
  - *Setup:* 200 random on-shell pairs for a sector with
    ω = 0.8|p|, a, b ∈ [0.2, 1]. Improvement structures
    ξ(η^{ij}k² − k^ik^j)·f with f ∈ {1, p₁·p₂, |p₁|² + |p₂|²}
    (k² = 0 on shell), compared with the canonical structure
    p₁p₂ + p₂p₁.
  - **Prediction (halt-grade identity):**
    max |Λ(improvement)| / |Λ(canonical)| < 1e-12.
  - Frozen consequence: **ξ and the whole improvement tower are
    invisible in the physical TT channel. The D = 4 ξ ambiguity
    dissolves there.** It survives only for off-shell/trace (strain)
    probes, as CP-1 found; that result is retained, not contradicted.
- **L-F (Q2: is the form forced once higher derivatives are
  allowed?).**
  - *Setup:* the extended family is T₁ = p₁p₂ + p₂p₁,
    T₂ = p₁p₁ + p₂p₂, T₃ = |p₁|²p₁p₁ + |p₂|²p₂p₂,
    T₄ = (p₁·p₂)T₁ and T₅ = (ω₁ω₂)T₁, evaluated on the same 200
    on-shell pairs.
  - **Predictions:**
    - (a) **per kinematic point, the TT images of all five have rank
      1** (to 1e-10): the TT *tensor structure* is forced;
    - (b) across points, the space of scalar **form factors** has
      rank **4** (T₂ ∝ T₁ exactly; T₃, T₄ and T₅ carry independent
      momentum-dependent scalars): the *form factor* is not forced;
    - (c) **IR suppression:** at symmetric kinematics a = b = p, the
      relative weight of the higher-derivative form factor T₃ against
      T₁ grows ×4 when p doubles (0.2 → 0.4), with ratio in
      [3.5, 4.5].
  - Frozen consequence: in TT the leading IR coupling is **forced
    (rank 1 tensor, constant form factor)**. Beyond leading order it is
    **CONSTRAINED-NONUNIQUE**, and the subleading form factor inherits
    the supplied geometric-coupling/Sel-4x choice.
- **L-K (Q3: when does the TT pair channel exist?).** The canonical TT
  vertex magnitude |Λ(p₁p₂ + p₂p₁)| on the on-shell locus, for:
  - (a) **exactly linear, v = c** (ω = |p|): cos θ = 1, the locus is
    collinear, and **the vertex is identically 0** (< 1e-12; aligned
    death);
  - (b) **subluminal linear**, v = 0.8: a non-collinear locus with
    vertex > 1e-3;
  - (c) **v = c in the IR with subluminal curvature**
    (ω = |p| − α|p|³): the vertex is nonzero and **∝ α**, with the
    ratio at α = 0.02 vs 0.01 (a = b = 0.5) in [1.8, 2.2];
  - (d) **superluminal curvature** (α = −0.01): no on-shell solution
    (cos θ > 1 for every a, b sampled), so **the channel is closed**.

  Frozen consequence: **the TT channel's existence is CLASS-SPLIT by
  the retained sector's light cone relative to the probe's.** It is
  identically dead for an exactly Lorentz-matched linear sector, open
  in proportion to subluminality (from v < c or from dispersion
  curvature), and closed for superluminal dispersion. With RS-1's
  per-sector v = c units choice, the channel lives **only** on
  dispersion curvature, which is microscopic retained-sector content.
  The cross-sector light-cone relation is U-1 territory (supplied).

## 2. OUTCOME RULE (frozen, mechanical)

- **Q1:** DERIVED-IN-CLASS ("ξ irrelevant in the physical TT channel")
  if L-ξ holds.
- **Q2:**
  - TT tensor forced (rank 1 per point) if L-F(a) holds;
  - form factor CONSTRAINED-NONUNIQUE if the L-F(b) rank is > 1;
  - "IR-forced" if L-F(c) shows suppression.
- **Q3:** CLASS-SPLIT (dead / open ∝ subluminality / closed) if L-K
  (a)–(d) all hold.
- **Overall:** reported per question. No absolute exponent, no ω⁷
  comparison.

## 3. CONTROLS

- Halt-grade: the L-ξ identity.
- Halt-grade: the L-K(a) collinear zero.
- Halt-grade: per-point rank-1 is checked by Gram eigenvalues.
- The on-shell condition is re-verified per sample to 1e-12.
- Seed 20260925.

## 4. DELIVERABLES AND STOP

1. `calc/tt1_channel.py` (pure stdlib; emits `TT1_CHANNEL_RESULT.json`,
   sha-hashed).
2. `TT1_TT_XI_VERDICT_01.md` and a closing comment on Issue #2.
3. **HARD STOP after the TT/ξ verdict.**
