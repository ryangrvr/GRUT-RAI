# EQ-1 — CAN GEOMETRIC-ONLY COUPLING BE SELECTED FROM EARNED STRUCTURE? CHARTER + PRE-REGISTRATION (frozen before evaluation)

**Date:** 2026-09-25 · **Authority:** GitHub Issue #2, owner comment
**5834587876** (CP-1 accepted at recorded strength; CLASS-SPLIT binding;
minimal-stress not discharged). **Target (owner, verbatim):** *Can
geometric-only coupling of the retained sector be selected from the
already-earned structure (𝔠_full, access, and G-2 spectral geometry),
without redefining that principle as the meaning of gravity?* Attacked as
a falsifiable selector problem. **Allowed outcomes (owner):
DERIVED/SELECTED · CONSTRAINED BUT NONUNIQUE · CLASS-SPLIT · IRREDUCIBLE
INPUT · FAILS/NULL**, plus the owner's required recording: *if all
admissible alternatives are influence-equivalent under the declared
access, record UNDERDETERMINATION, not derivation.*

**Required boundaries (owner), and how this charter meets them:**
1. *Do not insert the equivalence principle by wording or construction.*
   The geometric direction below is one of five basis couplings. The
   operational weak-equivalence statement is chartered as a named
   CANDIDATE selector (Sel-4). It is tested, and it is never used to
   build the family.
2. *No Einstein/GR field equations as the selector.* None appear.
3. *Retained sector fixed and separate.* Both dispersions below are
   given, not explained, and gaplessness is not attacked.
4. *ω⁷/class-4 fenced; v3 not reopened.* Exponents are compared only
   as classes inside GR-1's own instrument convention.
5. *G-2 geometry is not a synonym for metric coupling.* Leg L-G2
   measures what the G-2-recoverable data actually sees of each
   coupling.
6. *The D = 4 strain/TT split and the operator-ordering ambiguity stay
   open.* **Scope is 1+1 (one spatial dimension) only.** Uniform,
   zero-momentum probes cannot see ordering differences, and no claim
   about either open question is made.
7. *Underdetermination is recorded as such.* Leg L-A tests it directly.

Standing: ℏ located-not-generated; the GR-1 3D red remains red;
Λ_R, Matsubara, Π₀, U5 fenced; type-III boundary map only.

## 1. THE RETAINED SECTOR AND THE DECLARED COUPLING FAMILY (frozen)

Two given species (retained sector, not attacked): **A**, continuum
D_A(Q) = (q − αq³)², α = 0.05 (GR-1's L-X dispersion); **B**,
nearest-neighbour lattice D_B(Q) = 4 sin²(q/2); Q = q², ω² = D. The probe
is a uniform, time-dependent h(t) (zero spatial momentum, the GR-1
strain-probe class) modulating the sector along five basis directions
with coefficients **(μ, κ, λ, σ, ξ)**:

| Dir | Name | Action on the sector | On-shell pair vertex |
|---|---|---|---|
| M | mass | kinetic term × (1 + μh) | −½μD |
| K | stiffness | D → (1 + κh)D | −½κD |
| G | proper-distance generator | D(Q) → D((1 + λh)Q) | −½λQD′ |
| S | dispersion-shape | D → D + σhQ² | −½σQ² |
| X | improvement | ξh∂ₜ²φ² (CP-1's ξ term) | −2ξD |

V(q) = −½[(μ+κ)D + λQD′ + σQ²] − 2ξD. CP-1's metric (geometric)
coupling is the combination **(½, ½, −1, 0, 0)**, V = ½(QD′ − D).
Naming a direction G asserts nothing. Whether G is *selected* is the
question.

## 2. THE LEGS (frozen, with predictions written before any number)

Exponents use GR-1's instrument unchanged for species A (slope =
log₂[J(0.2)/J(0.1)]) and CP-1's lattice slope for species B.

- **L-X (the exponent counterattack):** M-only → 4; geometric
  (½,½,−1,0,0) → 8; **S-only → 8 (a non-geometric coupling in the +4
  class)**; X-only → 4 (A only); **tuned geometric + S**, with σ = −2α
  (A) or −1/12 (B) cancelling the leading term → **12 (a +8 class)**.
  Gates ±0.2 (±0.3 for the tuned case), on both species where listed.
  Frozen consequence: the +4 class does not identify geometric
  coupling, and the family contains a fine-tuned member in a different
  class.
- **L-H (𝔠_full as selector — expected NULL):** species-A baths
  (30 modes, ω_tot ∈ [0.05, 1.0]) for M, geometric, S, X and tuned;
  3×3 two-time Gram over {B(0), B(4), B(11)} PSD (min eig/C₀ ≥ −1e-12)
  for all.
- **L-G2 (what the G-2-recoverable geometry sees):** constant
  modulation h = 1e-3 along each direction. Measure the resistance
  density ρ = 1/P′(0) (G-2's static metric density, where P is the
  modulated potential dispersion) and the IR speed v² = P′(0)/m.
  **Predictions:** ρ_K = ρ_G (rel. diff < 1e-9); ρ_S = ρ_0 and
  v²_S = v²_0 (< 1e-9); M leaves ρ unchanged (< 1e-9) but shifts v²
  (> 1e-4). Frozen consequence: in all G-2-recoverable IR geometric
  data, the proper-distance direction is indistinguishable from
  stiffness and the shape direction is invisible. Recovered geometry
  cannot select geometric coupling (NULL as selector); it is not the
  coupling ansatz.
- **L-C (all earned constraints together):**
  - *IR conservation (CP-1, earned):* the IR operator must be ∝
    φ̇² + φ′². The row is [1, c_K, c_G, c_S, 0], with c_dir =
    lim B_dir(Q)/Q computed at Q = 1e-12. X is identically conserved.
  - *IR Weyl in D = 2 (CP-1, earned):* the row is [0, 0, 0, 0, 1].

  **Predictions (both species):** nullity 3; the geometric vector and
  S each lie in the null space (residual < 1e-8); the vertex image of
  the survivors has **rank 2**. Frozen consequence: earned structure
  leaves geometric and non-geometric (S) couplings side by side:
  CONSTRAINED-NONUNIQUE.
- **L-I (Sel-4, the CANDIDATE weak-equivalence selector, chartered as
  a hypothesis):** *a constant probe must be invisible to the sector's
  own intrinsic data* (its constant limit acts as a change of units).
  - The intrinsic invariant is Π = ω(q*)/(v_IR q*), with q* the
    intrinsic point where v_g = v_IR/2. Π is invariant under
    rescalings of length and time.
  - Compute dΠ/dh per direction by central difference (h = ±1e-4).
    X's constant limit is a total derivative and is implemented as no
    change (labeled by-construction).
  - **Predictions:** |dΠ/dh| < 1e-6 for M, K, G, X and > 1e-3 for S,
    on both species.
  - Appending the Sel-4 row to L-C: **nullity 2, vertex-image rank 1,
    survivor slope 8 ± 0.2 on both species.**

  Frozen consequence: G is selected **only** once Sel-4 is added.
- **L-A (declared-access underdetermination):**
  - **Species A:** match the geometric vertex with the material shape
    directions S and S₂ (D → D + h(σQ² + σ₂Q³)) at q = 0.1 and 0.2.
    **Prediction:** exact, with relative residual < 1e-9 at
    q ∈ {0.05, 0.3, 0.6}.
  - **Species B:** a 1-term S fit at q₀ = 0.01 leaves a relative
    residual whose ratio at q = 0.2 vs 0.1 is **∈ [3.5, 4.5]**. A
    2-term fit at q₀ = 0.01 and 0.02 gives a ratio **∈ [13, 19]** (a
    ladder: the separation enters at the first unmatched order).
  - **Static discriminator:** the species-A material mimic has
    |dΠ/dh| > 1e-3. Static (constant-probe) access separates what
    dynamical pair access cannot.

  Also reported only: the geometric vertex's species ratio
  c_A/c_B = (−α)/(−1/24) = 1.2. This is fixed by the dispersions, and
  would be the universality discriminator; universality is not
  earned, since S-1 PC-D has amplitudes sector-supplied.

## 3. OUTCOME RULE (frozen, mechanical, computed from the measurements)

- **DERIVED/SELECTED:** the vertex-image rank after the EARNED
  constraints (L-C) is 1, and the survivor has slope 8 on both species.
- **CONSTRAINED-NONUNIQUE:** earned rank ≥ 2 with the geometric vertex
  among the survivors.
- **IRREDUCIBLE INPUT:** G is selected (rank 1) only after the
  non-earned Sel-4 is added, AND a non-geometric counterexample (S)
  passes every earned constraint (L-H admissible, L-C null space, L-G2
  invisible).
- **UNDERDETERMINED (owner-required):** under declared pair access, a
  non-geometric coupling reproduces the geometric influence data
  (L-A exact on some species).
- **CLASS-SPLIT:** any of the above differs between species A and B.
- **FAILS/NULL:** any construction fails.

Components are reported separately and never averaged. **Import
consequence:** geometric coupling is DISCHARGED only on
DERIVED/SELECTED. Otherwise it stays supplied, restated in its reduced
form: if IRREDUCIBLE INPUT holds, that form is Sel-4 together with the
observable that tests it. Class-4 remains OPEN in every case, because
the retained-sector import is untouched.

## 4. CONTROLS

- Halt-grade: the M−K direction's pair vertex is identically zero,
  < 1e-15 (mass and stiffness are pair-vertex-identical on-shell).
- Halt-grade: the species-A 2-term exactness (a polynomial identity).
- Matched control: the geometric slope for species A must reproduce
  CP-1's recorded L-M continuum slope to 1e-9.
- The null-space tolerance is 1e-10 × max eigenvalue; every survivor
  is re-verified against every row.
- The bisection for q* runs to 1e-15.

## 5. DELIVERABLES AND STOP

1. `calc/eq1_equivalence.py` (pure stdlib; emits
   `EQ1_EQUIVALENCE_RESULT.json`, sha-hashed).
2. `EQ1_EQUIVALENCE_VERDICT_01.md` and a closing comment on Issue #2.
3. **HARD STOP at the verdict** (owner: "HARD STOP after the next
   geometry/equivalence verdict"). Forks (b) D = 4 TT/ξ, (c) the
   retained sector and (d) geometry selection stay separate.
