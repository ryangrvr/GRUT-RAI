# PREDICTION / UNIQUENESS POSSIBILITY MAP 01 — INSTRUMENT, ledger 0

**Date:** 2026-09-25 · **Chartered by:** owner direction ("we need some direction —
we have a theory that could work if we have a prediction or some uniqueness; we
need to see where that possibility lies before choosing"). · **Method:** three
independent read-sweeps over the frozen record (x-floor routes; uniqueness
routes; K_R low-frequency/D5 paths), every load-bearing statement cited to its
file. **This document banks nothing, moves no ledger entry, and opens no
campaign.** It is the reconnaissance the spine choice was waiting on.

---

## 0. WHAT ALREADY STANDS (so it is not re-derived)

- **One sourced prediction is already on the books:** the passivity no-go
  forbids a phantom-divide crossing — a clean w = −1 is "the program's one
  sourced prediction" (freeze §5). It is being tested for free: DESI DR3 is the
  standing watch. Confirmation of the crossing kills the family; a clean w = −1
  is the standing vindication channel. **No work required — only exposure.**
- **Every other proposed discriminator is recorded dead** (Γ_T ~62.7 orders
  under bound; decoherence 7–47 orders; "689 Hz" nonexistent as advertised;
  ISW ~32σ retired to ~2.0σ). Do not re-shoot these.
- **Closed pinning routes, do not re-open without new input:** R1 anomaly route
  → **NO PIN** ("x = α² is structurally unreachable"; the scheme freedom *is*
  the finding — `calc/RESULTS_anomaly_c0_map.md`); κ activation scale →
  **NO-PIN** ("kappa is not a parameter of GRUT" —
  `provenance/prereg/RESULT_KAPPA_2026-08-08.txt`).

After those closures the record's own summary stands: **"Remaining live pinning
route: rung3's Π₀ only"** (`STATE.md:62`) — plus the routes below that are not
x-pins but are prediction- or uniqueness-shaped in their own right.

---

## 1. PREDICTION ROUTES (each terminates in a verdict, whichever way it goes)

### P1 — rung3's Π₀: the only route to a genuine percent-level prediction

- **Object:** Π₀(ω→0, k→0), the P⁽⁰ˢ⁾ coefficient of the gauge-invariantly
  assembled retarded influence kernel of the pure-dS graviton sector.
- **The frozen decision tree** (`X_FLOOR_MAP.md` §2) makes every outcome
  contentful: **D1** (Π₀ = 0 forced → x = 0 *derived*, c₀ = 0 derived,
  `mu_linear` named for conditional graduation); **D2b** (x\* computed, below
  the edge → **"GRUT's first positive prediction — μ−1 = x\*/3, Σ−1 = x\*/6"**,
  conditional-theorem grade, discriminated by TT-auto on existing Planck data);
  **D2a** (x\* above the edge → "the program's first completed pre-registered
  kill-test"); **D1′** (Π₀ ≠ 0 established → the armed `mu_linear` demotion
  fires, honestly).
- **Recorded odds:** modal earned-under-determined; the value branch
  "likeliest lands" in the killed-on-arrival region. The D2 sub-fork lost its
  unconditional branch after the κ NO-PIN (band 0.037–0.358 is
  filter-conditional; the A6 Boltzmann-grade common-mode check is still
  OWED-OR-RETIRED before any κ=3 kill-grade use).
- **Blockers:** frontier-blocked as scoped in 2026-08 (gauge-dependent sector,
  contested secular growth, no assembled scalar observable in the literature).
  **But:** the in-house tranche named by the map — attack item 3,
  `calc/pi0_trace_channel.py` — **was never built** (the file does not exist).
  Under the reopen's no-external-dispatch direction this unbuilt tranche is the
  only permitted move on this route, and it exists precisely because part of
  the question was judged in-house-tractable. Note also: the 2026-08 frontier
  ceiling predates the Tier 1–4 chain; the program now owns a validated dS
  graviton one-loop pipeline it did not have when that ceiling was set.
- **Payoff class: PREDICTION (the only percent-level one on the map).**

### P2 — the Matsubara-ladder inheritance question: cheap, sharp, structural

- **Object:** "*does the projected memory kernel inherit the Matsubara
  ladder?*" — spacing exactly H, from the KMS factor alone; the escape route
  (spectral zeros at every rung) has **no free-level realisation**, so the
  interacting Σ "would have to manufacture what the free theory does not
  supply" (`STAGE_CLOSE_2026-08-19.md:63–65`). The record's own sizing: "a
  sharply posed question a specialist can engage in an afternoon."
- **Verdict either way is content:** inherited → single-pole fails at finite T
  *and* the dS graviton bath carries a **comb at spacing exactly H** — a
  distinctive structural signature of the state, candidate uniqueness-class
  content; not inherited → Σ produces the zeros, which would itself be a
  striking interacting result demanding a mechanism.
- **Companion check, flagged 2026-08-20 as outranking everything queued and
  never run:** the **clock-mismatch computation** — rung3's ladder rate is per
  static-patch Killing time, rung7's τ₂ ~ 1/H₀ is FRW cosmic time;
  "recomputing both rates in one coordinate either strengthens the stage's
  central adverse finding … or shows it to be a coordinate artifact." Cheap,
  in-house, decisive for whether the central adverse finding is even real.
- **Payoff class: STRUCTURAL PREDICTION / SIGNATURE (comb at spacing H) + a
  possible retraction of the central adverse finding.**

### P3 — Axis 2 via Λ_R: one number away from a verdict

- **State:** the benchmark consequence cell is CONVERGENT on axis 1 (Re χ(0) =
  3/2560π² exactly; s = 5, gapless) and **INDETERMINATE on axis 2 with the
  missing component precisely named**: one renormalization invariant,
  Λ_R = μ·exp(c4/2A). The μ-map is already computed
  (`WALL_KR_AXIS2_H0_ADJUDICATION.md`): a known window gives RESONANT, above
  it PURELY-RELAXATIONAL, below it neither label.
- **The verdict table is frozen and two-sided** (G3 cell): relaxational +
  convergent → "**derives what rung7 needs**"; resonant → "contradicts rung7's
  no-crossing microscopically — ontology takes the hit directly." Either
  answer is real content.
- **Blocker (a ruling, not a wall):** "Future numerical fixing is permitted
  only through an independently justified renormalization/matching condition …
  established without reference to … downstream outcomes"
  (`WALL_KR_LAMBDA_R_OWNER_RULING.md`). So the work item is precisely scoped:
  **a bounded search for an independently motivated matching condition for
  Λ_R** (symmetry/anomaly normalization, a physical subtraction point, a
  state-based condition) — renormalization theory, in-house-shaped, with the
  anti-laundering fence already written. D5 already certified c0 = c2 = 0 at
  H⁰ as *structural, not chosen* (the massless pole survives at H⁰); Λ_R is
  the one irreducible H⁰ constant; H² locals (c0′, c2′) remain fork-gated.
- **Payoff class: ADJUDICATION of the resonance/relaxational fork — the
  microscopic underpinning (or contradiction) of rung7.**

### P4 — the low-frequency class of K_R (ω → 0): highest stakes, highest cost

- **Object:** the frozen Q3-class outcome table (convergent → the register's
  side; divergent → "class-A was right"; cutoff-dependent → fork (ii), a new
  priced input), applied k→0 first, ω→0 LAST.
- **Blockers:** "**Nothing. No low-frequency calculation is currently
  licensed**" — Decision C's prerequisites 1–6 stand (an evaluator valid at
  ω ≲ 3.4H, i.e. a treatment non-perturbative in H; a registered IR
  prescription; a reduction proved stationary where the TT channel was
  exhibited **non-stationary** at up to 134% drift; a windowing price; an
  α-extraction error budget; a new versioned dispatch). The refusal boundary
  √(104/9)·H is "a result of the calculation. It cannot be loosened by
  declaration."
- **Honest read:** this is where "does the theory work at the frequencies that
  matter" lives, but it needs a genuinely new method (static-patch /
  non-perturbative in H). It is the *second* move, unlocked partly by P2 (the
  static-patch line is the same territory). Not the first shot.
- **Payoff class: the framework-level verdict (responsive-medium picture
  stands / needs restating).**

---

## 2. UNIQUENESS ROUTES

### U1 — U5/U6 deformability: the one route literally shaped "→ unique"

- **The pre-registered failure condition is the prize:** "if only **one**
  stable class exists → responsiveness is *unique* (a real result, still
  publishable)" (`GRUT_II_Agenda.md:43`; register sub_status: "FIRST-CLASS
  FAILURE CONDITION … NOT a failure of the exercise").
- **Factorization already banked at scaling level**
  (`calc/RESULTS_u5u6_deformability.md`): **KNOB 2 decidable** — "how many
  conserved charges beyond T_μν does the responsive vacuum carry?"; for pure
  gravity **both horns give count 1** (uniqueness at class level, conditional
  on the charge-content modeling input). **KNOB 1 research-grade** — the
  anomalous dimension at the interacting fixed point, with z = 1 itself an
  assumption, "a fixed-point (loop / ε-expansion) computation."
- **STATE.md's own ranking:** "the single live in-house-adjacent frontier is
  now that deformability proof."
- **Payoff class: UNIQUENESS ("responsiveness is unique"), publishable per its
  own charter — the legitimacy-building result class the owner asked about.**

### U2 — narrow uniqueness components already in hand (assemble, don't re-derive)

- T = H/2π "forced uniquely" by Hadamard/KMS given the background
  (`ROOT1_KERNEL_ORIGIN.md:91`) — missing a licence, a method non-perturbative
  in H, and a proved stationary reduction (the same gap as P4).
- a² as the unique integrating factor of the frozen mode equation; the D5
  *doctrine* unique (scheme requires ruling); L₀ unique classically (the trace
  anomaly co-breaks quantum-mechanically — `l0_r2_exact_unique_breaker`, open;
  the differentiator table: "IF proven: it would graduate mu_linear").
- Everything else is recorded **non-unique or NOT CLAIMED** (kernel selection,
  gauge, H² scheme, subsystem split) — uniqueness will not come from the
  kernel side without a new principle; the record is explicit.

---

## 3. THE MAP, RANKED (cost vs. decisiveness)

| # | route | first bounded instrument | cost | verdict guaranteed? | payoff if it lands |
|---|---|---|---|---|---|
| 1 | P2 clock-mismatch check | recompute rung3-ladder & rung7-τ₂ rates in one coordinate | **small, in-house, unblocked** | yes (artifact or strengthened) | possibly retracts the central adverse finding |
| 2 | P3 Λ_R matching condition | bounded search for an independently justified condition, fences pre-written | small–medium, in-house | yes IF a condition exists (a defensible "none exists" is also a recordable result) | axis-2 verdict: derives rung7's need or contradicts it |
| 3 | P2 ladder inheritance | the projected-kernel instrument (the "afternoon" question) | medium, static-patch territory | yes | comb-at-H signature or an interacting-Σ mechanism demand |
| 4 | U1 KNOB 2 | formalize the charge-content question; pure-gravity horn already → count 1 | medium, in-house-adjacent | conditional on the modeling input | **"responsiveness is unique"** (publishable per charter) |
| 5 | P1 Π₀ tranche | BUILD `calc/pi0_trace_channel.py` (attack item 3, never built) | medium–large, hardest in-house item | no — may return earned-under-determined | the only percent-level prediction path |
| 6 | U1 KNOB 1 | fixed-point RG of the reversible couplings | large, research-grade | no | closes the uniqueness horn unconditionally |
| 7 | P4 low-ω class | Decision C prerequisites (non-perturbative-in-H evaluator first) | largest; currently unlicensed | fork possible | framework-level verdict |

**Standing free exposure:** DESI DR3 (the no-crossing prediction) — watch, no work.
**Owed before any TT-auto kill-grade use:** the A6 Boltzmann-grade common-mode
check (OWED-OR-RETIRED, per the pre-registration).

## 4. WHAT THE MAP SAYS, IN ONE PARAGRAPH

The possibility the owner asked about is real and localized. There are exactly
**two prediction-shaped endpoints** (Π₀'s D2b branch — the percent-level
μ/Σ prediction — and the comb-at-spacing-H structural signature) and **one
uniqueness-shaped endpoint** (U5's one-class outcome, pre-registered as "a real
result, still publishable"). Everything else either adjudicates an internal
fork (Λ_R/axis 2, the low-ω class) — which is *direction*, because both sides
of each fork are contentful — or is recorded dead. The rational opening
sequence is bottom-up by cost: the clock-mismatch check (unblocked, small,
and capable of deleting the program's central adverse finding), then the Λ_R
matching-condition search and the ladder-inheritance instrument in either
order, with the Π₀ tranche build and KNOB 2 as the first medium-weight
commitments. Selection is the owner's; nothing here is opened by this document.

---

*Instrument, ledger 0. Sources: X_FLOOR_MAP.md; STATE.md; STAGE_CLOSE_2026-08-19.md;
calc/RESULTS_{u5u6_deformability, anomaly_c0_map, isw_tt_auto, isw_exclusion, x_no_pin}.md;
provenance/prereg/RESULT_KAPPA_2026-08-08.txt; GRUT_II_Agenda.md;
PHYSICS_LEDGER/{K_R_CONTRACT_OWNER_RULING, MICROSCOPIC_TARGET_BENCHMARK,
WALL_KR_OWNER_DECISION_PACKET, WALL_KR_OWNER_RULING_PHASE, WALL_KR_CONTRACT_BENCHMARK_VERDICT,
WALL_KR_AXIS2_H0_ADJUDICATION, WALL_KR_LAMBDA_R_OWNER_RULING, WALL_KR_D5_EXECUTION_VERDICT,
WALL_KR_H2_IR_OWNER_RULING, ROOT1_KERNEL_ORIGIN, WALL_KR_KERNEL_SELECTION,
WALL_KR_DISTINCTIVENESS_LEDGER}.md.*
