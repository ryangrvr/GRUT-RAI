# CLOCK-MISMATCH INSTRUMENT — VERDICT

**Date:** 2026-09-25 · **Charter:** `CLOCK_MISMATCH_CHARTER_01.md`,
pre-registration frozen at commit `b25d79f` **before** the instrument ran ·
**Instrument:** `calc/clock_mismatch_check.py` (pure stdlib, self-checking) ·
**Artifact:** `CLOCK_MISMATCH_RESULT.json` (sha `e3c757dd4c8ccb61…`) ·
**Battery: 23/23, zero failures, both controls detecting.**
Register untouched; ledger 0; nothing banked.

## THE VERDICT — (C)

> **The comparison remains underdetermined because the conversion itself
> requires an unresolved physical input.**

By the pre-registered §5 rule, mechanically:

- **Not (B).** The two filed clocks — static-patch Killing time and FRW cosmic
  time — are **the same clock on the shared comoving worldline** (D1, verified
  exactly from the hyperboloid embedding: T = t, worst residual 0.0e+00). No
  coordinate re-expression changes any number in the comparison. The mismatch
  is therefore not a coordinate artifact.
- **Not (A).** The shortfall is not invariant across the declared reading set:
  the H₀-frozen readings give D = 1.000 / 0.951 / 0.241 (all below t₉₀ = 4.32),
  while the e-fold promotion admits D = ln(1+z_on) ≥ 4.32 for any onset
  z_on ≥ 74. The verdict flips with the choice of an identification the
  register does not contain.
- **(C) holds** with the quantitative flip exhibited (charter requirement for
  a (C) that is not an escape) and the required inputs named:

**The named unresolved inputs (each flips or defines the verdict):**

1. **The dS→FRW kernel transport rule.** The toy kernel is exact on
   constant-H de Sitter; τ₂ lives on FRW with Ω_m ≠ 0 — *different solutions*
   (the 2026-08-19 X2 refusal). H_bath = H₀ (frozen) versus the adiabatic
   promotion H → H(z) are different unbanked rules; the register prices the
   question only as the +1 omission `background_time_translation_flow`, never
   as a usable rule.
2. **The onset/lag specification** (elapsed-since-when versus the relaxation
   constant τ₂ — the keystone map's surviving defect (i)). R4a is UNDEFINED
   (the filed lag 1/H₀ reaches before the initial time); R4b/R4c straddle t₉₀.
3. **The D3b reduction** — whether rung3's asserted object (static-patch tower
   / assembled TT response) reduces to an along-worldline kernel at all
   (wall A). Without it, the toy's licensed scope (D3a) does not cover the
   object the comparison is about.

## SUB-FINDINGS (scope, not verdict)

- **The clock concern itself is EXONERATED.** The addendum's "two clocks"
  framing dissolves at along-worldline scope: Killing time and cosmic time are
  the identity there. **The real seam is two backgrounds, not two clocks.**
  This matches the keystone map's second horn ("an artifact of scope rather
  than of coordinate") — now computed rather than anticipated.
- **The within-toy statement stands.** The control reproduction is exact
  (leading-rung share 6.12% at Ht = 1; t₉₀ = 4.32; t₉₉ = 6.68; the n²-weight
  mutant detected). Nothing here weakens the toy's own result in its own
  stationary clock.
- **Computed rider:** the filed lag τ₂ = 1/H₀ **exceeds the age of the
  universe** (H₀t₀ = 0.951 on the repo's declared Ω_m = 0.315 background) —
  the kernel is probed at a lag longer than the available history of the
  background it is applied to.
- **The e-fold escape is priced, not free:** across the R4b span (z = 74 → 0)
  the would-be ladder temperature T = H(z)/2π varies by a factor ~367; a
  fixed-T Matsubara structure does not exist across that span, so the
  promotion is a *different, unbanked kernel*, not a re-reading of the same
  one.

## WHAT THIS CHANGES — AND DOES NOT

- The stage's central adverse finding ("the cosmological sector operates about
  four e-folds short") is **neither deleted nor confirmed: it is re-typed.**
  As filed — a rung7/rung3 contradiction — it **cannot be filed**: its truth
  value is a function of an identification the register has never banked. As a
  within-toy statement it stands untouched.
- Any future filing of this comparison must first bank (and price) the
  transport rule it uses. The instrument makes that requirement concrete:
  three numbered inputs, each with the computed sensitivity attached.
- Proposed for the bank gate (NOT applied, per the no-banking fence): a
  `boundary_condition` annotation on `rung3_single_pole` and `rung7_wz`
  recording this verdict, so the comparison cannot be re-filed clockwise or
  background-blind a third time (it passed two pre-registrations before the
  addendum caught it).

## DEFECT HISTORY (disclosed)

Run 1: the embedding-typo control was blind at H = 1 (the erroneous factor H
is numerically invisible there) — rebuilt at H = 0.5 and now detecting.
Drafting: the R4 threshold was hardcoded (4.334) instead of sourced from the
reproduced t₉₀ — fixed to consume the control's own output. A sub-finding
printed a hardcoded ~372× where the computed factor is 367× — fixed to source
the computed value. All three are instrument defects, none touched a verdict
input.

## HARD STOP

Verdict (C) is recorded. The decision this stop waits on: **the owner reads
this verdict and selects the next instrument.** Per the owner's charter,
Λ_R, Matsubara-inheritance, Π₀, and U5 remain fenced until that selection.
