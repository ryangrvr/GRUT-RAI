# Interrogation brief — `p_tt_ansatz`: is the pure-TT projector FORCED or CHOSEN?

*Status: **EXECUTED 2026-08-02** — the interrogation was run by the overseer (five independent angles, each adversarially verified against the five kill-conditions below; core computation replicated in exact rational arithmetic). **Verdict: CHOSEN, unanimous** — diffeo invariance buys transversality and stops one condition short of tracelessness (Barnes–Rivers 6-dim → two Ward survivors P^(2) + P^(0,s); linearized EH is itself (1/2)k²[P^(2) − 2P^(0,s)], so no principle can force TT-only). Banked in `p_tt_ansatz.boundary_condition`; the question re-homed to `u4`/`u5` as "is ζ_vacuum = 0?"; `mu_linear`'s demotion opened as its own screened question. This brief is retained as the pre-registration record — the kill-conditions and the directional guard below are what the result was verified against.*

---

## Why this is the program's load-bearing question

The hand-inserted pure-TT projector (`p_tt_ansatz`, tier `assumed`, +1, confessed three times) is simultaneously:

1. **The root of the *scalar-sector* silence.** It annihilates the scalar/trace sector — which locks GRUT out of every scalar-sector cosmological anomaly (H₀, S8, low-ℓ) and out of the scalar-coupled laboratory channels. **Scope fence:** the program's empirical silence has at least three independent banked legs — P^TT (scalar-sector closure, *this* brief's subject), the Planck-magnitude suppression *within* the TT channel (rung4, 21–62 orders — untouched by this interrogation), and the rung3-conditional S(0)=0 decoherence quietness (rung8, itself open on the bath). Resolving P^TT resolves only the first leg.
2. **The foundation of the p_tt-gated phenomenology.** Per the banked dependency graph, the claims resting on P^TT are exactly `mu_linear` (μ=1=ΛCDM, a no-go export), `rung7_w2_wa_sign` (the wa-sign differentiator), and `rung9b_bridge`. (The sole fully-sourced prediction, w=−1 flat, is rung1/rung2/rung3-gated and stands *independent* of the projector.)
3. **Partly empirically selected** — the register already confesses this honestly: the trace-only branch (μ=4/3) was excluded by the ~32σ low-ℓ ISW + the separate-universe no-go, and P^TT inserted. The μ=1 phenomenology is thus partly an empirical input dressed as structure — banked, not hidden.
4. **The same projector whose orthogonality settled `rung9b` negative.** The classification outcome here retro-determines how that boundary reads.

**The stakes, stated before the work:** if P^TT is *forced*, GRUT's **scalar-sector** silence is a **consequence** — a derived property of the gravitational vacuum's response (the TT-magnitude and rung8 legs of the silence stand on their own, unaffected). If P^TT is *chosen*, the scalar-sector silence traces to **our assumption** — we picked that blindfold, and any scalar-sector observable would have to come from relaxing it inside the (i)–(iv) bar below. These are different programs. Which one we are in is currently unknown.

## The question, as a classification (u0 form)

**Q: Does the responsive-vacuum structure GRUT has already paid for (rung1 S_IF + rung2 KMS + background diffeo-Ward identities) FORCE the linear response into the pure spin-2/TT channel, or is the TT restriction an independent structural choice?**

Three first-class outcomes, all bankable:

- **(a) FORCED** — a derivation shows the scalar/trace linear response *vanishes identically* from the paid-for structure (action + Ward identities + passivity/KMS), with no reference to P^TT. Consequences: `p_tt_ansatz` graduates assumed→derived (−1 on the ledger, its own pre-screened change); `mu_linear`'s no-go export becomes clean; the *scalar-sector* silence becomes a theorem-grade property; `rung9b`'s **primary obstruction** (projector orthogonality) graduates from ansatz-backed to derived — *strengthening but not changing* the settled-negative, while its named rescue routes and the separate E4/S⁴ carrier front remain open exactly as banked. **This is the outcome the temptation runs toward — see the directional guard.**
- **(b) CHOSEN** — the analysis exhibits a nonvanishing trace-sector response admissible under the **full paid-for structure named in Q**: (i) realizable within the rung1 S_IF class, (ii) KMS-passive per rung2 (*not* merely Im χ ≥ 0 — the full detailed-balance lock), (iii) diffeo-Ward-consistent, **and** (iv) surviving the banked μ=4/3 / ~32σ ISW fence (admissible trace response strictly *below* the bound, not at μ=4/3). A channel failing any of (i)–(iv) is not a counterexample. If such a channel exists, P^TT is a genuine selection — partly empirical (the ISW exclusion), partly aesthetic. Consequences: the +1 stays; every "GRUT predicts ΛCDM" statement must read "GRUT-with-P^TT predicts ΛCDM"; the honest search space for any observable is the relaxation of the projector inside (i)–(iv).
- **(c) UNDER-DETERMINED** — the vanishing/non-vanishing of the trace response gates on unresolved structure (the bath Hilbert space, the conformal/Riegert–Paneitz sector, the coarse-graining choice). Consequences: named blocker, edged to the gating claim (likely rung3's bath question or u3's split origin); `p_tt_ansatz` stays assumed +1 with the blocker named in its `overturning_computation` (the claim carries no `boundary_condition` field; banking (c) either points there or explicitly adds the field).

## Pre-registered kill-conditions (any of these = the corresponding outcome CANNOT bank)

1. **The circular check is FORBIDDEN** (already banked in `mu_linear`): "confirm P^TT annihilates scalars" re-detects the inserted ansatz. Any "forced" argument that *presupposes* the projector — in the propagator, the measure, or the mode decomposition — is the circularity relocated. The derivation must start from the un-projected action.
2. **Ward-identity over-reach.** The diffeo-Ward identity alone does NOT force Einstein–Hilbert or pure-TT — it equally permits R²/f(R)/Lovelock, with f(R)'s propagating scalar mode as the standing counterexample (the banked GR-hosting fact: `rung5_gr_limit` / `NO_GO_LEDGER.md` entry 6 / `POSTULATE_MAP.md`'s "GR is borrowed, not hosted"; a dedicated gr-hosting node is *planned, not yet in the register*). Any "forced" claim resting solely on diffeomorphism invariance is over-claimed and dies.
3. **Adiabatic-mode relocation.** If the vanishing of the scalar response is obtained by inserting the Weinberg adiabatic mode / the ω↔x map by hand, that is the L0_r2 relocation pattern — the input moved, not discharged.
4. **Passivity misuse.** Passivity (ω·Im χ positive-semidefinite as a matrix) constrains signs and cross-channel magnitudes; it can *propagate* a channel's vanishing (a zero diagonal kills its cross-couplings) but can never *source* one. Any argument deriving channel annihilation from passivity *alone* is a category error and dies.
5. **Consistency-of-consequences check (both directions):** if "forced" arrives via a mechanism that would *also* force the trace response to vanish in systems where trace responses demonstrably exist (any real dielectric/viscoelastic medium has one), the mechanism proves too much and dies. **Pre-registered defense space:** a vacuum/media distinguisher is legitimate only if it is itself *paid-for structure* (e.g., an exact symmetry of the banked vacuum state) — and note that rung2's KMS/finite-T leg supplies a preferred thermal frame, so a "T=0 Lorentz invariance" defense must explicitly confront the KMS leg rather than assume it away.

## The directional-optimism guard (scrutinize THIS direction hardest)

**The temptation runs toward "forced," because it dignifies the silence** — it converts the program's scalar-sector emptiness from a cost we paid into a theorem we discovered. Prior firewalls in this program have repeatedly caught the builder leaning toward the flattering horn (rigid/unique on u5; "favors HOLDS" on rung3; "structural" on the arrow). The screen must therefore demand *more* of a "forced" verdict than of a "chosen" verdict: outcome (a) requires an explicit derivation surviving kill-conditions 1–5 **plus** an independent adversarial reproduction; outcome (b) requires one counterexample channel meeting the full (i)–(iv) admissibility bar above — a single channel, but against the *complete* paid-for structure, not a weakened proxy of it. This asymmetry is deliberate and pre-registered.

## Deliverables (for the eventual run — NOT this document)

1. The un-projected linear-response object: the full χ_μνρσ(ω,k) the paid-for S_IF defines, decomposed spin-0/spin-1/spin-2 *without* inserting P^TT. **Frontier-reservation (binding):** the conformal/Riegert–Paneitz portion of this decomposition is **FRONTIER-RESERVED — specialist, not in-house** — per `p_tt_ansatz.overturning_computation` and `mu_linear.boundary_condition` (and l0_r2's "do NOT re-attempt in-house" precedent). The in-house-safe scope is only the projector-independent bookkeeping and the checklist below.
2. The verdict per outcome (a)/(b)/(c) with the kill-condition checklist run line-by-line.
3. If (b): the admissible trace-channel family surviving the full (i)–(iv) bar (the honest observable search space).
4. If (c): the named gating claim and the edge to it.

## Relation to standing work

- `mu_linear.boundary_condition` already names this as its settling check (paraphrasing the banked text: deriving the scalar projection's vanishing from the action *without* inserting P^TT is open and partly frontier-reserved, overlapping the conformal/Riegert–Paneitz sector). This brief is that check, promoted to the program's front burner with its guards pre-registered.
- Overlaps the GR-hosting question (banked in `rung5_gr_limit` / `NO_GO_LEDGER.md`; a dedicated node is planned, not yet in the register): "can responsiveness host EH?" is the same question one level up.
- The u5/u6 bracket structure bears on it: the T_μν-forced Model-H bracket lives in the conserved-momentum (vector) sector — whether the gravitational analog *forces* spin-2/TT channel-purity or merely *permits* it is exactly this question in universality language.

*Per the standing invariant: this brief poses; it does not resolve. No derivation has been attempted. Screen before any dispatch or in-house run.*
