# RELAY — Part 4 of the x-floor brief: the kernel-not-constant reframe

> **RULED 2026-08-09 (overseer):** recommendation ACCEPTED — no new dial books (the freedom is
> priced at `eft_operator_basis`, count-once); the cut-conditionality qualifier and the R1
> discharged-by-construction note are APPLIED at `zeta_interior_family` / `mu_slip_interior.py`.
> Separately ruled in the same return: the staged node's tier resolution was REJECTED (the
> cited-not-edged precedent mis-read) and the node SPLIT — general lemma `passivity_channel_diagonal`
> (shown) + application `x_no_pin_theorem` (derived-pending); the KK static-transfer gap promoted to
> register node `kk_static_transfer`; and the stage-close growth exception logged as a narrow standing
> policy (see STAGE_CLOSE_2026-08-09.md addendum), not a precedent-by-momentum.

*To the overseer, before any ledger field moves — per the brief's own rider ("Relay the
reasoning before touching any ledger field; this is the one part that can move +13").
No register edit accompanies this document beyond the Part-1 node already staged and held
at the bank gate. Builder: 2026-08-09.*

## The reframe, stated

`S_IF.md` §5 declares x(ω,k²) ≡ c₀(ω,k²)/c₀^{trace-only}(ω,k²), a KERNEL unless something
forces it constant, and nothing in the corpus does. Consequence: **the constant-x interior
family is a one-dimensional cut through a function space**, and every artifact quoting "one
free parameter" has been understating the freedom. The brief instructs: default-broken toward
booking — if this means the family carries more unpriced freedom than banked, that is an input
and it books.

## The builder's analysis — both directions, honestly

**The case FOR booking (the brief's default):** the program's quotable surface (the window,
the natural-point verdicts, the D2 sub-fork) has been stated in constant-x language. If that
language hid a function's worth of freedom, the price was understated and a dial books.

**The case AGAINST booking now (what the register already says):** the freedom was never
unpriced — it is carried at `eft_operator_basis`, whose enumeration explicitly delivers the
survivors "with arbitrary coefficient functions of (omega, k^2)" (the calc's own words), and
`zeta_interior_family.ledger_note` already booked the COLLAPSE to constant-x at Δ0 on the
ground that "posing a to-derive restriction asserts no input — the collapse is this claim's
question, not its answer; booking happens at ACCEPTANCE (the rung7 precedent)." That warrant
was corrected 2026-08-09 (the consumed_by registration + armed trigger, after the TT-auto gate
came to rest on the collapse). So the register's standing position is: the function space is
the priced object; constant-x is a RESTRICTION of it, not an expansion; and the restriction's
booking fires at acceptance of anything that consumes it — with the trigger already armed.

Declaring x a kernel therefore does not ADD freedom to the register — it un-hides that the
constant-x cut was a cut. What it changes is **which artifacts are now known to be
cut-conditional**: every constant-x number (the x < ~0.59 window edge, the natural-point
verdicts, the TT-auto gate's κ-conditional edges) is conditional on "x approximately constant
over the observational band," an assumption with no banked support and three corpus lines
pointing against it (rung3_spectral_structure's frequency-resolved passivity; R1's
x_anom(k) form factor; the κ wave's unbanked k-structure finding).

## The builder's recommendation (yours to overrule; the default-broken instruction is noted)

1. **No new ledger dial books today.** The input is already priced at the family level and the
   collapse's Δ0 carries a corrected, trigger-armed warrant. Booking a second dial for the same
   freedom would double-home it (the count-once rule).
2. **What should move instead is prose + one qualifier, both flagged-not-silent:**
   - `zeta_interior_family`: record the declaration and the cut-conditionality — every
     constant-x number quoted from this node carries "CONDITIONAL on x ≈ constant over the
     band (declared, unsupported; see S_IF.md §5)" — the same shape as the banked
     CONDITIONAL-on-CHOSEN qualifier.
   - `mu_slip_interior` R1 (the x↔c₀ action-level map): marked DISCHARGED-BY-CONSTRUCTION,
     conditional on your acceptance of S_IF.md's declaration — a definition closes it, which
     is exactly what the file said the deeper step was.
3. **If you rule the other way** (the default-broken direction): the booking's natural home is
   `zeta_interior_family` (+1, "the constant-x restriction adopted as a working model of an
   un-derived kernel"), and it must be priced ONCE — either there or at any future acceptance
   of a gate number that consumes the collapse, never both. The armed trigger in the
   ledger_note would then be superseded and should be retired in the same edit.

## Also awaiting your acceptance from this wave (the bank gate holds them)

- The staged node `passivity_channel_diagonal` (shown, Δ0, NEW-FLAG at the gate; the gate's
  TIER-CONTRADICTION catch on the eft_operator_basis edge was resolved by the
  cited-not-edged precedent, recorded in the node's sub_status — reverse if you disagree).
- `bankgate.py --accept` after your review refreshes the baseline (the emergence_chain flag
  from the stage-close wave is also still standing).

*One process note, recorded without euphemism: this wave adds a node and two documents four
hours after your stage close ruled "hold state, not grow." The justification relied on: the
brief is yours, the node is physics (the register's own named attack item 2), and no guard was
added — but the tension is real and yours to adjudicate, not the builder's to wave through.*
