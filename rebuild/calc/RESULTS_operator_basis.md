# The open-EFT operator basis — forced-vs-chosen instrument (in-house calc + verdict)

*The instrument the `p_tt` interrogation runs on: enumerate the admissible tensor structures for the quadratic influence action under the register-declared symmetries only, and test whether P^TT is unique. Code: `calc/operator_basis.py` (pure stdlib, self-tested, multiple k̂). Register: `eft_operator_basis` (`to-derive`, ledger 0). **p_tt_ansatz is NOT an input — it is the object under test.** Default-BROKEN; the ledger fence is hard: no ledger field touched this wave.*

---

## Part 1 — the symmetry inventory (read from the register, not invented)

| # | Symmetry / structure | Declaring claim (sourced) |
|---|---|---|
| S1 | Background Lorentzian causal structure → retarded/causal K_R | `rung1_inin_action` (+3 declared inputs, ledger_note) |
| S2 | Gaussian/linear-response truncation → quadratic action | `rung1_inin_action` (+3 declared) |
| S3 | The system/bath split → the influence-functional setting | `rung1_inin_action` (+3 declared) |
| S4 | KMS/FDT lock + passivity (ω·Im χ ⪰ 0, matrix sense) | `rung2_kms_gate` (hard admission gate) |
| S5 | Diffeo invariance **as assumed**, with the banked Ward limitation (the identity permits EH/R²/f(R)/Lovelock alike) | `rung5_gr_limit` (statement + overturning_computation) |
| S6 | Background spatial homogeneity + isotropy (SO(3) at fixed k); parity-even inherited from the unperturbed background (no parity-violating input declared anywhere) | `mu_linear`'s frame (FRW linear cosmology) |
| S7 | Pair symmetry K_{ij,ab}=K_{ab,ij} (Onsager reciprocity) — automatic for the noise kernel; for the *retarded* kernel a substantive microscopic time-reversal assumption not implied by S1–S6; same "inherited, not chosen" status as parity (no time-reversal-breaking input declared). Robustness (firewall-verified): either of {parity-even, pair-symmetric} *alone* excludes the TT-birefringence structure | inherited (no declaring claim — flagged as such) |

**Not inputs:** P^TT (`p_tt_ansatz` — under test); α's placement (`rung9a`/`rung9b` — tested in Part E).

## The frame and the exhaustiveness statement (the KC5 fence, up front)

Spatial SVT frame at fixed background slicing and fixed k — **the same frame in which `p_tt_ansatz` and `mu_linear` state P^TT**. Within it the enumeration is exhaustive **to all derivative orders** (every SO(3)-invariant, parity-even, pair-symmetric kernel decomposes on the enumerated structures with arbitrary coefficient functions of (ω, k²) — k̂ is the only available direction). Not covered: parity-odd structures (excluded by the parity-even background); pair-antisymmetric parity-even structures (exactly **one** exists at this frame — the antisymmetric trace-longitudinal mixer — excluded by the same Ward test, so relaxing S7 alone changes nothing); and the **4d-covariant completion** (h_0μ, time-dependent gauge, conformal/Riegert–Paneitz sector) — **frontier-reserved** per `p_tt_ansatz.overturning_computation` and `mu_linear.boundary_condition`. All uniqueness statements are **at this frame/order**.

## Results (every number self-tested, three k̂'s)

- **Part A — the projector algebra verified**: completeness P2+P1+P0s+P0w = 1 (∼10⁻¹⁶), idempotence, mutual orthogonality, dof counts (2, 2, 1, 1).
- **Part B — the exhaustive count, COMPUTED**: the isotropic, parity-even, pair-symmetric kernel space at fixed (ω,k) has **rank 5** ({P2, P1, P0s, P0w, MIX}) — and exhaustiveness is now *computed, not asserted*: the self-test builds the little-group (rotations about k̂ + a reflection) rep and finds the pair-symmetric commutant is exactly **5-dimensional** (full commutant **6**: the +1 is the single pair-antisymmetric mixer, itself Ward-killed). P^TT is one of five *before* gauge constraints.
- **Part C — the Ward/gauge test (S5)**: the linearized-diffeo orbit g(ξ)_ij = k_(i ξ_j) is **annihilated by P2 and P0s** (∼10⁻¹⁶) and **hit by P1, P0w, MIX**. With the helicity argument (TT = helicity ±2, transverse-trace = helicity 0 → no invariant mixing), the Ward+isotropy-consistent family is **exactly two independent coefficient functions**: **K = c₂(ω,k²)·P^TT + c₀(ω,k²)·P0s**. **Gauge-footing asymmetry (stated, not hidden):** the two survivors are *not* on equal footing — P2 is invariant under the **full** linearized 4d diffeo group on FRW (helicity protection), while P0s's transverse trace is invariant only under the **spatial subgroup at fixed slicing**; its full 4d-covariant status requires the Bardeen completion — exactly the frontier-reserved portion.
- **Part D — the scalar channel is passivity-admissible**: exhibited (Debye c₀: ω·Im χ₀ ≥ 0 on a grid). S1 constrains coefficient analyticity, S4 constrains signs — **neither kills the scalar structure** (consistent with the banked matrix-passivity fence: passivity propagates a vanishing, never sources one).
- **Part E — the α coherence check**: tr[P0s·P2] = 0 exactly; P2's output is exactly tracefree. **α·χ·P^TT is formally admissible but the α value cannot be transported from the trace sector within the basis** — c₂=α (this file's name for the P^TT coefficient; the register's historical name for the same adopted constant is c₀) is an *adopted* external normalization. This **re-verifies, within the enumeration, the same orthogonality fact that is `rung9b`'s banked primary obstruction** — a *coherence check*, not new independent evidence (no new route or input enters) — and restates the `p_tt_ansatz` tier_note tension ("two orthogonal sectors bolted together").
- **Part F — SCDP comparison, fenced to banked facts**: the mainstream open-EFT names the *tensor*-sector friction Γ_T explicitly (banked); the existence of a named tensor friction **does not assert TT-uniqueness**; whether their full basis carries scalar-sector dissipative operators is **not banked** — the full-basis comparison *owes a primary-literature read* (overseer-verifiable).

## Verdict (default-BROKEN; graduates nothing; at the enumerated frame/order)

**(b) CHOSEN at this frame/order.** P^TT is **not unique**: under the complete declared inventory the admissible kernel family is **{c₂·P^TT + c₀·P0s}**, and the scalar channel is passivity-satisfiable. **Restricting c₀ = 0 is exactly the `p_tt_ansatz` choice — an input, not a consequence, at this order.** The full-covariant-order question stays **open** (that is the node's `to-derive` content, and the frontier-reserved portion of the p_tt interrogation).

**KC checklist (run line-by-line):** KC1 clean — the enumeration starts from full symmetric rank-2; TT never enters as input. KC2 clean — no radiative-dof argument used; the scalar survivor is *constrained-sector* response, exactly the KC2 distinction. KC3 clean — no empirical input enters the enumeration. KC4 — **witness**: the surviving scalar is *naturally identified with* the f(R)/scalar-tensor response channel (f(R)'s scalaron sources the helicity-0 transverse-trace sector at linear order), so the basis correctly fails to force TT in theories known to carry a scalar response. KC5 — **fence**: frame/order bounds stated above; no uniqueness claim beyond them.

**Directional guard:** the result lands on the **non-flattering** horn under the deliberately light bar ((b) = exhibit one admissible non-TT structure; exhibited). Had it landed "forced," it would have required surviving all five KCs *plus* independent adversarial reproduction before banking.

**Ledger fence (hard):** no ledger field touched. `eft_operator_basis` banks at 0 (posing a classification adds no input). Any `p_tt_ansatz` consequence — including the (b)-at-order result's implication that the +1 is *correctly* booked — is a separate screen; the p_tt interrogation (`BRIEF_p_tt_interrogation.md`) now runs **on this instrument**.

## Independent firewall (2026-08-02) — both directions, amber→green after fixes

Three lenses ran before acceptance: an independent physics/math referee (who **re-derived every computation** in a separate helicity-frame construction at four k̂'s, and independently computed the little-group commutant, closing the exhaustiveness gap), a fidelity/KC lens, and the adjudicator. **No blockers**: no wrong computation, no KC violation, no frame-level result read as full-order; the (b)-CHOSEN verdict and the f(R) witness confirmed apt. Required fixes, all applied: the **c₀/c₂ label inversion** at the α-placement crux (the coefficient set to α is the P^TT one — c₂ in this file's notation; the register's historical name is c₀); **"independently confirms rung9b" demoted to coherence-check language** (Part E recomputes rung9b's already-banked primary obstruction — no new route, no new evidence); the **gauge-footing asymmetry** between the two survivors stated (P2 full-4d-invariant; P0s spatial-subgroup-only, Bardeen completion frontier-reserved); **S7 pair symmetry** added to the inventory as its own flagged input (Onsager reciprocity — substantive for the retarded kernel, not implied by S1–S6) with the verified robustness note; **exhaustiveness computed** in the self-test (commutant dims 6/5); the KC5 fence extended with the pair-antisymmetric fact; KC4's "is" softened to "naturally identified with"; the helicity-note attribution corrected (Part B's exhaustive count + Schur, not Part A's orthogonality). Lineage fix banked on the node: `rung5_gr_limit` added to `depends_on` (the Ward test is load-bearing); `mu_linear` cited for the frame but deliberately not edged (avoids a spurious lineage path to the object under test).
