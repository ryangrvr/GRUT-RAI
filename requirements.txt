# GRUT-RAI runtime dependencies — tested versions
# Install: pip install -r requirements.txt
#
# Python: 3.10, 3.11, 3.12, 3.13 supported
# Python 3.14 / 3.15 NOT supported (numpy doesn't yet have wheels)

numpy>=1.24,<3.0
scipy>=1.10,<2.0
flask>=2.3,<4.0
python-dotenv>=1.0

# RAI Chat (Claude) — the AI narrative layer of GRUT-RAI.
# The chat.py module imports `anthropic` unconditionally; without this
# dependency installed, the RAI Chat tab silently falls back to keyword
# matching instead of calling Claude. Install it.
# If you don't set ANTHROPIC_API_KEY the chat still falls back, but the
# package needs to be present for the import to succeed.
anthropic>=0.30

# Testing
pytest>=7.0
