# GRUT-RAI runtime dependencies — tested versions
# Install: pip install -r requirements.txt
#
# Python: 3.10, 3.11, 3.12, 3.13 supported
# Python 3.14 / 3.15 NOT supported (numpy doesn't yet have wheels)

numpy>=1.24,<3.0
scipy>=1.10,<2.0
flask>=2.3,<4.0
python-dotenv>=1.0

# Optional: AI chat tab (Claude integration)
# Uncomment if you have an ANTHROPIC_API_KEY
# anthropic>=0.30

# Testing
pytest>=7.0
