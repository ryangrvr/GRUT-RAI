# GRUT_EMERGENCE_THEORY_STATUS_01 — what theory now exists, and the one test that produces an answer

**Date:** 2026-09-24. **Mode: theory-finding** (owner directive: machinery only where it
changes an answer). Sources: the VS Code U3 suite (16 calcs, read at verdict level),
`GRUT_EMERGENCE_HYPOTHESIS.md` (Sep 23 register map — now partly superseded by the Sep 24
calcs), the certified T3 record, the RRP core. Grade: assessment, unchallenged.

## 1. There is now a candidate theory, and it is materially different from old GRUT

**The candidate, in one paragraph:** reality is a finite, strictly local substrate whose
infinite-volume limit *generates* a continuum of persistent memory modes; the response
kernel K and its spectrum ρ(τ) are the theory's central objects; dissipation, passivity,
the kernel's form (Bernstein–Widder positive), and the system/bath *representation* are
all **derived** from the local ontology; gravity is the candidate universal bath (right
spectral class, TT selection emerging unimposed); the free sector — proven free within the
admitted principles — is {ρ(τ)'s support/dimension N, the macroscopic scale τ₀, the
coupling class, the arrow's direction}.

Three ways this is beyond the old program: **(i)** the continuum bath, dissipation, and
the split-as-representation are now *derived*, not postulated (continuum_origin 6/6;
infinite_bath 4/4; the U3-LEMMA: memory ⟺ persistent auxiliary variables) — the
hypothesis doc's own "first rung" (L0/L1, u3_split_origin) has materially moved: what
remains of it is only "why few modes," the sharp form of the old single-pole question.
**(ii)** The free inputs are not suspected free — they are *proven* free by six dedicated
no-go calcs (scale_origin, resistive_scale_generation, gravitational_clock,
geometry_spectral_selection, KMS-constraint, D-SELECT): the theory has honestly mapped its
own priced inventory, in exactly the RRP form (derived structure + supplied point).
**(iii)** The old single-pole bet is dead *by the program's own certified calculation* —
T3's absorptive response is branch-cut class — and the revised theory absorbs this
correctly: ρ(τ) with continuum support is now the central object, the single pole its
one-mode limit.

## 2. Stop digging where the ground is proven empty

Six in-house no-gos plus the RRP architecture verdict agree: N and τ₀ will not be derived
from the admitted principles. Their own R-UNIQ result shows ρ(τ) is *uniquely measurable
from K* (Stieltjes). So the free sector is an **empirical anchor to be measured, not a
derivation target** — the theory's content is the derived FORM (simple positive poles
only; passivity; branch-cut classes under gravitational coupling; the sign exports) plus
the gravity-as-bath identification. Every further calc aimed at deriving N or τ₀ is
machinery without an answer at the end.

## 3. The decisive test is available in-house: the T3 confrontation

The program already holds a certified piece of reality: **the T3 absorptive graviton
response** (Im Σ_R^TT through H⁶; leading flat-limit class ω⁴; power-law tails Δ⁻⁵, Δ⁻³,
Δ⁻¹; dissipative sign; validity ω ≳ 3.4H). The U3 mechanism, with the coupling instrument
repaired, predicts gravitational memory-kernel classes. **Confront them.** Spec:

- **T-1 (kernel class):** the repaired on-shell coupling's predicted matter-sector kernel
  class versus the tail structure T3 certifies, compared *inside T3's validity window
  only*. Both are power-law/branch-cut or the identification is dead at a certified wall.
- **T-2 (the soft-theorem gate — established physics constrains the vertex before any
  run):** total momentum conservation plus the equivalence principle forbid monopole and
  dipole graviton radiation; the leading *radiative* coupling of internal matter motion is
  quadrupole. Standard expectation therefore: the physical IR class is the **tidal/
  quadrupole class (J ~ ω⁵, K ~ t⁻⁶)** — V10's "control" may in fact be the physical
  vertex, and the minimal-stress ω³ prediction may be soft-theorem-violating. The
  repaired instrument's pre-registered prediction must be checked against the Weinberg
  soft-graviton constraint (already in the audited corpus) before the run. If the
  golden-rule calculation lands on the quadrupole class, that is not a failure — it is
  the derivation agreeing with the quadrupole formula, i.e., with reality.
- **T-3 (sign/passivity):** U3 derives strict passivity; T3 certifies the dissipative
  sign in-window. Must agree (they currently do).
- **T-4 (the d-consistency link):** d enters twice — the substrate continuum class and
  the graviton DOS are both ω^(d−1). One supplied d must serve both sectors; any tension
  is a kill.

**Outcomes, pre-registered:** class match on T-1/T-2/T-3 = the first discriminator-grade
internal support the program has ever had (two independently produced objects — certified
loop QFT and substrate mechanism — agreeing on non-fitted exponent classes). Mismatch =
the gravity-as-bath identification dies at a certified wall, cleanly. Either is an
*answer*, which is the point.

## 4. What the theory still owes before it is "working"

At hypothesis-map level, honestly: L7/L8 (quantum limit imported; Born outcomes not
derived — and RRP/Experiment-P say the tested routes cannot derive them: treat the Born
weights as anchor, not target); L11 (SM silent — leave silent; the RRP core says any
theory must satisfy the anomaly conditions, and the U3 substrate has not touched matter
content); the DESI w(z) sign exports remain the live external kill-channel. A "working
theory" claim requires, minimally: the T3 confrontation passed, ρ(τ) constrained by at
least one measured K, and the sign exports surviving DESI. Nothing less.

## 5. Standing machinery item (the only one that matters)

The on-shell golden-rule repair of the coupling vertex (U3_COUPLING_ADJUDICATION_01 F3) —
kept, because T-1/T-2 cannot run without it. All cosmetic repairs deferred.
