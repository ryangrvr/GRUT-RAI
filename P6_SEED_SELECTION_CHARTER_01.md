# P-6 — IS THE ACCESS SEED DYNAMICALLY SELECTABLE? OWNER RULING RECORD + CHARTER + PRE-REGISTRATION (frozen before evaluation)

**Date:** 2026-09-25 · **Authority:** GitHub Issue #2, owner comment
**5827344342**. **P-5 accepted at recorded strength**, with binding
wording: *"the access boundary is currently an unresolved structural
input"* is acceptable; "access is fundamental" NOT established; "access
is arbitrary bookkeeping" FALSIFIED by the boundary-change construction
in-class; "access is dynamically selected" NOT established; the access
seed is NOT to be called a new law merely because it remains non-derived.
The chain under test: influence hierarchy → sector structure → **access
seed?** → effective physics.

**Question (owner, verbatim in substance):** given a microscopic
dynamical theory and a candidate full influence hierarchy, does any
nontrivial principle actually select the physically relevant access
algebra/coupling seed, or is the seed irreducibly supplied? The candidate
selector under attack: **minimality/dynamical closure**. Binding
discipline: *minimal given a chosen interface/probe ≠ uniquely selected
by the dynamics*; entropy, complexity, least action, and "simplicity"
are PROHIBITED as selectors here (each would need its own charter and
counterexample battery); type-III stays a boundary map; gravity
ω⁷/class-4 and geometry fenced until the P-6 verdict; ℏ
located-not-generated; no proof-of-QM. All models exact finite spin
systems; noncommutative first (owner point G): every leg below uses
noncommuting candidate seed algebras except the declared degenerate
control.

**Machinery (frozen):** the *dynamical closure* cl(S; H) of a seed set S
is the smallest unital product-closed span containing S and closed under
ad_H = [H, ·] (Hilbert–Schmidt Gram–Schmidt, iterated to stability, one
extra sweep as the stability control). Where a leg says
*product-closure*, ad_H is deliberately excluded (leg D2's sufficiency
criterion). Subspace equality is measured by two-sided projection
residual (< 1e-8 = equal).

## 1. THE LEGS (frozen)

- **L-S (positive control — "selector works," owner deliverable 3):**
  reducible dynamics H_S = 0.5σ_z¹ + 0.8σ_x²σ_x³ + 0.3σ_z² (spin 1
  decoupled). **Gates:** cl({I, σ_x¹}; H_S) has dim = 4 (the site-1
  algebra exactly) and zero overlap (< 1e-9) with the hidden-sector
  operators (σ_z², σ_x²σ_x³). Frozen reading: dynamics DOES select
  access **exactly up to its own dynamically closed (block/central)
  decomposition** — SELECTED-IN-CLASS for reducible dynamics; standard
  central-decomposition structure, so NULL-REDUNDANT as a new principle.
- **L-A (minimality attack — interface-relativity, owner point A):**
  irreducible-block dynamics H_A = 0.9σ_z¹ + 1.1σ_z² + 0.7σ_x¹σ_x²
  (commutes with parity P = σ_z¹σ_z²). Two operational interfaces:
  I₁ = {σ_z¹} (parity-even) vs I₂ = {σ_x¹} (parity-odd). **Gates:**
  cl({I, σ_z¹}; H_A) = the parity commutant, dim 8; cl({I, σ_x¹}; H_A) =
  full, dim 16; the two minimal algebras DIFFER. Frozen consequence:
  minimality is **interface-relative** — different declared interfaces
  select different "minimal" algebras; no unique dynamics-selected seed.
- **L-B/E (causal + conservation attack and the counterexample
  requirement, owner points B, E):** 3-spin chain H_B = 0.9σ_z¹ +
  1.1σ_z² + 1.3σ_z³ + 0.7σ_x¹σ_x² + 0.6σ_x²σ_x³ (identical causal graph
  1–2–3 and the same conservation law P = σ_z¹σ_z²σ_z³ for both seeds).
  Seeds: {σ_z¹} (left end) vs {σ_z³} (right end) — both parity-even, so
  both closures ⊆ the 32-dim parity commutant (analytic; frozen gate
  dim ≤ 32 each). Measured: whether the two closures are EQUAL as
  subspaces (reported either way). Coupled physics: probe pure dephasing
  (λ = 1) through B = 0.5σ_z¹ vs B = 0.5σ_z³, initial |↓↓↓⟩, t ∈ (0, 10]
  (200 pts) — **gate: coherences differ > 0.01.** Frozen two-branch
  adjudication: closures EQUAL ∧ physics differs ⇒ the closure selector
  is **non-injective over physically inequivalent seeds** (it cannot be
  the selector) — UNDERDETERMINED-SEED, and owner point E's
  counterexample is DISCHARGED (two inequivalent seeds, identical causal
  graph + conservation + closure-admissibility, different coupled
  physics); closures UNEQUAL ⇒ causal structure + conservation admit
  inequivalent minimal algebras from symmetric data — non-selection
  again, SEED-RELATIVE, and E is discharged by the same pair. Either
  branch refutes unique causal selection; which branch obtains is
  reported, never chosen.
- **L-C (symmetry attack — degenerate control, owner points C, G):**
  H_C = 0.9(σ_z¹ + σ_z²) + 0.7σ_x¹σ_x², exactly swap-symmetric
  (W H_C W† = H_C, halt-grade < 1e-12). Degenerate seed orbit {σ_z¹} vs
  {σ_z²}. **Gates:** coherences through B = 0.5σ_z¹ vs 0.5σ_z² identical
  < 1e-9 (analytic, halt-grade); the closures are W-images of each other
  (W·cl₁·W† = cl₂, residual < 1e-8). Symmetry-broken companion
  H_C′ = H_C + 0.3σ_z²: coherences differ > 0.01. Frozen consequence:
  symmetry **constrains equivariantly but cannot select on a degenerate
  orbit** — any covariant selector is indifferent between
  symmetry-related seeds; the choice is supplied (or spontaneous), not
  derived.
- **L-D1 (predictive sufficiency is order-relative, owner point D):**
  reuse the P-4 order-6 pair (3-spin baths, y_A = (1, 2, 3) vs y_B
  solved in-run, equal p₁, p₂, different p₃, common ω₀ = 1): in-access
  hierarchy identical through declared order 4 (m₂, m₄ and C(t) match
  < 1e-9); the **newly coupled probe** (λ = 0.4 instead of the declared
  weak probe) separates them — **gates:** Δ(λ = 0.4) > 1e-6 and
  λ-scaling ratio Δ(0.4)/Δ(0.2) within [0.7, 1.4] × 2⁶. Frozen
  consequence: "minimal sufficient access at declared order n" fails at
  order n+2 — sufficiency-based selection is order-relative, never
  final (P-4's ladder consumed at seed level).
- **L-D2 (explicit path-dependence test, owner deliverable 4):** on H_A,
  target interface B₀ = σ_x¹; K = the ad_H-orbit span of B₀ (iterated to
  stability). Sufficiency criterion for a seed-extension S ⊆ pool:
  product-closure (NO ad_H) of {I, B₀} ∪ S ⊇ K. Frozen ordered pool:
  G1 = σ_z¹, G2 = σ_y¹, G3 = σ_x¹σ_x², G4 = σ_z², G5 = σ_y¹σ_x².
  Backward elimination from the full pool in FORWARD order (G1 first)
  vs REVERSE order (G5 first); each result verified sufficient and
  minimal (no single remaining element removable). **Gate:**
  M_fwd ≠ M_rev — two different minimal sufficient seeds for the SAME
  interface and dynamics ⇒ **PATH-DEPENDENT**. (If they coincide, the
  leg reports PATH-INDEPENDENT-IN-CASE as found; the gate is the
  construction's honesty, not its success.)
- **L-F (representation control, owner point F):** on the L-B chain with
  seed σ_z¹: entangling V = exp(−i·0.6σ_x²σ_z³); transform (H, B, ψ₀).
  **Gates:** coherence invariant < 1e-9 (halt-grade analytic identity);
  the closure transforms covariantly, V·cl(S; H)·V† = cl(VSV†; VHV†)
  (residual < 1e-8). Refactorization cannot masquerade as seed
  selection (D-1/P-5 control preserved).

## 2. OUTCOME TAXONOMY (owner minimum, frozen mechanical)

**SELECTED-IN-CLASS** (L-S: reducible dynamics selects its blocks; no
further) · **PATH-DEPENDENT** (L-D2 gate) · **UNDERDETERMINED-SEED**
(L-A interface-relativity; L-B/E's branch; L-C degeneracy; L-D1
order-relativity) · **REPRESENTATIONAL** (L-F) · **NULL-REDUNDANT**
(closure = bicommutant-standard; block selection = central
decomposition — standard structure) · **FAILS** (any construction
failing). Components reported separately, never averaged. Decision rule
frozen from the ruling: a genuine selector found ⇒ record as CANDIDATE
selection law + immediate fresh counterexample attack before any
promotion; path dependence or inequivalent admissible seeds ⇒ the access
seed is retained as **non-derived input** and the owner may open
geometry or gravity.

## 3. CONTROLS

Halt-grade analytic identities: L-C degenerate coherence equality, L-C
swap invariance of H, L-F refactorization invariance — a breach HALTS as
an instrument bug, never physics. Matched controls (same-seed recompute,
0 to 1e-12) ride every coherence comparison. Every closure verified
stable under one extra sweep. L-D2 elimination is deterministic (frozen
pool order); both endpoints re-verified sufficient + minimal. All
thresholds frozen above; no selector beyond minimality/closure is
evaluated (prohibition recorded).

## 4. DELIVERABLES AND STOP

1. `calc/p6_seed_selection.py` (pure stdlib; emits
   `P6_SEED_SELECTION_RESULT.json`, sha-hashed).
2. `P6_SEED_SELECTION_VERDICT_01.md` + closing comment on Issue #2.
3. **HARD STOP at the verdict.** Gravity and geometry stay fenced until
   then; they open only per the owner's frozen decision rule above.
