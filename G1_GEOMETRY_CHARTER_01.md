# G-1 — DOES INFLUENCE DATA + ACCESS RECONSTRUCT GEOMETRY? OWNER RULING RECORD + CHARTER + PRE-REGISTRATION (frozen before evaluation)

**Date:** 2026-09-25 · **Authority:** GitHub Issue #2, owner comment
**5827519334**. **P-6 accepted at recorded strength**, binding: access seed
= irreducible input, not a new law; minimality interface-relative; closure
NULL-REDUNDANT as selector; symmetry does not resolve reachable-sector
degeneracies; path dependence is a genuine obstruction; the finite type-I
result is NOT generalized to QFT/type-III; the preserved L-C red stays on
the record; no further selector escalation. **The geometry leg opens.**
Chain: microscopic dynamics → influence hierarchy → sector structure →
access (non-derived input) → **geometry?** → gravity.

**Question (owner, verbatim in substance):** does the relational/influence
structure of P-1→P-6 contain enough invariant information to reconstruct
an effective geometry, or is geometry another irreducible input? The map
under attack: **full influence data + access structure → geometry.**
**Gravity is NOT allowed to define geometry** (no gravity-specific field
equations anywhere in G-1); ℏ located-not-generated; type-III boundary
map only; Λ_R, Matsubara, Π₀, U5, ω⁷/class-4 fenced. **Rider H
(mandatory):** all substrates here are static, so two-time kernels are
stationary BY CONSTRUCTION of these models; stationarity/KMS is not
primitive and nothing below assumes it beyond the constructed class
(NONSTATIONARITY_RESULT_01 carries).

**Substrates (exact finite models; every reconstruction below uses ONLY
interface data — response kernels at declared accessed sites — never
substrate labels/coordinates):** classical harmonic networks ẍ = −Kx
(unit masses; on-site pinning 0.5 where stated; leapfrog dt = 0.01
unless stated; impulse sources; "arrival time" τ = first |x_j| > 1e-4,
mean-subtracted where the model has a zero mode), plus one genuinely
noncommutative model (owner requirement): the XX spin chain in the
one-magnon sector (N = 31, hopping 1, on-site 0.5; amplitudes via exact
unitary stepping; arrival = first |A_ij| > 1e-4).

## 1. THE LEGS (frozen)

- **L-DIM (dimension attack, owner D):** pinned lattices — 1D chain
  N = 61 (source 30), 2D grid 31×31 (source (15,15)), 3D lattice 13³
  (source (6,6,6)). Ball growth from interface arrival times only:
  N(τ) = #{sites arrived ≤ τ}. Frozen procedure: τ_edge = arrival time
  of the nearest boundary site; τ₂ = 0.6·τ_edge, τ₁ = τ₂/2;
  d̂ = log[N(τ₂)/N(τ₁)]/log[τ₂/τ₁]. **Gates:** d̂ ∈ [d−0.35, d+0.35]
  for d = 1, 2, 3. Dimensionality is RECOVERED, not inserted, iff all
  three gates pass. Quantum leg: same procedure on the XX chain
  (T = 30, dt = 0.05), gate d̂ ∈ [0.65, 1.35]; chain additivity
  τ₅(25) vs τ₅(15) + τ₁₅(25) within 0.2 relative (two source runs).
- **L-MET (metric attack with held-out probe, owner E):** on the 2D
  grid, five anchor sites A1 = (6,6), A2 = (6,24), A3 = (24,6),
  A4 = (24,24), A5 = (15,12) (labels used only to BUILD the model;
  reconstruction sees only the 5×5 arrival-time matrix from five source
  runs; T = 50). Classical MDS (double-centering + eigendecomposition)
  embeds the anchors in the τ-metric. **Gates:** (i) reciprocity at
  trace level for the pair (A1, A4): max|x_{A4}^{(A1)}(t) −
  x_{A1}^{(A4)}(t)| < 1e-9 (analytic, K symmetric — halt-grade);
  (ii) top-2 MDS eigenvalues capture ≥ 0.85 of the total (the metric
  data itself says "2-dimensional" — independent of L-DIM);
  (iii) **held-out probe:** site X = (20,18), one source run; X is
  trilaterated from its measured times to A1, A2, A3 ONLY (linearized
  trilateration), then its times to A4 and A5 are PREDICTED from the
  embedding — gate: relative error < 0.2 on both. Lattice anisotropy
  (axis vs diagonal) is REPORTED as a measurement, not gated.
- **L-CRV (inhomogeneity/defect attack, owner F):** unpinned chain
  N = 81 (zero mode projected out by mean subtraction; displacement
  source at site 10), spring constants k_e = 1 for edges e < 40 and
  k_e = 2.25 for e ≥ 40. Unpinned dispersion makes the front speed
  exactly ∝ √k. **Gates:** (i) the reconstructed local metric density
  tracks the independently declared inhomogeneity: measured speed ratio
  (sites 60→70 vs calibration 10→30) within 0.15 relative of
  √2.25 = 1.5; (ii) held-out travel-time prediction
  τ_pred(10→70) = Σ_e 1/(v₀√k_e) (v₀ calibrated on 10→30 only) matches
  measured τ(10→70) within 0.15 relative. Explicit scope line: this
  recovers a position-dependent METRIC DENSITY (effective line
  element), not a curvature tensor — full curvature reconstruction is
  NOT claimed by G-1 either way.
- **L-REP (representation attack, owner B):** pinned ring N = 40,
  O = the frozen product of 30 Givens rotations G(i, (7i+3) mod 40,
  θ = 0.3 + 0.02i), i = 0..29. Transform K → OKO^T, sources and
  readouts → O·(vectors). **Gate:** the interface kernel trace is
  identical to < 1e-9 (halt-grade analytic identity) — hence every
  reconstruction downstream is invariant. Scrambled site labels carry
  no geometric meaning: geometry, if recovered, is representation-
  invariant.
- **L-ISO (isometry/non-uniqueness attack, owner C — the
  counterattack from below):** pinned 2D grid 10×10 accessed at the
  single corner site. Lanczos tridiagonalization (full
  reorthogonalization; truncation at β < 1e-8 — degeneracy truncation
  expected from the source's mirror symmetry) produces a **1D chain
  network** (after the ±1 sign-flip gauge, a genuine tridiagonal
  network; any negative effective on-site terms are reported).
  **Gate:** the complete single-site response trace of the 2D grid and
  of the 1D chain are identical to < 1e-7 over T = 30 (dt = 0.005;
  exact polynomial identity of the leapfrog recurrence on the Krylov
  space — halt-grade). Frozen consequence: a 2D geometry and a 1D
  chain generate IDENTICAL complete single-site influence data ⇒ under
  single-site access, geometry — including dimension — is
  **GEOMETRY-UNDERDETERMINED**; the Lanczos map is standard
  mathematics (NULL-REDUNDANT as a principle). Combined with L-DIM/
  L-MET (multi-site access DOES recover dimension and distances):
  **reconstructable geometry is a function of the access structure.**
- **L-TOP (geometry counterattack from above, owner G):** pinned
  rings N = 40 vs N = 80, identical couplings, identical local
  structure (translation-invariant bulk — same local spectral/influence
  statistics by construction). Single-site autocorrelation kernels,
  T = 80. **Gates:** (i) identical on the window [0, 10] to < 1e-8
  (fronts have not wrapped; Lieb–Robinson-suppressed, not strictly
  analytic — ok-grade); (ii) differ on [0, 80] by > 1e-3 (the N = 40
  wrap returns at t ≈ 28). Frozen consequence: local influence data
  cannot fix global topology inside the causal horizon of the
  observation window; the interface distinguishes topology exactly
  when the window crosses the wrap time. Geometry, like access (P-5),
  lives at its changes — here, at the horizon.

## 2. OUTCOME CLASSES (owner minimum, frozen mechanical mapping)

**GEOMETRY-RECONSTRUCTED-IN-CLASS** (only if dimension + metric +
inhomogeneity all recover AND no counterattack produces ambiguity) ·
**GEOMETRY-PARTIAL** (geometry-related invariants — dimension, causal
metric class, distances, metric density — recovered at the declared
multi-site access, while full metric/curvature is not claimed and/or a
counterattack bounds the reconstruction) · **GEOMETRY-UNDERDETERMINED**
(inequivalent geometries share the declared interface data — expected
branch: L-ISO at single-site access; L-TOP inside the horizon) ·
**REPRESENTATIONAL** (L-REP; label/coordinate scrambles) ·
**NULL-REDUNDANT** (reconstruction procedures reducing to standard
structure — Lanczos/Jacobi, MDS — recorded as such, never promoted) ·
**FAILS** (any construction fails). Components reported separately per
access class, never averaged. Per the owner's decision rule: promotion
beyond PARTIAL requires surviving ALL counterattacks; a visually
plausible reconstruction is not a verdict.

## 3. CONTROLS

Halt-grade analytic identities: L-MET reciprocity, L-REP invariance,
L-ISO Krylov identity — a breach HALTS as an instrument bug, never
physics. Matched control (one repeated source run, diff 0 to 1e-12)
rides the 2D grid. Ball counts verified monotone in τ. All thresholds,
windows, sites, couplings, rotation lists, and the trilateration scheme
frozen above; no gravity input anywhere; substrate coordinates are
never read by any reconstruction step (only by model construction and
by gate BOOKKEEPING that names which site a reconstruction refers to).

## 4. DELIVERABLES AND STOP

1. `calc/g1_geometry.py` (pure stdlib; emits `G1_GEOMETRY_RESULT.json`,
   sha-hashed).
2. `G1_GEOMETRY_VERDICT_01.md` + closing comment on Issue #2.
3. **HARD STOP at the verdict.** Gravity remains a separate downstream
   test and stays fenced; a second geometry attack or a gravity
   comparison opens only by explicit owner ruling per the decision
   rule.
