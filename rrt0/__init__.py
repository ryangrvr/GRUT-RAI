"""RRT0 package root.

The importable layout is::

    rrt0/            <- this package
        __init__.py
        model/       <- core model code (core, sectors, reducibility)
        tests/       <- test suite

All internal and test imports use the ``rrt0.model.*`` prefix.
"""
