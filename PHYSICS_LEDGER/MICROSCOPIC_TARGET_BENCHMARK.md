# Microscopic rung3/Class-C target — pre-registered benchmark and live conflict

**Date:** 2026-08-23 · PHYSICS_LEDGER · No claims.json edit. No banking.

## The target

```
Σ(x,x′) → G_R^TT(x,x′) → K_R → J(ω)
```

**Question:** does the assembled gravitational response naturally produce the low-ω spectral
structure the registered model assumes, or a qualitatively different one?

## Pre-registered benchmark: a CONVERGENCE BOUNDARY, not an inequality

Re χ(0) ≥ 0 holds for any passive kernel — it discriminates nothing. What the low-ω exponent
controls is whether the static response integral (2/π)∫ Im χ(ω′)/ω′ dω′ **converges**:

| low-ω form of Im χ | Re χ(0) |
|---|---|
| registered s=3 (Im χ ~ ω²) | converges |
| finite-T s_eff=2 (Im χ ~ ω) | converges |
| Ohmic s=1 (Im χ → const) | log divergent |
| class-A white floor, s_eff → 0 | power divergent |

## THE LIVE CONFLICT — stated on this artifact's face

Two computed answers already exist and they sit on **opposite sides of the convergence boundary**:

1. **Register: s=3** (flat-space DOS~ω² ⇒ super-Ohmic), tier `derived-pending`, with the ToE's own
   §2.3 circularity caveat ("what forces the analytic class is… single-pole restated").
2. **Class-A worldline reduction: s_eff → 0**, horizon-forced white floor — adverse at proxy scope,
   fenced ("different objects", "NOT a verdict on class C"), unbanked.

The microscopic calculation is an **ADJUDICATION between these two**, not a fresh exploration.

### The three-way fork (recorded live; do not resolve by assumption)

- (i) the white-floor finding is wrong at its own scope;
- (ii) the white floor is right but an IR cutoff exists — then it must be **named and priced**
  (a new register input);
- (iii) χ(0) genuinely diverges — then the responsive-medium/finite-static-response picture needs
  restating, and "finite memory" acquires a different operational meaning.

## Wall status at attempt time

Walls A–C stand. The full assembly Σ→G_R^TT→K_R requires the graviton-probe influence functional
(wall A) and the RG half (wall B); the TTW premise issue (wall C) touches the in-out vs retarded
structure. **Per the standing dispatch guard:** if the assembly cannot be built in-house, that is
reported as a first-class result naming which obstruction applies — not silently worked around.

**Current status of this attempt: OBSTRUCTED AT WALL A** — no graviton-probe assembly exists in
`calc/`; building one is the staged `gw_tensor_friction.py` work, out of scope for this session.
The benchmark above is therefore **pre-registered for whoever runs the assembly**, with the pass/fail
boundary fixed in advance so the adjudication cannot drift after seeing the answer.

## Scoping correction carried from owner

The Priority-2A result is stated as: **"derived absence of a converged dispersive sign change for
the registered spectral model within the tested low-frequency validity regime."** Not a theorem
about all admissible GRUT spectra.