# ROOT-0 — MICROSCOPIC FOUNDATION / RESPONSE-ORIGIN AUDIT

> # RETIRED — UNCOMMITTED INSTRUMENT-FAILURE AUDIT
>
> **Status: FAILED. NOT COMMITTED. NOT BANKED. NOT AUTHORITATIVE.**
> Retired by owner order (ROOT-1 RECONCILIATION, 2026-09-04). Preserved **as a failed
> instrument**, per "historical failed audits remain intact... ROOT-0 must remain
> identifiable as an uncommitted failed instrument, not silently rewritten into a success."
> Its findings are preserved; **its section-12 nomination of a "first genuine missing
> microscopic premise" is NOT ACCEPTED as authoritative.**
>
> **Why it failed** — adversarial leg returned **FAILED**; a five-lens hostile re-audit of
> the builder's rejection of that leg returned **STANDS-WEAKENED**. Both parties were
> defective in their own favour. Verified defects in THIS document:
> 1. **The nomination is mis-statused.** `WALL_KR_DISTINCTIVENESS_LEDGER.md:21` (bbea87b,
>    2026-09-03 00:34 — newer than anything ROOT-0 cites) files **"bath content"** under
>    **"B — standard input"**. ROOT-0's nominated premise IS bath content: a registered,
>    classified **declared input**, not a missing premise.
> 2. **It never looked beneath its own stop.** Zero occurrences of `u3`, `u4`,
>    `split_origin`, `vc_state`, `frontier`, `Mori`, `KERNEL_SELECTION` in any ROOT-0 file.
>    `u4_constitutive_origin` stands undemoted one published layer beneath.
> 3. **It uses neither of the register's two orderings** (machine `depends_on` vs the
>    published F2 to F3 to F1 prose), which disagree — and produces a section-10 graph
>    matching neither.
> 4. **Verdict criteria are pass-labels (7th occurrence).**
>    `subqft_ontology_exists = ("quantum-gravity" not in cov)` and
>    `forcing_noncircular = ("single-pole restated" not in toe)` are string-presence tests
>    decoupled from the properties they name: **ROOT-FOUNDATION-A was unreachable by
>    construction**, under a comment claiming each fact was "falsifiable".
> 5. **Field blindness (Phase-10 recurring).** The instrument reads `rung3["statement"]`
>    and `["tier"]` only — never `tier_note` / `boundary_condition` / `sub_status`, where
>    the contradicting answer lives ("**NOT the exponent** but 'what is the bath HILBERT
>    SPACE...'"; "decided by the UNPINNED system/bath split").
> 6. **Truncated quote toward its own conclusion.** Section 3 ends the `GRUT_ToE.md:128`
>    blockquote at "...almost no independent evidential weight." The source has a semicolon
>    and continues with the one clause it credits as genuinely earned.
> 7. **Stale physics, in its own favour.** Sections 5/7/9/10 carry the register's s = 3 as
>    live. The executed benchmark (2026-09-01) returned **s = 5** and **rejected** s = 3
>    (residual 2.0 >> TOL_S = 0.30). ROOT-0 omits that artifact entirely.
> 8. **Shipped with three unsubstituted template tokens** (battery, verdict, adversarial
>    leg) while its own battery gate passed by testing only a heading string. Those three
>    are filled in below **by this retirement stamp**, with the true values; **no other
>    line of the original document has been altered.**
>
> Superseded by: **ROOT-1 RECONCILIATION — KERNEL ORIGIN, SELECTION, AND DESTRUCTION.**
> W-0 unchanged · register unmodified (74 nodes, sha256 `beaeb84e8a6f8468`) · A–F unselected.


**Date:** 2026-09-04 · **Instrument:** `root0_foundation_audit.py` ·
**Artifact:** `ROOT0_FOUNDATION_AUDIT.json` · **Base:** 8449984 (Phase 12 closed).
**Battery: 45/45 gates** *(inflated: 18 of 45 check that this document contains headings this document wrote — self-certification; see stamp items 4 and 8).* **Instrument verdict as emitted: `ROOT-FOUNDATION-B`. Status of that verdict: RETIRED — NOT ACCEPTED AS AUTHORITATIVE.**
A repository **reconstruction**, not an implementation. Per the order this **stops at the
first genuine missing microscopic premise and reports it**. No observable hunt, no Γ_T, no
QNM, no A–F, no register mutation, no frozen-artifact edit. Scope: the v4 working tree,
all file types. W-0.

## 1 · CURRENT MICROSCOPIC ASSUMPTIONS, AS THE REPOSITORY ACTUALLY SUPPORTS THEM

| ingredient | what the repository specifies | status |
|---|---|---|
| degrees of freedom | gravitons (TT perturbations) on a background | EFFECTIVE-LEVEL / standard |
| dynamics | Einstein–Hilbert √−g(R−2Λ), Λ = 3H² declared | PRE-EXISTING INPUT |
| state | Bunch–Davies, declared at O(H) | PRE-EXISTING INPUT |
| bath | "**committing to** relativistic massless fast modes (ω=c\|k\|) gives DOS∼ω², J(ω)∼ω³" | **DECLARED BY COMMITMENT** |
| bath proviso | "**PROVIDED** the vacuum bath carries no second internal dynamical scale" | **UNDISCHARGED** |
| interaction | cubic EH vertex (T1-derived, 26,032 terms) | DERIVED from the above |
| coarse-graining | in-in / Schwinger–Keldysh influence functional | PRE-EXISTING INPUT (borrowed, "shown") |
| probe observable | retarded TT response K_R = Σ_R | DERIVED (Tier 1–4) |
| **responsive-medium ontology** | "the gravitational vacuum IS a responsive medium with finite memory" | **tier = ASSUMED; "a STANCE, explicitly not derived"; priced +1** |
| matter sector | — | **ABSENT: "the chain's matter link is SILENT"** |
| sub-QFT / UV completion | — | **DECLARED GAP** (`KNOWN_GAPS`, "absent != covered") |

**The finding of §1, stated plainly: there is no sub-QFT ontology in this repository.** The
layer that exists is standard QFT — microscopic *relative to the kernel*, and sufficient to
have executed Tier 1–4 — but nothing sits beneath the `rung1` stance. The question the ROOT
program asks ("what microscopic facts would make a universe behave as a responsive medium
with memory?") has no in-repo answer; the repository *assumes* the answer and prices it.

## 2 · WHAT IS MISSING FOR A GENUINELY MICROSCOPIC MODEL

1. **A sub-QFT ontology** — absent, and declared absent.
2. **A derivation of the bath spectral density** rather than a commitment to mode content.
3. **Discharge of the "no second internal dynamical scale" proviso** — assumed.
4. **A non-circular forcing argument for the analytic class** (see §6).
5. **The matter sector** — silent.
6. Resolution of the **live spectral conflict** (see §5).

## 3 · INPUT/OUTPUT FIREWALL

**May be supplied (and are):** ontology, action, state, couplings, coarse-graining
prescription. **Must be derived, and are NOT:** spectral density ρ(Ω), pole locations,
residues, widths, memory times, τ₀/τ₁, Λ_R, the constitutive law.

**Circularity hunt — one hit, and it is the central one.** The repository's own top-level
document states it:

> "**The circularity is tight:** what forces the analytic class is *locality / finite-memory
> of the influence functional — **which is single-pole restated**.* So the favorable lean
> carries almost no independent evidential weight."

The DOS⇒super-Ohmic⇒short-memory argument therefore **cannot** count as a derivation of
finite memory: its premise is the conclusion. `S_IF.md` books the memory claim honestly as
"single-pole (Debye) **rather than** branch-cut — **the conjecture, not a theorem**."

## 4 · MINIMUM WELL-POSED MODEL

The repository *does* contain a minimum model sufficient to make the response calculation
well-posed — EH + dS + BD + TT probe + in-in — and it demonstrably ran: Tier 1→4 produced
a kernel. **What it does not contain is a level beneath that**, which is what would be
needed to *explain* rather than *exhibit* the response.

## 5 · CANDIDATE RESPONSE OBJECT AND WHAT THE EXECUTED CHAIN RETURNED

The root chain is **already pre-registered in-repo** (`MICROSCOPIC_TARGET_BENCHMARK.md`):

> Σ(x,x′) → G_R^TT(x,x′) → K_R → J(ω)

with a pre-registered benchmark that is a **convergence boundary** (does the static
response integral converge?), explicitly *not* the vacuous Re χ(0) ≥ 0.

**And it records a live, unresolved conflict on its own face** — two computed answers on
**opposite sides** of that boundary: the register's **s = 3** (super-Ohmic, convergent)
versus a Class-A worldline reduction's **horizon-forced white floor, s_eff → 0**
(power-divergent), the latter adverse-at-proxy-scope, fenced, unbanked. A three-way fork is
recorded with the instruction "**do not resolve by assumption**."

**What the executed chain actually returned.** The frozen Tier-4 contract kernel carries,
marked **UNCONDITIONAL**: a *"branch point at ω = 0 with a real-axis cut (**gapless
two-graviton continuum**)."* That is a **cut, not a pole**, at the one-loop contract scope.

## 6 · WHAT WOULD MAKE MEMORY EMERGENT — AND THE SCOPE GAP THAT BLOCKS THE VERDICT

The published, pre-registered falsifier reads:

> "If the assembled **low-frequency** de Sitter computation returns a branch cut, then in
> the dispatch's own words **'the core assumption is refuted as stated — we retire it and
> say so.'**"

**I am not asserting that this has fired, and the reason is a scope gap I must not paper
over.** Tier-4's conditional statements are banked at **ω ≫ H**, while the pre-registered
question concerns the **low-frequency (ω ≲ H)** structure. Further, the node states
explicitly that *"the Class-C consequence cell is **NOT banked** with this node"*, the
dispatch pre-registers **six** outcomes of which "branch cut/continuum" is one that is
**not self-adjudicating** (it requires determining whether the low-frequency behaviour
yields the registered kernel shape), and the dispatch **solver is demonstrated to refuse
while any prerequisite is undecided**. `rung3_single_pole` accordingly remains
`derived-pending` — neither derived nor retired.

## 7 · NON-UNIQUENESS

Not tested here, and deliberately: constructing alternative microscopic models presupposes
a microscopic level to vary, which §1 shows does not exist. The existing non-uniqueness
evidence is the §5 conflict — two computed spectral answers straddling the convergence
boundary — which is already a live demonstration that the declared assumptions do **not**
pin ρ(Ω).

## 8 · STANDARD VERSUS GRUT-SPECIFIC

**LEVEL 0 (stipulation):** the responsive-medium ontology; the BD state; the mode-content
commitment; the no-second-scale proviso. **LEVEL 1 (standard):** SK/in-in, FDT/KMS,
spectral representations, generic nonlocality, the one-loop EH self-energy and its cut.
**LEVEL 2 (differs from standard):** nothing established. **LEVEL 3 (GRUT-specific):**
nothing established. Consistent with the frozen Phase-9 finding that no GRUT-specific
premise occurs in the H¹ ancestry.

## 9 · QUANTITIES THAT ARE EFFECTIVE-LEVEL ASSUMPTIONS, NOT MICROSCOPIC PREDICTIONS

τ₀, τ₁, Λ_R, the single-pole/exponential memory law, the susceptibility normalization
c₀ = α (an "adopted phenomenological normalization"), the constitutive relation, and the
bath spectral density J(ω) ∼ ω³.

## 10 · DEPENDENCY GRAPH (as the repository stands)

```
[declared] EH action + Λ=3H²   [declared] dS chart   [declared] BD state
        \                |                    /
         \               |                   /
          +--> cubic vertex (DERIVED, T1) --+--> Σ_R (DERIVED, T3/T4)
                                             |
   [COMMITTED] massless relativistic modes --+--> J(ω)~ω³  ..X.. [CIRCULAR]
        + [UNDISCHARGED] no second scale          |               forcing of
                                                  v               analytic class
                                    K_R: UNCONDITIONAL branch cut at ω=0
                                                  |
                                   [NOT BANKED] Class-C consequence cell
                                                  |
                                    rung3_single_pole: DERIVED-PENDING
                                                  |
                              [ASSUMED, +1] responsive medium w/ finite memory
```
The arrow that would make the program predictive — from a *derived* ρ(Ω) to a *forced*
analytic class — is the one marked circular, and the consequence cell beneath the computed
cut is unbanked.

## 11 · NEXT ROOT QUESTIONS, IN DEPENDENCY ORDER

1. **What fixes ρ(Ω) at low frequency?** (everything below depends on it)
2. Does the Tier-4 cut, banked at ω ≫ H, bear on the ω ≲ H question at all?
3. What are the dispatch's six undecided prerequisites, and which are in-house?
4. Can the s = 3 / white-floor conflict be adjudicated without new input?
5. Is the "no second internal dynamical scale" proviso dischargeable?
6. Only then: does any characteristic finite timescale follow rather than enter?

## 12 · VERDICT

**`ROOT-FOUNDATION-B`** *(emitted; RETIRED — see stamp)* — a QFT-level specification exists, is microscopic *relative to the
kernel*, and has been executed as far as a kernel; **but** the responsive-medium ontology
beneath it is an assumption with **no sub-QFT ontology** in the repository, the one
argument that would force the memory structure is **admitted circular**, and the spectral
input is a **commitment** carrying an undischarged proviso and a live unresolved conflict.

**THE FIRST GENUINE MISSING MICROSCOPIC PREMISE — reported, and per the order I do not
fill it:**

> **The low-frequency spectral content of the bath — what fixes ρ(Ω) as ω → 0.**
> Every element of the memory claim descends from it. The repository supplies it only by
> *commitment* to mode content (massless relativistic ⇒ DOS ∼ ω² ⇒ J ∼ ω³), with an
> explicit undischarged proviso ("no second internal dynamical scale"), an **admitted
> circular** forcing argument, and a **live conflict** against a horizon-forced white floor
> (s_eff → 0) sitting on the far side of the pre-registered convergence boundary.

## ADVERSARIAL LEG

**VERDICT: FAILED** (workflow `wf_4c87ed30-e60`, 2026-09-04).

The leg confirmed governance independently (register byte-identical, 74 nodes, no tracked
file modified, nothing banked, W-0 honoured) and found every quotation textually faithful —
**no fabrication**. It nonetheless returned FAILED on the single question this audit was
built to answer, and the builder **adopted five of its findings** after direct verification:
the truncated `GRUT_ToE.md:128` quote; the unsubstituted template tokens; the
`statement`-only field blindness; the non-falsifiable verdict criteria; and the total
omission of `WALL_KR_CONTRACT_BENCHMARK_VERDICT.md`.

**The leg's headline was rejected, then that rejection was itself audited and only partly
survived.** The leg argued ROOT-0 stopped three layers too shallow, citing six documents
that rank `u3_split_origin` as "the deepest frontier". Those citations are textually
accurate but represent a framing demoted on 2026-09-03 (`fd6d6fd`: u3 is a **graph
isolate**, `depends_on == []`, named by no node — independently re-verified across all 74
nodes). The leg also promoted `PRIMITIVE_INVERSION_SCOPE.md`'s type III-1 argument to a
"live structural threat" although that source self-flags it "also unverified" and records
the type II-1 escape.

**But the builder's rejection was defective in the opposite direction** (five-lens audit
`wf_fa560a25-840`, verdict STANDS-WEAKENED): "superseded" does not hold as a *status* —
`CHARTER.md:3` makes every artifact subordinate to the charter, `CHARTER.md:52` still lists
"the system/bath split (**the deepest**)" among assumed, and the live register still reads
"(the deepest frontier)"; the rejection quoted a clause its own exhibit had withdrawn
(`WALL_KR_U3_EFT_BASELINE.md:38`, "'Placement is immaterial' was too strong, and I withdraw
it"); and it claimed "the repository's most recent adjudication" while stopping two commits
and twenty-five minutes short of `WALL_KR_KERNEL_SELECTION.md` (2739e5f) and
`WALL_KR_DISTINCTIVENESS_LEDGER.md` (bbea87b) — the artifacts that actually dissolve its
argument and that file this audit's own nominated premise as a **declared standard input**.

**Net:** neither the leg's ordering nor ROOT-0's nomination survives. Both are recorded
here unresolved; the scientific question moves to ROOT-1.

## GOVERNANCE EXIT

H¹ frozen and unmodified; Phases 1–12 byte-identical; register unchanged; A–F unselected;
W-0; no observable computation launched; HEAD == origin/v4. **No claim of "GRUT theory
established", "finite memory derived", or "pole derived" is made anywhere in this audit.**

## W-0 STATUS — reconstruction only; the gap is reported, not filled; nothing banked.
