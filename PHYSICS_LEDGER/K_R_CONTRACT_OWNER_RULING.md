# K_R^(contract) — THE THREE DECLARATIONS (owner ruling; one page)

**Date:** 2026-08-30 · Derived from the owner's ruling principles of this date
("choose by the frozen benchmark/contract definition, not by computational
convenience; record the order of limits explicitly; preserve the stronger
unfixed construction unless the frozen record requires otherwise; dimensional
continuation with no IR scale unless the calculation demonstrates need")
applied to `K_R_CONTRACT_DECLARATION_SHEET.md`. **Status: RULED BY PRINCIPLE —
AWAITING OWNER COUNTERSIGN.** Tier 1 does not begin until countersigned.
No computation was performed.

---

## 1. D1 — probe kinematics: **OPTION 1a**
**Controlled k → 0 limit with the executed two-axis isotropy gate.**
*Why from the frozen record:* the benchmark object is ω-only (*"Im chi =
J/omega (friction)"* — no k anywhere in the registered family); the probe is
the long-wavelength GW (`gw_tensor_friction`); rung3's DOS construction
produces an ω-only object. The physical meaning of "long-wavelength probe"
IS k → 0 taken first. Option 1b (literal k = 0) is rejected as the
convenience option that silently resolves the order of limits; 1c keeps k in
the data and does not match the frozen ω-only definition. Matter-stage
methodological continuity is noted as secondary, not the reason.

## 2. D1/D3 — order of limits: **k → 0 FIRST, at fixed ω > 0; the IR class
read afterward on the ω-only object (ω → 0 last).**
*Why:* this is the registered family's own structure — J(ω) is defined for
all ω > 0 as a function of ω alone, and its s-class is read as ω → 0 on that
object. Any other order would ask the Q3-class question about an object the
benchmark never defined. Recorded explicitly as binding; joint rays ω ∝ k
are NOT the declared limit and may be computed only as disclosed
diagnostics, never as the verdict-bearing object.

## 3. D2 — working gauge: **OPTION 2a — gauge-UNFIXED, orbit-tracked.**
*Why:* Declaration 5 (frozen) makes the asymmetry load-bearing: *"the second
computation imposes a genuine gauge slice and must reproduce the
gauge-invariant content of the computation that imposed none."* The frozen
record does not require the fixed-gauge route, and cost is not a reason
(owner principle, verbatim). D4 therefore runs **verbatim as frozen**
(unfixed vs synchronous). **No amendment is required.**

## 4. D3 — bath state + IR: **OPTION 3a — BD-analogue Option-B adiabatic**,
with the flat massless anchor gated at H⁰ per order, and the exact-dS state
(Option-A analogue) retained as the declared cross-check target.
**IR: dimensional continuation ONLY; NO explicit IR scale.**
*Why:* the V3 amendment (frozen) ruled Option B *"the PRIMARY computational
route"* for the de Sitter loop as a standing methodological commitment — the
contract object lives in the same assembly. 3a subsumes 3c's flat-massless
content as its own anchor order while remaining the declared-primary route,
so the cheapest physical option's content is captured without adopting it as
primary. **If the calculation demonstrates an IR scale is necessary: STOP —
the preregistered fork (ii) fires verbatim ("named and priced — a new
register input"). It may not appear as an implementation convenience.**

## 5. Amendments required: **NONE.**
The ruled set {1a, 2a, 3a + dim-continuation} is the zero-amendment path:
every frozen protocol (Declaration 5 / D4, the V3 Option-B commitment, the
G0 wiring, the benchmark's outcome table) runs verbatim.

## 6. Consequences for Tier 1 onward
- **Tier 1** builds the dS TT-TT-TT vertex in the unfixed, orbit-tracked
  representation with the Section-D chart discipline (H-graded, flat
  known-answer anchor). The heaviest-cost path is adopted **knowingly** —
  the convenience profiles in the decision sheet document that the cheap
  options were visible and declined.
- **Tier 2** mode integrals are performed at the declared k→0-first
  prescription with the isotropy gate executed; the flat H⁰ anchor doubles
  as the bridge-subsumption checkpoint.
- **The Q3-class adjudication table is already fixed** (benchmark, frozen):
  convergent ⇒ the register's side; divergent ⇒ "class-A was right";
  cutoff-dependent ⇒ fork (ii). It applies to the k→0-first, ω→0-last
  object and to nothing else.
- **Tier 3's** Ward classification and D4 execution inherit the unfixed
  reference object cleanly (the A4 machinery is the template).

**HARD STOP.** These declarations now exist before the answers they control.
Tier 1 begins only on the owner's countersign of this page.
