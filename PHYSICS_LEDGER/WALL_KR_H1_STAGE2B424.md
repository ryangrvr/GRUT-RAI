# H¹ CAMPAIGN — STAGE 2B.4.2.4: GATED PROVENANCE RECORD

**Date:** 2026-09-03 · **Instrument:** `wall_kr_h1_stage2b424_provenance.py` ·
**Artifact:** `WALL_KR_H1_STAGE2B424_RESULT.json` · **Battery: 18/18, zero failures.**
**Provenance tracing only — no theorem, no classification, no routing interpretation,
Route-B not executed.** Read-only on frozen artifacts; register sha256 identical pre/post;
A-F unselected; nothing banked. W-0.

## THE RECHARACTERIZATION — CONFIRMED UNDER GATE

The earlier endpoint / 8-class structure is a **representation artifact of the global
subtraction**, not an intrinsic decomposition of the frozen bucket-loop. Traced key-by-key
through the frozen `assemble()` machinery (labels read from the machinery, never invented):

- **36 native routing keys** populated;
- **PRIMARY GATE: 0 keys with nonzero endpoint dependence** — every per-key O(H) mixed object
  is endpoint-free outside phases;
- **DECISIVE FORK — OUTCOME A:** the aggregate pre-angular object is **identically zero,
  pointwise in n̂, before any angular moment/projector trace** — the d-moments are **not**
  load-bearing;
- **0 native cells feed any of the 8 exceptional q⁰ (d−1)² classes** — the eight-class core
  has **no native provenance.**

## THE THREE-PART STATEMENT

**A — Representation result.** The stored `B_mixed` (292 terms; 146 u + 146 u′; 39 denominator
classes; 8 exceptional q⁰ (d−1)² classes; one (d, ω²) rational core) is a **true, exact
property of the global-subtraction representation** B_lines − B_pureconf. It stands as recorded
data; nothing about it is retracted.

**B — Intrinsic provenance result.** The native frozen bucket loop cancels **key-local and
pre-angular**: every populated nu-routing key's O(H) mixed object is endpoint-free and ω-free;
the aggregate pre-angular object is zero pointwise in n̂; zero native cells feed the eight
exceptional classes. (14 keys vanish individually pre-angular; the other 22 cancel collectively
in the pre-angular aggregate — collective cancellation is the aggregate identity, not an
unresolved case.)

**C — Status. `SUPERSEDES AS CHARACTERIZATION, NOT AS DATA`.** The endpoint / 8-class picture
must no longer be called the intrinsic algebraic heart of Protection 2; the intrinsic picture
is B. The 2B.4.2.3 record assigned no routing interpretation, so nothing there is rewritten —
this recharacterizes it transparently. Frozen artifacts stand byte-identical.

## WHAT THIS DOES AND DOES NOT SAY

**Recorded verbatim:** *"the frozen construction exhibits key-local, pre-angular O(H)
cancellation."* ω enters only through the C-matrix weights (m_key is ω-free); the pre-angular
vanishing is q- and d-symbolic, with no d = 3 used — so the cancellation is **vertex/routing
algebra, not a dimension-dependent angular identity.** That is the fork's "more conventional"
side.

**Not** called: theorem, flat-vertex theorem, Class 1, GRUT-specific, symmetry protection.
Those await the independent Route-B derivation (documented as a target, not executed): prove
Σ_k V_k·m_k = 0 from the flat C⁰ entries plus the product-rule residual insertion directly,
keeping d symbolic.

## RE-REPRESENTATION DISCLOSED

An earlier run of this stage hit the 20-minute rule (W-algebra rebuilt inside the cell loop);
re-represented so the per-key mixed object m_key is built once per routing key, frozen
conventions unchanged. An intermediate framing also imposed a false A0/A1/AU per-key trichotomy
(assuming each key cancels alone) — corrected to the aggregate pre-angular gate, since keys
cancel collectively. Both disclosed; the frozen object was never touched.

## VERDICT: DEFERRED — provenance established; the C2′/Protection-2 verdict and classification
await Route B.

## W-0 STATUS — provenance recorded, recharacterization confirmed under gate; no low-frequency physics; A-F unchanged; nothing banked.
