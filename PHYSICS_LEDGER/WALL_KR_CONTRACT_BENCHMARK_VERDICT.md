# CONTRACT-LEVEL BENCHMARK CONSEQUENCE: VERDICT

**Date:** 2026-09-01 · **Authorization:** owner (consequence only; K_R
immutable) · **Instrument:** `wall_kr_contract_benchmark.py` ·
**Artifact:** `WALL_KR_CONTRACT_BENCHMARK_RESULT.json` · **Battery:
28/28, zero failures, all five controls detecting.** Order discipline:
C1–C7 classified the contract object with the registered comparator
sealed out; C8 opened it only after the primary verdicts were recorded.
**W-0: computed-and-reported, NOT banked. HARD STOP.**

## PRIMARY QUESTION — ANSWERED

*"What does the massless-graviton contract K_R imply under the
pre-registered benchmark consequence rules?"*

**The contract vacuum's response is GAPLESS, more super-Ohmic than the
register assumed (s = 5, not s = 3), firmly CONVERGENT on axis 1 — and
axis 2 is INDETERMINATE with the missing component precisely named (the
D5 local conditions). The registered s = 3 power is not the flat
vacuum's class; it re-enters as the curvature-induced O(H²) component
with an H²-proportional coefficient the registered family excludes.**

## THE MAPPED OBJECT (recorded before any comparator existed)

    Im chi(omega) = (3/1280 pi) omega^4 + (13/480 pi) H^2 omega^2  > 0
    J_eff(omega)  = (3/1280 pi) omega^5 + (13/480 pi) H^2 omega^3

χ = −K_R (the frozen χ = −G orientation; passivity gated). The real
local slot never touches Im χ. Classification-grade normalization: every
verdict below is invariant under overall positive rescaling; no fitted
amplitude exists (the charter-§3 amplitude theorem is not executed and
fitting to J is barred).

## AXIS 1 — s-CLASS AND CONVERGENCE

- **Flat slice (H = 0, unconditional to ω → 0):** the log-slope of
  Im χ is EXACTLY 4 at every ω — a pure power law (the registered
  non-power-law rule is not triggered). **s = 5** in the registered
  J-convention.
- **Curved (H > 0), in-domain:** log-slope in (3.8, 4] across the
  controlled band; the O(H²) component carries the s = 3 SHAPE
  (J ⊃ H²ω³). The ω → 0 s-limit at fixed H is INAPPLICABLE — the
  perturbative domain terminates at ω ~ H before the limit is reached
  (reported as such, not converted into a verdict).
- **Bucket: s ≥ 2, firmly, at every admissible point and slice.**
- **Re χ(0) computed in every case, as the axis demands:** H = 0:
  **Re χ(0) = 3/(2560π²) exactly** (WC = 1 units) — CONVERGENT with no
  cutoff anywhere in the instrument. H > 0: convergent over the whole
  controlled domain; the sub-boundary tail (the horizon/white-floor
  regime) is outside the truncation's scope and is left exactly there.

**Mechanism finding (adjudicates the register's rung3 derivation):**
rung3's s = 3 came from DOS ~ ω² with an assumed coupling weight; the
actual TT-TT-TT vertex is two-derivative, contributing ω⁴ in |V|² on
the gapless two-graviton cut — the microscopic loop produces J ~ ω⁵.
The register's anchor row is answered by the computation: **not
confirmed as stated at flat contract scope.**

## AXIS 2 — RELAXATIONAL / RESONANT: INDETERMINATE (component named)

Re χ = (3/1280π²)ω⁴log(μ²/ω²) + (13/480π²)H²ω²log(μ²/ω²) − locals.
The registered test ("Re χ > 0 throughout" vs "sign change in-domain")
is **scheme-hostage**: the undetermined local constants (μ absorbable)
can place or remove a sign change anywhere — exhibited executably: the
reference-slice crossing sits exactly at ω = μ and moves with μ.
Verdict per the registered row 4: **INDETERMINATE; the missing
component is the D5/frozen renormalization conditions.**
Scheme-robust substatement: for EVERY real local choice there is no
resonance-class feature in-domain (Im χ monotone positive, no pole, no
peak; the Tier-4 conditional bound excludes resummed-denominator
zeros). Conditionals recorded, not chosen: D5 ⇒ Re χ > 0 in-window
gives row 1; D5 ⇒ an in-window sign change gives the resonant row.

## C8 — AGAINST THE REGISTERED FAMILY (opened last)

- **Leading flat order: STRUCTURAL MISMATCH** — computed J ~ ω⁵ vs
  registered ω³ (slope residual 2.0 ≫ the registered TOL_S = 0.30).
- **The registered power re-appears as the O(H²) component** — same ω³
  shape, but with an H²-proportional coefficient, which the registered
  family excludes ("H_dependence: NONE declared").
- **Same decision-axis result:** both sit on the CONVERGENT side of
  the frozen boundary (the computed response more strongly).

## C9 — THE REGISTERED CONSEQUENCE CELL

| item | verdict |
|---|---|
| axis 1 | s ≥ 2 AND CONVERGENT (H = 0 unconditional; H > 0 in-domain, scope boundary explicit) |
| axis 2 | INDETERMINATE — missing component: D5 local conditions |
| cell row | **row 4 as registered: "cannot adjudicate; report which component is missing"** |
| row-1 availability | conditional on D5 giving Re χ > 0 in-window: "derives what rung7 needs — relaxational content becomes derived; **single-pole specifically does NOT**" — recorded as AVAILABLE, not banked |
| divergent row | NOT triggered anywhere in the controlled domain; the white-floor regime itself (ω ≲ H, horizon-forced) is outside the truncation and remains unadjudicated at contract scope — exactly where the separate noise-sector fork lives |
| single-pole | no pole certified anywhere; the resummed object shows no in-domain pole (Tier-4 conditional bound); rung3's anchor remains underived, consistent with the frozen register stance |
| discharges | **NOTHING discharged.** The +1 was already retired solely by Q1^TT ∧ Q5^TT; this cell does not touch it |
| open prerequisites | D5 local conditions (axis 2); the ω ≲ H regime (white floor / noise fork, owner-held); the §3 amplitude theorem if absolute scale is ever wanted |

The registered no-collapse rule is obeyed: row 1 is neither a win nor a
loss, and it is not even reached without D5.

## C10 — MATTER vs CONTRACT (no transplant)

| | matter K_R | contract K_R |
|---|---|---|
| spectrum | GAPPED (threshold 2m) | GAPLESS (branch point ω = 0) |
| s-question | asked in the gapped sense (registered IR domain empty) | s = 5 flat + H²ω³ curvature component |
| pole | certified pole-from-cut on g < 0 branch (matter scope only) | none certified; none in-domain |
| validity | massive gap protects the IR | terminates at ω ~ H |

No result was transferred in either direction.

## CONTROLS (C11) — all detecting

Wrong IR scaling (injected ω² drags the slope), wrong sign (passivity),
wrong branch (advanced completion flips Im χ), wrong normalization
(fails the Tier-4 anchor identity), and the **deliberately imposed
s = 3 target — rejected by the registered tolerance itself**
(residual 2.0 ≫ TOL_S = 0.30).

## LIMITATIONS AND BOUNDARIES

χ read at the kernel level per the frozen matter-stage precedent
(review-verified against the sealed J-instrument) and the Tier-4
identity. **Sensitivity disclosure (review-mandated): this reading is
load-bearing — the excluded dressed-G_R alternative would give
Im χ → const (Ohmic, the log-divergent row) on the reference slice,
the opposite side of the registered boundary; and the K_R = Σ_R
identity it rests on was derived by this campaign after the 2026-08-23
registration. If the owner rules the registered object is the dressed
response, the axis verdicts must be re-run on it.** The registered
cell rows are quoted IN FULL in the artifact, with the stale row-1
"+1 partially discharges" clause surfaced as a SUPERSESSION (the +1
was retired 2026-08-30, post-registration) rather than edited out.
The ω ≲ H regime is out of scope everywhere. The D5 slot is symbolic
everywhere. The α = −2 noise finding was never consulted.

**Adversarial review before this freeze:** the kernel-level reading,
bucket semantics (s = J-exponent; boundary slope 42/11 verified), the
scheme-hostage axis-2 argument (both registered outcomes shown
reachable by scheme choice — no scheme-invariant evaluation exists),
and the row-4 placement all CONFIRMED. Repairs adopted: control #5 had
compared two hard-coded literals (the print-statement-fact class) —
now exercised on both actual objects (computed slope 4 vs the
registered family's ω→0 slope 2); the truncated "verbatim" cell table
restored in full with the supersession note; the C1 sensitivity
disclosure added; dead code removed.

## HARD STOP

No K_R/tier/benchmark edits, no IR scale, no Ward repair, no Bardeen
terms, no +1 revisit, no matter-pole import, no ω ≪ H. **The cell is on
the owner's desk.**
