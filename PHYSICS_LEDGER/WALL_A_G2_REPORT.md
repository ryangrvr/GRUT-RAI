# Wall A / G2 — assembly result: OBSTRUCTED AT WALL A

**Date:** 2026-08-23 · JSON: `WALL_A_G2_RESULT.json` (authoritative). No banking. No claims.json edit.

## What was assembled (and what it is)

The only component constructible in-house without importing the answer: the **tree-level
probe-exchange response** — a scalar probe coupled to TT gravitons through L_int = h_{ij}T^{ij}/M_Pl,
response Im χ(ω) = Σ_k |V_k|² δ(ω−k/a) → continuum limit |V(ω)|²·ρ(ω).

With declared coupling power p and k-continuum counting:

| coupling p | recovered slope | predicted from declared inputs | class |
|---|---|---|---|
| p=1 | +3.963 | 4 (= 2p+2) | s≥2 |
| p=2 | +5.963 | 6 (= 2p+2) | s≥2 |

Reproduction exact to numerical precision — which is precisely why it carries **no information**:

> **The tree-level exchange spectrum is pure kinematics: DOS × |V|². It contains no de Sitter
> physics (H appears nowhere; curvature nowhere; a flat-space computation gives the identical
> object) and no vacuum dynamics. G0 label: `INHERITED-FROM-DOS-MODEL` on every exponent.**

## The wall, named per component

Genuine vacuum memory/dissipation is ONE-LOOP: Im Σ_R from the graviton self-energy. That assembly
requires:

- **(a)** the graviton 3-vertex on dS contracted to TT-TT-TT (known form, hundreds of terms,
  Tsamis–Woodard class),
- **(b)** the KC5-RESERVED 4d-covariant completion of the operator basis (`eft_operator_basis`
  register record: UNBUILT),
- **(c)** renormalization of UV divergences (wall B half-discharged).

**G2 STOPS HERE per the standing dispatch guard.**

## Object registry (runtime-emitted)

J(ω) spectral density ω_power=0 · γ(t) friction kernel −1 · f(w)=J/ω friction/response spectrum −1 ·
Im χ(ω) response −1. Convention anchored empirically (Debye passive under frozen convention).

## Convergence note

Tree-object integrand ~ω^(2p+1) at small ω → trivially CONVERGENT for both p — but on an inherited
object, so this convergence says nothing about gravity.

## Verdict for the adjudication

Per the brief's sharper framing: **the graviton-probe assembly cannot be completed in-house without
either importing the registered spectral structure or building components (a)+(b)+(c), each of which
is itself frontier work.** The decisive finding, stated per protocol:

> **At the current status of GRUT, the low-frequency spectral density that rung3 assumes is not
> derivable in-house: every in-house route to J(ω) passes through either a flat-space DOS insertion
> (kinematics, self-confirming) or the unbuilt one-loop dS self-energy (walls). The s=3 vs white-floor
> conflict between the register and the class-A reduction remains UNADJUDICATED — now with the precise
> list of what its adjudication requires.**

## Standing

+1 on `rung1_ontology_finite_memory` unchanged (its discharge condition — deriving finite memory —
requires exactly the blocked assembly). Three-way fork preserved verbatim from
MICROSCOPIC_TARGET_BENCHMARK.md. No claims.json edit.