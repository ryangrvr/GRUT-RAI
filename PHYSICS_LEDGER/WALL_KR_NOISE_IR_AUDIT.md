# NOISE / IR FORK AUDIT — RECORD

**Date:** 2026-09-01 · **Instrument:** `wall_kr_noise_ir_audit.py` ·
**Artifact:** `WALL_KR_NOISE_IR_AUDIT_RESULT.json` · **Battery: 27/27,
zero failures, six controls detecting.** · **Frozen inputs and register:
byte-identical.** · **No IR regulator, scale, or prescription
introduced.** · **W-0: unbanked. HARD STOP.**

## CLASSIFICATION: **NOISE-A**

**The α = −2 behavior is confined to the equal-time/secular
(internal-coincidence) mode-sum class — a regime no registered
observable consumes — and requires no new prescription for the
registered noise observable, whose finite-frequency kernel is an exact
polynomial.**

## 2. WHAT α = −2 ACTUALLY IS (the identification, earned)

Three mechanical steps, none from the label:

1. **The scan definition, from the frozen source AND the frozen record's
   own words.** `fork_scan` Laurent-expands the measure-weighted
   Wigner integrand at fixed ω with the oscillatory phases
   series-expanded — and the frozen T3-1 criterion note says so itself:
   *"oscillation series-expanded."* The scanned object is the
   **no-oscillation-credit bound** of the radial integrand, taken
   **before** the cone/support assembly.
2. **The scan reproduced verbatim** from the frozen cache: leading
   exponent α = −2, 1/q² coefficient exactly 4ω⁴/15.
3. **The faithful home of that divergence located:** at **internal
   coincidence (Δ = 0), where the phases equal 1 exactly** — nothing
   expanded, no credit to strip — the un-expanded integrand has the
   *identical* leading behavior (4ω⁴/15)/q². **The α = −2 object IS the
   equal-time/secular mode-sum class**: a Keldysh-combination
   (Σ_> + Σ_<) statement at H², u_b-free, power-IR-divergent at d = 3.

## 3. DOMAIN DETERMINATION (brief §5: option C/D, demonstrated)

- **Not (A) inside the Gate-E controlled domain:** at fixed ω > 0 the
  delta support pins q = ω/2 > 0 — the assembled kernel never samples
  q → 0.
- **Not (B) the ω → 0 kernel limit:** the assembled kernel
  N^{H2}(ω) = 13ω²/(240π) per H² is **exact** — a pure polynomial for
  all ω > 0, finite everywhere, **→ 0 as ω → 0**. No zero-frequency
  asymptotic was extrapolated; no white-floor claim is made in either
  direction (ω ≲ H stays outside the truncation regardless).
- **Not (E) the finite-frequency kernel anywhere** — same exactness.
- **Yes (C/D): only the equal-time/secular internal-coincidence mode
  sum** — equivalently the zero-support q → 0 sector.

## 4. AUTHORITY SWEEP — five entries, ZERO licensing

D3/Option-3a ("dimensional continuation ONLY; NO explicit IR scale");
the state declaration (fixes the BD-analogue state and H-grading —
specifies no zero-mode, finite-volume, switching or coincidence
prescription); the declaration sheet's IR sub-choice (divergences are
CLASSIFIED, the honest default); benchmark fork (ii) (the route, not a
license); the Tier-2 KMS scope note. Downstream comparison artifacts
were not counted as authorities.

## 5. DEPENDENCY GRAPH (brief §6)

- **Gate-E does not consume it** — the lock uses on-cone content at
  fixed ω > 0 only; it never evaluates Δ = 0 (verified against the
  Gate-E artifact).
- **The registered benchmark does not consume it** — both axes live on
  χ(ω) at finite frequency; Re χ(0) is a finite-frequency KK integral.
- **The rung2 KMS gate does not consume it** — frequency-grid residuals.
- **What WOULD consume it:** equal-time/coincidence variances
  (⟨h²⟩-class) and secular-growth diagnostics. **None is a registered
  contract observable today.** Registering one re-opens this as fork
  (ii) — a new input, named and priced then. Requirement recorded;
  **no value chosen.**

## 6. SCALE-FREE STATUS (brief §7)

For the **registered** observable no resolution is needed: the
finite-frequency restriction — already built into every registered
consumer — *is* the scale-free interpretation, found in the existing
formalism, not invented. For the coincidence object itself **no
scale-free interpretation exists in the record** (its u_b-free 1/q²
coefficient is a single positive term; nothing cancels it), and none
was manufactured.

## 7. CONTROLS — all detecting

**A.** evaluation moved into the excluded IR regime (ω = H) raises a
DOMAIN violation — not misclassified as a physical failure; **B.** a
perturbed exponent (α = −1) mismatches the frozen record; **C.** a
shadow evaluation carrying a finite q_min is flagged UNLICENSED;
**D.** perturbing a term of the assembled kernel breaks the
exact-polynomial test (the exactness claim is not vacuous); plus the
runtime-sentinel token scan.

## 8. FIREWALLS HONORED

Retarded and noise sectors kept separate in both directions (the
corrected retarded H² log finding untouched; nothing transferred).
Parameter count unchanged: **H⁰ = 1 (Λ_R); c0′/c2′ unresolved,
fork-gated; zero new inputs; no scale hidden in Λ_R.**

## 9. RELATION TO GATE-E

**LEAVES GATE-E-A UNCHANGED.** The lock never evaluates the coincidence
object; no separate Gate-E limitation is exposed — the boundary was
already declared on Gate-E's own face. Nothing retroactively modified.

## DEFECT HISTORY (gate-side, disclosed)

Run 1: my note-matching assumed exactly one "FORK FIRES" note; the
frozen record has two relevant notes — and the one my filter hit first
(the criterion note) turned out to be the better source, containing
"oscillation series-expanded" verbatim. Gates rebuilt to use both.
The physics never moved.

## HARD STOP

The noise fork is **audited and closed as NOISE-A**: no prescription
needed for anything registered; the coincidence/secular class stays
fenced with fork (ii) as its future route. Owner's desk: T4 +
consequence-cell adjudications; the fork-(ii) option remains available
but uninvoked.
