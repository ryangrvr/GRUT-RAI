# OWNER ADJUDICATION — WALL-A RESPONSE-STAGE CLOSURE (2026-08-30)

**Character of this document:** owner adjudication, recorded by the builder on
the owner's explicit rulings of 2026-08-30. **Nothing is computed here.** No
prior scientific result artifact is modified. Source commits and hashes below.

**Chain closed by this stage:** structure (A3-4, commit `456487c`) → gauge
(A4, `b2f00c1`) → scheme (PV, `7427b26`) → benchmark (J(ω), `e613c32`) →
dependency audit (`a4d729e`). Frozen kernel `dd77b1943e2068c6…888c4ae1`.
Register file at adjudication time: `provenance/claims.json` sha `a70a2ad1…`
(read-only here).

---

## RULING 1 — THE +1 IS DISCHARGED

**The node (quoted from the register, verbatim in substance):**
`response_lorentz_covariance`, tier *assumed*, booked 2026-08-24 (+1) on the
owner's explicit go: *"the vacuum response kernel itself belongs to the
Lorentz-covariant subspace identified by the flat-limit membership test"* —
licensing the `11 →(THIS NODE) 3` step of the kinematic chain.

**The preregistered discharge rule (Declaration 4, frozen `87e2d24d…`,
2026-08-25):** the +1 is dischargeable **only** by Q1 INSIDE **and** Q5
INSIDE, per the owner's condition ("…lies in that 3-dimensional
Lorentz-covariant subspace **without imposing it as an input**"); Q3 and Q4
do not vote; *"any discharge claim citing other evidence is invalid"*;
discharge is an owner ruling at the bank gate.

**The evidence that satisfies it:**
- **Q1^TT = INSIDE** — the nonlocal TT block equals a(ω,k,H,m)·P₂^TT
  **exactly** (symbolic identity) at H⁰, H¹, H², with P₂ nowhere imposed
  (A3-4 layer 2, 31/31 gates; predicates hash-frozen at `0bb30fc` before the
  TT numbers existed).
- **Q5^TT = INSIDE** — the H → 0 limit exists per channel (structural
  grading) and matches Q1's flat placement.
- Both survived: gauge/orbit (A4: operator identity through O(H²)) and
  scheme (PV: absorptive 7.02e-17; real part 3.46e-6 modulo the declared
  local polynomial).

**OWNER RULING: the +1 is DISCHARGED**, solely on Q1^TT = INSIDE ∧
Q5^TT = INSIDE. **Explicitly recorded: Q3 played no role; Q4 played no
role; J(ω) played no role; PV played no logical role in the discharge
condition (it is robustness evidence, not discharge evidence); the Ward
finding played no role.** No other evidence is added to the rule,
retroactively or otherwise.

*Register application note:* the tier/ledger application of this discharge to
`provenance/claims.json` is the one-line register action that, per this
campaign's standing practice (the 2026-08-24 booking itself was applied "on
the owner's explicit go"), awaits the owner's explicit register-edit
instruction. The discharge is RULED and RECORDED here; the register file was
not edited by this document, per the stage's artifact rules.

## RULING 2 — K_R: OPEN / UNCOMPUTED

K_R (the retarded dissipation kernel of the SK influence action,
`rung1_inin_formalism`) is **mathematically distinct** from the computed
Σ_R^finite: it lies downstream of the `G_R^TT` dressing the benchmark's own
text records as *"OBSTRUCTED AT WALL A."* **Recorded:** the present TT
response does **not** silently constitute K_R; no claim requiring K_R is
promoted to completed status; the benchmark's ledger-consequence cell at
K_R scope **remains open** unless separately chartered. No K_R equivalence
is invented; K_R is not inferred from the TT result. The prerequisite
specification for a future K_R charter stands in
`J_OMEGA_ADJUDICATION_REPORT.md` §3.

## RULING 3 — SINGLE-POLE: NOT DERIVED; NOT "DISPROVED"

**Recorded exactly:** the register's single-pole finite-memory statement
(`rung1_ontology_finite_memory`) is *"a STANCE, explicitly not derived"*
(tier **assumed**, the register's own words). The microscopic calculation
produced a **gapped branch-cut response** at the computed scope. Therefore:
**the calculation does not derive the registered single-pole law** — and this
must not be described as a theorem disproved. The computed result and the
ontological stance are two separate records; the benchmark's own row-1
wording now has quantitative content: the excess strength of the single-pole
commitment over what the microphysics yields is explicit.

## RULING 4 — WARD FINDING: CLASS B, UNRESOLVED, RETAINED

The non-TT vector-channel Ward residual **remains on the record**: nonzero,
nonlocal/cut-carrying in the primary construction, absent from the
independent absorptive two-body cut (both pairings), CLASS B under the
owner's interpretive bound (`ea04d7d`). It is **not repaired, not removed,
not permitted to invalidate the TT result** (the completion class has zero
TT projection — executed gates, A4/W5), **and not explained away by the
benchmark comparison.** The separation, stated explicitly:

> TT physical response = **robust**.
> Full non-TT vector Ward completion = **unresolved**.

## RULING 5 — THE STAGE-LEVEL SCIENTIFIC STATEMENT

> The microscopic one-loop calculation yields a finite TT response in the
> preregistered three-family structure, robust under gauge/orbit and scheme
> comparisons, with a gapped, convergent, purely relaxational spectrum. It
> does not directly reproduce the registered gapless s=3 family. A non-TT
> vector-channel nonlocal Ward residual remains unresolved. The distinct
> K_R benchmark-pipeline object remains uncomputed.

This is **not** proof of GRUT; it is **not** disproof of GRUT; it is **not**
complete benchmark closure. It is the completion of the response stage at
its declared scope.

---

## Provenance

| item | value |
|---|---|
| frozen kernel | `dd77b1943e2068c643f4181438814a378c26bc8693b616be812fa5f5888c4ae1` |
| A3 declarations (v1) | `87e2d24d…` (+ V2 `6f2a762f…`, V3 `b0b9983b…`, V4 `f6127ca6…`) |
| layer-2 predicate freeze | commit `0bb30fc` |
| A3-4 / A4 / PV / J commits | `456487c` / `b2f00c1` / `7427b26` / `e613c32` |
| dependency audit | `a4d729e` |
| register at adjudication | `provenance/claims.json` sha `a70a2ad1…` (read-only) |
| W-0 / registry integrity | verified this stage; barred-inputs registry `faa977d4…` intact |

## HARD STOP

No K_R build, no further loop calculation, no PV or J(ω) rerun, no
Q1/Q3/Q4/Q5 alteration, no s ≥ 2 → s = 3 reinterpretation, no Ward repair,
no new operators, no refits. **No further scientific computation is
authorized until a new owner charter explicitly names the next question.**
