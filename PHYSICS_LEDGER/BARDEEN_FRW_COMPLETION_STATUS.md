# Bardeen/FRW completion — status: OBSTRUCTED AT WALL A

**Date:** 2026-08-23 · Honest stop. No proxy attempted. No shortcut taken.

## What the completion requires

Starting from the 3D Ward-surviving family {P², P⁰s, X_sw}, the Bardeen/FRW completion must:

1. Construct the curved/time-dependent gauge-invariant basis (u^μ enters; the basis may grow).
2. Identify the dS state and whether KMS/FDT applies to the relevant response.
3. Test the closure theorem's premises directly against the actual gravitational setting.
4. Only then reduce X_sw — if the premises hold.
5. Keep c₀ = 0 completely separate throughout.

## Why this is obstructed

Every step above requires the **one-loop de Sitter TT self-energy** Im Σ_R^TT(ω) — the object whose assembly IS Wall A:

- Gate 1 (curved basis): the basis structures are determined by the background + the self-energy's own transformation properties. Without Σ_R you don't know what transforms how.
- Gate 2 (KMS applicability): whether KMS holds depends on the state AND the response kernel's analytic structure — which requires Σ_R.
- Gate 3 (closure theorem premises): the theorem's conditions reference the actual (K_R, N) pair — which requires Σ_R to construct.

All three gates sit downstream of the wall. **No proxy is admissible** (G2 demonstrated why: tree-level exchange is kinematics-only, carrying INHERITED-FROM-DOS-MODEL labels on every exponent).

## What would unblock it

The staged `gw_tensor_friction.py` work — building the smallest defensible graviton-probe assembly from the frozen Class-C contract. That is research-grade work requiring:
- The dS TT-TT-TT vertex (hundreds of terms, Tsamis–Woodard class)
- Renormalization under the frozen D5 contract
- Dual-gauge verification per D4

Each of those is a multi-session component. They are not approximable, not substitutable, and not bypassable.

## The honest scientific sentence

> GRUT has derived a low-frequency property of its registered effective spectral model, but the microscopic gravitational origin of that spectral model remains unestablished because the required one-loop de Sitter TT self-energy assembly is incomplete — and the incompleteness is now itemised into three named components, each of which is frontier-reserved research work.

## What this means for GRUT's distinctive claim

| claim | status |
|---|---|
| finite-memory responsive vacuum | **ASSUMED** (+1 on `rung1_ontology_finite_memory`) |
| single-pole relaxation structure | ASSUMED (stronger than needed for no-crossing; Category 3 empty) |
| registered s=3 spectrum | derived from DOS×coupling assumptions, not from gravity |
| no-crossing | follows from pure relaxation, which follows from the assumption — circular until the loop is computed |

None of these are false. None of them are established. The gap between assumed and established is precisely the one-loop dS TT self-energy, and it is now named, itemised, and fenced.