# G0 — Mode-counting declaration (pre-assembly)

**Date:** 2026-08-23 · Declared BEFORE any spectral answer exists, per the Wall-A entry gates.

## Current state of the assembly

**No graviton-probe assembly exists** (Wall A stands; building it is the staged
`gw_tensor_friction.py` work). G0 is therefore declared now as a **binding precondition on the
future assembly**, so it cannot be skipped or softened when the machinery arrives.

## Declaration that binds any future assembly

1. **The registered J(ω) = ω³·exp(−(ω/ω_c)²) must NOT be supplied as an input or target.**
   The spectral behaviour is determined from the assembled object and compared to the register
   only afterward.
2. **The mode-counting basis must be named at the top of the assembly file.** If modes are counted
   with flat-space DOS ~ ω² (massless relativistic continuum), the resulting s=3 is NOT independent
   evidence — it is the assumption restated, labelled `INHERITED-FROM-DOS-MODEL`.
3. **The solver refuses `DERIVED` labels for any exponent that entered through mode counting.**
   Only structure emerging from the assembled gravitational object (self-energy, propagator,
   kernel) earns DERIVED.
4. **Known prior finding carried:** the static-patch bath is *less* super-Ohmic than flat space
   (`RESULT_FRAME_MIGRATION_2026-08-19`: "the ω² density of states was a flat, infinite-volume,
   comoving continuum count"), and the class-A reduction found a horizon-forced white floor. Both
   are recorded so the assembly starts from the honest conflict, not from the flattering default.

## G1 precondition (restated)

Before any gravitational answer: inject a known admissible Ohmic bath (s≈1) and verify the
complete assembly recovers s≈1. Also plant one super-Ohmic and one white-floor control.
If the machinery cannot recover the planted Ohmic case, STOP — the instrument cannot adjudicate
the s=3 vs s≤1 conflict, whatever gravity returns.

## G3 reporting contract

Two axes, reported independently (see MICROSCOPIC_TARGET_BENCHMARK.md):
Axis 1 spectral class + convergence integral · Axis 2 analytic character
(PURELY-RELAXATIONAL / RESONANT / INDETERMINATE). Non-power-law forms are reported as such;
no effective exponent is fitted.