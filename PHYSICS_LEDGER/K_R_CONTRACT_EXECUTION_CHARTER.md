# K_R^(contract) — EXECUTION CHARTER (setup stage; NOTHING BUILT)

**Date:** 2026-08-30 · Architecture + prerequisite validation ONLY.
**Companion:** `K_R_CONTRACT_DEPENDENCY_MAP.json`.
**Inputs (hash-verified, unmodified):** frozen kernel `dd77b194…`; A3
declarations v1–v4 (`87e2d24d…`/`6f2a762f…`/`b0b9983b…`/`f6127ca6…`);
registry `faa977d4…`; K_R charter audit (`f5c674e`) + owner charter
(`ffb3299`); Wall-A closure (`c1c3f51`); matter-K_R closure (`ca4f65b`) +
sign adjudication (`f719003`, `01002df`); `BARDEEN_FRW_COMPLETION_STATUS.md`;
`MICROSCOPIC_TARGET_BENCHMARK.md` (`f6513b1e…`); rung1/rung2/rung3/rung7
register nodes (read-only; the +1 retirement of this date is the separately
authorized register action).

---

## STEP 1 — THE CONTRACT OBJECT, DEFINED (and the full object dictionary)

**K_R^(contract):** the retarded dissipation kernel of the graviton-probe SK
influence action S_IF (`rung1_inin_formalism`), for the probe = a
long-wavelength TT metric perturbation, with the bath = **the gravitational
vacuum's own massless fast modes** (`rung3`: DOS ~ ω² from ω = c|k|), built
through the frozen pipeline `Σ → G_R^TT → K_R → J(ω)` under D5
(renormalization) and D4 (dual gauge).

| object | is | is NOT |
|---|---|---|
| Σ_R^finite | the frozen massive-matter one-loop TT self-energy (`dd77b194…`) | the contract bath's self-energy |
| K_R^(matter) | the closed matter-scope Dyson experiment (g < 0 branch physical) | any statement about the vacuum kernel |
| K_R^(contract) | THIS charter's target | derivable from the matter object |
| G_R | the Dyson-dressed retarded propagator, `G_R = 1/(G0⁻¹ − Σ)` (rung3 verbatim; resummed form ruled) | a new definition |
| S_IF / N | the SK influence action and its noise kernel; N = coth·Im χ (rung2 FDT lock) | free objects — KMS-gated |
| χ | the FDT/friction-positive response (Im χ ≥ 0; the sign-mapping audit's dictionary χ = −G applies) | the standard self-energy |
| TT projection | strictly downstream of the full non-TT assembly (frozen A3 discipline) | an input |
| G_R^TT dressing | the Dyson step on the TT block | a tensor-structure change (P₂-diagonal by Q1) |

**UNDERDEFINED (owner-resolved before Tier 1; carried from the owner
charter, unchanged):** (i) probe kinematics — the matter-stage ruling
(controlled k→0 + isotropy gate) is the natural template but was ruled for
the MATTER object; its adoption at contract scope needs the owner's word;
(ii) the graviton-loop gauge-fixing/measure choice inside the TT-TT-TT
sector (the frozen record fixes D4 as the CHECK but not the working gauge of
the cubic-vertex loop); (iii) the de Sitter state prescription for the
massless graviton bath at the loop level (BD for the matter scalar was
declared; the graviton-sector analogue must be declared, not assumed).

## STEP 2 — DEPENDENCY GRAPH (per-edge detail in the JSON companion)

```
massless graviton bath (state declaration iii)
        │  frozen: rung3 mode content; NEW: bath propagator + state gates
        ▼
TT-TT-TT cubic vertex (Tsamis–Woodard class)
        │  frozen: A1 discipline as template; NEW: the vertex itself + gates
        ▼
graviton loop response (the contract Σ^grav)
        │  frozen: engine representation lessons; NEW: the loop
        ▼
D5 renormalization (frozen contract; NEW execution on graviton content)
        ▼
D4 dual-gauge check (frozen protocol; NEW execution at this level)
        ▼
G_R^TT dressing (resummed form ruled; first-order cross-check mandatory)
        ▼
K_R^(contract) (influence-functional reduction; sign dictionary applies)
        ▼
benchmark pipeline (the two-axis table at CONTRACT scope — the cell that
J(ω)-stage left open)
```

## STEP 3 — UNBLOCK LIST, CLASSIFIED

| item | status |
|---|---|
| TT-TT-TT dS cubic vertex | **REQUIRED, NOT YET EXECUTED** (frozen: "not bypassable") |
| D5 renormalization | **REQUIRED, NOT YET EXECUTED** at graviton-loop level (frozen contract exists) |
| D4 dual-gauge | **REQUIRED, NOT YET EXECUTED** at this level (protocol frozen; A4 machinery reusable as template) |
| massless graviton bath | **REQUIRED, UNDERDEFINED in part** (state prescription iii) |
| influence-functional reduction | **REQUIRED**; the matter-stage machinery is the validated template, applied to new content |
| retarded/KMS structure | **ALREADY SATISFIED as law** (frozen −i0 branch law + rung2 KMS gate); execution gates required per tier |
| normalization | **DETERMINED BY DERIVATION** (the sign-mapping dictionary + master-unit anchor); theorem gate owed per tier |

## STEP 4 — MINIMAL VALIDATION ARCHITECTURE (independent checks per component)

- **A. TT-TT-TT vertex:** flat-limit known-answer check (the flat cubic
  graviton vertex is standard and independently derivable); total symmetry
  under leg exchange; linearised-gauge/Ward check on each leg (executed, not
  assumed); H-order gates against the exact chart (a² = 1+2Hu+3H²u², the
  Section-D discipline).
- **B. Massless bath:** propagator normalization theorem gate (the massless
  analogue of the A3-1 Im-law anchor); retarded −i0 prescription carried;
  KMS/positivity gate (rung2 as executable check); IR regularity disclosed
  (massless ⇒ the gap CLOSES — the IR treatment must be declared before
  computing, since the whole Q3-class question changes character).
- **C. D5:** UV pole reproduction against the frozen counterterm basis;
  pole-only MS discipline (zero finite discretion — the frozen doctrine);
  locality of every subtraction (the V4 classifier, ported).
- **D. D4:** the A4 response-level machinery (orbit formula, synchronous
  solver, trace-cancellation theorem) re-derived for the graviton loop —
  agreement of TT content between gauges as an operator identity where
  provable, exact symbolic check where not.
- **E. Influence action:** the retarded/noise (K_R, N) pair verified against
  the rung2 FDT lock; normalization by derived theorem gate; TT reduction
  strictly downstream.
- **F. Dyson dressing:** resummed AND first-order, both computed, divergence
  region between them reported (the ruled matter-stage discipline, carried).

No component may become a hidden source of the final answer: each carries a
negative control that must detect, per the campaign's standing pattern.

## STEP 5 — REPRESENTATION STRATEGY (binding; the anti-Phase-11 rules)

Mandatory, from this campaign's own paid-for lessons: sector-graded,
phase-stripped algebra (the {1, ε₁, ε₂, ε₁ε₂} discipline that took Ricci
from 182 min to 8.9 s); numeric/rational momentum probes with symbolic
verification only at the fitted stage; NO abstract Function objects through
curvature products; cache every expensive validated block (the assembly-cache
pattern, tagged and fingerprinted); per-block elapsed prints; **the 20-minute
rule: any block exceeding ~20 minutes without reusable progress is STOPPED
and re-represented, never budget-raised first** (the H2_V1V1onB lesson: the
representation fix beat three budget raises); memory hygiene (sectors freed,
clear_cache — the swap-death lesson); loop-flip/relabeling identities hunted
BEFORE brute force.

## STEP 6 — MASSIVE/MASSLESS BRIDGE

**Classification: SUBSUMED by Tier 2** (the contract bath IS massless — the
bridge is not a patch on the matter object but intrinsic new content), and
**SEPARATE OPEN QUESTION** as an interpretive link between the closed matter
result and the contract result. NOT REQUIRED as a standalone stage. Not
calculated here.

## STEP 7 — WARD/BARDEEN BOUNDARY

K_R^(contract) is TT-projected by its definition (the probe is TT; the
`p_tt_ansatz` form) ⇒ it **bypasses** the Class-B vector residual in the
same proven sense as the matter object (zero TT projection of the residual
class — executed A4/W5 gates). It does NOT resolve it, and the graviton
loop's own non-TT Ward structure is a NEW question of the same class: the
charter requires the graviton-loop Ward contraction be computed and
classified (the wall_a4_ward_diag pattern) as part of Tier 3, with any new
residual recorded as a finding, never repaired silently. The existing
Class-B finding remains a separate record throughout.

## STEP 8 — ACCEPTANCE CRITERIA (fixed before computation)

- **K_R valid:** all Tier gates green; tensor structure P₂-diagonal or the
  deviation recorded as a finding; D4 agreement; D5 poles reproduced;
  retarded sign per the frozen dictionary; normalization by theorem gate;
  the full object independently reproduced once (dispersive vs direct, the
  PV pattern) at the declared tolerance.
- **K_R partially validated:** a named subset of gates green with the
  failing set exactly declared (the expected_red discipline).
- **K_R blocked:** a component stopped under the 20-minute/representation
  rule with no admissible re-representation found — reported as a
  first-class result naming the obstruction (the standing dispatch guard).
- **K_R unresolved:** gates green but the two independent reproductions
  disagree — both records kept, neither averaged (the PV doctrine).

## STEP 9 — COSTED BUILD PLAN (Tiers 1–7 NOT executed)

| tier | content | difficulty |
|---|---|---|
| 0 | this charter + the three owner declarations (i)–(iii) | done / owner |
| 1 | TT-TT-TT vertex + gates A | **multi-session** (hundreds of terms; the single largest new object) |
| 2 | massless bath + graviton loop response + gates B | **multi-session** (a Wall-A-class loop campaign on new content) |
| 3 | D5 + D4 execution + graviton-loop Ward classification | sessions (frozen protocols, new content) |
| 4 | influence functional + gates E | days (template validated at matter scope) |
| 5 | G_R^TT dressing, both orders + gates F | hours (matter-stage machinery) |
| 6 | K_R^(contract) assembly + acceptance battery | days |
| 7 | benchmark two-axis table at contract scope | hours (the J-stage instrument pattern) |

Each tier ends at a hard stop with owner review. **The next session begins
with Tier 1 only, after owner/reviewer acceptance of this charter.**

## STEP 10 — MATTER-RESULT BOUNDARY (binding)

**matter K_R pole result ≠ contract K_R pole result.** The matter result is
citable only as a separate lower-scope result. No parameter, pole location,
spectral behavior, sign-magnitude, or analytic-structure conclusion transfers
between the two objects — the only things that carry are VALIDATED MACHINERY
(evaluators, continuation discipline, gates) and the sign DICTIONARY (which
is convention algebra, not physics content). The contract answer may
contradict the matter answer without either being wrong.

## Integrity at commit

Frozen hashes unchanged (verified); register changed ONLY by the separately
authorized +1 retirement (net +17 → +16, auditor suite green 16/16); Wall-A
and matter-K_R artifacts untouched; W-0 for everything in this charter.

**HARD STOP.** Nothing built. The primary question is frozen: *what is the
minimum, validated, independently checked path from the frozen
massless-graviton inputs to K_R^(contract)?* — answered above as a mapped,
tiered, gated path awaiting the owner's Tier-1 go and the three
declarations.
