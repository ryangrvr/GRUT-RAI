# TIER 3 — THE CONTRACT-LEVEL MASSLESS-GRAVITON LOOP: VERDICT

**Date:** 2026-09-01 · **Authorization:** owner 2026-08-31 (loop
construction + validation ONLY; D4/D5/K_R downstream) · **Instrument:**
`wall_kr_tier3_loop.py` (final sha in the artifact) · **Artifact:**
`WALL_KR_TIER3_LOOP_RESULT.json` (merged four-stage record, re-read and
re-hashed per T3-10) · **Companion:** `WALL_KR_TIER3_FORK_INVOCATION.md`
(the fork-(ii) decision document). **W-0: computed-and-reported, NOT
banked. HARD STOP after this report.**

## PRIMARY QUESTION — ANSWERED

*"Can the validated dS TT-TT-TT vertex and validated massless TT bath be
assembled into a stable, independently checked contract-level one-loop
response without triggering an undeclared IR prescription?"*

**YES at H⁰ and H¹; NO at O(H²) — and the NO arrives declared, fenced,
and priced, which is the designed outcome, not a failure.** The flat
(H⁰) loop exists in closed form and is validated by three mutually
independent routes; the H¹ sector vanishes identically (both CTP
combinations — the loop's first curvature correction is O(H²), not
O(H)); the H² sector samples the Tier-2 armed class and the
preregistered fork (ii) fires: **no regulator was introduced, no
subtraction performed, no integration run in that sector.** The IR
prescription was never chosen implicitly — that is the tier's central
discipline result.

## THE FLAT ANCHOR (T3-4) — three routes, one number

    Im Sigma_R^{H0}(omega > 0) = -(3/1280) omega^4 / pi        [kappa = 1, d = 3]

- **Route 1 (this instrument):** graded operator assembly — Tier-1
  C-matrices (tie-gated against BOTH frozen Tier-1 caches), Tier-2
  kernels, derivative-operator vertex action, exact angular moments,
  distributional radial support (delta^(n)(omega − 2q), exact inner time
  integral gated symbolically).
- **Route 2 (blind independent implementation):** a separate agent, its
  own code, no access to this instrument — identical result, and the
  identical intermediate contraction polynomial
  A(q, ω) = (2/15)(4ω⁴ − 24ω³q + 64ω²q² − 84ωq³ + 49q⁴).
- **Route 3 (brute numeric):** direct angular quadrature of the raw
  contraction, no moment formulas, no phase collectors — ratio to Route
  2: 1.0000002 (quadrature-limited).

The general-d closed form is recorded in the artifact. Scale-free pure
power (the massless two-particle cut); **Im Σ_R(ω>0) < 0** — the passive
orientation of the frozen χ = −G dictionary, derived not imposed.
Isotropy gate (D1): the identical kernel for +/× polarizations and a
rotated direction, at the k_ext = 0 evaluation point of the controlled
limit (the finite-k_ext loop is the K_R tier's gate — disclosed).

## THE IR-FORK VERDICT (T3-1) — the tier's headline

| sector | retarded Σ₋ | noise Σ₊ | verdict |
|---|---|---|---|
| H⁰ | α = +1 | α = 0 | clean — integrated & validated |
| H¹ | ≡ 0 | ≡ 0 | empty (structural result) |
| H² | **α = −1 (log)** | **α = −2 (POWER)** | **FORK (ii) FIRES — fenced** |

The criterion was executed on BOTH CTP combinations (amended per the
adversarial review before any graded integration ran, and the amendment
is itself part of the record): the Tier-2 armed class is noise-defined,
and the retarded difference is softer by exactly one power through
oscillation parity in every sector — pricing by the retarded log alone
would understate the input by a full divergence class. All pole
coefficients (both combinations, every order ≤ −1, symbolic Δ) are in
the artifact; the coefficients are gated u_b-free; the verdict is gated
configuration-independent across three external configs at ALL H orders;
the detection path carries its own planted-defect control. **See
`WALL_KR_TIER3_FORK_INVOCATION.md` for the named-and-priced candidate
register inputs — the owner's decision, not the builder's.**

## VALIDATION BATTERY (T3-6), all green across the four stages

Reduce 13/13 · Assemble 16/16 · Flat 24/24 · Grade 11/11 (the merged
artifact re-checks zero failures). Highlights: the λφ³ toy gate
CALIBRATES the symmetry factor (it caught my own mis-derived prefactor
in run 3); the H⁰ C-matrices agree between the two independent frozen
Tier-1 caches; internal-slot exchange symmetry (G5-inherited) gated; H⁰
stationarity exact; slot Hermiticity c′(ω) = −c*(−ω) exact; corrupted
vertex and corrupted bath (W→W/2 ⇒ ×1/4 exactly) detected; wrong-IR
control shows cutoff-dependence exactly where the fork forbids cutoffs;
the exact inner time integral K_n gated against an independent form; the
Gaussian-δ radial numeric matches the closed form; a PV-leak reality
gate guards the distributional builder's precondition.

## T3-7 UV/FINITE — bounded per the charter

Im Σ_R^{H0} is a pure power: the nonlocal content is the cut itself;
Re Σ_R is scheme-polynomial (the LOCAL slot) + the KK log structure,
with the subtraction constants deferred to the K_R tier's frozen
renormalization conditions — NOT chosen here. No D5.

## T3-8 WARD DIAGNOSTIC — a finding, not a repair

The gauge-image external contraction of the H⁰ loop is **nonzero** at
k_ext = 0 (recorded in the artifact): the Class-B structure has a
graviton-loop analogue. No patch applied, no target imported from the
matter-sector residual; classification deferred per the charter.

## DEFECT HISTORY (all instrument-side; the frozen inputs never failed)

- r1–r5 (representation): exp-explicit and per-term-average assemblies
  stalled past the 20-minute rule twice → phase-absorbed markers, then
  factorized moment-lookup assembly (~4 min full-H).
- r3 (combinatorics): the toy gate convicted my own Wick prefactor
  derivation; the implemented rule was correct.
- **r6–r9 (the valuable catch):** the flat anchor first reported
  +3ω⁴/(1024π). The independent route disagreed (−3ω⁴/(1280π)); a third
  numeric route sided with it; the adversarial review then LOCATED the
  defect — the cone collector lost the 124 ratio-stored terms whose
  denominators hide a common phase factor, and my "conjugate-odd" gate
  asserted the wrong Hermiticity identity. Repaired (lossless per-term
  phase stripping + the ω-flip identity), the three routes now agree
  exactly. Two compensating-error patterns were exposed by convergent
  independent evidence — the review architecture worked.
- Adversarial-review upgrades adopted wholesale: noise-side fork
  classification (MAJOR), planted-defect fork control (MAJOR), u_b
  symbolic scan, config-independence gates, grade-stage completeness
  gates, PV-leak reality gate, full pole-data recording.
- Assumption-mismatch symbol traps (the campaign's recurring class):
  plain-vs-real `u` (Tier-2 lesson, guarded), plain-vs-real `q_i`
  (caught on the ward config — the only q-carrying config; the three
  physical configs are exactly q-free, so no loop result was touched).
- Harness: several lambdify under-bindings caught by crashes, fixed.
- **Mutation battery** (calc-layer floor), run in an isolated scratch
  copy (the Tier-2 overwrite lesson): **M1** (sign-flipped vertex
  C-entry) KILLED — 4 failures at assemble (config gates) + 6 at flat
  (independent-route and both isotropy gates); **M2** (flipped
  distributional sign) KILLED — 6 failures; **M3** (swapped Wightman
  ordering) KILLED — the absorptive response collapses to zero;
  **M4** (assembly prefactor 1/2 -> 1/4) **NOT COMPLETED — timed out**
  on a loaded machine (>4 h in the integrand serialization step) and was
  killed rather than left running. Disclosed as an open verification
  item: M4's target constant is separately covered by the toy-gate
  calibration and by the three-route agreement on the absolute flat
  coefficient (a prefactor error would break both), but the mutant
  itself was not executed to completion. **3 of 4 killed; 1 unrun.**

## SCOPE LIMITATIONS

k_ext = 0 evaluation point (D1 order inherited; finite-k_ext at the K_R
tier). Wigner base-time convention declared; kernels gated
u_b-independent through O(H²) (accidental to the H² budget — recorded).
The H² sector's pole data is classification-grade only; nothing in it is
integrated or banked. The Kubo/Dyson sign convention for Σ in the
dressed G_R is recorded for the K_R tier to match against the frozen
matter-sector convention — not re-adjudicated here.

## HARD STOP

No final K_R, no G_R^TT dressing, no D5, no full D4, no benchmark
consequence cells, no comparator, no new pole classification, no bath or
Tier-1 edits, no Ward repair. **The loop record is frozen pending owner
inspection; the fork-(ii) decision is on the owner's desk.**
