# TIER-3 FORK ADJUDICATION — DOES K_R CONSUME THE DIVERGENT NOISE OBJECT?

**Date:** 2026-09-01 · **Owner brief:** one bounded audit of the frozen
SK/K_R definitions; classify A/B/C; no regulator, no H² integration, no
IR scale, no retroactive criterion change, no modification of the frozen
T3 result. **Executed as a READ of the frozen record.** The frozen T3
artifact (sha 4c016e93…) is untouched.

---

## 1. THE FROZEN DEFINITIONS, VERBATIM

**rung1_inin_formalism** (register, tier `shown`): *"The gravitational
vacuum's response is described by a single Schwinger–Keldysh influence
action S_IF with **retarded dissipation kernel K_R** and noise kernel N
(doubled x_r/x_a fields)."* — K_R is BY DEFINITION the retarded member
of the pair; N is its partner within S_IF, not a component of K_R.

**rung2_kms_gate** (register, tier `shown`): *"In equilibrium the noise
kernel N is locked to Im[chi] by FDT with a coth factor; admissible
kernels must satisfy KMS detailed balance."* And the decisive ledger
note: *"**−1: N removed as an independent input (locked to Im chi by
chi + T_KMS)**."* — the register itself DE-BOOKED N as an ingredient:
in the frozen accounting, N carries no independent physical content; it
is DERIVED from the retarded data (in equilibrium), and the gate is a
VERIFICATION (*"any candidate (chi, N) pair whose residual
|G_K − coth·(G_R − G_A)| exceeds tol FAILS"*).

**K_R_CHARTER_AUDIT** (frozen): *"K_R is the retarded dissipation
kernel of the influence action"*; the construction chain *"(T2)
influence-functional reduction to the probe's quadratic **retarded**
[kernel]"*; **K_R_OWNER_CHARTER §2**: the register's frontier note
names the missing input as *"the TRANSPORT SELF-ENERGY Sigma
controlling **G_R = 1/(G0⁻¹ − Σ)**"* — the retarded Dyson form,
verbatim.

**MICROSCOPIC_TARGET_BENCHMARK** (frozen): the ENTIRE decision
apparatus is stated on retarded data — the convergence table's rows are
Im χ statements (*"finite-T s_eff=2 (Im χ ~ ω) | converges"*, *"Ohmic
s=1 (Im χ → const) | log divergent"*, *"**class-A white floor,
s_eff → 0** | power divergent"*): **the white floor itself is an Im χ
(response-exponent) statement, not a noise-spectrum statement.** Both
adjudication axes (χ(0) convergence; relaxational/resonant character)
consume χ_R. The three-way fork (i)/(ii)/(iii) is about χ(0).

**K_R_CONTRACT_EXECUTION_CHARTER** (frozen): the object table books
*"S_IF / N | the SK influence action and its noise kernel; **N =
coth·Im χ (rung2 FDT lock)** | free objects — KMS-gated"*; Tier-4
**gate E**: *"the retarded/noise (K_R, N) pair verified against the
rung2 FDT lock."*

**WALL_A_A3_DECLARATIONS** (frozen fence): *"STATE SPECIFICATION ≠
KMS/detailed-balance OF THE INTERACTING RESPONSE"* — whether the
interacting kernels satisfy the FDT lock is a COMPUTED outcome, never
an assumption.

## 2. THE DEPENDENCY, AS FROZEN

    Sigma_R --> G_R = 1/(G0^-1 - Sigma)  [retarded Dyson, verbatim]
        --> influence-functional reduction to the probe's quadratic
            RETARDED kernel  [charter audit T2]
        --> Im chi --> both benchmark axes, omega -> 0 LAST  [D1]

    N enters the chain in exactly ONE place: Tier-4 gate E, as the
    FDT-lock VERIFICATION partner (N = coth . Im chi in equilibrium)
    -- and the register's own ledger removed N as an independent input.

## 3. CLASSIFICATION: **B** (determinate, with one chartered caveat)

**K_R^(contract) as frozen is retarded-only.** No frozen text makes the
independently-assembled SK noise sum Σ_> + Σ_< an ingredient of K_R; the
register de-booked N as an input; the benchmark's decision variable —
including the white-floor arm of the live conflict — is stated on χ_R.

**The chartered caveat (stated, not resolved):** Tier-4 gate E requires
verifying the (K_R, N) pair against the equilibrium FDT lock. The T3
finding (noise α = −2 vs retarded α = −1 at O(H²)) means that AT O(H²)
the actual SK noise of the graviton loop and the FDT-image of the
retarded data cannot both be finite — the verification as written
cannot pass there. Under the frozen fence this is a **computed
KMS-admissibility finding about the non-stationary dS state at O(H²)**
(the same physics Tier 2 recorded: dS thermality is a static-patch,
non-perturbative statement; the graded state satisfies the T = 0
adiabatic FDT per order, and the divergence lives in the
secular/occupation sector). It lands as a rung2-scope finding for the
owner to classify — it does not enter the K_R construction itself.

## 4. EXACT CONSEQUENCE FOR THE H² FORK

1. **The noise-side α = −2 power divergence is retained as a separate
   SK-state finding** (per the owner's B-branch instruction): it
   obstructs the Tier-4 gate-E FDT verification at O(H²) and any
   downstream use of the secular/equal-time class — for THOSE objects
   the fired fork stands at full power-class price
   (`WALL_KR_TIER3_FORK_INVOCATION.md` remains the decision document).

2. **The retarded α = −1 does not, by itself, obstruct the frozen
   retarded chain.** The kernel-level α is a statement about the
   BI-TIME kernel Σ_R(u,u′) at fixed Δ (its mode integral is
   log-divergent — that object does not exist without a scale). But the
   frozen D1 ruling defines the contract object at FIXED ω (k → 0
   first; ω → 0 LAST), the Dyson dressing operates at fixed ω, and the
   fixed-ω absorptive data is δ-supported at q = ω/2 — the q → 0 region
   is never sampled, the same protection Tier 2 proved for the bath.
   This is the Tier-2 split reappearing at loop level: fixed-ω objects
   constructible with NO IR scale; fixed-Δ/equal-time representations
   divergent.

3. **Where the retarded H² IR question actually lands:** in the
   benchmark's OWN three-way fork, evaluated ω → 0 LAST at the K_R
   tier — which is where the frozen record always intended it.
   Scaling-grade reading of the RECORDED H² cone coefficients (no
   integration performed; the H-grading is a series in H/ω, so each H²
   carries ω⁻²): Im Σ^{H²}(ω) ~ H²ω² against the flat ω⁴ — the
   dispersion integral's IR end is then convergent at fixed ω > 0, and
   the ω → 0 adjudication limit reproduces exactly the H²/ω² validity
   boundary Tier 2 recorded. **Consequence: the ω → 0 decision axis at
   contract scope cannot be evaluated inside the graded truncation**
   (declared in Tier 2: "Option-A/fork territory") — but that is the
   benchmark's adjudication OUTCOME structure, not a missing input to
   the construction. One substitution-level evaluation of the recorded
   H² cone data (forbidden under the current "do not integrate H²" —
   flagged, not performed) would convert the scaling-grade statement to
   gate-grade on the owner's authorization.

4. **Nothing is un-fired retroactively.** The T3-1 criterion, its
   amendment, the fired verdict, and the frozen artifact stand exactly
   as committed. This adjudication determines what the fired fork
   GOVERNS: (a) the noise/secular class and the gate-E verification at
   O(H²) — fork fully active, owner must classify or price; (b) the
   retarded fixed-ω chain — constructible as frozen, with its IR
   question owned by the benchmark's preregistered three-way fork at
   the K_R tier.

## 5. WHAT WAS NOT DONE

No H² integration; no regulator; no IR scale; no criterion change; no
modification of any frozen artifact; no choice among the three
IR-completion candidates; no evaluation of the benchmark cell. The one
quantitative statement above (Im Σ^{H²} ~ H²ω²) is dimensional scaling
of coefficients already recorded in the frozen T3 artifact, labeled
scaling-grade, and its gate-grade confirmation awaits authorization.

**HARD STOP.**
