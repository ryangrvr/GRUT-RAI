# μ-CONVENTION AUDIT AND RULING

**Date:** 2026-09-01 · **Authorization:** owner, post-Axis-2 ·
**Instrument:** `wall_kr_mu_convention_audit.py` · **Artifact:**
`WALL_KR_MU_CONVENTION_RESULT.json` · **Battery: 20/20, zero failures.**
**Frozen inputs touched: NONE.** Governance + theory-convention audit —
**no spectral calculation was performed.** Axis-2 remains **C**,
unchanged and not re-adjudicated.
**W-0: computed-and-reported, NOT banked. HARD STOP.**

## RULING: **μ-RULING-C**

**NO PRE-EXISTING NUMERICAL μ CONVENTION; A NUMERICAL μ WOULD BE A NEW
INPUT.**

The ruling is derived mechanically from the candidate table below — it
is the count of candidates that supply a number at contract scope — and
no Axis-2 quantity enters the derivation.

## A. AUTHORITATIVE SOURCE DECLARATIONS

1. **`WALL_A_A3_DECLARATIONS.md`** (sha `87e2d24d…`, frozen, predates
   Axis-2), Declaration 1 / F2: *"MINIMAL SUBTRACTION — pole terms only
   are subtracted; finite parts … left exactly as the loop produces
   them; **μ is kept symbolic and its dependence recorded** as part of
   Π_local^scheme's data."* — **a refusal to fix μ**, verified to
   assign no numeric value anywhere in the document.
2. **`K_R_CONTRACT_EXECUTION_CHARTER.md`** (sha `5416fa45…`, frozen),
   STEP 10: only *"VALIDATED MACHINERY … and the sign DICTIONARY"* cross
   the matter/contract boundary; *"No parameter … transfers."*
3. **`K_R_OWNER_CHARTER.md`**: fixes the **amplitude** chain (A1 vertex
   normalization, master-unit measure anchored by the A3-1 Im law, the
   friction convention) and assigns **no** numeric renormalization
   scale.

## B. DIMENSIONAL RESULT

μ enters exactly twice: as the measure factor **μ^(3−d)** of the D5
*spatial* continuation, and inside **log(μ²/ω²)** in the kernel.
Executable checks: μ·∂Σ/∂μ = 2Aω⁴ is μ-independent, and with the
certified local slot the kernel is **exactly homogeneous of degree 4**
under the joint rescaling (ω, μ) → (λω, λμ).

**[μ] = [ω] = [WC] = frequency** (equivalently spatial wavenumber; the
bath is massless with ω = |k|). Consequently μ/WC and ω\*/μ are
dimensionless, and — as the owner anticipated — **identifying μ
numerically with 1 × WC (or any multiple) is an additional
renormalization convention, not a dimensional necessity and not
something D5 derived.**

## C. CANDIDATE CONVENTIONS

| candidate | provenance | status | supplies a number? |
|---|---|---|---|
| (i) Declaration 1 MS, μ symbolic | frozen, predates Axis-2 | **registered — but a refusal to fix μ** | **No** |
| (ii) matter-stage MS-bar with μ = 1 | `wall_j_omega_comparison.py` line 211, inside `chi_A` ("the FROZEN response, **evaluated** as computed") | an **evaluation slice** in a comparison instrument, **matter scope**, in that kernel's master units; STEP 10 bars parameter transfer — a numerical renormalization scale is a *parameter*, not convention algebra | **No** |
| (iii) μ = WC (plant validity scale) | WC = 1.0 defined in `wall_a_g1_ohmic_plant.py` | **BARRED BY CONSTRUCTION** — that file is a registry barred input ("G1 Ohmic plant (carries registered J(ω) explicitly)"), and the registry's `forbidden_direction` is *"registered J(omega) → Sigma_R construction"*. Setting the **response's** renormalization scale from the **comparator's** validity scale is exactly that forbidden flow | **No** |
| (iv) μ = ω, or any ω-dependent choice | nowhere declared | would be invented here; also not a fixed scale | **No** |
| (v) a physical scale (H, m, …) | nowhere declared for the contract H⁰ sector | the H⁰ sector contains no H by construction; m is matter scope | **No** |

**Count supplying a number at contract scope: 0.**

## D. INDEPENDENCE FROM AXIS-2

Checked at **source level**: the audit's executable body contains no
Axis-2 outcome token — no regime name, no boundary number, no ω\* value
— with the audit's own token list excised from the scan by
self-immune markers. The Axis-2 artifact is hash-pinned for provenance
only; its `out` block is never dereferenced. The ruling therefore
cannot have been steered by the spectral result.

## E. NEW-INPUT TEST

Zero pre-existing conventions supply a numerical μ at contract scope.
Adopting one would **not** merely instantiate an already-registered
convention — it would **introduce a new free input**: a
renormalization-point declaration.

**What IS registered:** the *scheme* (dS-invariant dimensional
regularization per Declaration 1; the spatial continuation per Option
β) and the *subtraction* (pole-only MS, zero finite discretion).
**What is NOT registered: the renormalization POINT.**

## F. OWNER RULING (the finding, not a choice made here)

**μ-RULING-C stands.** If the owner elects to declare a numerical μ, it
must be **priced as a new register input** and justified from the
normalization/renormalization setup **alone** — the critical principle
bars any spectral, memory, benchmark, or outcome-referencing
justification. This audit deliberately does **not** convert C into A or
B by inventing a convention.

## DEFECT HISTORY (gate-side, disclosed)

The anti-circularity gate failed **on itself** twice: its banned-token
list necessarily contains the forbidden strings (run 1), and the
excision markers then matched their own finder lines rather than the
comment block (run 2). Both repaired — the marker is now built by
concatenation so the finder cannot match itself. No physics involved.

## HARD STOP

Axis-2 status: **C, unchanged.** No frozen artifact was read for its
content beyond the declarations quoted, and none was modified.
