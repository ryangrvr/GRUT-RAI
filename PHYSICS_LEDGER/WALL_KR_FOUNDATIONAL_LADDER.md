# FOUNDATIONAL-LADDER AUDIT — u3 / u4 / u6

**Date:** 2026-09-02 · **Instrument:** `wall_kr_foundational_ladder_audit.py` ·
**Artifact:** `WALL_KR_FOUNDATIONAL_LADDER_RESULT.json` · **Battery: 27/27, zero failures.**
**Read-only** (register sha256 identical pre/post; worktree unchanged). **No physics. No
A-F selection. No graph mutation.** W-0.

## THE ANSWER: THEY ARE A LADDER, AND THE REGISTER ALREADY SAYS SO

The question is settled by the register's own text. **`u4`'s statement contains the ladder
verbatim:**

> "THREE distinct layers: why-split (U3, Frontier 2) -> why-constitutive (U4, Frontier 3) ->
> which-kernel (rung3, Frontier 1)."

So the three questions are **not** independent research programs. The ordering was written
down at booking time — and **none of it is encoded as a graph edge.** All three nodes declare
`depends_on: []`.

## THE MATRIX

| | u3_split_origin | u4_constitutive_origin | u6_constitutive_order |
|---|---|---|---|
| **A — graph dependency** | none | none | none |
| **B — conceptual prerequisite** | **none in the register** | **u3's OBJECT** ("GIVEN coarse-graining") | **u4** ("a branch of U4") |
| **C — shared terminology only** | — | no | no |
| **D — documentary** | — | the stated ladder | its own first clause |
| **classification** | **CONFIRMED INDEPENDENT** | **CONDITIONALLY INDEPENDENT** | **CONCEPTUALLY DOWNSTREAM** |

`emergence_chain` is **type D** — documentary/rendering dependence on the register. It is not
part of the ladder; it renders whatever the register holds.

## THE DISTINCTION THAT DECIDES THE TAXONOMY

`u4` presupposes u3's **object** but is explicitly **not entailed by u3's answer**. Its own
statement says so:

> "DISTINCT from U3 (why coarse-grain at all): deriving coarse-graining does NOT hand you
> linear response."

This is why `u4` is **conditionally independent** rather than conceptually downstream. It
needs coarse-graining *granted*, not *explained*. **You can pursue u4 without resolving u3** —
and the register says resolving u3 would not answer u4 anyway.

`u6` is different: it is a self-declared *branch* of u4, and its object — constitutive
organization — is precisely the existence u4 asks the origin of. It is genuinely downstream.

`u3` presupposes nothing in the register. Its statement names U1's presupposition and places
itself *below* rung1. Nothing registered is its prerequisite, which is why it earns
CONFIRMED — **on content, not on its empty edge list.** The gate forbids the latter, and no
node here was granted independence on that basis.

## MISSING EDGES — FOUR, REPORTED ONLY

1. `u4 -> u3` — presupposition of object plus the stated ordering.
2. `u6 -> u4` — "a branch of U4".
3. `u5 -> u4` — **also** "a branch of U4" (the whole U-family branch structure is unencoded,
   not merely one edge).
4. `u4 -> rung3` — the ladder's third rung.

Together with the `u5 -> chi(omega,k)` finding from `309d983`, the pattern is now clear: **the
dependency graph encodes calculational lineage but not conceptual architecture.** Nothing was
added or rewritten.

## u6's RELATION TO info_i2 IS AN ANTI-DEPENDENCY

Worth stating because it looks like a hidden dependency and is not. `u6`'s overturning clause
*guards against* info_i2: "do NOT re-import info_i2's refuted zeta / beyond-standard
machinery", and names "info_i2-adjacent" as a **first-class failure mode**. Adjacency is a
declared outcome to be avoided, not an inherited prerequisite — so u6 does not pick up
info_i2's owner-decision dependence.

## WHERE THE EXISTING CAMPAIGN SITS

The register's own ladder places the entire K_R campaign at the **third rung** —
which-kernel / rung3 / Frontier 1, the *least* foundational layer. `u3` and `u4` are strictly
more foundational layers that the register **already maps but has never executed**.

That reframes the current blockage. A, C and F block progress *along the third rung*. They do
not block the first two, which have never been started.

## FOUNDATIONAL RESEARCH ORDER

The smallest scientifically coherent ordering — **stated, not recommended for execution**:

1. **`u3_split_origin`** — why a system/bath split at all. Deepest; presupposes nothing
   registered; upstream of the contested lineage.
2. **`u4_constitutive_origin`** — why coarse-graining yields response form. Needs u3's object
   as *given*, not u3's answer — so it may be pursued **in parallel with, or before**, u3 is
   resolved.
3. **`u6_constitutive_order`** — order parameter for constitutive organization. Its object
   exists only once u4's subject matter is granted.

The one ordering constraint that is genuinely binding is **u6 after u4**. The u3/u4 relation
is an ordering of depth, not of entailment.

**No execution is proposed, and no node is recommended over another.**

## HARD STOP

Read-only confirmed by hash and worktree. A-F unselected. No physics, no low-frequency
regime, no graph mutation, no register rewrite, no banking. Four missing edges reported and
left unwritten.
