# FORK (ii) INVOCATION — THE O(H²) CONTRACT-LOOP SECTOR (owner decision required)

**Date:** 2026-09-01 · **Invoked by:** Tier 3's T3-1 IR-fork gate (executed
before any radial integration; criterion amended per the adversarial review
to classify BOTH CTP combinations — the amendment is disclosed in the run
record and predates every graded-sector integration). **Status: INVOKED,
NOT ADOPTED. Nothing is regulated, subtracted, or integrated in the fenced
sector. The decision below is the owner's alone.**

## What fired, exactly

The one-loop contract-level self-energy of the TT probe (frozen Tier-1
vertex × frozen Tier-2 massless bath, k_ext = 0 evaluation point of the D1
limit), classified per H sector by the small-q Laurent exponent α of the
exact angular-averaged radial integrand (measure q^{d−1}, d = 3; u_b
symbolic, pole coefficients gated u_b-free; configuration-independence
gated across three external configs):

| sector | retarded Σ₋ = Σ_> − Σ_< | noise Σ₊ = Σ_> + Σ_< | verdict |
|---|---|---|---|
| H⁰ | α = +1 | α = 0 | IR-integrable — proceeds |
| H¹ | vanishes identically | vanishes identically | empty |
| H² | **α = −1 (scaleless log)** | **α = −2 (scaleless POWER)** | **FORK FIRES** |

This is the Tier-2 armed class realized in the loop: the k⁻³ superhorizon
noise enhancement (exact in the BD state) enters the O(H²) loop integrand
with only a partial parity cancellation in the retarded combination and
none in the noise combination. The full pole coefficients of both
combinations (all orders ≤ −1, symbolic Δ, d = 3) are recorded in the
Tier-3 artifact (`fork_verdicts` and the `H2_fence_record`).

**The price is set by the noise side (α = −2, power class), not the
retarded log** — the Tier-2 armed definition is noise-stated, and the K_R
contract's FDT/noise leg consumes exactly the combination that diverges
faster. Pricing it at log strength would understate the input by one full
divergence class (adversarial-review finding, upheld).

## What the fork requires (frozen wording)

The benchmark's fork (ii), verbatim class: *"the white floor is right but
an IR cutoff exists — then it must be **named and priced** (a new register
input)."* The D3 ruling: an IR scale *"may not appear as an implementation
convenience."*

## The named candidate input (drafted for the owner; NOT adopted)

**Name:** an infrared completion prescription for the superhorizon
(k ≲ H) graviton-bath occupation entering the O(H²) contract loop.
Candidate physical forms the owner may choose among (each a DIFFERENT
register input with different downstream physics):

1. **Horizon-scale occupation cutoff** — the BD superhorizon modes are not
   part of the *bath* (they are background/zero-mode sector): bath support
   restricted to k > εH with ε an O(1) parameter. Price: one new
   dimensionless register input ε (tier: assumed-unless-derived), and the
   O(H²) loop becomes ε-log-dependent (retarded side) and ε-power-dependent
   (noise side).
2. **Secular-sector resummation** — the k ≲ H modes are treated
   nonperturbatively (stochastic/secular-growth handling), outside the
   graded expansion. Price: a new methodological register input (the
   resummation scheme), strictly beyond the frozen Option-B grading.
3. **The white-floor reading** — the divergence is physical: the O(H²)
   contract response is genuinely dominated by the superhorizon sector and
   the response framework needs reformulating at this order (the
   benchmark's "class-A was right" arm applied at O(H²) only, with H⁰
   intact).

The builder recommends none of them; the frozen adjudication table and the
owner's charter govern. **Until ruled: the H² sector stays fenced, the H⁰
flat anchor is the only banked-class loop content, and every Tier-3
deliverable that references H² carries this fence explicitly.**

## What does NOT fire

The H⁰ (flat) sector is IR-integrable in both combinations and fully
validated (three independent routes agree exactly on the absorptive
coefficient). The bath itself remains a validated input per Tier 2 — this
fork concerns the LOOP's sampling of the armed class at O(H²), exactly
where Tier 2's armed-fork condition said it would live.
