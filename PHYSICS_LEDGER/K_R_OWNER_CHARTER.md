# K_R OWNER CHARTER — the three inputs, the unblock list, and the tiered plan

**Date:** 2026-08-30 · Definition stage under the owner's K_R ruling.
**NO K_R COMPUTATION OCCURRED.** No frozen artifact modified; the +1 ruling
untouched; Σ_R immutable. Baseline `f5c674e`.
**Companion:** `K_R_OWNER_CHARTER.json`.

**The central structural finding (from the frozen record, before the tiers):**
the record contains **two distinct K_R-scope objects**, and conflating them is
the error this charter exists to prevent:

- **K_R^(matter)** — the frozen massive-scalar Σ_R^TT dressed into a probe
  kernel. The probe–bath coupling is the countersigned A1 h–φφ vertex
  (exists); no new loop integrals arise. **The frozen unblock list does not
  apply to this object.**
- **K_R^(contract)** — the gravitational vacuum's own kernel, the object the
  benchmark's consequence cell and `rung3` are written on. Its bath includes
  the graviton's own massless modes (`rung3`: *"relativistic massless fast
  modes"*), which is exactly why the frozen unblock list demands the dS
  TT-TT-TT vertex (Tsamis–Woodard class), D5 renormalization, and D4
  dual-gauge — *"not approximable, not substitutable, not bypassable."*

K_R^(matter) is cheap and answers *"what does the computed response dress
into."* K_R^(contract) is the multi-session object that adjudicates the
benchmark cell at contract scope. **Which one any future stage builds is an
owner scope decision, made here, in advance.**

---

## 1. PROBE KINEMATICS — DETERMINED IN PART; residual choice flagged

**What the frozen record determines:** the probe is a long-wavelength TT
metric perturbation (the staged work is named `gw_tensor_friction`; the
plant's registered convention is the ω-only friction kernel Im χ = J/ω); the
benchmark object carries **no k**. The bath-side mode integration is already
inside the loop (our Σ integrated it); the k in Σ_R^TT(ω,k) is the **probe's**
momentum. Therefore the ω-only reduction is the probe's k → 0.

**What the record does NOT uniquely determine:** literal k = 0 evaluation vs
a controlled k → 0 limit. **UNDERDEFINED — residual choice for the owner**,
with the audit's observations recorded (not as a ruling): at k = 0 the TT
projection needs a direction and is defined by continuity/isotropy of the
k → 0 limit; a *controlled limit with an executed isotropy gate* (the limit
computed along two axes and required to agree) is the only version that makes
"the transverse projection remains well-defined" a checked fact rather than
an assumption. Not chosen because it simplifies anything — chosen, if the
owner confirms, because it is the only self-verifying option on the table.
**AWAITING OWNER CONFIRMATION.**

## 2. DYSON TRUNCATION — FORM DETERMINED BY THE FROZEN TEXT; outcome open

**The frozen record determines the form.** `rung3_single_pole`'s own frontier
note names the missing input as *"the TRANSPORT SELF-ENERGY Sigma controlling
**G_R = 1/(G0⁻¹ − Σ)**"* — the geometric/resummed Dyson form, verbatim in the
register. The register's "single-pole" question is a question about the poles
of **that resummed object**; a first-order insertion cannot even express it.
**Ruling recorded: RESUMMED, per the frozen text — not per any preference.**

**Mandatory disclosures attached to this ruling (owner's instruction,
executed here):**
- A resummed denominator with a branch-cut Σ can generate poles that the
  bare cut does not have. **This is an OPEN OUTCOME, not an expected one** —
  either result (pole emerges / no pole emerges) is a genuine rung3 finding,
  and the truncation choice now exists *before* the answer does.
- Resumming a one-loop Σ is a declared scheme whose O(Σ²) content is beyond
  controlled accuracy; every K_R artifact must carry this validity-domain
  statement on its face. A first-order-insertion cross-check is REQUIRED
  alongside the resummed object (agreement region = the controlled domain;
  divergence between them = where resummation is doing uncontrolled work —
  reported, never hidden).

## 3. PROBE-COUPLING NORMALIZATION — DETERMINED BY DERIVATION; gate owed

No freedom exists in the frozen record: the chain is fixed by (i) the
countersigned A1 vertex normalization (κa²/2 form), (ii) the engine's
master-unit convention (16π² × standard measure, anchored by the A3-1 Im law
— the same anchor the PV normalization-theorem gate used), and (iii) the
registered friction convention Im χ = J/ω. **Status: DEFINED BUT NOT YET
EXECUTED** — the derivation is a Tier-1 theorem gate (derive the constant,
verify it is kinematics-independent), never a fit, and **fitting it to J(ω)
is barred** exactly as the owner ordered.

## 4. THE FROZEN UNBLOCK LIST — classified per item

| item | for K_R^(matter) | for K_R^(contract) |
|---|---|---|
| dS TT-TT-TT vertex | **CAN BE OMITTED ONLY BY OWNER RULING** (the matter bath couples via A1, which exists; omission = the scope ruling itself) | **REQUIRED** (frozen text: "not bypassable") |
| D5 renormalization | **DEFINED AND ALREADY EXECUTED** at the frozen level (no new loop integrals arise in the dressing) | **REQUIRED, NOT YET EXECUTED** (graviton-loop content is new) |
| D4 dual-gauge | **DEFINED AND EXECUTED at Σ level** (A4, operator identity through O(H²)); carrying it through the dressing is a Tier-1 gate | **REQUIRED, NOT YET EXECUTED** |

No item is bypassed for convenience: the matter-scope column is not a bypass
of the contract-scope column — it is a **different, smaller object**, and the
charter says so everywhere it appears.

## 5. THE TIERED CHARTER (Tiers 1–3 NOT executed in this stage)

**TIER 0 — definitions/conventions (THIS DOCUMENT).** Inputs: the frozen
record. Outputs: the rulings above + the two open confirmations (probe-limit
prescription; scope 2a vs 2b). Cost: none. Hard stop: owner confirmation.

**TIER 1 — validated components.** (a) normalization theorem gate (§3):
minutes. (b) k → 0 isotropy/continuity gate on the frozen kernel: minutes.
(c) Dyson algebra gates: the resummed and first-order objects built
symbolically from the immutable Σ, KMS/passivity and retarded-analyticity
gates executed, negative controls (wrong-sign dressing must break passivity):
hours. (d) *contract scope only:* the dS TT-TT-TT vertex under the frozen
D5/D4 contracts: **multi-session, Tsamis–Woodard class** — the honest cost of
the full-contract path. Hard stop after each component; every gate green
before the next.

**TIER 2 — K_R construction.** **2a (matter scope):** dress the frozen Σ per
Tiers 0–1, reduce to the probe kernel at the confirmed kinematics; no new
loop integrals; cost: hours. **2b (contract scope):** the graviton-loop
assembly consuming Tier 1(d); cost: multi-session (a campaign of its own,
comparable to Wall-A's loop work). Independent validation both cases: the
dispersive route (PV machinery, validated to 3.5e-6) against the direct
algebra. Acceptance: gates green + controls behaving + validity-domain
statement on the artifact face. Hard stop before Tier 3.

**TIER 3 — benchmark consequence.** The two-axis evaluation (convergence
boundary + relaxational/resonant) on K_R, exactly as the benchmark froze it;
cheap once Tier 2 exists. **What K_R^(matter) can adjudicate:** the
matter-sector statement and the Dyson pole-from-cut question for the computed
response. **What it cannot:** the benchmark's consequence cell at contract
scope and rung3's anchor question about the *vacuum's* kernel — those bind to
K_R^(contract) unless the owner rules the matter scope sufficient. Hard stop:
owner adjudication of the cell.

## 6. WARD FINDING — K_R is TT-scoped by definition; the residual is EXCLUDED, not resolved

K_R is defined on the TT sector (the `p_tt_ansatz` form; the GW probe). The
Class-B vector residual has zero TT projection by executed gates (A4/W5), so
K_R **deliberately excludes** it by construction — it neither requires
resolving it nor resolves it. The two findings remain separate records.

## 7. MASSIVE/MASSLESS BRIDGE

- For K_R^(matter): **NOT REQUIRED**.
- For K_R^(contract): **SUBSUMED** — the contract bath is the massless
  graviton sector, so the "bridge" is not a patch on the matter object but an
  intrinsic property of the contract-scope assembly.
- As an interpretive question (matter-scope K_R vs the massless-derived s=3
  family): **SEPARATE OPEN QUESTION**, exactly as the J-adjudication recorded.

## 8. Statement of non-computation

No K_R quantity, vertex, dressing, or limit was computed in this stage.

## 9. The two confirmations that gate everything downstream

1. **Probe-limit prescription** (§1): controlled k → 0 with executed isotropy
   gate — confirm or replace.
2. **Scope** (§5): charter Tier 2a (matter, cheap, answers the Dyson question
   for the computed response), Tier 2b (contract, the full multi-session
   object), 2a-then-2b, or decline.

Until both are given, the hard stop stands and nothing is built.
