# K_R^(contract) — OWNER DECISION SHEET (the three declarations before Tier 1)

**Date:** 2026-08-30 · Decision support ONLY. No computation. No vertex build.
Frozen sources quoted; where the record is silent, that silence is stated.
**Welded rule (owner, verbatim class): the massless-bath IR prescription may
not be chosen for computational convenience.** To make that enforceable, each
option below carries an explicit "convenience profile" — so convenience is
visible, and visibly not the motive.

Decision tree: **declare (D1, D2, D3) → Tier 1 (TT-TT-TT vertex) → massless
bath → D4/D5 → K_R^(contract).**

---

## DECLARATION D1 — probe kinematics at contract scope

**The question:** what kinematic prescription defines the ω-only contract
object from the (ω, k)-dependent response?

**Frozen bearings:**
- The staged work is named `gw_tensor_friction` — the probe is a
  long-wavelength gravitational wave (benchmark + Bardeen status doc).
- The registered convention is ω-only: *"Im chi = J/omega (friction)."*
- Matter-stage precedent (owner-ruled 2026-08-30, matter scope only):
  *controlled k → 0 limit + explicit isotropy gate*, literal k = 0 not
  silently substituted.

**What the frozen record does NOT fix:** whether the matter prescription
transfers to contract scope — and, critically, the **order of limits**, which
was harmless for the massive bath and is NOT harmless here (see D3: the
massless cut reaches ω = k, so k → 0 at fixed ω, ω → 0 at fixed k, and joint
rays ω ∝ k can inhabit different IR classes).

| option | consequence downstream | convenience profile |
|---|---|---|
| **1a** adopt the matter template (controlled k→0 + isotropy gate, order: k→0 first at fixed ω) | methodological continuity; the s-class is then read along the fixed-ω slice; comparability with the ω-only registered family is direct | moderate cost; the gates exist |
| **1b** literal k = 0 from the start | simplest algebra; but the TT projector needs the continuity argument, and the order-of-limits question is silently resolved rather than declared | cheapest — which is exactly why it must not win by default |
| **1c** declared finite-k window + reported k→0 extrapolation | most conservative; keeps the order-of-limits question visible in the data | most expensive |

**Interlock:** D1 and D3 jointly determine which IR limit the Q3-class
question is asked about. They should be declared together.

## DECLARATION D2 — the graviton-loop working gauge

**The question:** in what gauge is the TT-TT-TT loop computed?

**Frozen bearings:**
- The matter loop's own precedent: **gauge-UNFIXED, full h, orbit-tracked**
  — Declaration 5 records this as deliberate: *"A1 fixed NO gauge … the A4
  comparison is therefore gauge-UNFIXED (full h, orbit-tracked) vs
  synchronous, which is the stronger test: the second computation imposes a
  genuine gauge slice and must reproduce the gauge-invariant content of the
  computation that imposed none."*
- D4 (frozen) is the CHECK protocol — it presumes one side imposed nothing.
- The Tsamis–Woodard-class literature the frozen record names works in
  fixed gauges; the frozen record does not import their choice.

| option | consequence downstream | convenience profile |
|---|---|---|
| **2a** gauge-unfixed, orbit-tracked (the campaign's precedent) | D4 runs verbatim as frozen (unfixed vs synchronous); the Ward classification (Tier 3) has its clean reference object; heaviest term count | most expensive — but the only option needing NO protocol amendment |
| **2b** fixed working gauge + D4 as cross-gauge check | large cost reduction; but the frozen D4 asymmetry ("imposed none") is weakened — requires a V-series amendment to Declaration 5 BEFORE Tier 1, citing and superseding the frozen clause | cheap; must be bought with an amendment, not a shortcut |
| **2c** two fixed gauges, mutual check | intermediate cost; still amends D4's asymmetry | intermediate |

**Note:** whichever is chosen, the choice must exist before the first vertex
term is written — the same discipline that made today's Dyson-order ruling
meaningful.

## DECLARATION D3 — graviton-bath state + IR prescription (the consequential one)

**The question:** what state, and what infrared treatment, define the
massless graviton bath?

**Frozen bearings:**
- `rung3` (verbatim): *"relativistic massless fast modes (omega = c|k|)
  gives DOS ~ omega^2, J(omega) ~ omega^3 … WITHIN the collisional/
  analytic-bath regime, and PROVIDED the vacuum bath carries no second
  internal dynamical scale."*
- The matter-stage state declaration was **Bunch–Davies via the Option-B
  adiabatic route** (V3 amendment: expansion parameter declared, dressing
  consistent per order, flat anchor at H → 0, Option A retained as
  cross-check target).
- The benchmark's validity domain: ω ≪ ω_c; its **live conflict**: register
  s = 3 (convergent) vs **class-A white floor s_eff → 0 (power divergent,
  horizon-forced)** — *the IR prescription is exactly where this conflict
  lives.*
- The benchmark's own fork (ii) (verbatim): *"the white floor is right but
  an IR cutoff exists — then it must be **named and priced** (a new register
  input)."* An IR regulator is not bookkeeping; it is a register event.
- Today's J-stage record: the massive-loop response was GAPPED, which made
  the registered IR domain empty. **The massless bath CLOSES the gap** — the
  Q3-class question becomes live for the first time, and its answer will be
  whatever the IR prescription lets it be. This is why the welded rule
  exists.

| option | consequence downstream | convenience profile |
|---|---|---|
| **3a** BD-analogue adiabatic (Option-B extension: same chart, H-expansion, flat-limit anchor per order) | maximum continuity with the frozen methodology; the dS graviton IR subtleties (the record's own Tsamis–Woodard naming) enter order by order and must be classified, not regulated away | moderate; machinery exists |
| **3b** exact dS graviton state (Option-A analogue) as primary | strongest claim; hardest construction; the frozen record retains this class as CROSS-CHECK TARGET, not primary | most expensive |
| **3c** flat massless limit first (H = 0, m = 0) as primary, dS as robustness | cleanest IR question (pure massless flat bath: DOS ~ ω² is exactly rung3's stated regime); defers the dS-specific IR issues to a declared second stage | cheapest of the physical options — flagged as such |
| **IR sub-choice (applies to all):** dimensional continuation only, NO explicit IR scale | any divergence appears as a pole/log to be CLASSIFIED — the honest default; if an IR scale is later needed, it triggers the benchmark's fork (ii): named and priced in the register | — |

**What the three outcomes adjudicate (fixed in the benchmark before any of
this existed):** convergent ⇒ the register's side; divergent ⇒ *"class-A was
right; response framework needs reformulating"*; a cutoff-dependent answer ⇒
fork (ii), a new priced input. **Whichever option is declared, the outcome
classes must be written down before Tier 2 computes a single mode integral.**

---

## The recommendation structure (not a recommendation)

The sheet deliberately makes no choice. It records one asymmetry the owner
should see: options 1b and 3c are the cheap ones, and 2b is the cheap one
bought with an amendment. If the declared set happens to be {1a or 1c, 2a,
3a or 3b}, the project has chosen the expensive, maximally-preregistered
path everywhere; if any cheap option is declared, the sheet's convenience
profile documents that the choice was made with the cost visible — which is
what the welded rule requires. Either way the choices will exist before the
answers do.

**HARD STOP stands. Nothing computes until (D1, D2, D3) are declared and
Tier 1 is accepted.**
