{
 "artifact": "STATIC_PATCH_DISCHARGE_GATE_01",
 "date": "2026-09-14",
 "protocol": {
  "file": "program/STATIC_PATCH_DISCHARGE_GATE_01_PROTOCOL.md",
  "frozen_at": "e76953a",
  "edited_after_freeze": false,
  "charter_5_3_prescreen": true,
  "banked": null,
  "register_edits": 0
 },
 "verdict": "MIXED: DISCHARGE-UNAVAILABLE + NEW-OMISSION-SURFACED (UNDERDETERMINED rejected)",
 "execution_note": "first run lost adv-available + verifier to a session limit; harness refused an incomplete-panel ruling; resumed with cached completed roles, dead roles re-run live (wf_3716ed7d-f25)",
 "survey": {
  "rows": [
   {
    "result": "background_time_translation_flow (the node under discharge; assumed, +1)",
    "background": "None declared by the node itself — it prices a flow the formalism USES; the corpus's actually-declared background is flat FRW (S_IF.md §1), which carries no timelike Killing vector",
    "regime": "All regimes: it is the license for any single-frequency kernel K_R(ω,k). Its own discharge text concedes the WKB/quasi-stationary substitute holds only H≪ω",
    "label_structure": "Licenses the joint (ω,k) label corpus-wide (S_IF §3: 'at fixed (ω, k) in the enumerated frame')",
    "evidence": "provenance/claims.json node: statement 'a kernel of a single frequency K_R(omega, k) is definable. BOOKED 2026-08-18 as an OMISSION'; overturning_computation verbatim: 'OVERTURNED / DISCHARGED IF: the declared background is one carrying a timelike Killing vector -- the de Sitter static patch is the named candidate -- in which case the flow is a PROPERTY OF THE BACKGROUND rather than a commitment, and this node retires into that declaration at zero. ... Note the WKB escape is CLOSED and recorded at rung3_single_pole: quasi-stationarity holds for H << omega and the single-pole claim lives at omega -> 0, exactly where it fails.' R1 fence: 'Cosmological perturbation theory on FRW and the secular/non-stationary de Sitter programs work on a background with NO timelike Killing vector.' R5 wiring: edges into rung1_inin_formalism, rung1_ontology_finite_memory, rung2_kms_gate."
   },
   {
    "result": "kr_contract_retarded_tier4 (the Tier-4 kernel)",
    "background": "A THIRD background, neither flat FRW nor static patch: flat Minkowski contract (Tier-2 propagator G0_R(ω,k)=2κ²/((ω+i0)²−k²); Tier-3 flat absorptive value) with an O(H²) de Sitter-perturbative correction; node domain string: 'open-systems-EFT / de Sitter contract scope'; the loop declarations use BD mode functions positive-frequency w.r.t. η→−∞ (Poincaré/conformal chart, not the static patch)",
    "regime": "H≪ω, HARD-GATED: eps_H=(104/9)H²/ω² ≤0.1 controlled, 0.1–1 flagged boundary, ≥1 REFUSED; ω≪H is a raised exception. Already exactly the H≪ω effective object the re-declaration would require",
    "label_structure": "Single-ω retarded kernel K_R(ω)=Σ_R(ω) assembled from the joint-(ω,k) flat propagator; L=log(μ²/ω²)+iπ",
    "evidence": "Node statement: 'Validity: omega >> H with eps_H = (104/9) H^2/omega^2; omega << H refused by the evaluator.' PHYSICS_LEDGER/wall_kr_tier4_retarded.py:30-36: 'VALIDITY DOMAIN (T4-4, hard-wired) ... eps_H >= 1 : REFUSED (raises DomainRejected -- extrapolation into omega <~ H is forbidden, and the refusal is itself a gated control)'; header: 'the Tier-2 bare retarded propagator G0_R(omega, k) = 2 kappa^2/((omega+i0)^2 - k^2)'; 'HARD STOP ... no omega << H.' PHYSICS_LEDGER/WALL_A_A3_DECLARATIONS.md: 'positive frequency w.r.t. the BD vacuum in the far past η → −∞'; alternative-states table: 'thermal (static-patch) initial state | Would ASSUME the equilibrium that question (ii) must compute. PROHIBITED as an input for the primary run.'"
   },
   {
    "result": "calc/gw_tensor_friction.py (Γ_T closure computation, REFUSE verdict)",
    "background": "Flat FRW LCDM cosmic clock, by frozen declaration; kernel imported at its flat-contract scope",
    "regime": "LIGO band f=10–1024 Hz: H0/ω ~1e-20, so the H² validity term is 1.0 in double precision — deepest-possible H≪ω; horn (b) exhibits (unbanked) the ωτ₂≫1 achromatic limit and a z=0.5 lookback amplitude",
    "label_structure": "Single ω; explicitly forbids static-Killing quantities",
    "evidence": "Header verbatim: 'CLOCK DECLARATION (SPEC section 4 / keystone C5): single FRW COSMIC clock throughout. Every rate below is d/dt_cosmic; no static-Killing or e-fold quantity is imported.' Also: 'the flat-contract scope is carried via the [1 + (104/9)(H0/w)^2] validity term the kernel itself supplies -- no external-background transplant occurs.' Line ~151: 'hcorr ... printed directly: 1+hcorr == 1.0 in double.'"
   },
   {
    "result": "mu_linear (no-go export; the μ=Σ=η=1 conditional leg)",
    "background": "Flat FRW, fixed-slicing spatial-SVT frame (the KC5-fenced frame)",
    "regime": "Sub-horizon quasi-static per the corpus's own clock audit — but the empirical kill legs live at horizon scale and late times: low-ℓ ISW cross (~2.0σ) and DESI Σ0 lensing (~3.5σ), i.e. k~H0, NOT a deep-subhorizon-only statement",
    "label_structure": "k-space SVT decomposition at fixed slicing (k²Ψ Poisson bookkeeping); quasi-static ω→0 end of the joint label",
    "evidence": "S_IF.md §1: 'flat FRW for every banked observable; the linear-cosmology frame is the fixed-slicing spatial-SVT frame of mu_linear -- [eft_operator_basis, KC5 fence]'. RUNG3_KEYSTONE_MAP.md conversion row C5: 'mu_linear / ISW / sigma0-screen exclusions; GW dephasing bounds vs LIGO band | single FRW cosmic clock throughout | identity | sub-horizon quasi-static | derived | fine internally'. calc/isw_exclusion.py header: exclusion computed against 'the low-ell temperature-galaxy CROSS-CORRELATION'. KC5 fence (eft_operator_basis boundary_condition): 'spatial-SVT fixed-slicing frame ... exhaustive at all derivative orders WITHIN the frame; parity-odd ... and the 4d-covariant/Riegert-Paneitz completion NOT covered -- that portion FRONTIER-RESERVED'."
   },
   {
    "result": "rung2_kms_gate / gate/kms.py",
    "background": "Background-agnostic code, but presupposes global stationarity (single T, G_A=conj(G_R), pure ω-grid); its ledger_note ALREADY imports static-patch physics onto 'the declared background': T=T_dS=H/2π forced by horizon regularity — a horizon/Killing-flow fact invoked while the declared background is flat FRW",
    "regime": "All ω on a grid (ω=0 skipped); equilibrium/KMS statements only",
    "label_structure": "ω only; NO k label anywhere in the gate",
    "evidence": "gate/kms.py: 'G_K(omega) = coth(hbar*omega/2 k_B T) * (G_R(omega) - G_A(omega))', 'T<=0: KMS/detailed-balance undefined'. Node depends_on includes background_time_translation_flow. ledger_note verbatim: 'on the declared background the Hadamard/KMS condition forces T = T_dS = H/2pi uniquely (any other T is singular at the horizon...)'. RUNG3_KEYSTONE_MAP.md D6: 'T_dS = H/2π is the temperature of the Bunch-Davies state with respect to the static Killing (boost) flow ... Quoting coth(ω/2T_dS) against frequencies defined in any other clock silently transports a Killing-clock object across. The attribution is derived; the transport itself is the assumption, and it is currently unpaid.'"
   },
   {
    "result": "rung3_single_pole (the anchor conjecture)",
    "background": "Nominally the declared background; but its 2026-08-19 evidence campaign ran on the dS STATIC PATCH (Matsubara ladder, multipole tower), per static-patch Killing time",
    "regime": "ω→0 EXACTLY — the transport limit η=lim_{ω→0} Im G_R(ω)/ω — which is where H≪ω fails. The corpus states this itself and closed the WKB escape on it",
    "label_structure": "Single-ω kernel in the conjecture; the static-patch evidence uses a STATIC MULTIPOLE decomposition (l≥2, radial master equation), a different label from (ω,k)",
    "evidence": "boundary_condition WKB closure verbatim: 'the quasi-stationary/WKB defense of writing this framework's kernel at fixed (omega,k) on a non-stationary FRW background is VALID FOR H << omega -- and this node's claim is a statement about the OPPOSITE limit. ... So the WKB reading is valid in the regime the single-pole conjecture does not live in, and invalid in the one it does.' Tier_note: at fixed l 'the surviving rungs are the entire infinite tower n >= l+1 with lowest rate (l+1)H, multipole-dependent ... SINGLE-POLE ... NAMES A SINGLE RATE THAT DOES NOT EXIST AT FREE LEVEL'; and the owner scope correction: 'A POLE-FREE FREE RESPONSE IS NULL, NOT ADVERSE ... Everything that bears on this node rests on the interacting self-energy Sigma.' Its separately-reserved tier question stands (protocol hard fence)."
   },
   {
    "result": "rung4_love_kk / calc/gw_dissipation_bounds.py",
    "background": "Flat-space GW propagation over luminosity distance (SI units, D=40–400 Mpc), FRW only through distance anchors",
    "regime": "LIGO band 10–1024 Hz — H≪ω by ~20 orders; verdict real-but-unobservable (21–62 orders below sensitivity); KK causality statement is over all ω",
    "label_structure": "ω only (chi(ω), v_g(ω)); no k label; power-law branch fence added 2026-08-20 excludes the far-IR Lorentzian tail from the |Re|~|Im| step",
    "evidence": "calc/gw_dissipation_bounds.py header + F_BAND=[10,100,1024] Hz; 'SCOPE FENCE ADDED 2026-08-20 ... FALSE for a far-IR Lorentzian tail, where Im/Re = omega*tau -> 6.3e20 at 100 Hz for tau_2 ~ 1/H0.' Node tier_note: 'for a Planck-cutoff vacuum it is ~10^21-22 ... orders BELOW LIGO phase sensitivity ... Outcome (B) signed off -- CLOSED.'"
   },
   {
    "result": "rung7_wz / calc/wz_dark_energy.py (w(z) dark energy)",
    "background": "Flat FRW LCDM explicitly: E(z)=sqrt(OM(1+z)³+OL), OM=0.31, flat; FRW cosmic time",
    "regime": "ω ~ H(z) BY CONSTRUCTION — the opposite of H≪ω: the vacuum is forced at the Hubble frequency itself, x=H(z)τ with τ₂~1/H0, so H/ω ~ O(1). The corpus's clock audit adds that w(z) is not an axis quantity, so no static-patch reduction reaches it",
    "label_structure": "Neither joint (ω,k) nor static multipole: a time-domain relaxor w(z)=-1+eps·R(H(z)τ) on the FRW clock",
    "evidence": "calc/wz_dark_energy.py: 'the cosmological expansion forces the vacuum at frequency ~ H(z)'; 'OM = 0.31 # matter density; OL = 1-OM (flat)'. RUNG3_KEYSTONE_MAP.md §6.3: 'The framework's system/bath split and its cosmological exports (mu_linear, w(z)) live naturally on the COSMIC clock; modular/KMS structure lives on the STATIC clock; D2 shows these coincide only on the axis, while w(z) is not an axis quantity. Bridging them is either a theorem (derived) or a new priced input.' Node statement: SOURCED prediction is w=-1 flat; the evolving piece rests on the inserted two-scale commitment (+2 of +3)."
   },
   {
    "result": "calc/static_patch_tt_response.py (the program's existing static-patch computation)",
    "background": "dS STATIC PATCH, computed not cited: PART 0 checks R_mn=3H²g_mn on g=diag(−f,1/f,r²,r²sin²θ), f=1−H²r²",
    "regime": "Structure entirely at ω ~ H: purely imaginary discrete frequencies, gap and spacing both O(H) ('their ratio is O(1) and contains no small quantity'). FREE theory only",
    "label_structure": "STATIC MULTIPOLE decomposition (l, radial master equation, tortoise coordinate) — not the (ω,k) label; graviton has no l=0,1",
    "evidence": "File header (A): 'omega_{n,l}/H = -i[l + 2n + ...] PURELY IMAGINARY, DISCRETE'; (A-RETRACTED)/PART 3b: 'the exact Jost pair ... gives ingoing amplitude B = -A for EVERY omega. B never vanishes, so these are not quasinormal modes and nothing in the c = 0 family is'; 'every downstream part is now marked conditional.' What its scope establishes: (B)/(C) the graviton potential c=0 derived (axial sector), and the adverse tower-family evidence into rung3 — with the rung3 owner correction that the free static-patch response is 'NULL, NOT ADVERSE'. It is a calc-layer object; nothing banks a static-patch background declaration."
   },
   {
    "result": "kk_static_transfer (the static-modulus transfer question)",
    "background": "Frame inherited from mu_linear's coupling (spatial-SVT); background enters only through that frame",
    "regime": "ω→0 STATIC modulus, reached through a KK integral over ALL ω — needs the whole frequency axis including the ω→0 end where the H≪ω license fails",
    "label_structure": "ω-axis dispersion relation for the c0 channel; k enters only as the channel index",
    "evidence": "Node statement verbatim: 'does the per-channel DISSIPATIVE sign floor ... transfer to the STATIC/quasi-static scalar modulus that mu(x) actually couples to? CANDIDATE ROUTE: Kramers-Kronig -- ... Re c0(0) = (2/pi) * integral_0^inf domega Im c0(omega)/omega ... THE OBSTRUCTION: gravitational response kernels may require SUBTRACTED dispersion relations ... UNTIL THIS IS COMPUTED, the quasi-static x inherits NO sign from the passivity floor.'"
   },
   {
    "result": "passivity_channel_diagonal + x_no_pin_theorem",
    "background": "6-dim spatial-SVT fixed-slicing frame (KC5 fence); frame-level algebra, background enters only via the S6 frame declaration",
    "regime": "Pointwise in ω, every frequency ('anti-passive response in either channel, at any frequency'); matrix-PSD algebra otherwise regime-free",
    "label_structure": "Joint (ω,k²) coefficients on the two Ward-surviving projectors",
    "evidence": "S_IF.md §3/§6: 'omega*Im c2 >= 0 AND omega*Im c0 >= 0, channel by channel, pointwise'; kernels 'decomposed on the Ward-surviving projector pair ... at this frame/order'. p_tt_ansatz note: 'its banked home is the 6-dim SVT frame whose KC5 fence frontier-reserves the h_0mu/4d completion.' Both nodes sit in the eight-node demotion cone (see flow map)."
   },
   {
    "result": "eft_operator_basis (the two-survivor enumeration) + p_tt_ansatz (CHOSEN)",
    "background": "Explicitly 'the declared background' with 'the FRW homogeneity/isotropy frame' as a named symmetry input (S6); the p_tt interrogation extended it to full Lorentz-covariant order (flat-compatible)",
    "regime": "All (ω,k²) at quadratic order within the frame; 4d-covariant completion FRONTIER-RESERVED",
    "label_structure": "Joint (ω,k²), SO(3) at fixed k",
    "evidence": "eft_operator_basis statement: 'enumerate the admissible tensor structures for K_R on the declared background under the REGISTER-DECLARED symmetries only (... the FRW homogeneity/isotropy frame ...)'. calc/operator_basis.py:17-19 verbatim: 'S6 mu_linear's frame: background spatial homogeneity + isotropy (FRW linear cosmology) -- SO(3) rotations + translations at fixed k; parity-even inherited from the unperturbed background (... NOTED as inherited, not chosen).' p_tt_ansatz note: '(on the FRW frame mu_linear uses: 5 independent form factors, GRUT keeps 1, zeroes 4)'."
   },
   {
    "result": "zeta_interior_family (constant-x window x < ~0.59)",
    "background": "Flat FRW late-time data: DESI Σ0 lensing bound, central-inputs loose-upper (F-MAP fence)",
    "regime": "Late-time linear cosmology, horizon-to-subhorizon k; the constant-x cut is 'a ONE-DIMENSIONAL CUT through a function space' of x(ω,k²)",
    "label_structure": "x(ω,k²) declared a kernel, not a constant (S_IF §5)",
    "evidence": "S_IF.md §5: 'the constant-x cut's window is x < ~0.59 (DESI Sigma0 lensing, central-inputs loose-upper, F-MAP fence) [zeta_interior_family]'."
   },
   {
    "result": "vacuum cluster (21 vc_* nodes)",
    "background": "Flat-space QFT for the spine (additive-constant symmetry; vc_lorentz_invariant_vacuum postulates a Lorentz-invariant vacuum) + flat FRW for the data-reduction nodes (vc_rho_lambda's scheme is 'base-LCDM, FLAT, w FIXED = -1')",
    "regime": "No kernels, no frequency regime — inventory/pricing of underived inputs; own ledger scope, sealed from GRUT's",
    "label_structure": "Neither (ω,k) nor multipole: no response kernel exists in this cluster",
    "evidence": "VACUUM_CLUSTER_MAP.md: 'Status: BANKED under its own scope (2026-08-04). 13 nodes at ledger_scope: vacuum-cluster ... GRUT's ledger is untouched at +13 and the cluster's own ledger is 0 -- the two vocabularies cannot bleed.' vc_flatness_in_reduction: 'The reduction of CMB/BAO data to a dark-energy density assumes SPATIAL FLATNESS.' vc_lorentz_invariant_vacuum: 'The vacuum is Lorentz invariant, forcing <T_munu> proportional to g_munu.' (Note: the protocol says 21 nodes; the map header says 13 banked 2026-08-04 — later additions brought the vc_* count in claims.json to 21.)"
   },
   {
    "result": "response_lorentz_covariance (shown, +1 retired)",
    "background": "FRW gauge-allowed space vs Lorentz-covariant FLAT-compatible subspace — the node itself records that FRW is not globally Lorentz invariant",
    "regime": "Kinematic/algebraic (11-dim → 3-dim membership tests); flat limit H→0 is the test",
    "label_structure": "Bilinear response structures, flat-limit membership",
    "evidence": "Statement verbatim: 'the vacuum response kernel itself belongs to the Lorentz-covariant subspace identified by the flat-limit membership test -- NOT that the background is Lorentz invariant (the FRW background is not globally Lorentz invariant; the priced assumption is a property of the RESPONSE).' ledger_note: '+1 ... RETIRED 2026-08-30 per this node's own discharge clause ... The priced RESPONSE property is now DEMONSTRATED unimposed, not assumed.'"
   },
   {
    "result": "rung8_falsifier (tabletop stakes) — for completeness",
    "background": "Flat lab spacetime; stakes (κ, ω_c) at laboratory frequencies",
    "regime": "H≪ω by ~39+ orders; rung2 note: 'T_dS ~ 3e-30 K is exp(-10^21)-negligible at lab frequencies'",
    "label_structure": "ω only",
    "evidence": "rung2_kms_gate ledger_note verbatim: 'rung8's tabletop stakes (kappa, omega_c) contain no T either (T_dS ~ 3e-30 K is exp(-10^21)-negligible at lab frequencies, and S(0)=0 is finite-T robust per rung3's analyticity check).'"
   }
  ],
  "homogeneity_pricing": "FLAT-FRW/HOMOGENEITY IS UNPRICED IN GRUT SCOPE; IT IS PRICED ONLY INSIDE THE SEALED VACUUM-CLUSTER SCOPE, AND THE CORPUS HAS TWICE RECORDED THE GAP IN ITS OWN WORDS. (1) The string 'S6' occurs ZERO times in provenance/claims.json (verified by search); the S1-S7 inventory including 'background homogeneity/isotropy + inherited parity-evenness (S6)' lives only in S_IF.md §2 prose and the calc/operator_basis.py header, where S6 is glossed 'mu_linear's frame: background spatial homogeneity + isotropy (FRW linear cosmology)' with parity 'NOTED as inherited, not chosen' — no node, no delta. S_IF.md itself is 'CONSTRUCTION DOCUMENT, ledger 0'. eft_operator_basis names 'the FRW homogeneity/isotropy frame' among 'REGISTER-DECLARED symmetries only' but is a to-derive question node at delta 0 — it consumes the frame, it does not price it. (2) The only priced flatness/homogeneity items are vacuum-cluster-scoped: vc_flatness_in_reduction (postulate: 'The reduction of CMB/BAO data to a dark-energy density assumes SPATIAL FLATNESS') and vc_averaging_commutes (postulate), whose 2026-08-04 sweep note records that the candidate 'homogeneous and isotropic AND averaging commutes AND one global a(t) AND one Omega_k' was DELIBERATELY NOT booked whole — 'the METRIC-ANSATZ half is VISIBLE in vc_flatness_in_reduction's statement (which makes it a SPLIT question of that node, not an omission)' — and VACUUM_CLUSTER_MAP.md seals that scope: 'GRUT's ledger is untouched ... the two vocabularies cannot bleed.' So even the priced half cannot be cited as GRUT-scope pricing. (3) The corpus's own audits name the residue: RUNG3_KEYSTONE_MAP.md §6.2: 'The register lacks the vocabulary this whole audit needs ... no register field mentions foliation, cosmic time, or proper time — which is how the C1/C2 mismatch passed two pre-registrations'; and D6: quoting coth(ω/2T_dS) against frequencies in another clock is a transport where 'the attribution is derived; the transport itself is the assumption, and it is currently unpaid.' (4) One near-miss pricing exists and was retired: response_lorentz_covariance priced a response-property (+1, 2026-08-24) while explicitly declining to price the background ('NOT that the background is Lorentz invariant'), and that +1 was retired 2026-08-30 on demonstration. Net: the flow (one aspect of stationarity) is priced at +1; the flat-FRW spatial side (homogeneity/isotropy/flatness of the declared background) is carried as unpriced prose in GRUT scope — an asymmetry the gate's NEW-OMISSION outcome is pre-registered to catch.",
  "flow_usage_map": "WHERE THE FLOW IS WIRED AND WHO ACTUALLY USES A SINGLE-ω KERNEL. Explicit R5 edges from background_time_translation_flow: rung1_inin_formalism, rung1_ontology_finite_memory (via the 2026-08-23 split), rung2_kms_gate. The tier-propagation cascade (commit 67059a8, per RAI_CAPABILITY_TEST_01_CERTIFICATION.md §3) demoted EIGHT nodes shown→derived-pending through the rung1/rung2 cone: rung1_inin_formalism, rung2_kms_gate, rung4_love_kk, kr_contract_retarded_tier4, info_i1_renorm_as_information, passivity_channel_diagonal, rung7_w1_wz_map, u1_form_universality ('git diff d5e9a99..67059a8 -- provenance/claims.json is the authority'). These eight are what the discharge would mechanically re-promote. ACTUAL FLOW CONSUMPTION BY REGIME: (a) H≪ω consumers — kr_contract_retarded_tier4 (hard-gated, ω≪H raises DomainRejected), gw_tensor_friction (LIGO band, H0/ω~1e-20, and its clock declaration FORBIDS static-Killing imports), gw_dissipation_bounds/rung4 (LIGO band), rung8 tabletop — for these the static-patch vs FRW distinction is invisible at their working point, and the WKB/quasi-stationary license already covers them per the flow node's own note. (b) ω→0 / ω~H consumers, where H≪ω fails — rung3_single_pole (η=lim_{ω→0}, WKB closure: 'valid in the regime the single-pole conjecture does not live in'), kk_static_transfer (static modulus via a dispersion integral reaching ω→0), rung7_wz (vacuum forced at ω~H(z); 'w(z) is not an axis quantity'), the low-ℓ/horizon-scale legs of mu_linear's exclusion. (c) ω-grid equilibrium consumer — rung2's gate (no k label; its own ledger_note already imports the static-patch horizon to fix T=H/2π). (d) Non-consumers — the vacuum cluster (no kernels), mu_linear's algebraic bookkeeping per se (quasi-static identity clock, keystone row C5 'fine internally'), response_lorentz_covariance (kinematic membership). JOINT-LABEL GEOMETRY, AS THE CORPUS ITSELF DERIVED IT (RUNG3_KEYSTONE_MAP.md): D3b — 'the FULL two-point object Σ(x;x′) does not reduce to a function of a time difference in any single global clock; only the static patch offers a global reduction, because Killing stationarity makes z a function of T−T′ everywhere'; D3a — along any comoving worldline the BD correlator IS stationary in cosmic proper time (worldline reduction licensed WITHOUT a Killing vector); D2/D4 — the static and cosmic clocks coincide only on the axis, and rate comparisons across more than O(1/H) are clock-dependent. The static patch also covers only r<1/H around one worldline, so a static-patch re-declaration supplies a global (T, static-multipole-l) label, not the flat-slicing (ω,k) label the entire banked kernel corpus is written in — the physics crux is verified in-corpus at PRIMITIVE_INVERSION_SCOPE.md §5: 'GRUT's kernel is written in cosmological slicing at (ω, k) on an FRW background. In flat slicing de Sitter has no global timelike Killing vector ... the flow that is geometric by theorem and the flow the kernel actually uses are, on the face of it, different flows.'",
  "surprises": [
   "The corpus PRE-REGISTERED this exact discharge attempt's failure mode as its most-likely kill, before this gate existed: PRIMITIVE_INVERSION_SCOPE.md §5 'Standing prediction: this is where it breaks' and kill condition K2 'flow mismatch unbridgeable (§5). Static-patch Killing flow cannot be connected to the cosmological-slicing flow the kernel uses. Kill.' — plus K3 'one-for-one relabel ... Net ledger does not move. Kill — and this outcome is the most likely one.' Any DISCHARGE-AVAILABLE verdict must overcome the program's own sealed prediction.",
   "The binary framing 'Killing vector or nothing' is already refined in-corpus: RUNG3_KEYSTONE_MAP.md D3a derives that along any single comoving geodesic the BD correlator IS stationary in FRW cosmic proper time (verified to 12 decimals) — a worldline flow exists WITHOUT a timelike Killing vector, licensing along-worldline single-ω kernels at that scope only; D3b shows the spatially-separated object reduces in NO global clock except static-Killing. So a third route (worldline re-declaration) exists in the corpus's own texts alongside the static-patch route, with a different, narrower scope.",
   "The Tier-4 kernel is computed on neither declared background: it is a FLAT Minkowski contract (flat Tier-2 propagator, flat Tier-3 absorptive value) with an O(H²) perturbative correction, labeled 'de Sitter contract scope', with BD/Poincaré-chart (η→−∞) mode functions — and its A3 declarations explicitly PROHIBIT the static-patch thermal state as an input ('Would ASSUME the equilibrium that question (ii) must compute. PROHIBITED'). A static-patch re-declaration would collide with a frozen, immutable declarations document.",
   "rung2's ledger_note already quietly uses the static patch on 'the declared background': T=T_dS=H/2π is 'forced' by horizon regularity ('any other T is singular at the horizon') — a Killing-horizon fact invoked under a flat-FRW declaration. RUNG3_KEYSTONE_MAP.md D6 books the residue explicitly: 'the transport itself is the assumption, and it is currently unpaid' — an already-recorded unpriced presupposition sitting exactly where the gate's NEW-OMISSION outcome points.",
   "gw_tensor_friction.py's frozen clock declaration ('single FRW COSMIC clock throughout ... no static-Killing or e-fold quantity is imported', keystone C5) means the corpus's most recent banked-adjacent computation is contractually incompatible with a static-patch clock — a re-declaration would either re-scope it or contradict its SPEC.",
   "The program's only existing static-patch computation (calc/static_patch_tt_response.py) retracted its own headline: PART 3b proves B=−A for every ω ('these are not quasinormal modes and nothing in the c=0 family is'), and rung3's owner correction ruled the free static-patch response 'NULL, NOT ADVERSE'. So the named candidate background's in-corpus track record is one derived potential (c=0, axial), one retraction, and a null — no banked result currently LIVES on the static patch.",
   "The register's clock vocabulary gap is itself documented: 'no register field mentions foliation, cosmic time, or proper time — which is how the C1/C2 mismatch passed two pre-registrations' (RUNG3_KEYSTONE_MAP.md §6.2) — meaning a background re-declaration currently has no register field to be declared IN.",
   "Scope arithmetic asymmetry relevant to cost accounting: the results that would sail through a static-patch re-declaration (Tier-4 kernel, GW bounds, tabletop) are exactly those already H≪ω-fenced where the flow question is moot; the results the flow node exists FOR (rung3's ω→0 transport limit, rung7's ω~H(z) w(z), kk_static_transfer's static modulus, mu_linear's horizon-scale exclusion legs) live where the static-patch (T,l) label and the FRW (ω,k) label demonstrably diverge (D2/D3b/D4) — and rung7's w(z) is 'not an axis quantity', outside the static patch's r<1/H chart altogether."
  ]
 },
 "advocates": {
  "DISCHARGE-UNAVAILABLE — with t": {
   "availability_analysis": "THE DISCHARGE IS UNAVAILABLE AT THREE NESTED DEPTHS, AND THE PRECISION OF EACH IS THE PHYSICS FINDING.\n\nLAYER 1 — WRONG OBJECT (label algebra). The node licenses \"a kernel of a single frequency K_R(omega, k)\" — a JOINT spectral label: omega conjugate to a time flow AND k conjugate to flat-slicing comoving translations, simultaneously. A joint label requires commuting generators. On de Sitter the static-patch time-translation generator is a boost of so(4,1): in the flat chart (ds² = −dt² + e^{2Ht}dx², the corpus's own embedding, RUNG3_KEYSTONE_MAP.md §1.2) it is ξ = ∂_t − H x^i∂_i, and [ξ, ∂_j] = +H ∂_j ≠ 0 — standard physics the corpus itself cites via its embedding derivations, with the corpus's derived form at D3b: \"the FULL two-point object Σ(x;x′) does not reduce to a function of a time difference in any single global clock; only the static patch offers a global reduction, because Killing stationarity makes z a function of T−T′\" — a reduction whose spatial organization is STATIC MULTIPOLES about one worldline, exactly the (n,l) label calc/static_patch_tt_response.py actually uses (graviton l≥2, radial master equation), NOT (ω,k). So a static-patch declaration licenses K_R(ω_T; l,m) — a different object from the one every banked kernel is written in (S_IF.md §3: \"at fixed (ω, k) in the enumerated frame\"). The only background on which (ω,k) is an EXACT joint label is Minkowski — which is why the corpus's only fully-joint-label kernel, kr_contract_retarded_tier4, is computed on a flat Minkowski CONTRACT (G0_R(ω,k)=2κ²/((ω+i0)²−k²)) with O(H²) corrections and a hard DomainRejected gate at ω≲H. The physical content of the non-commutation is not a technicality: [flow, translations] = H·translations IS cosmological redshift. A re-declaration that made (ω,k) exact would declare away the very non-stationarity that rung7's w(z) (\"the cosmological expansion forces the vacuum at frequency ~ H(z)\", calc/wz_dark_energy.py) and gw_tensor_friction's friction are selling as physics.\n\nLAYER 2 — WRONG CHART COVERAGE. The static patch covers r < 1/H about ONE worldline (f = 1−H²r², calc/static_patch_tt_response.py PART 0). The banked observables that live at ω≲H — exactly where the flow license is needed per the node's own WKB note — are comoving-slice quantities at k~H0: mu_linear's low-ℓ ISW cross (~2.0σ) and DESI Σ0 (~3.5σ) kill legs, zeta_interior_family's x<~0.59 window, rung7's w(z). A comoving mode k~H0 has wavelength 2π/H0, three times the patch diameter; D2 derives \"w(z), a quantity of the cosmological background averaged over a comoving slice, is not an origin-worldline quantity,\" and §6.3 states \"w(z) is not an axis quantity.\" These observables are not definable within the declared patch without a NEW super-horizon FRW-continuation input.\n\nLAYER 3 — WRONG BACKGROUND. The banked cosmological background is not de Sitter but flat ΛCDM with OM=0.31 hard-coded (calc/wz_dark_energy.py, calc/gw_tensor_friction.py, calc/isw_exclusion.py): H(z) varies by ~78% over the DESI range (E(1)=√3.17≈1.78). A matter-loaded FRW carries NO timelike Killing vector, even approximately, at the redshifts where every data-facing banked result lives. The discharge clause requires \"the declared background\" — the S_IF §1 slot covering EVERY banked observable — to carry the vector; re-declaring it static-patch would be false to the corpus's own data-reduction chain, not merely expensive.\n\nREGIME PARTITION (who actually needs the license): (a) H≪ω consumers — Tier-4 (eps_H ≤ 0.1 gate, ω≲H REFUSED), rung4/GW at LIGO band (H0/ω ~ 1e-20), rung8 tabletop (~39 orders), gw_tensor_friction (1+hcorr==1.0 in double) — for all of these the WKB/quasi-stationary license ALREADY holds per the flow node's own note; the static-vs-FRW distinction is sub-precision; the re-declaration buys nothing. (b) ω≲H consumers — rung3's η=lim_{ω→0} Im G_R/ω, kk_static_transfer's dispersion integral reaching ω→0, rung7's ω~H(z), mu_linear's horizon-scale legs — here the license is genuinely needed, and here the static patch delivers the wrong label, the wrong chart, and (matter era) no Killing vector at all. THE CROWN OF THE OBSTRUCTION: one scale, three failures, all O(H). The same H that breaks quasi-stationarity (making the license needed at ω≲H) also gaps the static tower at O(H) with \"no parameter separating the leading pole from the rest\" (static_patch_tt_response.py part D), bounds the patch at r=1/H that k~H0 modes overflow, and measures the non-commutation [ξ,∂_i]=H∂_i that destroys the joint label. Quasi-stationarity failure and static-patch usability failure are THE SAME failure, so no background re-declaration can pry them apart: the discharge is redundant wherever it is valid and invalid wherever it is needed. This is the stationarity/homogeneity trade at H stated exactly: de Sitter offers global stationarity only by surrendering the translations that define k (keeping SO(3) about one pole → l), or homogeneity only by surrendering stationarity (flat slicing, no timelike KV, ω WKB-effective for H≪ω only). The (ω,k) object the entire banked corpus is written in is the Minkowski coincidence of the two symmetries. The corpus pre-registered exactly this: PRIMITIVE_INVERSION_SCOPE.md §5 \"Standing prediction: this is where it breaks,\" K2 \"flow mismatch unbridgeable... Kill,\" K3 \"one-for-one relabel... Net ledger does not move. Kill — and this outcome is the most likely one.\"",
   "case_points": [
    {
     "point": "The discharge clause licenses the wrong object: a static-patch declaration yields K_R(ω_T; l,m), not K_R(ω,k). No timelike Killing vector on de Sitter commutes with the flat-slicing translations that define k ([ξ, ∂_i] = H∂_i — and that non-commutation IS redshift, the framework's own cosmological mechanism). The only (ω,k)-exact background is Minkowski — which is why the corpus's sole fully-joint-label kernel is a flat Minkowski contract.",
     "evidence": "S_IF.md §3 'at fixed (ω, k) in the enumerated frame'; flow node statement 'a kernel of a single frequency K_R(omega, k) is definable'; RUNG3_KEYSTONE_MAP.md D3b (only static-Killing reduces globally, in T−T′); calc/static_patch_tt_response.py's (n,l) multipole label; PHYSICS_LEDGER/wall_kr_tier4_retarded.py:10 flat G0_R(ω,k); PRIMITIVE_INVERSION_SCOPE.md §5 'In flat slicing de Sitter has no global timelike Killing vector'."
    },
    {
     "point": "Regime complementarity — one scale H, three obstructions: the same H that breaks quasi-stationarity (making the license needed at ω≲H) gaps the static tower at O(H) with no separating parameter, bounds the patch at r=1/H that k~H0 observables overflow, and measures the label non-commutation. The static patch licenses single-ω kernels only where WKB already does; the discharge is redundant where valid and invalid where needed.",
     "evidence": "Flow node overturning_computation: 'quasi-stationarity holds for H << omega and the single-pole claim lives at omega -> 0, exactly where it fails'; static_patch_tt_response.py (D): 'THERE IS NO PARAMETER SEPARATING THE LEADING POLE FROM THE REST… gap and spacing are both O(H)'; wall_kr_tier4_retarded.py:30-43 eps_H hard gate, DomainRejected at ω≲H; keystone D2 (patch/slice share only the axis)."
    },
    {
     "point": "The matter era has no Killing vector at all: the banked cosmology is OM=0.31 flat ΛCDM; H(z) evolves ~78% over the DESI range; the re-declared background would be factually false for every data-facing banked observable — availability fails before cost is even reached. The static patch exists only for the Λ-only attractor, which contains none of the banked data.",
     "evidence": "calc/wz_dark_energy.py:29 'OM = 0.31 # matter density; OL = 1-OM (flat)' and header 'the cosmological expansion forces the vacuum at frequency ~ H(z)'; calc/isw_exclusion.py (low-ℓ cross-correlation); keystone D5 (ΔN requires H(z) history); RUNG3_KEYSTONE_MAP.md §6.3 'w(z) is not an axis quantity'."
    },
    {
     "point": "Cost exceeds retirement: −1 retired vs ≥+2 R1-fenced new inputs (N1 patch↔slice transport — booked in-corpus as 'currently unpaid'; N2 super-horizon FRW continuation — the currently unpriced homogeneity presupposition forced from prose into price), plus two frozen-document collisions (A3's PROHIBITED static thermal state; gw_tensor_friction's SPEC clock declaration forbidding static-Killing imports). Even at par it is the pre-registered K3 kill.",
     "evidence": "Keystone D6: 'the transport itself is the assumption, and it is currently unpaid'; 'S6' occurs 0 times in provenance/claims.json (verified by search); VACUUM_CLUSTER_MAP.md 'the two vocabularies cannot bleed'; WALL_A_A3_DECLARATIONS.md:156 'PROHIBITED as an input for the primary run'; calc/gw_tensor_friction.py 'no static-Killing or e-fold quantity is imported'; PRIMITIVE_INVERSION_SCOPE.md K3."
    },
    {
     "point": "The corpus pre-registered this exact failure before the gate existed, and everything derived since confirmed its direction: D2/D3b/D4 (numerically verified clock derivations), the static tower's structure, and the one static-patch computation's retraction. An AVAILABLE verdict must overturn a sealed prediction with derived counter-evidence; none exists in the corpus.",
     "evidence": "PRIMITIVE_INVERSION_SCOPE.md §5 'Standing prediction: this is where it breaks' + K2/K3 (read at lines 126-142); RUNG3_KEYSTONE_MAP.md §1.2 embeddings 'verified against the hyperboloid'; static_patch_tt_response.py 'A-RETRACTED… every downstream part is now marked conditional'; rung3 tier_note owner correction 'A POLE-FREE FREE RESPONSE IS NULL, NOT ADVERSE'."
    },
    {
     "point": "The eight re-promotions — the discharge's headline attraction — are tier moves at ledger delta 0, not credit, and are not free: P1A rules the reversal 'a register change requiring its own re-certification', and CERTIFICATION_02 is void-pending-rerun if its conditional fails. Meanwhile the corpus's own precedent for retiring a priced input (response_lorentz_covariance, +1 retired 2026-08-30) is retirement by DEMONSTRATION — a computation exhibiting the property unimposed — not by re-declaration.",
     "evidence": "program/P1A_EDGE_ADJUDICATION_01.md:129 'the static-patch discharge mechanically reverses the demotions as a register change requiring its own re-certification'; RAI_CAPABILITY_TEST_01_CERTIFICATION.md §3 (eight demotions listed, 'git diff d5e9a99..67059a8 — the authority'); response_lorentz_covariance ledger_note 'DEMONSTRATED unimposed, not assumed'."
    },
    {
     "point": "Silent re-scoping is the protocol's named failure mode, and it is the only route to a self-consistent static-patch corpus: the surviving sub-corpus is exactly the H≪ω sector (Tier-4, GW bounds, tabletop), which never needed the license; everything the flow node exists FOR (rung3's ω→0 limit, rung7's w(z), kk_static_transfer's static modulus, mu_linear's horizon-scale legs) must be re-scoped out of its banked claim to survive.",
     "evidence": "Protocol §3: 'A discharge that \"succeeds\" by silently re-scoping banked observables is a failure reported as one'; the flow_usage_map regime partition, verified per-node above; S_IF.md §1 'flat FRW for every banked observable' — the slot the re-declaration must overwrite."
    }
   ],
   "cost_accounting": {
    "retires": "Exactly one priced input: background_time_translation_flow's +1 (net +16 → +15). Secondary, non-ledger: the eight tier demotions (rung1_inin_formalism, rung2_kms_gate, rung4_love_kk, kr_contract_retarded_tier4, info_i1_renorm_as_information, passivity_channel_diagonal, rung7_w1_wz_map, u1_form_universality) 'mechanically reverse' — but per P1A_EDGE_ADJUDICATION_01.md:129 only 'as a register change requiring its own re-certification'; tier moves carry ledger delta 0, and CERTIFICATION_02's conditional makes the reversal a process cost, not a credit.",
    "new_inputs": [
     "N1 — PATCH↔SLICE TRANSPORT (≥+1). Every banked statement is written at (ω,k) in the flat-slicing frame; under the re-declaration the background's exact label is (ω_T; l,m) in one patch, so recovering the banked corpus requires asserting the Killing-clock→cosmic-clock transport corpus-wide. The corpus already books this residue as unpaid: keystone D6 — 'Quoting coth(ω/2T_dS) against frequencies defined in any other clock silently transports a Killing-clock object across… the transport itself is the assumption, and it is currently unpaid.' R1 FENCE PASSES: named positions deny it — the corpus's own DERIVED D3b (the assembled Σ(x;x′) reduces in no global cosmic clock; verified numerically), and the secular/non-stationary de Sitter programs already cited as the flow node's own R1 denial class, whose entire position is that dS correlators are NOT stationary in the cosmological frame beyond H≪ω.",
     "N2 — SUPER-HORIZON FRW CONTINUATION (+1). The declared background covers r<1/H about one worldline; keeping w(z), the ISW cross, DESI Σ0, and the ζ-window defined requires declaring the background beyond the patch homogeneous FRW. This is precisely the currently-UNPRICED homogeneity presupposition being forced from prose into GRUT-scope price: 'S6' occurs zero times in provenance/claims.json (verified); homogeneity/flatness is priced only inside the sealed vacuum-cluster scope (vc_flatness_in_reduction, vc_averaging_commutes), and VACUUM_CLUSTER_MAP.md seals it — 'the two vocabularies cannot bleed' — so the cluster cannot be cited as pre-payment. R1 FENCE PASSES: the averaging/backreaction position (in-corpus at vc_averaging_commutes' statement) denies that single-patch dynamics fixes slice-averaged observables, and the corpus's own D2 DERIVES that w(z) is not an origin-worldline quantity. This is simultaneously the gate's coexisting NEW-OMISSION finding — and on this route it lands on the COST side, because the re-declaration is what makes it load-bearing.",
     "N3 — FRAME TRANSFER OF THE TWO-SURVIVOR ENUMERATION (+1 or owed work). eft_operator_basis's rank-5 → two-survivor result (the basis of S_IF's kernel structure, passivity_channel_diagonal, x_no_pin_theorem) is computed in the SVT frame at fixed k and is 'exhaustive at all derivative orders WITHIN the frame' (KC5 fence — frame-internal by construction). The static patch's natural frame is Regge–Wheeler multipoles; the corpus's one computation there covers the axial sector only, with the polar sector unverified and one headline retraction (PART 3b). Either the enumeration is redone in the multipole frame (unbanked work with an adverse track record) or its transfer is asserted (a new input).",
     "N4 — STATE RE-DECLARATION COLLISION (frozen-document cost, not a ledger delta). The banked loop declarations define the state as BD positive-frequency at η→−∞ — a conformal-chart specification whose asymptotic past is not the static chart's — and WALL_A_A3_DECLARATIONS.md's alternative-states table PROHIBITS the static-patch thermal state as an input ('Would ASSUME the equilibrium that question (ii) must compute'). Under a static-patch declaration the natural state IS the prohibited one; either way a frozen, immutable declarations document must be rewritten — the protocol's 'breaks banked frames' clause satisfied literally."
    ],
    "net": "Retire 1; add ≥2 priced inputs (N1, N2; N3 likely a third), plus two frozen-document collisions (N4; gw_tensor_friction's SPEC clock declaration) and a mandatory re-certification. Net ledger moves ADVERSE by at least 1 — strictly worse than the pre-registered K3 configuration ('one-for-one relabel… Net ledger does not move. Kill'), which the program sealed as its most-likely outcome. Even under the most lenient booking — compounding N1+N2 into one declaration bundle, which OMISSION_STANDARD_v2's atomisation precedent (run on this very node: 'stationarity' was itself split into background-flow vs state-invariance) forbids — the best case is exactly K3's par, still a pre-registered kill. And the ledger credit side cannot be padded with the eight re-promotions: they are delta-0 tier moves conditioned on re-certification."
   },
   "honest_weaknesses": [
    "D3a is a genuine crack in my 'Killing vector or nothing' framing: the corpus itself DERIVES that along any single comoving geodesic the BD correlator is stationary in cosmic proper time (verified to 12 decimals) — a flow exists WITHOUT a timelike Killing vector, and T_dS = H/2π is its temperature in the cosmic clock. This licenses along-worldline single-ω kernels at zero background cost. My reply — it delivers K(ω) with NO k label, so it cannot discharge the node as written (the node licenses K_R(ω,k)) — holds, but an opponent can legitimately build a NARROWER re-scoped discharge from D3a, and the flow node's own text never says 'joint label or nothing' in so many words; the joint-label reading rests on its statement's 'K_R(omega, k)' plus S_IF §3.",
    "If the owner accepts OVERT (declared, not silent) re-scoping — amputating the ω≲H sector (rung3's living limit, rung7's evolving w(z), kk_static_transfer, mu_linear's horizon-scale legs) and re-homing the corpus on the H≪ω contract — the re-declaration becomes nearly coherent. My case then rests on the protocol's failure clause and on the amputated results being wanted, which is a governance judgment, not physics. The physics only says: you cannot have both the static patch and the ω≲H claims.",
    "Booking granularity is owner's discretion: the ledger 'prices INPUTS, not importance' (owner ruling 2026-08-18, in the flow node's own ledger_note), and a lenient compound booking of N1+N2 as one declaration input would bring the arithmetic to par (K3's kill-at-par rather than my strictly-adverse net). My '≥+2' rests on the atomisation precedent; precedents can be distinguished.",
    "rung2's situation cuts both ways and I have chosen a direction: its ledger_note ALREADY invokes horizon regularity (T = H/2π 'forced… any other T is singular at the horizon') under a flat-FRW declaration — an unpaid static-patch import the re-declaration would make honest and internal. I book that as cost-when-made-load-bearing (D6: 'currently unpaid'); an AVAILABLE advocate books it as an honesty credit for the re-declaration — surfacing debt already incurred. The direction of that booking is genuinely the owner's call; my case survives without it but is cleaner with it.",
    "My Layer-1 commutator ([ξ, ∂_i] = H∂_i) is standard de Sitter algebra reached through the corpus's cited embedding formalism; the corpus never writes the commutator itself. The protocol admits 'standard physics the corpus itself cites', and D3b is the corpus's own derived equivalent — but strictly, one step of my central algebra is mine, not the register's, and a hostile referee could demand it as a fresh calc before crediting Layer 1 (Layers 2 and 3 need no such step).",
    "The 'matter era has no KV' point (Layer 3) can be partially deflected by a two-background scheme: declare the VACUUM SECTOR's contract background static-patch while data reduction keeps FRW — formally how the Tier-4 flat contract already operates at calc level. My rebuttal (this converts the discharge into an explicit priced bridge, i.e., N1 again, and the discharge clause requires changing THE DECLARED background of S_IF §1, not a calc-level contract) is strong, but the scheme is not incoherent, and a sufficiently careful AVAILABLE advocate would propose exactly it — at which point the argument is purely about cost, where I still win on N1+N2."
   ],
   "rebuttals_of_rivals": [
    {
     "rival": "DISCHARGE-AVAILABLE-NET-FAVORABLE (the tidiness- and framework-favorable verdict, the builder's documented bias direction)",
     "rebuttal": "Its three prongs each invert on inspection. (1) 'Every actual consumer is H≪ω-fenced, where the static/FRW distinction is invisible' — true, and fatal to the discharge, not to me: in that regime the flow node's own text says the WKB/quasi-stationary substitute ALREADY licenses the kernel, so the +1 being retired does no work there; the +1 exists for the ω≲H consumers, and for those the static patch supplies the wrong label ((ω_T,l) vs (ω,k)), the wrong chart (r<1/H vs comoving-slice k~H0), and — in the matter era — no Killing vector at all (OM=0.31 hard-coded in the banked calcs). A discharge valid only where the assumption was never needed retires nothing: the clause requires the flow to become 'a PROPERTY OF THE BACKGROUND', and the background the banked observables actually declare (flat FRW, S_IF §1) provably lacks the property — while the background that has it cannot host the observables. (2) 'rung2 already uses the static patch covertly; re-declaring makes it honest' — the corpus books that residue as UNPAID (D6: 'the transport itself is the assumption, and it is currently unpaid'), so formalizing it converts a recorded unpaid residue into a priced input: that is my cost line N1, not their credit line. (3) 'Eight nodes re-promote, the ledger improves' — the re-promotions are delta-0 tier moves gated on 'its own re-certification' (P1A verbatim), and the ledger arithmetic is −1 vs ≥+2: strictly worse than K3's par, which the program pre-registered as a kill. Finally, AVAILABLE must overturn the corpus's own sealed prediction (§5 'Standing prediction: this is where it breaks'; K2) with derived counter-evidence, and every derivation since sealing (D2/D3b/D4/D6, the O(H)-spaced tower, the PART-3b retraction, the NULL ruling) has landed on the prediction's side."
    },
    {
     "rival": "NEW-OMISSION-SURFACED (as the primary verdict, instrument-favorable)",
     "rebuttal": "The omission is real and I rely on it: flat-FRW homogeneity is unpriced in GRUT scope ('S6' occurs zero times in claims.json; the S1–S7 inventory lives in S_IF §2 prose and the calc/operator_basis.py header; the only priced flatness/homogeneity nodes are vacuum-cluster-scoped and sealed — 'the two vocabularies cannot bleed'). But the protocol pre-commits that NEW-OMISSION 'may coexist with either outcome above' — it composes with UNAVAILABLE rather than competing with it, and booking it is the OWNER's act under OMISSION_STANDARD_v2, not this gate's. As a primary verdict it would answer the wrong question: the gate's question is whether the named discharge route is available and at what cost, and the omission is evidence ON that question — it is the unpriced presupposition the re-declaration would be forced to price (my N2), which is a cost finding for the route, not a substitute verdict. The correct disposition is UNAVAILABLE with the omission-surfaced finding attached as the coexisting package the protocol already provides for."
    },
    {
     "rival": "UNDERDETERMINED (the record cannot decide; name the deciding computation and stop)",
     "rebuttal": "Nothing that gates availability is uncomputed. The deciding facts are all derived, in-corpus, and verified: D3b (no global one-clock reduction of the assembled two-point object except static-Killing — numerically verified), D2 (patch and slicing share only the axis; w(z) is not a worldline quantity), D4 (rate conversions local only, exponential↔power-law beyond O(1/H)), the static tower's closed form (gap and spacing both O(H), no separating parameter), OM=0.31 hard-coded in every data-facing calc, and the sealed K2/K3 kill conditions. What remains genuinely open is whether a patch→slice bridge THEOREM could someday connect the flows — but the corpus has already classified that hope: keystone §6.3, 'Bridging them is either a theorem (derived) or a new priced input (§7's test)', and D6 books the transport as currently an assumption. An unproven bridge theorem is not indeterminacy about the route's availability TODAY; it is the route's price. UNDERDETERMINED would also duck the protocol's own standard — 'what the corpus as banked, plus standard physics the corpus itself cites, forces' — which forces an answer here: the corpus as banked is (ω,k)-native, and standard de Sitter kinematics the corpus cites and partially derives says no background declaration on an expanding spacetime makes that label exact. The only computation a hostile referee could demand — writing out [ξ_static, ∂_i] = H∂_i from the corpus's own embedding — is one line of the algebra the keystone map already machine-verified the embeddings for, not an open research question."
    }
   ],
   "scope_survival": [
    {
     "result": "kr_contract_retarded_tier4 (Tier-4 kernel)",
     "fate": "SURVIVES UNCHANGED — because its scope never used the discharge. It is a flat Minkowski contract (Tier-2 G0_R(ω,k) = 2κ²/((ω+i0)²−k²)) with O(H²) corrections, hard-gated eps_H = (104/9)H²/ω² ≤ 0.1, ω≲H REFUSED by DomainRejected. The re-declaration adds nothing in its domain and collides with its frozen state declaration if pressed (BD at η→−∞ is a conformal-chart specification; the static thermal alternative is PROHIBITED).",
     "evidence": "PHYSICS_LEDGER/wall_kr_tier4_retarded.py:10, 30-43 ('eps_H >= 1 : REFUSED… HARD STOP… no omega << H'); node domain 'de Sitter contract scope'; WALL_A_A3_DECLARATIONS.md:132 ('positive frequency w.r.t. the BD vacuum in the far past η → −∞') and :156 ('thermal (static-patch) initial state… PROHIBITED as an input for the primary run')."
    },
    {
     "result": "calc/gw_tensor_friction.py (Γ_T closure, REFUSE verdict)",
     "fate": "CONTRACT CONTRADICTION: its SPEC-frozen clock declaration is incompatible on its face with a static-patch background declaration, and the distinction is numerically invisible in its band — so the re-declaration can only cost (rewriting a frozen SPEC) and never gain.",
     "evidence": "Header verbatim: 'CLOCK DECLARATION (SPEC section 4 / keystone C5): single FRW COSMIC clock throughout. Every rate below is d/dt_cosmic; no static-Killing or e-fold quantity is imported'; '1+hcorr == 1.0 in double' at LIGO band (H0/ω ~ 1e-20); keystone C5 row: 'single FRW cosmic clock throughout | identity | sub-horizon quasi-static | derived | fine internally'."
    },
    {
     "result": "mu_linear (no-go export + conditional μ=Σ=η=1 leg)",
     "fate": "KILL LEGS LOST OR RE-PRICED: the empirical legs (low-ℓ ISW cross ~2.0σ, DESI Σ0 ~3.5σ) are comoving-slice quantities at k~H0 whose wavelength exceeds the patch diameter; the KC5-fenced fixed-slicing SVT frame is a flat-slicing object. Survives only by paying N2 — i.e., by re-importing the FRW background the discharge just un-declared. The structural separate-universe leg is FRW-native.",
     "evidence": "S_IF.md §1 (the KC5-fenced frame declaration); calc/isw_exclusion.py header ('the low-ell temperature-galaxy CROSS-CORRELATION'); keystone D2 ('w(z), a quantity of the cosmological background averaged over a comoving slice, is not an origin-worldline quantity'); mu_linear statement (2.0σ + 3.5σ legs); patch radius 1/H from f = 1−H²r²."
    },
    {
     "result": "rung7_wz (w(z) dark energy, +3)",
     "fate": "LOST AS BANKED: forced at ω~H(z) on a matter-loaded ΛCDM background (H(z) varying ~78% over the DESI range) that carries no timelike Killing vector at all; 'not an axis quantity', so no static-patch reduction reaches it at any price short of N1+N2 jointly.",
     "evidence": "calc/wz_dark_energy.py:23-30 ('the cosmological expansion forces the vacuum at frequency ~ H(z)'; OM=0.31 flat); RUNG3_KEYSTONE_MAP.md §6.3 ('w(z) is not an axis quantity… Bridging them is either a theorem (derived) or a new priced input'); D5 (ΔN needs the H(z) history)."
    },
    {
     "result": "rung3_single_pole (the anchor conjecture — the intended beneficiary)",
     "fate": "GAINS NOTHING USABLE: its claim lives at ω→0, where the corpus's own static-patch computation shows the free structure is an infinite tower gapped and spaced O(H) with no separating parameter (and the free level is owner-ruled NULL, not adverse — everything rests on the interacting Σ, which no re-declaration supplies). The license would arrive in the ω_T clock, and D4 cuts the bridge (exponential↔power-law beyond O(1/H)) to its cosmological consumers rung7/mu_linear. Its separately-reserved tier question is protocol-fenced and untouched by this analysis.",
     "evidence": "rung3 boundary_condition WKB closure ('valid in the regime the single-pole conjecture does not live in, and invalid in the one it does'); static_patch_tt_response.py part (D) ('no parameter separating… only by WAITING'); rung3 tier_note ('A POLE-FREE FREE RESPONSE IS NULL, NOT ADVERSE… rests on the interacting self-energy Sigma'); keystone D4."
    },
    {
     "result": "kk_static_transfer (dissipative→static sign transfer)",
     "fate": "INTEGRAL ORPHANED: the dispersion identity χ(0) = χ_∞ + (2/π)∫_0^∞ dω Im χ/ω runs the SVT scalar channel c0 at fixed k through the ω→0 end; under the re-declaration the ω→0 end is (ω_T,l)-labeled while the channel is k-labeled — the integrand's two ends live in different frames absent N1. The banked answer (the transfer question IS sign(χ_∞), a contact datum) is frame-level algebra and survives as such, but its application to μ(x)'s coupling exits the frame.",
     "evidence": "Node statement ('Re c0(0) = (2/pi) * integral_0^inf domega Im c0(omega)/omega… THE OBSTRUCTION… SUBTRACTED dispersion relations'); sub_status (ANSWERED at (b)+(c), χ_∞ pending); frame inheritance from mu_linear's coupling."
    },
    {
     "result": "rung2_kms_gate",
     "fate": "THE ONLY GENUINE BENEFICIARY — and it immediately re-exports the problem: the gate itself is ω-only (kms.py contains no k label anywhere), and its T = H/2π note already imports horizon regularity, so the re-declaration would make D6's transport internal FOR THIS NODE. But its consumer-facing output — S_IF §3's per-channel KMS lock n_i(ω) = coth(βω/2)·Im c_i(ω) — is applied on channels defined at fixed k, handing the label problem straight back.",
     "evidence": "gate/kms.py (grep for word-boundary 'k': zero matches; ω-grid only, 'ω=0 skipped'); rung2 ledger_note ('forces T = T_dS = H/2pi uniquely (any other T is singular at the horizon)'); keystone D6 ('the transport itself is the assumption… currently unpaid'); S_IF §3 KMS-lock row."
    },
    {
     "result": "passivity_channel_diagonal + x_no_pin_theorem",
     "fate": "FRAME-ORPHANED: joint (ω,k²) coefficients on the Ward-surviving SVT projector pair; the banked two-survivor exhaustiveness is frame-internal by the KC5 fence. The multipole-frame analog is unbanked — the corpus's one static-patch computation covers the axial sector only, polar unverified, with one retraction. Survives as within-frame algebra; its frame no longer matches the declared background's exact label.",
     "evidence": "S_IF §3/§6 (channel-by-channel pointwise passivity on the projector pair at fixed (ω,k²)); calc/operator_basis.py:29-31 ('within this frame it is exhaustive…'); eft_operator_basis boundary_condition ('CHOSEN AT THE ENUMERATED FRAME/ORDER… spatial-SVT frame at fixed slicing and fixed k'); static_patch_tt_response.py scope notes ('SCOPE: axial sector; the polar sector… not verified here')."
    },
    {
     "result": "rung4_love_kk / calc/gw_dissipation_bounds.py",
     "fate": "SURVIVES: LIGO band (H≪ω by ~20 orders), flat-space propagation over distance anchors, and the 2026-08-20 power-law fence already excludes the far-IR ω~H tail from the |Re|~|Im| step. Needs nothing from the re-declaration; its KK statement's all-ω reach touches ω~H only through the already-fenced tail.",
     "evidence": "calc/gw_dissipation_bounds.py F_BAND=[10,100,1024] Hz; node scope-fence text ('FALSE for a far-IR Lorentzian tail… fence added 2026-08-20'); tier_note (21–62 orders below sensitivity, CLOSED at outcome B)."
    },
    {
     "result": "rung8_falsifier (tabletop stakes)",
     "fate": "SURVIVES TRIVIALLY: lab frequencies, H≪ω by ~39+ orders; T_dS exp(−10²¹)-negligible; no static-patch quantity within reach of its stakes.",
     "evidence": "rung2 ledger_note ('T_dS ~ 3e-30 K is exp(-10^21)-negligible at lab frequencies, and S(0)=0 is finite-T robust')."
    },
    {
     "result": "vacuum cluster (21 vc_* nodes)",
     "fate": "SEALED AND UNAFFECTED — and unavailable as pricing cover: no kernels exist in the cluster, and the scope seal forbids citing vc_flatness_in_reduction / vc_averaging_commutes as having pre-paid the GRUT-scope homogeneity input (N2).",
     "evidence": "21 vc_* nodes counted in claims.json (verified); VACUUM_CLUSTER_MAP.md ('GRUT's ledger is untouched… the two vocabularies cannot bleed'); vc_averaging_commutes' deliberate half-booking ('the METRIC-ANSATZ half is VISIBLE in vc_flatness_in_reduction's statement')."
    },
    {
     "result": "eft_operator_basis + p_tt_ansatz (CHOSEN verdicts)",
     "fate": "VERDICTS SURVIVE AS FRAME-LEVEL ALGEBRA, but the register's declared symmetry inventory becomes internally inconsistent: S6 names 'SO(3) rotations + translations at fixed k', and the re-declared background's flow does not commute with those translations — the corpus would declare a flow that breaks its own declared frame on the same page.",
     "evidence": "calc/operator_basis.py:17-19 (S6 verbatim); eft_operator_basis statement ('REGISTER-DECLARED symmetries only… the FRW homogeneity/isotropy frame'); Layer-1 non-commutation (D3b + standard dS algebra via the §1.2 embeddings)."
    },
    {
     "result": "response_lorentz_covariance (+1 retired 2026-08-30)",
     "fate": "UNAFFECTED (flat-limit membership test, H→0) — and it stands as the corpus's retirement precedent: the one priced input ever retired was retired by DEMONSTRATION (computation exhibiting the property unimposed), not by re-declaration. The discharge route under examination matches no in-corpus retirement shape.",
     "evidence": "Node statement ('NOT that the background is Lorentz invariant… the priced assumption is a property of the RESPONSE'); ledger_note ('RETIRED 2026-08-30… DEMONSTRATED unimposed, not assumed')."
    },
    {
     "result": "zeta_interior_family (constant-x window)",
     "fate": "RE-PRICED WITH mu_linear: its binding bound is the DESI Σ0 lensing datum — a late-time, horizon-to-subhorizon comoving-slice measurement outside any single static patch; survives only through N2.",
     "evidence": "S_IF.md §5 ('x < ~0.59 (DESI Sigma0 lensing, central-inputs loose-upper, F-MAP fence)')."
    }
   ],
   "verdict_argued": "DISCHARGE-UNAVAILABLE — with the NEW-OMISSION finding (unpriced flat-FRW homogeneity in GRUT scope) attached as the protocol's pre-registered coexisting package, booked on the cost side of this route. The static-patch re-declaration cannot deliver the object the flow node licenses (K_R(ω,k) — a joint label no de Sitter background makes exact, because the static-patch flow fails to commute with the translations that define k, and that non-commutation is redshift itself); it cannot reach the observables the license exists for (the ω≲H sector lives on comoving slices a single patch of radius 1/H cannot contain, on a matter-loaded ΛCDM background that carries no timelike Killing vector at all); and its full price is at least two R1-fenced new inputs plus two frozen-document collisions against a retirement of exactly +1 — strictly worse than the pre-registered K3 kill-at-par the program itself sealed as the most likely outcome. The obstruction, stated precisely, is the physics finding: on an expanding background, stationarity and homogeneity are exchanged at the scale H — de Sitter offers a global ω_T only in one observer's multipole frame, or a global k only on a non-stationary slicing, never both — so the quasi-stationary license and the static-patch license fail in the same place for the same reason, and no background re-declaration can buy at ω≲H what H≪ω already gets free."
  },
  "NEW-OMISSION-SURFACED (primary": {
   "verdict_argued": "NEW-OMISSION-SURFACED (primary), coexisting with UNDERDETERMINED on the availability question proper. The investigation's primary, verdict-independent yield is an unpriced presupposition: the SPATIAL half of the very background-symmetry commitment whose TIME half was booked at +1 on 2026-08-18 — the declared background's spatial-translation/rotation flow (S_IF's S6), which licenses the k label, the d3k measure, the SVT frame, and the SO(3)-at-fixed-k enumeration corpus-wide, occurs zero times as a register booking (\"S6\" appears 0 times in provenance/claims.json, verified by search), and is textually absent from the statements of at least seven GRUT-scope nodes whose content cannot be established, stated, or used without it. Run honestly through OMISSION_STANDARD_v2, the candidate passes conjunct 1 (discharge-blocking, invisible in the presupposing nodes' texts), conjunct 2 per-cluster (R3: vacuum-cluster bookings are sealed and cannot bleed), the R1 fence (named deniers exist, including the discharge route's own candidate background, which denies it in-chart by computation), and the multiplicity signal — the \"certainly an omission\" shape per the booked flow node's own language. A second, already-prose-recorded residue (the D6 Killing-clock-to-cosmic-clock transport, \"currently unpaid\") is flagged for owner disposition as omission-or-split-of-rung2. On availability itself the record does not force a verdict: the only exhibited route (global static-patch re-declaration) collides with three frozen strata and fails K3 label arithmetic, but two in-corpus partial routes (a scoped/layered re-declaration via the section-6.3 bridge, and the D3a worldline route on the currently declared background) turn on named, unperformed computations — so the gate must name the deciders and stop, per the protocol's UNDERDETERMINED consequence. Both omission packages are inert; booking is the owner's under OMISSION_STANDARD_v2, exactly as the protocol pre-commits.",
   "availability_analysis": "THE ROUTE AS WRITTEN IS A RE-DECLARATION, AND THE RECORD SPLITS THREE WAYS RATHER THAN DECIDING. The discharge clause (claims.json, background_time_translation_flow.overturning_computation) requires \"the declared background\" to carry a timelike Killing vector; S_IF.md section 1 declares flat FRW \"for every banked observable,\" which carries none — the corpus states this itself (PRIMITIVE_INVERSION_SCOPE.md section 5: \"In flat slicing de Sitter has no global timelike Killing vector\"). Three candidate resolutions exist in the corpus's own texts, and the record decides none of them. (1) GLOBAL static-patch re-declaration: verifiably collision-bound as banked — PHYSICS_LEDGER/WALL_A_A3_DECLARATIONS.md line 156 declares the thermal static-patch initial state \"PROHIBITED as an input for the primary run\" (frozen); calc/gw_tensor_friction.py lines 6-7 carry the frozen clock SPEC \"single FRW COSMIC clock throughout... no static-Killing or e-fold quantity is imported\" (keystone C5); and RUNG3_KEYSTONE_MAP.md D2 derives that w(z) \"is not an origin-worldline quantity\" while the static chart covers r<1/H. (2) SCOPED/LAYERED re-declaration (kernel-definition layer stationary, exports on FRW): its availability turns on the section-6.3 dichotomy, verbatim \"Bridging them is either a theorem (derived) or a new priced input\" — neither is exhibited anywhere in the repo. (3) THE WORLDLINE ROUTE (D3a): the corpus DERIVED, on the currently declared background with no re-declaration, that along any comoving geodesic the BD correlator is stationary in cosmic proper time (verified to 12 decimals) — a flow-property-of-the-background at worldline scope, i.e. a candidate PARTIAL discharge the clause's binary framing (Killing vector or nothing) does not anticipate; whether it reaches the assembled spatially-separated object is, in the corpus's own words (D3b re-scoped consequence), \"itself part of wall (A)'s assembly\" — an unperformed computation, and C7's status line supplies the controlling category: \"unperformed reduction, not a contradiction.\" THE PHYSICS CRUX, COMPUTED NOT ASSUMED: from the keystone map's own embedding formulas (constraint verified symbolically: -X0^2+Xi^2+X4^2 = 1/H^2 exactly), the static-patch time translation pulls back to the flat chart as d_T = d_t - H x^i d_i, and [d_T, P_j] = +H P_j != 0; a plane wave e^{ikx} is not a flow eigenfunction. So no joint (omega,k) eigenbasis exists on the candidate background — the joint label the ENTIRE banked kernel corpus is written in (S_IF section 3, \"at fixed (omega, k) in the enumerated frame\") survives re-declaration only as an H<<omega effective object. And the corpus has already located the flow assumption's entire load at the opposite end: rung3's owner-ordered WKB closure reads \"H << omega fails exactly at omega -> 0, and nowhere else does the approximation matter.\" Therefore the consumers a re-declaration could serve exactly (Tier-4 kernel, hard-gated eps_H<0.1; GW band at H0/omega ~ 1e-20 where the kernel's own correction satisfies 1+hcorr == 1.0 in double precision; tabletop) are those for which the flow question is ALREADY moot under the existing WKB license, and the consumers the +1 exists FOR (rung3's eta = lim_{omega->0} Im G_R/omega; rung7's vacuum forced at omega ~ H(z), calc/wz_dark_energy.py line 23; kk_static_transfer's dispersion integral reaching omega -> 0; mu_linear's horizon-scale kill legs) live where the (omega,k) and (T,l) labels demonstrably diverge (D2/D3b/D4) or outside the chart altogether. THE DECIDERS, NAMED (the UNDERDETERMINED consequence requires this): U1 — the wall-(A) assembly reduction: does the gauge-invariant assembled TT response reduce to an along-worldline one-time kernel, to the static-Killing form, or to neither (D3b names this open); U2 — the section-6.3 bridge theorem or its priced-input booking, without which the discharge's net cost is unsummable; U3 — register vocabulary legislation: RUNG3_KEYSTONE_MAP.md section 6.2 records \"no register field mentions foliation, cosmic time, or proper time,\" so a re-declaration has no field to live in until the owner legislates one (the program's freshest precedent, P1B at HEAD d4a385a, shows LEGISLATION-REQUIRED is a real outcome class); U4 — the interacting static-patch self-energy Sigma (rung3 owner correction: \"Everything that bears on this node rests on the interacting self-energy Sigma\"; the program's only static-patch computation, calc/static_patch_tt_response.py, is free-theory, retracted its QNM headline via the computed B = -A counterexample, and was ruled \"NULL, NOT ADVERSE\"). Until U1-U4, availability is underdetermined at the scopes that matter — while the omission finding stands on every branch.",
   "cost_accounting": {
    "retires": "By this gate: nothing (CHARTER 5.3 pre-screen; banks nothing, edits nothing). Under the hypothetical static-patch re-declaration IF someday ruled available: the flow node's +1 retires (net +16 -> +15; current net verified by summing GRUT-scope ledger_delta = +16, matching STATE.md's REGISTER-SYNC line), and the eight tier demotions (rung1_inin_formalism, rung2_kms_gate, rung4_love_kk, kr_contract_retarded_tier4, info_i1_renorm_as_information, passivity_channel_diagonal, rung7_w1_wz_map, u1_form_universality; git diff d5e9a99..67059a8 is the authority) mechanically reverse per P1A's recorded route — but P1A_EDGE_ADJUDICATION_01.md (ruled EDGE_CORRECT at HEAD, post-dating this gate's protocol freeze) makes that reversal 'a register change requiring its own re-certification', so the re-promotions carry a re-certification bill and are tier moves, not ledger deltas.",
    "new_inputs": [
     "(1) The static-patch background+state declaration itself, >= +1. R1 fence: named positions deny it — the flow node's own fence text names 'the secular/non-stationary de Sitter programs' as working on a background with no timelike Killing vector, and the denial changes cluster content (whether equilibrium KMS at T_dS governs response at omega ~ H). It additionally collides with the frozen A3 row PROHIBITING the thermal static-patch state as a primary-run input — a declaration the owner would have to supersede, a procedural cost on top of the ledger one.",
     "(2) The spatial-translation/homogeneity license for the joint (omega,k) label — the surfaced omission, and it books on EVERY branch. Under the status quo it is the unpriced presupposition Omega-A (+1, the mirror of the flow's +1). Under the re-declaration it is DENIED by the declared background in-chart ([d_T, P_j] = H P_j, computed from the corpus's own embedding), so the label needs an explicit priced effective-license. The discharge pays the time side by breaking the space side: one-for-one at best — the pre-registered K3 shape ('Net ledger does not move. Kill — and this outcome is the most likely one'), with in-corpus precedent at l0_r2_exact_unique_breaker: an omega-to-spatial-structure map was ruled 'RELOCATED (mode -> omega<->x map), not discharged'.",
     "(3) The D6 clock-transport commitment for non-worldline objects (+1 unless the section-6.3 bridge theorem is exhibited). Corpus-recorded verbatim: 'the attribution is derived; the transport itself is the assumption, and it is currently unpaid.' R4-atomised honestly: the worldline half is DERIVED (D3a; C3 'now partly derived... must still name that scope'); the unpaid residue is the assembled/off-worldline half — which rung2's ledger-affecting T-subsumption justification already uses today ('any other T is singular at the horizon' — a static-Killing-structure argument run 'on the declared background' that is flat FRW).",
     "(4) Chart-extension for off-axis exports — not an integer but banked-scope losses the protocol counts as failure: w(z) averages comoving slices over multi-Hubble history outside r < 1/H and 'is not an axis quantity'; mu_linear's empirical kill legs live at k ~ H0 where the (omega,k)-to-(T,l) divergence is O(1); kk_static_transfer's integral reaches omega -> 0. 'A discharge that succeeds by silently re-scoping banked observables is a failure reported as one.'"
    ],
    "net": "Status quo: +16. Static re-declaration branch: -1 (flow) + 2..3 (items 1-3) = net +17..+18, strictly worse, plus a re-certification and two frozen-document supersessions, minus banked-result scope losses (item 4) — nowhere near DISCHARGE-AVAILABLE-NET-FAVORABLE's bar ('strictly improves or holds with zero banked-result loss'). Status-quo branch: the omission bookings still apply (Omega-A +1; Omega-B +1 or a split of rung2's T-subsumption), so the honest ledger direction of this whole investigation is UP, not down — stated plainly because it is the instrument-favorable direction and the anti-optimization clause requires the advocate to name it: the arithmetic is forced by verified strings, not preferred."
   },
   "scope_survival": [
    {
     "result": "kr_contract_retarded_tier4 (Tier-4 kernel)",
     "fate": "SURVIVES at its existing scope on every branch, and gains nothing from the discharge: it is already the H<<omega effective object a re-declaration would produce, with omega<<H refused in code (DomainRejected). Its base object is the flat joint-label propagator, and its state declaration collides with a static-patch re-homing.",
     "evidence": "PHYSICS_LEDGER/wall_kr_tier4_retarded.py lines 10-11: G0_R(omega,k) = 2 kappa^2/((omega+i0)^2 - k^2); node statement: 'Validity: omega >> H... omega << H refused by the evaluator'; WALL_A_A3_DECLARATIONS.md line 132 (BD positive frequency w.r.t. eta -> -infinity, Poincare chart) and line 156 (static-patch thermal state PROHIBITED as primary-run input)."
    },
    {
     "result": "gw_tensor_friction / rung4_love_kk / rung8 (LIGO band and tabletop)",
     "fate": "UNAFFECTED numerically (H0/omega ~ 1e-20; the kernel's own H^2 validity term is 1.0 in double precision) — they survive by being places where the flow question is moot; a global re-declaration would CONTRADICT their frozen clock SPEC rather than help them.",
     "evidence": "calc/gw_tensor_friction.py lines 6-7 ('single FRW COSMIC clock throughout... no static-Killing or e-fold quantity is imported') and line 151 ('1+hcorr == 1.0 in double'); rung2 ledger_note: T_dS ~ 3e-30 K is exp(-10^21)-negligible at lab frequencies."
    },
    {
     "result": "rung2_kms_gate",
     "fate": "FORMALLY SURVIVES (pure omega-grid, no k label anywhere in gate/kms.py) — but it is the node where the unpaid D6 transport already does ledger-affecting work: its -1 T-subsumption is justified by horizon regularity, a static-Killing argument invoked under the flat-FRW declaration. A re-declaration would license that argument at last while charging the transport for every non-worldline application of the coth lock (S_IF section 3 applies coth at every (omega,k) — a spatially-separated object, exactly D3b's case).",
     "evidence": "gate/kms.py (omega_grid only; T<=0 rejected); rung2_kms_gate.ledger_note: 'forces T = T_dS = H/2pi uniquely (any other T is singular at the horizon)'; RUNG3_KEYSTONE_MAP.md D6: 'the transport itself is the assumption, and it is currently unpaid'; P1A: the gate 'runs against the declared background'."
    },
    {
     "result": "rung3_single_pole (anchor conjecture, omega -> 0)",
     "fate": "THE UNDERDETERMINATION LOCUS. The flow node exists for this limit ('nowhere else does the approximation matter'). A static re-declaration would give it a global stationary reduction only in (T,l) labels, where the free-theory evidence is a gapless multipole tower with no small parameter and the owner has ruled the free response NULL-NOT-ADVERSE; everything turns on the uncomputed interacting Sigma (decider U4). Its separately-reserved tier question is untouched per the hard fence.",
     "evidence": "WKB closure verbatim in claims.json: 'quasi-stationarity holds for H << omega... the single-pole claim lives at omega -> 0, exactly where it fails... nowhere else does the approximation matter'; calc/static_patch_tt_response.py PART 3b (B = -A for every omega, headline retracted) and part (D) (gap and spacing both O(H), 'no parameter separating the leading pole from the rest')."
    },
    {
     "result": "mu_linear (no-go export; KC5-fenced SVT frame)",
     "fate": "SURVIVES ONLY UNDER THE STATUS QUO. Its fixed-slicing spatial-SVT frame has no static-chart equivalent (the chart breaks spatial homogeneity; computed commutator), and its empirical kill legs are horizon-scale (low-ell ISW cross ~2.0 sigma, DESI Sigma0 ~3.5 sigma at k ~ H0) where the label divergence is O(1). Under re-declaration: lost or re-scoped — protocol counts that as failure. Its frame is also the omission's home: the node contains ZERO frame vocabulary (svt/slicing/frame/foliation all absent) despite being S6's named 'declaring claim'.",
     "evidence": "Verified greps: mu_linear has 0 occurrences of svt, slicing, frame, fixed-slic, foliat; its two homogeneity/isotropy grep hits are physics false-positives (anisotropic stress). calc/operator_basis.py line 17: 'S6 mu_linear's frame: background spatial homogeneity + isotropy'."
    },
    {
     "result": "rung7_wz (w(z) dark energy)",
     "fate": "LOST under the re-declaration absent the unexhibited bridge theorem: the vacuum is forced at omega ~ H(z) by construction (the opposite of H<<omega), w(z) is a comoving-slice-averaged quantity over multi-Hubble history — 'not an axis quantity' — outside the static chart's r<1/H. Under the status quo it survives, and it is a total-invisibility consumer of the spatial commitment (zero occurrences of FRW/homogen/isotrop/background/cosmic in the whole node).",
     "evidence": "calc/wz_dark_energy.py lines 23-25 ('the cosmological expansion forces the vacuum at frequency ~ H(z)'); RUNG3_KEYSTONE_MAP.md section 6.3 item 3 ('w(z) is not an axis quantity. Bridging them is either a theorem (derived) or a new priced input'); D2 ('not an origin-worldline quantity'); verified keyword absence in claims.json."
    },
    {
     "result": "kk_static_transfer",
     "fate": "RE-OPENED under re-declaration: its dispersion route integrates Im c0(omega)/omega down to omega -> 0 — precisely where the H<<omega label license fails — and its channel structure (c0 at fixed k-channel) is SVT-frame-bound; a static-label rebirth would be a recomputation in (T,l), not a relabel. Survives as an open question under the status quo.",
     "evidence": "Node statement verbatim: 'Re c0(0) = (2/pi) * integral_0^inf domega Im c0(omega)/omega... UNTIL THIS IS COMPUTED, the quasi-static x inherits NO sign from the passivity floor.'"
    },
    {
     "result": "passivity_channel_diagonal + x_no_pin_theorem",
     "fate": "SURVIVE as frame-internal algebra on both branches — but their frame's license IS the surfaced omission: their coefficients live at joint (omega,k^2) on the Ward-surviving projectors, and neither node's text carries any background-frame vocabulary (both grep hits were 'amplitude-homogeneous', false positives). Under re-declaration the projector frame itself is chart-unlicensed at horizon scale.",
     "evidence": "S_IF.md section 3 (kernels decomposed on the projector pair at fixed (omega,k)); verified false-positive contexts in both nodes; both sit in the eight-node demotion cone (P1A route)."
    },
    {
     "result": "eft_operator_basis + p_tt_ansatz (two-survivor enumeration)",
     "fate": "RE-DERIVATION OWED under re-declaration: the enumeration is exhaustive within the SO(3)-rotations-plus-translations-at-fixed-k frame; the static chart supplies SO(3)-about-a-point plus time translations, NOT translations-at-fixed-k, so the two-survivor result is frame-bound and the (T,l) enumeration is a new computation. Under the status quo they survive, with eft_operator_basis as the omission's one in-statement appearance — a delta-0 to-derive QUESTION node whose phrase 'REGISTER-DECLARED symmetries only' dangles for S6 (no register declaration exists).",
     "evidence": "calc/operator_basis.py lines 17-19 and the KC5 fence paragraph ('within this frame it is exhaustive... because k-hat is the only available direction'); eft_operator_basis.statement; 'S6' = 0 occurrences in provenance/claims.json."
    },
    {
     "result": "zeta_interior_family (constant-x window x < ~0.59)",
     "fate": "RIDES WITH mu_linear: its window is set by horizon-scale late-time data (DESI Sigma0 lensing) read through the SVT frame; survives status quo, re-scoped-or-lost under re-declaration. Its isotropy grep hit is a false positive (scalar anisotropic stress).",
     "evidence": "S_IF.md section 5 (the window and its F-MAP fence); verified boundary_condition context."
    },
    {
     "result": "vacuum cluster (21 vc_* nodes)",
     "fate": "UNAFFECTED on every branch and UNAVAILABLE as GRUT-scope pricing: sealed own-scope ledger. Its 2026-08-04 homogeneity disposition does not transfer — the fence-fail was edge-specific (vc_ir_gravity_is_gr's deniers AFFIRM homogeneity) and R3 forbids inheriting drop lists across clusters.",
     "evidence": "VACUUM_CLUSTER_MAP.md: 'GRUT's ledger is untouched at +13 and the cluster's own ledger is 0 — the two vocabularies cannot bleed (verified by execution)'; vc_averaging_commutes statement (the deliberate half-booking, verbatim); provenance/OMISSION_STANDARD_v2.txt R3: 'A DROP LIST IS NEVER INHERITED ACROSS CLUSTERS. Re-run the fence every time.'"
    },
    {
     "result": "response_lorentz_covariance (retired +1)",
     "fate": "UNTOUCHED — and it is the corpus's model of how a background-adjacent priced input actually retires: by exhibited computation (membership tests, countersigned), not by re-declaration. The contrast is the argument: the flow node's discharge clause proposes retirement by declaration; the register's only successful retirement was by demonstration.",
     "evidence": "Node statement and ledger_note: '+1... RETIRED 2026-08-30 per this node's own discharge clause... The priced RESPONSE property is now DEMONSTRATED unimposed, not assumed'; it explicitly declined to price the background ('NOT that the background is Lorentz invariant')."
    }
   ],
   "case_points": [
    {
     "point": "THE PRIMARY YIELD IS A MIRROR-IMAGE OMISSION, AND THE TELLS PASS ON VERIFIED STRINGS. The booked flow node prices 'a one-parameter family of time translations under which the setup is invariant, so that two-point kernels depend on the time DIFFERENCE alone and a kernel of a single frequency K_R(omega,k) is definable.' The exactly parallel proposition — the declared background carries spatial translations (plus rotations) under which the setup is invariant, so kernels depend on x - x' alone and a single-k label is definable — is priced nowhere in GRUT scope. Conjunct 1: suppose it false — kr_contract's G0_R(omega,k), mu_linear's k^2 Psi bookkeeping, passivity/x_no_pin's c_i(omega,k^2), zeta's x(omega,k^2), p_tt/eft's fixed-k projectors, and S_IF section 3's d3k integral can none of them be established, stated, or used. Conjunct 2: not booked — 'S6' occurs 0 times in provenance/claims.json (verified); the S6 pointer chain is S_IF section 1 (ledger-0 prose) -> calc/operator_basis.py ('S6 mu_linear's frame') -> mu_linear, which contains ZERO frame vocabulary; RUNG3_KEYSTONE_MAP.md section 6.2 independently records 'no register field mentions foliation, cosmic time, or proper time.'",
     "evidence": "provenance/claims.json (flow node statement; S6 count 0; per-node keyword absence verified — three apparent hits were false positives: 'amplitude-homogeneous' at passivity/x_no_pin, 'anisotropic stress' at rung3/zeta/mu_linear); S_IF.md sections 1-3; calc/operator_basis.py lines 10-27; RUNG3_KEYSTONE_MAP.md section 6.2."
    },
    {
     "point": "MULTIPLICITY IS THE BOOKED PRECEDENT'S OWN 'CERTAINLY' SHAPE. The flow node's tier_note rules: 'IF SEVERAL NODES PRESUPPOSE IT, IT IS CERTAINLY AN OMISSION, because multiplicity rules out its being any one node's hidden conjunct' — and it ruled the S4/KMS in-statement appearance an ENTAILMENT of a named concept, not an appearance of the candidate. The spatial candidate is presupposed by at least seven GRUT-scope nodes and appears in the statement of exactly one (eft_operator_basis, a delta-0 to-derive question node that cannot price the license it consumes). The precedent forecloses the split-question objection in exactly the form it will be raised.",
     "evidence": "background_time_translation_flow.tier_note verbatim (the three checkable facts and the S4 adjudication); eft_operator_basis tier to-derive, ledger_delta 0; the verified statement-visibility table (one in-statement, two side-field, at least eight fully absent)."
    },
    {
     "point": "THE R1 FENCE PASSES ON THREE INDEPENDENT DENIERS, ONE OF THEM SUPPLIED BY THE DISCHARGE ROUTE ITSELF. (a) R1's own worked example: 'FLATNESS PASSES — non-flat cosmologies are named positions AND the denial changes what the cluster says.' (b) The backreaction/inhomogeneous programs are named deniers with an in-register instance (vc_averaging_commutes books exactly that denial in vacuum scope). (c) Decisively and newly: the corpus's own named candidate background denies the commitment in-chart — computed from the keystone map's own embedding, the static time translation is d_T = d_t - H x^i d_i and [d_T, P_j] = +H P_j != 0, so adopting the candidate destroys global spatial-translation invariance of the operative chart and turns the joint (omega,k) label into an H<<omega effective object. The denial changes what the cluster says: no exact joint label, chart-local enumeration, unlicensed SVT frame at horizon scale.",
     "evidence": "provenance/OMISSION_STANDARD_v2.txt R1; vc_averaging_commutes.statement; the symbolic computation run this session from RUNG3_KEYSTONE_MAP.md's embedding (constraint = 1/H^2 exact; commutator = +H P_j; plane wave not a flow eigenfunction), consistent with the corpus's own derived D3b and PRIMITIVE_INVERSION_SCOPE.md section 5."
    },
    {
     "point": "THE ASYMMETRY IS THE FINDING, AND THE CORPUS RECORDED THE GAP TWICE IN ITS OWN WORDS. The time half of one background-symmetry commitment is priced (+1, R5-wired, P1A-adjudicated EDGE_CORRECT at HEAD); the spatial half of the same commitment is carried as ledger-0 prose. The register's own audits name the residue: section 6.2 (the missing clock/foliation vocabulary, 'which is how the C1/C2 mismatch passed two pre-registrations') and D6 (the unpaid transport). A register that prices d-omega's license and inherits d3k's license silently cannot claim the flow booking settled its background commitments — the gate protocol pre-registered exactly this catch ('is flat-FRW/homogeneity itself priced anywhere, or prose-carried?... an OMISSION finding in its own right, booked by the owner under OMISSION_STANDARD_v2, not by this gate').",
     "evidence": "program/STATIC_PATCH_DISCHARGE_GATE_01_PROTOCOL.md sub-question 2; RUNG3_KEYSTONE_MAP.md sections 6.2 and D6; program/P1A_EDGE_ADJUDICATION_01.md (EDGE_CORRECT; 'the flow is a named open input with a named discharge route that retires it at zero and mechanically reverses the demotions')."
    },
    {
     "point": "R4 ATOMISATION RUN HONESTLY, WITH DISPOSITIONS FOR EVERY PART (R2). The raw candidate 'flat-FRW/homogeneity/S6' is compound. Book ONE atom: (a) the background spatial-symmetry flow (translations + rotations of the declared slicing, licensing the (k, SO(3)) label) — the exact spatial twin of the booked node. Dispositions for the rest: (b) the STATE's spatial invariance — ABSORBED (the BD/A3 declaration and the acknowledged vc_state_expectation_functional shared input carry it, mirroring how the flow booking absorbed state-half (b) into rung2's KMS content); (c) spatial FLATNESS in GRUT scope — a SPLIT/atom question for the owner (its vacuum-scope booking cannot bleed per R3; its GRUT-scope fence run is weaker: curved-FRW deniers keep a k label, only reshaped); (d) parity-evenness — a RIDER, 'inherited, not chosen', conditional on the ansatz being priced; (e) the fixed-slicing choice — FAILS the fence (a convention no named position denies; cf. the corpus's own 'split = convention' practice); (f) 'one global a(t)' — belongs to the rung7/data-reduction chain, disposed as a split question if (a) books. The calibration correction (builder over-predicts findings, two-for-two) is honored by booking one atom, not six.",
     "evidence": "provenance/OMISSION_STANDARD_v2.txt R2/R4 and the calibration correction; background_time_translation_flow.tier_note (the R4 template); calc/operator_basis.py S6/S7 'inherited, not chosen' lines; S_IF.md section 1 (state declaration and vc_state_expectation_functional acknowledgment)."
    },
    {
     "point": "AVAILABILITY IS UNDERDETERMINED WITH NAMED DECIDERS, NOT MERELY UNDECIDED. The record contains three live routes and forces none: the global re-declaration is collision-bound (A3 PROHIBITED row; C5 clock SPEC; D2's derived w(z) obstruction); the scoped route needs the section-6.3 bridge ('either a theorem (derived) or a new priced input' — neither exists); the D3a worldline route is DERIVED on the currently declared background but its reach to the assembled object 'is itself part of wall (A)'s assembly' (unperformed). The deciders are concrete and in-corpus: U1 the assembly reduction, U2 the bridge theorem-or-booking, U3 the register vocabulary legislation (P1B precedent: LEGISLATION-REQUIRED is a real outcome), U4 the interacting static-patch Sigma. Naming them and stopping is the protocol's own UNDERDETERMINED consequence — and it composes with NEW-OMISSION-SURFACED, which the protocol says 'may coexist with either outcome above.'",
     "evidence": "program/STATIC_PATCH_DISCHARGE_GATE_01_PROTOCOL.md pre-registered outcomes; RUNG3_KEYSTONE_MAP.md D3a/D3b/C7 and section 6.3; PRIMITIVE_INVERSION_SCOPE.md sections 4-6; program/P1B_ADJUDICATION_01.md (LEGISLATION-REQUIRED, stanza OPEN)."
    },
    {
     "point": "THE K3 ARITHMETIC IS PRE-REGISTERED AND THE LABEL GEOMETRY ENFORCES IT. A joint (omega,k) spectral label requires two commuting symmetry flows. Flat FRW supplies the spatial one and lacks the timelike one (the booked +1); the static patch supplies the timelike one and destroys the spatial one (computed commutator). Any background re-declaration therefore RELOCATES the label commitment rather than retiring it — PIS K3 verbatim: 'one-for-one relabel... Net ledger does not move. Kill — and this outcome is the most likely one'; PIS section 4 pre-registered the same for thermal-time ('No ledger gain. Relabeling.'); and l0_r2_exact_unique_breaker's tier_note supplies the in-register precedent ('The assumption is RELOCATED (mode -> omega<->x map), not discharged'). The discharge clause's 'retires into that declaration at zero' is arithmetically unavailable for the corpus as banked: zero is not on the menu.",
     "evidence": "PRIMITIVE_INVERSION_SCOPE.md sections 4-6 (K2/K3, the standing prediction); l0_r2_exact_unique_breaker.tier_note verbatim; the commutator computation; S_IF.md section 3 (the joint-label action)."
    }
   ],
   "rebuttals_of_rivals": [
    {
     "rival": "DISCHARGE-AVAILABLE (retire the +1 into a static-patch declaration; re-promote the eight)",
     "rebuttal": "Four independent blocks, all from the record. (1) The clause requires 'the DECLARED background' to carry the Killing vector, and the declared background is flat FRW 'for every banked observable' (S_IF.md section 1) — so availability means re-declaration, and the only exhibited re-declaration target collides with three frozen strata: WALL_A_A3_DECLARATIONS.md line 156 PROHIBITS the static-patch thermal state as a primary-run input; calc/gw_tensor_friction.py's frozen SPEC forbids importing any static-Killing quantity; D2 derives that w(z) is not an origin-worldline quantity and the chart ends at r = 1/H. An advocate for AVAILABLE must either exempt those results (silent re-scoping, which the protocol counts as failure) or propose superseding frozen declarations (owner acts, outside this gate). (2) 'Retires at zero' fails computed label arithmetic: [d_T, P_j] = H P_j means the re-declaration buys the omega label by breaking the k label — the +1 relocates; K3 was pre-registered as the most likely outcome and PIS section 4 already ruled the identical move 'No ledger gain. Relabeling.' (3) The tidiness payoff is smaller than it looks: the eight re-promotions are tier moves, not ledger deltas, and P1A (EDGE_CORRECT, at HEAD) makes the reversal 'a register change requiring its own re-certification' — CERTIFICATION_02 does not survive by default. (4) This verdict is the doubly-named bias direction (tidiness- AND framework-favorable, the builder's documented pattern of four audit-caught framework-favorable collapses), and every computation that could overcome the corpus's sealed standing prediction ('this is where it breaks') is unperformed: U1, U2, U4 do not exist in the repo. What AVAILABLE needs, the record does not contain; what the record contains, AVAILABLE must overwrite."
    },
    {
     "rival": "DISCHARGE-UNAVAILABLE (the obstruction is the finding; record it and close)",
     "rebuttal": "UNAVAILABLE-as-final overclaims in two directions and under-reports in a third. (1) It converts a pre-registered PREDICTION into an adjudicated no-go without the deciding computation: PIS section 5 says the flows are 'on the face of it, different flows' and K2 is conditional ('IF they cannot be connected') — while the corpus itself has already connected a piece (D3a: along-worldline stationarity in cosmic proper time, derived, verified to 12 decimals; C3 re-graded 'now partly derived'; C7's category is 'unperformed reduction, not a contradiction'). Banking unbridgeability now is predicting the outcome — the exact move the discipline forbids (the rung3 spine-test lesson: the earned verdict replaced 'the previously-guessed', and guessing was named as the forbidden move). (2) It forecloses the worldline route, which requires NO re-declaration at all: D3a exhibits a time-translation structure that IS a property of the currently declared background at worldline scope, so whether the flow node partially discharges or re-scopes on FRW-as-declared is a live, unruled question — the record underdetermines it pending wall-(A)'s assembly reduction (U1). (3) It leaves the gate's verdict-independent yield on the table: the protocol pre-commits that an unpriced presupposition found here 'is an OMISSION finding in its own right,' and the omission (S6 spatial side; plus the D6 transport residue) books on the UNAVAILABLE branch just as surely as on any other. The correct composite is NEW-OMISSION-SURFACED plus availability-underdetermined-with-named-deciders — which preserves everything UNAVAILABLE would record (the collisions, the K3 arithmetic, the chart geometry) as the obstruction inventory inside the availability analysis, without banking an unproven no-go or discarding the omission package."
    }
   ],
   "honest_weaknesses": [
    "The invisibility tell is genuinely weaker for my primary candidate than it was for the booked flow node. The flow's booking rested on 'stationar' occurring ZERO times even in S_IF.md prose; my candidate is NAMED in that same prose (S_IF section 2's S6 line) and in one GRUT-scope node statement (eft_operator_basis). I argue conjunct 2 is about register booking, not prose visibility, and the S4/KMS precedent handles the in-statement appearance — but an adjudicator who reads visibility broadly could re-classify Omega-A as a split question of eft_operator_basis plus a prose-carried declaration, booking it at delta 0 as a declaration node rather than +1 as an omission. The unpriced-ness survives either way; the classification and the integer do not.",
    "My candidate was pre-named by the gate protocol itself ('e.g. spatial homogeneity/flatness') and the surveyor's homogeneity_pricing row did substantial legwork — an instrument-favorable finding suggested by the instrument that profits from it, in a program whose calibration correction records the builder over-predicting finding counts two-for-two. Mitigation, not exoneration: the decisive facts are machine-checkable (S6 = 0 in claims.json; mu_linear's zero frame vocabulary; the computed commutator), and I booked one atom rather than the six the compound offered.",
    "The strongest fence denier I cite for GRUT scope — the static patch chart itself — is the discharge route's hypothetical, not the standing declaration. Under the status quo the live deniers are the inhomogeneous/backreaction programs, and an adjudicator could rule their denial targets the ADEQUACY of the averaged description (already booked, vacuum scope, vc_averaging_commutes) rather than the background-symmetry license per se. Per R3 a cross-cluster re-book is legitimate, but then the finding is a re-book with new GRUT-scope edges, not a wholly new omission — same +1, humbler headline.",
    "UNDERDETERMINED must survive the charge of being the lazy verdict, and three of my four named deciders are known-hard or external: U4 is the dS-IR controversy in local form (the corpus's own words), U1 is wall (A)'s assembly, U2 is an unexhibited theorem. For the GLOBAL route my position is operationally close to UNAVAILABLE-for-now; the difference is real only for the scoped and worldline routes, and if the owner rules those routes outside the discharge clause's plain text ('a timelike Killing vector'), the availability half of my verdict collapses toward the rival's.",
    "Omega-B (the D6 transport) is weak as a NEW finding: the keystone map already records it verbatim as 'currently unpaid,' D3a has discharged its worldline half, and its residual multiplicity is thin (rung2's subsumption justification; the S_IF section 3 lock at non-worldline scope). Booking it may honestly resolve to a split of rung2's T-subsumption rather than a standalone omission — I flag it for owner disposition rather than claiming the classification.",
    "There is a principled-asymmetry escape I cannot fully close: if the owner rules that S_IF section 1's 'flat FRW for every banked observable' IS the register's booking of the spatial symmetry (declaration-as-booking, the way conventions are carried at ledger 0), Omega-A fails conjunct 2 in spirit and the finding reduces to missing R5 wiring against a declaration node. My counter is that the identical reading was available for the TIME half on 2026-08-18 — the same S_IF declaration existed — and the program booked +1 anyway; but that counter shows the two halves were treated differently, it does not prove which treatment the owner will now call correct.",
    "I verified the corpus at HEAD d4a385a rather than the briefed e76953a (e76953a is an ancestor; no evidence file — claims.json, S_IF.md, keystone map, PIS, calc/ — changed between them; the deltas are P1A/P1B program files). P1A's EDGE_CORRECT ruling post-dates my briefing and I have USED it (it strengthens the demotions' standing and prices the reversal); if the gate intended advocates sealed at e76953a, that use is a protocol deviation I am declaring rather than hiding."
   ]
  },
  "DISCHARGE-UNAVAILABLE — with a": {
   "availability_analysis": "THE CLAUSE'S OWN TEXT SETS THE BAR. The discharge fires only if \"the declared background is one carrying a timelike Killing vector\" (provenance/claims.json, background_time_translation_flow, overturning_computation, verified verbatim). The corpus's actually-declared background is \"flat FRW for every banked observable; the linear-cosmology frame is the fixed-slicing spatial-SVT frame of mu_linear\" (S_IF.md §1, verified). So the route is a RE-DECLARATION of the background under every banked result — and the availability question is whether the banked corpus survives being read on the new background. It does not, for a reason the corpus has already derived in its own texts.\n\nTHE LABEL COLLISION (the precise obstruction — this is the physics finding). The banked corpus is written, node by node, in the joint flat-slicing label (ω, k): S_IF.md §3's action is defined \"at fixed (ω, k) in the enumerated frame\"; eft_operator_basis enumerates \"under the REGISTER-DECLARED symmetries only (... the FRW homogeneity/isotropy frame)\"; calc/operator_basis.py:17 glosses that frame as \"SO(3) rotations + translations at fixed k\"; passivity_channel_diagonal and x_no_pin_theorem are pointwise statements about c_i(ω,k²) on the Ward-surviving projector pair; the KMS lock is n_i(ω)=coth(βω/2)·Im c_i(ω) per channel on those (ω,k²) coefficients; mu_linear's bookkeeping is k²Ψ at fixed slicing. That joint label exists because the FRW comoving foliation supplies spatial translations on each slice. The de Sitter static patch supplies the OTHER half: a timelike Killing boost ∂_T — and its surviving spatial symmetry at fixed T is SO(3) rotations about one observer worldline, NOT translations. The static-patch Killing flow does not commute with flat-slicing spatial translations (standard dS isometry algebra, which the corpus itself uses rather than merely asserts): the corpus's one existing static-patch computation, calc/static_patch_tt_response.py, is forced to decompose in STATIC MULTIPOLES — a radial master equation f d/dr(f dψ/dr)+(ω²−V)ψ=0 with V=f(r)(l(l+1)/r²+cH²), labels (ω_Killing, l, m), graviton l≥2, no k anywhere in the file — and RUNG3_KEYSTONE_MAP.md D3b derives the general statement: \"the FULL two-point object Σ(x;x′) does not reduce to a function of a time difference in any single global clock; only the static patch offers a global reduction, because Killing stationarity makes z a function of T−T′ everywhere.\" PRIMITIVE_INVERSION_SCOPE.md §5 states the collision explicitly: \"GRUT's kernel is written in cosmological slicing at (ω, k) on an FRW background. In flat slicing de Sitter has no global timelike Killing vector ... the flow that is geometric by theorem and the flow the kernel actually uses are, on the face of it, different flows.\"\n\nCONSEQUENCE: after the re-declaration, the exactly-defined spectral label is (ω_Killing, l, m) on a chart covering r<1/H around one worldline (f=1−H²r², horizon at r=1/H — PART 0 of the static calc). The banked (ω,k) label survives only as an APPROXIMATE object where the two flows locally agree: on/near the axis (D1: T=t at r=0 exactly; D2: nowhere off-axis) and for intervals/frequencies with H≪ω (D4: rate comparisons across more than O(1/H) are clock-dependent — e^{−Γt}=(HT)^{−Γ/H}, a power law in the other clock). So the re-declaration converts every exact banked (ω,k) statement into an H≪ω effective statement. That is not a discharge of the flow assumption; it is a re-scoping of the entire corpus to the regime where — per the flow node's OWN note — the WKB/quasi-stationary substitute already licenses the label WITHOUT any Killing vector (\"quasi-stationarity holds for H << omega\"). Where the Killing vector would add something the WKB substitute cannot (ω→0, ω~H, horizon-scale k), the static patch does not carry the banked label at all.\n\nTHE FATAL ASYMMETRY, result class by result class: (a) The results that survive H≪ω re-scoping unharmed — kr_contract_retarded_tier4 (hard-gated: eps_H=(104/9)H²/ω², eps_H≥1 raises DomainRejected, header \"no omega << H\"), gw_tensor_friction (LIGO band, H0/ω~1e-20, validity term \"1+hcorr == 1.0 in double\"), rung4/gw_dissipation_bounds (LIGO band), rung8 tabletop (T_dS~3e-30 K, exp(−10²¹)-negligible) — are exactly the results for which the flow question is ALREADY moot at their working point. The discharge is idle there: it retires nothing those scopes were paying. (b) The results the flow node exists FOR — rung3_single_pole's η=lim_{ω→0} Im G_R(ω)/ω (its boundary_condition, verified: the WKB defense \"is VALID FOR H << omega -- and this node's claim is a statement about the OPPOSITE limit\"; the escape is CLOSED, owner-ordered 2026-08-18), kk_static_transfer's dispersion integral Re c0(0)=(2/π)∫₀^∞ dω Im c0(ω)/ω reaching ω→0, rung7_wz's w(z) with the vacuum \"forced at frequency ~ H(z)\" (calc/wz_dark_energy.py:23), and mu_linear's empirical kill legs at k~H0 (keystone line 244: \"~2σ t-g cross + ~3.5σ lensing\") — live exactly where the static-patch (T,l) label and the FRW (ω,k) label demonstrably diverge (D2/D3b/D4), and in rung7's case outside the static chart altogether: w(z) is \"a quantity of the cosmological background averaged over a comoving slice ... not an origin-worldline quantity\" (D2), and keystone §6.3: \"w(z) is not an axis quantity.\" So the re-declaration helps only where help was never needed and fails to deliver the license precisely where the priced assumption does load-bearing work.\n\nFROZEN-DOCUMENT COLLISIONS seal availability off: (1) calc/gw_tensor_friction.py's clock declaration (SPEC section 4 / keystone C5, verified at lines 6-7): \"single FRW COSMIC clock throughout. Every rate below is d/dt_cosmic; no static-Killing or e-fold quantity is imported.\" A static-patch background declaration makes the background's own time a static-Killing quantity; the corpus's most recent banked-adjacent computation is contractually incompatible with the re-declared background — it must be re-scoped (protocol-defined failure) or contradicted. (2) PHYSICS_LEDGER/WALL_A_A3_DECLARATIONS.md (frozen, blind-protected) declares BD mode functions \"positive frequency w.r.t. the BD vacuum in the far past η → −∞\" — a Poincaré-chart surface not contained in the static patch — and its alternative-states table rules \"thermal (static-patch) initial state ... PROHIBITED as an input for the primary run.\" Declaring the static patch as THE background while the state's defining data live outside the declared chart is not a coherent self-contained declaration. (3) The Tier-4 kernel is computed on neither background — a flat Minkowski contract (G0_R=2κ²/((ω+i0)²−k²)) with O(H²) BD-chart corrections, inputs \"frozen, never edited\" — so the re-declaration cannot re-home the corpus's central kernel; it can only orphan its provenance. (4) There is no register field to declare a background/foliation IN: keystone §6.2, verified — \"no register field mentions foliation, cosmic time, or proper time — which is how the C1/C2 mismatch passed two pre-registrations.\" The re-declaration would have to live in S_IF.md §1 prose (a ledger-0 construction document), i.e., a silent re-scope of every banked observable — the exact move the protocol pre-defines as failure.\n\nAND THE CORPUS PRE-REGISTERED THIS FAILURE: PRIMITIVE_INVERSION_SCOPE.md §5 \"Standing prediction: this is where it breaks,\" kill condition K2 \"Static-patch Killing flow cannot be connected to the cosmological-slicing flow the kernel uses. Kill,\" K3 \"one-for-one relabel ... Net ledger does not move. Kill — and this outcome is the most likely one after K2.\" The sealed prediction is confirmed, not overturned: what I have exhibited is K2's mechanism (the label collision, quantitative via D2/D3b/D4) producing an outcome strictly worse than K3 (net ledger moves the WRONG way, plus banked-scope loss). VERDICT ON AVAILABILITY: the named static-patch route is UNAVAILABLE as a discharge — it is available only as a priced re-foundation that costs more than it retires and de-homes banked observables. The corpus's third route (D3a's worldline stationarity — \"along any single comoving geodesic, the restricted correlator IS stationary in cosmic proper time,\" verified to 12 decimals) is real but is not this discharge: it carries no timelike Killing vector (the clause's text does not fire), it licenses along-worldline kernels only (not the assembled (ω,k) corpus — D3b), and it is a node REWRITE narrowing what is priced, an owner action outside this clause.",
   "case_points": [
    {
     "point": "The discharge clause requires the DECLARED background to carry the Killing vector, and the declaration it would replace is load-bearing for every banked observable — so this is a re-foundation, not a retirement. The clause's zero-cost reading ('retires into that declaration at zero') is true only in the counterfactual world where the static patch already WAS the declared background; in the actual corpus the declaration must first be created, and creating it is a priced act.",
     "evidence": "claims.json background_time_translation_flow.overturning_computation ('the declared background is one carrying a timelike Killing vector') vs S_IF.md §1 ('flat FRW for every banked observable') — both verified verbatim. The gate protocol itself concedes the framing: 'So the route is a background RE-DECLARATION, not a free retirement' (STATIC_PATCH_DISCHARGE_GATE_01_PROTOCOL.md)."
    },
    {
     "point": "The static patch cannot carry the corpus's joint (ω,k) label: Killing stationarity buys a global frequency ONLY in the frame whose spatial symmetry is SO(3) about one worldline, so the exact spectral label becomes (ω_Killing, l, m) on r<1/H. The banked corpus's every kernel statement — the S_IF action, the two-survivor enumeration, passivity/no-pin, the KMS lock, mu_linear's k²Ψ bookkeeping — is written in (ω,k) on comoving slices. The re-declaration therefore converts exact banked statements into H≪ω effective ones (D1/D4 locality of the clock agreement), which is a corpus-wide silent re-scope — the protocol's named failure mode.",
     "evidence": "S_IF.md §3 'at fixed (ω, k) in the enumerated frame'; calc/operator_basis.py:17 'SO(3) rotations + translations at fixed k'; calc/static_patch_tt_response.py PART A (radial master equation, (ω,l) labels, no k in the file); RUNG3_KEYSTONE_MAP.md D3b ('only the static patch offers a global reduction, because Killing stationarity makes z a function of T−T′ everywhere'), D2 (off-axis no identification), D4 ('Any comparison of rates across more than O(1/H) of elapsed time is clock-dependent'); PRIMITIVE_INVERSION_SCOPE.md §5 ('the flow that is geometric by theorem and the flow the kernel actually uses are, on the face of it, different flows'). All verified in-file."
    },
    {
     "point": "The discharge is IDLE everywhere it is safe and ABSENT everywhere it is needed. The H≪ω consumers (Tier-4 kernel, GW friction, GW dissipation bounds, tabletop) already carry the license through the WKB/quasi-stationary substitute the flow node itself records — no Killing vector needed at their working points (H0/ω~1e-20; eps_H hard gate). The ω→0 and ω~H consumers (rung3's transport limit, kk_static_transfer's dispersion integral, rung7's w(z) forced at ω~H(z), mu_linear's k~H0 kill legs) are exactly where 'quasi-stationarity holds for H << omega' FAILS — and there the static patch supplies the wrong label ((T,l), one chart) or no chart at all (w(z) is a comoving-slice average, not an axis quantity). Retiring the +1 buys nothing at the first class and delivers nothing at the second.",
     "evidence": "Flow node overturning_computation: 'quasi-stationarity holds for H << omega and the single-pole claim lives at omega -> 0, exactly where it fails' (verified). PHYSICS_LEDGER/wall_kr_tier4_retarded.py:31-36 (eps_H ladder, DomainRejected) and line 41 'no omega << H'. calc/gw_tensor_friction.py:29-30 ('the flat-contract scope is carried via the [1 + (104/9)(H0/w)^2] validity term the kernel itself supplies'). rung3_single_pole.boundary_condition WKB closure (verified). kk_static_transfer.statement (KK integral over all ω, subtraction obstruction — verified). calc/wz_dark_energy.py:23 ('the cosmological expansion forces the vacuum at frequency ~ H(z)'). RUNG3_KEYSTONE_MAP.md §6.3 ('w(z) is not an axis quantity') and line 244 ('~2σ t-g cross + ~3.5σ lensing')."
    },
    {
     "point": "The re-declaration collides with two FROZEN instruments: gw_tensor_friction.py's SPEC-level clock contract ('no static-Killing or e-fold quantity is imported') and the Wall-A A3 declarations (BD state defined at η→−∞, a surface outside the static chart; 'thermal (static-patch) initial state ... PROHIBITED as an input for the primary run'). A declaration that contradicts frozen contracts can only proceed by re-scoping them — and the protocol pre-defines a discharge that 'succeeds by silently re-scoping banked observables' as 'a failure reported as one.'",
     "evidence": "calc/gw_tensor_friction.py:6-7 (verified verbatim); PHYSICS_LEDGER/WALL_A_A3_DECLARATIONS.md:132 ('positive frequency w.r.t. the BD vacuum in the far past η → −∞') and :156 (PROHIBITED row) — verified; STATIC_PATCH_DISCHARGE_GATE_01_PROTOCOL.md sub-question 3 (silent re-scope = failure)."
    },
    {
     "point": "There is no instrument to make the re-declaration WITH: the register has no field for background, foliation, or clock — the corpus's own audit says this vocabulary gap is how the C1/C2 clock mismatch passed two pre-registrations. So the re-declaration would live in S_IF.md prose (ledger 0), unpriced and unvalidated — reproducing at corpus scale the exact defect class (unpriced presupposition carried in prose) that the flow node was booked to end. The discharge route, executed today, would UNDO the omission repair it is attached to.",
     "evidence": "RUNG3_KEYSTONE_MAP.md §6.2 ('no register field mentions foliation, cosmic time, or proper time — which is how the C1/C2 mismatch passed two pre-registrations') — verified. S_IF.md header ('CONSTRUCTION DOCUMENT, ledger 0') — verified. Flow node tier_note: booked as an OMISSION because the presupposition was 'INVISIBLE IN THE TEXT' — the re-declaration would recreate invisibility one level up."
    },
    {
     "point": "The corpus PRE-REGISTERED this exact failure and the program's own subsequent record honored it: §5's standing prediction, K2 (flow mismatch unbridgeable) and K3 (one-for-one relabel, 'the most likely one'), sealed before this gate existed; and the P1A certification records that the stanza's 'only pre-authorized moot route (the static-patch discharge) did not occur.' A DISCHARGE-AVAILABLE verdict must overturn a sealed in-corpus prediction using only corpus-plus-standard-physics — and the corpus's derived content (D2/D3b/D4/D6) all points the other way. My verdict is the sealed prediction CONFIRMED with its mechanism made precise: the obstruction is the label collision, and its quantitative form is D4's exponential-to-power-law clock divergence beyond O(1/H) plus D3b's no-global-reduction theorem.",
     "evidence": "PRIMITIVE_INVERSION_SCOPE.md §5 and §6 (K2, K3) — verified verbatim; program/RAI_CAPABILITY_TEST_01_CERTIFICATION.md §3 (the moot-route sentence) — verified; RUNG3_KEYSTONE_MAP.md §6.4 'K3 remains the presumed outcome' — verified."
    },
    {
     "point": "The one place the static patch is genuinely already inside the banked corpus — rung2's T=T_dS=H/2π 'forced by horizon regularity' under a flat-FRW declaration — is an argument for the coexisting NEW-OMISSION finding, not for availability: the corpus itself books the residue as 'the transport itself is the assumption, and it is currently unpaid' (D6). Officializing the static clock pays that debt in one direction (the gate's ω-only equilibrium statements) while creating the same debt at strictly larger scope for every (ω,k²) object the KMS lock touches and every cosmological export. An unpaid debt discovered is an omission to book, not a discharge to grant.",
     "evidence": "rung2_kms_gate.ledger_note ('on the declared background the Hadamard/KMS condition forces T = T_dS = H/2pi uniquely (any other T is singular at the horizon...)') — verified; RUNG3_KEYSTONE_MAP.md D6 — verified; gate/kms.py is pure-ω, no k label (survey row, spot-checked)."
    }
   ],
   "cost_accounting": {
    "retires": "Exactly one priced input: background_time_translation_flow's +1 (net +16 → +15 on the input ledger), and — mechanically, per the P1A ruling's recorded route — the eight tier demotions of commit 67059a8 reverse (rung1_inin_formalism, rung2_kms_gate, rung4_love_kk, kr_contract_retarded_tier4, info_i1_renorm_as_information, passivity_channel_diagonal, rung7_w1_wz_map, u1_form_universality, derived-pending → shown), 'as a register change requiring its own re-certification' (P1A_EDGE_ADJUDICATION_01.md §6, verified). Note what the reversal is: tier bookkeeping conditional on the new declaration's acceptance, not ledger credit — the ledger 'prices INPUTS, not importance' (owner ruling 2026-08-18, recorded in the flow node's ledger_note).",
    "new_inputs": [
     "INPUT 1 — the static-patch background declaration itself: 'banked observables are computed on the dS static patch, an observer-dependent chart of extent r<1/H with Killing time T.' R1 fence (an input is something a named position denies): PASSES — the denying positions are the very ones the flow node's own R1 fence names ('Cosmological perturbation theory on FRW and the secular/non-stationary de Sitter programs work on a background with NO timelike Killing vector'), plus the corpus's entire data-reduction side (flat-FRW LCDM reductions: rung7's E(z), mu_linear's ISW/DESI legs, vc_rho_lambda's scheme). Denial changes what the framework says: w(z) and horizon-scale exclusions cease to be formulable on the declared chart. This input is STRICTLY STRONGER than the +1 it replaces: the flow node priced only 'a time-translation flow exists'; this declaration prices flow + specific chart + foliation + horizon. Cost ≥ +1.",
     "INPUT 2 — the clock-transport bridge: 'Killing-clock kernel statements transport to comoving-clock observables.' Required the moment the declared background's time is static while every banked export (w(z), mu_linear's legs, LIGO-band rates as d/dt_cosmic) stays cosmic. R1 fence: PASSES — the denying position is IN-CORPUS AND DERIVED: D4 proves rate comparisons across more than O(1/H) are clock-dependent (e^{−Γt} = (HT)^{−Γ/H}), D2 proves no off-axis identification, D3b proves no global one-clock reduction of the assembled object. The corpus has already priced this input's status: 'Bridging them is either a theorem (derived) or a new priced input' (keystone §6.3) and 'the transport itself is the assumption, and it is currently unpaid' (D6). No theorem exists in the corpus. Cost +1.",
     "INPUT 3 — the frame conversion for the S6-consuming chain: either (a) re-derive the operator enumeration, passivity algebra, and no-pin theorem on the static chart's (ω, l, m) decomposition — a NEW uncomputed derivation whose survivor structure is unknown (the static calc's l≥2 tower has different kinematics; nothing banked transfers by symbol substitution), or (b) declare the old (ω,k) frame a retained 'effective frame at H≪ω' — a new scoping input, denied by every use of the frame at horizon scale (zeta_interior_family's DESI window, mu_linear's low-ℓ legs — exact-statement consumers). Either horn is ≥ +1 or an unpriced re-derivation debt; taking neither horn is the silent re-scope the protocol defines as failure. Note the exposure this prices was FREE under flat FRW only because S6 is carried as unpriced prose — the string S6 occurs ZERO times in provenance/claims.json (verified by count), living only in S_IF.md §2 and calc/operator_basis.py:17; the re-declaration forces the unpriced half of the background declaration into the open, which is the coexisting NEW-OMISSION finding."
    ],
    "net": "Retire 1 priced input; acquire a minimum of 2 (declaration + transport) and realistically 3 (frame conversion) — net ledger moves +1 to +2 in the WRONG direction — AND banked-result loss is nonzero (rung7's w(z) de-homed from the declared chart; mu_linear's two empirical kill legs and zeta_interior_family's window outside the chart's reach; kk_static_transfer's route broken as posed; gw_tensor_friction's frozen SPEC contradicted). The protocol's DISCHARGE-AVAILABLE-NET-FAVORABLE bar is 'ledger net strictly improves or holds with ZERO banked-result loss': both prongs fail simultaneously. This is the corpus's own pre-registered K3 outcome ('every import discharged is replaced by an algebraic assumption of equal or greater cost') realized in its worse-than-predicted form: cost strictly greater, plus scope loss — K2's mechanism producing a super-K3 outcome. The eight re-promotions do not offset this: they are conditional tier moves purchased by the new inputs, i.e., 'shown, conditional on two-to-three priced assumptions' replacing 'derived-pending on one' — tier inflation in the builder's documented bias direction, not corpus value."
   },
   "scope_survival": [
    {
     "result": "kr_contract_retarded_tier4 (Tier-4 retarded kernel)",
     "fate": "SURVIVES BY IRRELEVANCE, NOT BY RE-HOMING — its hard gate already refuses the only regime where the re-declaration means anything (eps_H≥1 raises DomainRejected; 'no omega << H'), so the static-vs-FRW distinction is invisible at its working point. But it is computed on a THIRD background (flat Minkowski contract + O(H²) BD/Poincaré-chart corrections, inputs 'frozen, never edited'), and its A3 declarations define the state at η→−∞ — outside the static chart — and PROHIBIT the static-patch thermal state. Under the re-declaration its provenance becomes alien to the declared background it nominally sits under.",
     "evidence": "PHYSICS_LEDGER/wall_kr_tier4_retarded.py:5-11, 31-36, 41 (verified); PHYSICS_LEDGER/WALL_A_A3_DECLARATIONS.md:132, 156 (verified)."
    },
    {
     "result": "calc/gw_tensor_friction.py (Γ_T closure, REFUSE verdict)",
     "fate": "CONTRACT CONTRADICTION — survives only if NOT re-declared. Its frozen SPEC forbids importing any static-Killing quantity while the re-declaration would make static-Killing time the background's own primitive clock. Re-scoping it is the protocol's named failure; leaving it is a standing contradiction with the declared background.",
     "evidence": "calc/gw_tensor_friction.py:6-7 'single FRW COSMIC clock throughout ... no static-Killing or e-fold quantity is imported' (verified verbatim); keystone row C5 'single FRW cosmic clock throughout ... fine internally' (verified)."
    },
    {
     "result": "mu_linear (μ=Σ=η=1 no-go export)",
     "fate": "ALGEBRA SURVIVES INTERNALLY, FRAME AND EMPIRICAL LEGS DO NOT TRANSFER — the fixed-slicing spatial-SVT frame (KC5 fence) is defined by the comoving foliation's translations, which the static chart lacks (SO(3)-about-one-worldline only); and the kill legs are horizon-scale comoving-slice observables (low-ℓ ISW cross ~2.0σ, DESI Σ0 lensing ~3.5σ, k~H0) that a r<1/H chart cannot state. Keeping them means keeping flat FRW for exactly the observables the discharge clause says are re-declared — i.e., the discharge does not happen for this node.",
     "evidence": "S_IF.md §1 (frame declaration, verified); eft_operator_basis.boundary_condition KC5 fence 'spatial-SVT fixed-slicing frame ... exhaustive at all derivative orders WITHIN the frame' (verified); RUNG3_KEYSTONE_MAP.md line 244 '~2σ t-g cross + ~3.5σ lensing' and D2 (comoving-slice averages not origin-worldline quantities) (verified); calc/isw_exclusion.py header (low-ℓ cross-correlation observable, verified)."
    },
    {
     "result": "rung3_single_pole (anchor conjecture)",
     "fate": "NOT RESCUED — RE-POSED IN A LABEL WHERE ITS FREE-LEVEL RECORD IS NULL-TO-ADVERSE. Its claim is the ω→0 transport limit, exactly where the WKB license fails (closure recorded in the node, owner-ordered). On the static patch its object becomes (ω_Killing, l≥2): the corpus's own static computation found purely imaginary discrete frequencies gapped and spaced O(H), no quasinormal modes anywhere in the c=0 family (B=−A for every ω, PART 3b retraction), spectral function vanishing at the first l Matsubara rungs so the slowest surviving rung is (l+1)H — 'a SINGLE RATE THAT DOES NOT EXIST AT FREE LEVEL' — with the owner's correction that this free response is 'NULL, NOT ADVERSE' and everything rests on the uncomputed interacting Σ. The discharge would trade an unlicensed limit for an unlicensed conjecture in a different decomposition. Its separately-reserved tier question stays untouched per the gate's hard fence.",
     "evidence": "rung3_single_pole.boundary_condition WKB closure and tier_note ('the surviving rungs are the entire infinite tower n >= l+1 ... multipole-dependent'; 'A POLE-FREE FREE RESPONSE IS NULL, NOT ADVERSE') — all verified in claims.json; calc/static_patch_tt_response.py PART A and PART 3b (verified)."
    },
    {
     "result": "rung7_wz (w(z) dark energy, +3)",
     "fate": "DE-HOMED FROM THE DECLARED CHART — LOST AS BANKED. w(z)=−1+eps·R(H(z)τ) forces the vacuum at ω~H(z) (H/ω~O(1), the opposite of H≪ω) on the FRW cosmic clock with flat-LCDM E(z); w(z) is a comoving-slice average and 'not an axis quantity,' so no static-patch reduction reaches it, and the chart's r<1/H extent cannot hold the averaging domain. The corpus's +3 node's observable cannot be stated on the re-declared background; keeping it keeps flat FRW.",
     "evidence": "calc/wz_dark_energy.py:23,29 (forcing frequency, OM=0.31 flat — verified); RUNG3_KEYSTONE_MAP.md D2 and §6.3 ('w(z) is not an axis quantity ... Bridging them is either a theorem (derived) or a new priced input') — verified."
    },
    {
     "result": "kk_static_transfer (dissipative-to-static sign transfer)",
     "fate": "BROKEN AS POSED — its candidate route is a Kramers-Kronig integral over the ENTIRE ω axis including ω→0, the end where the H≪ω effective (ω,k) label fails; under the re-declaration the integrand below ω~H is no longer the banked object (the exact object there is per-l static-multipole), so the question must be re-posed per channel per l with unknown subtraction structure — on top of its already-named subtraction obstruction.",
     "evidence": "kk_static_transfer.statement ('Re c0(0) = (2/pi) * integral_0^inf domega Im c0(omega)/omega ... gravitational response kernels may require SUBTRACTED dispersion relations') — verified in claims.json."
    },
    {
     "result": "rung2_kms_gate",
     "fate": "THE ONE PARTIAL BENEFICIARY, AND THE BENEFIT IS ALREADY AVAILABLE WITHOUT RE-DECLARATION. Its gate is pure-ω (no k label), so the label collision does not bite the gate code; and its T=H/2π attribution would finally match its background. But D6's transport debt is merely relocated, not paid — every (ω,k²) channel the KMS lock constrains in S_IF §3 still lives in the comoving frame — and D3a already licenses the stationary worldline reduction at T_dS=H/2π in COSMIC proper time with no Killing vector and no re-declaration.",
     "evidence": "rung2_kms_gate.ledger_note (T forced by horizon regularity — verified); RUNG3_KEYSTONE_MAP.md D6 ('the transport itself is the assumption, and it is currently unpaid') and D3a (worldline stationarity, 12-decimal verification) — verified."
    },
    {
     "result": "passivity_channel_diagonal + x_no_pin_theorem + eft_operator_basis/p_tt_ansatz (the S6-frame algebra chain)",
     "fate": "SUSPENDED PENDING RE-DERIVATION — the two-survivor enumeration and the channel-diagonal passivity algebra are statements ON the Ward-surviving projector pair OF the S6 frame (SO(3)+translations at fixed k). The static chart's decomposition is (ω,l,m) with l≥2 kinematics; no banked theorem transfers by substitution, and the survivor count on the new decomposition is uncomputed. These are three of the corpus's structural results plus two of the eight nodes the discharge claims to re-promote — re-promoted into a frame their proofs no longer inhabit.",
     "evidence": "S_IF.md §3 (projector decomposition and Ward-scope correction — verified); calc/operator_basis.py:17-19 (S6 gloss — verified); eft_operator_basis.statement ('under the REGISTER-DECLARED symmetries only (... the FRW homogeneity/isotropy frame)') — verified."
    },
    {
     "result": "zeta_interior_family (constant-x window x<~0.59)",
     "fate": "WINDOW SOURCE OUTSIDE THE CHART — the DESI Σ0 lensing bound is a horizon-scale comoving-slice observable; same fate as mu_linear's legs.",
     "evidence": "S_IF.md §5 (window and F-MAP fence — verified in file); keystone line 244 (lensing leg — verified)."
    },
    {
     "result": "vacuum cluster (21 vc_* nodes)",
     "fate": "SURVIVES BY SEAL — no kernels, no frequency labels, own ledger scope ('the two vocabularies cannot bleed'). But note the asymmetry it creates: the only PRICED flatness/homogeneity in the whole corpus sits inside this sealed scope (vc_flatness_in_reduction), so the seal that protects the cluster also blocks citing it as GRUT-scope pricing — the GRUT-side spatial declaration stays unpriced prose either way.",
     "evidence": "VACUUM_CLUSTER_MAP.md line 5 (seal language — verified); vc_* count = 21 in claims.json (verified by count); S6 zero occurrences in claims.json (verified by count)."
    },
    {
     "result": "rung4_love_kk / gw_dissipation_bounds + rung8 tabletop stakes",
     "fate": "SURVIVE UNCHANGED — LIGO-band and lab-frequency scopes, H≪ω by 20-39+ orders; the flow question is moot at their working points, which is precisely why their survival is no evidence for availability: they are the discharge's idle sector.",
     "evidence": "calc/gw_dissipation_bounds.py F_BAND and 2026-08-20 scope fence (verified in survey, spot-checked); rung2_kms_gate.ledger_note tabletop sentence (T_dS~3e-30 K — verified)."
    },
    {
     "result": "calc/static_patch_tt_response.py (the named candidate's own track record)",
     "fate": "THE CHART'S RESIDENT COMPUTATION BANKS NOTHING — one derived potential (c=0, axial sector), one same-day retraction (PART 3b: B=−A for every ω, nothing quasinormal), one owner-ruled NULL. No banked result currently lives on the static patch; the re-declaration would move the corpus's foundation onto a chart whose only in-corpus occupant is a calc-layer object with a retraction in its header.",
     "evidence": "calc/static_patch_tt_response.py:40-42 (A-RETRACTED header), :420-422 (B=−A), PART A (verified); rung3 owner correction 'NULL, NOT ADVERSE' (verified)."
    }
   ],
   "rebuttals_of_rivals": [
    {
     "rival": "DISCHARGE-AVAILABLE-NET-FAVORABLE (the tidiness- and framework-favorable verdict): 'The computational core (Tier-4 kernel, GW bounds, tabletop) is already H≪ω-fenced so the re-declaration costs nothing there; rung2's T=H/2π already secretly needs the static patch; the flow node retires at zero per its own clause; eight nodes re-promote; the corpus becomes more honest about where its KMS structure lives.'",
     "rebuttal": "Each limb inverts under its own evidence. (1) 'Costs nothing where fenced' concedes the discharge is IDLE there — the WKB substitute already licenses H≪ω with no Killing vector (the flow node's own note), so retiring the +1 buys those scopes nothing they were paying for. (2) Where the +1 does load-bearing work (ω→0, ω~H, k~H0), the static patch supplies the (T,l) label on r<1/H — not the banked (ω,k) label — per the corpus's DERIVED D2/D3b/D4; the license is not delivered, it is relocated to a chart the observables don't inhabit (w(z) 'not an axis quantity'). (3) 'Rung2 already needs it' is D6's argument for PRICING the transport ('the transport itself is the assumption, and it is currently unpaid'), not for waiving it — an unpaid debt discovered is an omission, not a discharge. (4) 'Retires at zero' reads the clause's counterfactual as an actual: the clause prices the world where the static patch WAS the declaration; creating that declaration costs ≥2 R1-passing inputs (declaration + transport) plus the S6-chain conversion. (5) The eight re-promotions are tier bookkeeping conditional on the new inputs and on 'its own re-certification' (P1A §6) — and re-promoting passivity_channel_diagonal and the operator-basis chain into a frame their proofs no longer inhabit is exactly the over-unification failure on the builder's documented ledger. (6) The verdict must also overturn the corpus's sealed §5/K2/K3 prediction, with the protocol pre-naming this direction as the bias direction. (7) Decisively: the protocol's bar is 'strictly improves or holds with ZERO banked-result loss' — rung7's w(z), mu_linear's kill legs, kk_static_transfer's route, and gw_tensor_friction's SPEC all fail scope survival, so the bar is missed on both prongs regardless of how the ledger arithmetic is argued."
    },
    {
     "rival": "NEW-OMISSION-SURFACED as the PRIMARY verdict: 'The real finding is that flat-FRW spatial homogeneity/flatness (S6) is unpriced in GRUT scope — zero claims.json occurrences, priced only inside the sealed vacuum cluster — and D6's clock transport is unpaid; book the omissions and stop there.'",
     "rebuttal": "Both facts are true, verified, and ADOPTED here as evidence — the protocol explicitly says NEW-OMISSION 'may coexist with either outcome above,' and it coexists with mine: I hand the owner the omission package's contents (S6 unpriced; D6 transport unpaid; no register field for foliation/clock per §6.2). But as a PRIMARY verdict it abandons the commissioned question: the gate was frozen to answer 'is the discharge available, and at what net cost' — and the corpus as banked SUFFICES to answer it (the label collision is derived, the frozen collisions are textual, the arithmetic closes). Stopping at the omission would also get the dependency backwards: the S6 gap is not an alternative finding to UNAVAILABLE, it is part of UNAVAILABLE's mechanism — because the spatial half of the background is unpriced prose, the re-declaration has no register hook to operate through and its third cost surfaces as an unpriced re-derivation debt. Book the omission AND record the obstruction; the omission alone neither makes the re-declaration coherent nor cheap."
    },
    {
     "rival": "UNDERDETERMINED: 'Whether static-patch (T,l) results transport to (ω,k) observables is exactly wall A's uncomputed assembly (D3b's reduction question); until someone computes it, the gate cannot rule — name the computation and stop.'",
     "rebuttal": "The gate's frozen standard is 'what the corpus AS BANKED, plus standard physics the corpus itself cites, FORCES' — not what a future computation might show. As banked: the transport is an assumption with a recorded price tag ('currently unpaid' — D6; 'either a theorem (derived) or a new priced input' — §6.3), the assembled reduction 'has never been performed' (D3b), and the divergence of the two labels beyond O(1/H) is DERIVED, not open (D4's exponential-to-power-law result, D2's off-axis non-identification). An available discharge needs the bridging theorem to EXIST; the corpus contains, in its place, the theorem's absence plus its price. 'A theorem might arrive' does not make the discharge available today at net ≤0 — it makes it available at the cost of a new priced input, which is not availability but the UNAVAILABLE verdict's literal content (costs more than it retires). The arithmetic is decidable on the record; UNDERDETERMINED would misreport a decided question as an open one. What IS legitimately named for the future: if wall A's assembly ever derives the worldline/global reduction as a theorem, INPUT 2 retires and the question may be re-posed — that is the one computation my verdict identifies, and its absence today is part of the finding, not a bar to it."
    }
   ],
   "honest_weaknesses": [
    "The non-commutation step itself ([static Killing boost, flat-slicing spatial translations] ≠ 0 in the dS isometry algebra, hence no joint (ω_Killing, k) eigenbasis) is standard physics the corpus uses BY EXHIBITION (the static calc's forced (ω,l) decomposition; D3b's 'only the static patch offers a global reduction') but never states as an explicit commutator computation. The protocol admits 'standard physics the corpus itself cites,' and I claim by-use citation suffices — but a hostile reader could demand the one-line algebra be written into the gate record rather than imported, and the referee should note the citation is by-use, not by-statement.",
    "The A3 collision is narrower than it first looks: the frozen prohibition is on the static-patch thermal STATE as an input, not on a static-patch BACKGROUND declaration. My extension — that declaring the static patch while keeping a state defined at η→−∞ (outside the chart) is incoherent as a self-contained declaration — is a coherence argument I constructed, not frozen text. A careful re-declarer could try 'static-patch background, BD state specified via chart extension,' and my objection would then rest on the transport/label arguments alone (which I believe carry it, but the A3 limb would drop out).",
    "INPUT 2's status as a NEW cost is contestable: D6 says the clock transport is unpaid TODAY, under flat FRW — so a rival can argue the discharge merely SURFACES an existing latent debt rather than creating one, and my net arithmetic double-counts it against the discharge. My reply is scope-based (under FRW the transport is used in one node's attribution note; under the re-declaration it becomes load-bearing for every cosmological export), but the 'already owed' reading genuinely shades some of INPUT 2's weight from the cost column into the coexisting NEW-OMISSION column, and an honest referee might score net inputs as +1-not-+2 accordingly. Even at +1 the DISCHARGE-AVAILABLE bar still fails on the zero-banked-loss prong.",
    "My verdict keeps eight nodes at derived-pending — a real, ongoing epistemic cost the rival verdict would lift. I argue tier moves are not ledger credit (owner ruling: the ledger prices inputs) and that the re-promotions would be conditional on the new inputs; but the protocol's phrase 'net cost to the corpus' is not formally restricted to input-counting, and a referee who weighs eight tier re-promotions as corpus value could narrow my margin. My position needs the explicit standard: re-promotion purchased by new priced inputs into an un-re-derived frame is inflation, not value — I believe the corpus's bias-history file supports exactly this reading, but it is a reading.",
    "The D3a worldline route is a genuine third path my verdict fences out on the clause's letter (no Killing vector, so the named route doesn't fire): a node REWRITE narrowing the priced commitment to 'worldline flow + along-worldline kernels, joint-(ω,k) extension priced separately' might reduce the corpus's exposure below the current +1 without any background re-declaration. That is not this gate's question and banks nothing here, but a hostile reader could say my UNAVAILABLE verdict on the named route coexists with a cheaper unnamed route the flow node's clause simply failed to name — i.e., the clause, not the discharge idea, is what fails. I flag it for the owner rather than absorb it.",
    "Verification scope: I verified every quoted passage I rely on against the working tree at HEAD d4a385a (e76953a, the protocol freeze, is a confirmed ancestor and the protocol file is unchanged); but I took the certification document's §3 as authoritative for the eight-demotion list rather than re-running git diff d5e9a99..67059a8 -- provenance/claims.json (the document the protocol itself cites names that diff as the authority), and I did not independently re-derive the numerics inside the static-patch calc, the 12-decimal D3a check, or the ISW/DESI significances — I rely on them as banked.",
    "The homogeneity-pricing asymmetry cuts slightly against my rhetorical frame in one respect: since the corpus already carries flat-FRW spatial structure as unpriced prose (S6, zero register occurrences), a rival could argue the corpus's existing practice tolerates prose-carried background structure, so a prose-carried static-patch re-declaration is no worse than the status quo. My answer — that the gate exists precisely because the flow node ENDED that tolerance for the temporal half, and the discharge route cannot be executed by reinstating the tolerated defect one level up — is strong on the program's own discipline ethic, but it is an appeal to the program's norms, not to a frozen rule that forbids prose declarations."
   ],
   "verdict_argued": "DISCHARGE-UNAVAILABLE — with a coexisting NEW-OMISSION-SURFACED finding handed to the owner (the protocol expressly allows coexistence). The named static-patch re-declaration is not a retirement-at-zero; it is a priced re-foundation that (i) swaps the corpus's exact joint (ω,k) flat-slicing label for a static-Killing (ω,l,m) label on a single observer's r<1/H chart — the two labels being incompatible by the corpus's own derived results (D2/D3b/D4; the static Killing flow does not commute with the flat-slicing translations that define k) — thereby demoting every banked exact kernel statement to an H≪ω effective statement; (ii) is idle exactly where it is safe (the H≪ω-fenced Tier-4/GW/tabletop sector, already licensed by the WKB substitute without any Killing vector) and undelivered exactly where the +1 does work (rung3's ω→0 transport limit, kk_static_transfer's full-axis dispersion integral, rung7's ω~H(z) comoving-slice w(z), mu_linear's k~H0 kill legs); (iii) collides with two frozen instruments (gw_tensor_friction.py's no-static-Killing clock contract; the A3 declarations' η→−∞ state and static-patch-state prohibition) and has no register field to be declared in (the documented foliation/clock vocabulary gap); and (iv) costs, under the R1 fence, a minimum of two and realistically three new priced inputs (the chart declaration itself; the D6 clock-transport bridge the corpus already books as 'the assumption ... currently unpaid'; the S6 frame conversion for the operator-basis/passivity/no-pin chain) against the single +1 it retires — net ledger strictly worse, with nonzero banked-result loss, failing both prongs of the protocol's availability bar. This is the corpus's own sealed prediction (PRIMITIVE_INVERSION_SCOPE.md §5, K2/K3) confirmed with its mechanism made precise: the obstruction is the label collision between Killing stationarity and comoving homogeneity — at H you may have a global frequency or a global k, never both exactly — and that precision is the physics finding this gate exists to record."
  },
  "DISCHARGE-AVAILABLE-NET-FAVORA": {
   "verdict_argued": "DISCHARGE-AVAILABLE-NET-FAVORABLE — via the corpus-grounded scoped-flow re-declaration (the H≪ω + along-worldline form), NOT via a global static-patch background re-declaration. The flow node retires at zero under its own clause's mechanism (flow = PROPERTY of the background, not a commitment), the property being supplied by two things the corpus itself derived on the ALREADY-declared flat-FRW background: exact along-worldline stationarity (keystone D3a, verified to 12 decimals) and controlled H≪ω joint (ω,k) stationarity (the WKB license the owner recorded VALID 2026-08-18 at rung3.boundary_condition, with the corrections computed and hard-gated in the banked Tier-4 kernel as eps_H=(104/9)H²/ω²). Net +16 → +15, strictly improving; zero banked-result loss, demonstrated per-result below; the eight demotions mechanically reverse per the P1A ruling's own recorded route, ledger-neutrally.",
   "availability_analysis": "TWO ROUTES MUST BE DISTINGUISHED; the gate question licenses both (\"dS static patch, or a corpus-grounded alternative such as declaring the flow at the H≪ω scope every banked kernel result already occupies\"), and the corpus forces the choice between them.\n\nROUTE R-GLOBAL (re-declare the background AS the dS static patch): UNAVAILABLE, and the corpus itself proves it. (1) Label geometry: keystone D3b (derived, screened) — \"the FULL two-point object Σ(x;x′) does not reduce to a function of a time difference in any single global clock; only the static patch offers a global reduction\" — but that reduction is in (T, static-multipole-l), while every banked kernel is written in flat-slicing (ω,k) (S_IF.md §3 \"at fixed (ω, k) in the enumerated frame\"); the static-patch Killing vector does not commute with flat-slicing spatial translations, so R-global would re-home the label of the entire kernel corpus — exactly the protocol's named failure (\"a discharge that 'succeeds' by silently re-scoping banked observables is a failure reported as one\"). (2) Frozen-contract collisions: PHYSICS_LEDGER/WALL_A_A3_DECLARATIONS.md line 156 PROHIBITS the thermal static-patch state as a primary-run input; calc/gw_tensor_friction.py's SPEC-frozen clock declaration (\"single FRW COSMIC clock throughout... no static-Killing or e-fold quantity is imported\", keystone row C5). (3) Coverage: the chart covers r<1/H of one worldline; w(z) \"is not an axis quantity\" (keystone §6.3), and D2 shows off-axis identification fails. (4) Track record: the program's only static-patch computation (calc/static_patch_tt_response.py) carries a PART-3b retraction and the owner's \"NULL, NOT ADVERSE\" scope correction — no banked result lives there to receive a re-declaration.\n\nROUTE R-SCOPE (declare the flow at the scope the banked corpus already occupies): AVAILABLE, and every ingredient is already in the corpus's own texts, which is the protocol's stated admissibility condition (\"alternatives may be proposed only from the corpus's own texts\"). The discharge clause's operative mechanism is \"the flow is a PROPERTY OF THE BACKGROUND rather than a commitment, and this node retires into that declaration at zero.\" The clause (written 2026-08-18) named the static patch because at that date the corpus knew only one way to make the flow a property: an exact global Killing vector. Three days later the keystone screen DERIVED two weaker properties of the actually-declared background that are exactly strong enough: (a) D3a — along any comoving geodesic the BD correlator IS stationary in cosmic proper time (z = H⁻²cosh(HΔt), verified numerically to 12 decimals; \"a one-time Fourier transform exists w.r.t. t ON THE WORLDLINE\", temperature T_dS=H/2π \"derived from the embedding rather than cited\"); (b) the H≪ω quasi-stationary/WKB license, which the owner ordered recorded as \"VALID FOR H << omega\" (rung3_single_pole.boundary_condition, 2026-08-18) and which the banked Tier-4 kernel implements as physics, not prose: statement text \"Validity: omega >> H with eps_H = (104/9) H^2/omega^2; omega << H refused by the evaluator\", code-enforced three-band gate with DomainRejected at eps_H≥1 (PHYSICS_LEDGER/wall_kr_tier4_retarded.py, T4-4 hard-wired). Meanwhile the k half of the joint label needs no license at all: flat-FRW spatial translations are EXACT isometries of the declared background — a point that inverts the physics crux, because R-scope keeps k exact where R-global would break it. The static patch enters R-scope only as derived local geometry (D1: T=t on the axis, exact; D2: divergence off-axis quantified), never as a state input or clock transplant, so A3 and the gw_tensor_friction SPEC are untouched.\n\nTHE CONCRETE DECLARATION TEXT (inert package for owner ratification, per protocol):\n(1) Register act, exactly the response_lorentz_covariance retirement pattern (assumed→shown, +1→0, \"per this node's own discharge clause\", owner go — commit trail in STATE.md gloss and the RLC ledger_note): background_time_translation_flow — tier assumed → shown; ledger_delta 1 → 0; dated discharge note: \"DISCHARGED [date] per this node's clause at its PROPERTY-OF-THE-BACKGROUND mechanism, executed at the scope the corpus derived rather than at the named static-patch candidate (which breaks banked frames — STATIC_PATCH_DISCHARGE_GATE_01): the declared flat-FRW background carries (a) EXACT stationarity along every comoving worldline (keystone D3a, screened 2026-08-21, banked by this ratification) and (b) APPROXIMATE joint (ω,k) stationarity at H≪ω with corrections computed where used (kr_contract_retarded_tier4 eps_H, hard-gated). K_R(ω,k) is thereby definable as a PROPERTY at that scope — k exactly (flat-FRW spatial isometries), ω at H≪ω or along-worldline. NO single-ω object is declared at ω≲H; every consumer there carries its own recorded fence (rung3 WKB closure; T4 DomainRejected; kk_static_transfer obstruction; rung7's time-domain construction). The commitment this node priced — an exact GLOBAL flow, the thing FRW perturbation theory and the secular-dS programs deny — is no longer claimed at any point of consumption; what remains in use is derived (D3a) or standard-physics-controlled (WKB at H≪ω, owner-recorded VALID 2026-08-18). Priced property → demonstrated-at-scope property; +1 retires; net +16 → +15. The D6 transport residue at ω~H (keystone: 'the transport itself is the assumption, and it is currently unpaid') is carried forward VERBATIM, confined to the fenced regime.\"\n(2) S_IF.md (ledger 0, construction document): §1 background line gains the scope sentence; §3's \"at fixed (ω, k) in the enumerated frame\" gains \"(quasi-stationary scope, §1)\"; §6 item 1's \"at any frequency\" gains \"(within the declared scope; the ω≲H tail is fenced — kk_static_transfer)\". This is also the vocabulary act keystone §6.2 says the discharge FORCES (\"the flow must be named to be discharged\") and it pays C3's standing debt (\"legitimate for worldline kernels; must still name that scope\").\n(3) Demotion reversals: the eight (rung1_inin_formalism, rung2_kms_gate, rung4_love_kk, kr_contract_retarded_tier4, passivity_channel_diagonal, u1_form_universality, rung7_w1_wz_map, info_i1_renorm_as_information — authority: git diff d5e9a99..67059a8, corrected comment in provenance/expected_red.py:86-90) return derived-pending → shown, per the P1A ruling's own text (\"a named discharge route that retires it at zero and mechanically reverses the demotions\"; §6: \"the static-patch discharge mechanically reverses the demotions as a register change requiring its own re-certification\") — re-certification = re-running P1A §4's fixed-point scan, which lands green with the flow node at shown/0. SEVERABILITY: the owner may ratify (1)+(2) and hold rung1_inin_formalism's re-landing pending the P1B legislation on their same desk (P1B_RATIFICATION_PACKAGE_01), since the package's net improvement is carried entirely by (1) — every tier move is delta-0.\n\nRATIFICATION MECHANICS ARE ALREADY OWNER-SIDE LAW: the register anticipated this exact event in three places — the P1A stanza's GOES-MOOT clause names \"the static-patch migration discharges background_time_translation_flow\" with the mechanics spelled out; P1A ruled the route; P1B's own cost list certifies \"a static-patch discharge re-promoting rung1 would legally re-land at shown\" under current booked law. OPEN_PASSES.txt line ~182 even pre-declares the two known instrument false-positives the migration's first register commit will meet, \"so that a blocked edit is not diagnosed as a migration problem.\" The corpus has been holding the door open for this discharge; the only thing it lacked was the availability-and-cost analysis this gate exists to produce.",
   "cost_accounting": {
    "retires": "background_time_translation_flow's +1 (assumed → shown at delta 0, in-place, the response_lorentz_covariance pattern: that node retired +1 with 'net +17 -> +16' on 2026-08-30, owner go, 'The priced RESPONSE property is now DEMONSTRATED unimposed, not assumed' — verbatim ledger_note). Here the demonstrated-at-scope property replaces the priced commitment identically. The eight tier reversals ride along at ledger_delta 0 each (rung1_inin_formalism +4, rung2_kms_gate -1, and six at 0 — deltas UNCHANGED by tier moves, per the register's own convention that the demotion commit 67059a8 changed 'tier field only, non-tier fields byte-equal', P1A §4), so the promotions carry ZERO net weight: the bias-suspect half of the package is ledger-inert by construction.",
    "new_inputs": [
     "N1 — WKB/adiabatic validity of the single-ω label at H≪ω with O(H²/ω²) control. R1 fence ('an input is something a named position denies'): FAILS AS INPUT — nothing to price. The flow node's own tier_note names the denying positions for the UNSCOPED flow: 'Cosmological perturbation theory on FRW and the secular/non-stationary de Sitter programs work on a background with NO timelike Killing vector.' Both of those named positions themselves USE sub-horizon WKB frequency labels (that is how FRW perturbation theory treats every mode inside the horizon); they deny the global flow, not the scoped license. In-corpus standing: owner-ordered record 'VALID FOR H << omega' (rung3.boundary_condition 2026-08-18); banked as physics in kr_contract_retarded_tier4's statement and hard-gated code (T4-4). Price 0.",
     "N2 — D3a exact along-worldline stationarity. Not an input at all: DERIVED in-corpus from the hyperboloid embedding, verified to 12 decimals, hostile-screened 2026-08-21 (record: provenance/SCREEN_RECORD_2026-08-21_keystone_map.md, verdict AMBER — amended; 'banking remains the owner's'). Its register entry rides this package's ratification (the CHARTER 5.3 relay the keystone file itself demands). Price 0. NOTE: the package is robust even if the owner declines to bank D3a — every BANKED consumer is covered by N1 alone (all banked verdicts sit at H≪ω; see scope table), D3a only upgrades the worldline sector from controlled to exact.",
     "N3 — exact spatial translations on flat FRW (the k label). Standard geometry of the ALREADY-declared background (S_IF §1); denied by no named position. Price 0.",
     "N4 — the refusal: no single-ω object declared at ω≲H. A fence, not a claim; it forfeits territory rather than asserting anything, so the R1 test is vacuous. Already enforced in banked code (DomainRejected) and banked prose (WKB closure; kk_static_transfer's 'inherits NO sign'). Price 0."
    ],
    "net": "GRUT-scope net +16 → +15, STRICT improvement (verified: sum of ledger_delta over claims.json = +16 GRUT / 0 vacuum-cluster today; the flow node's ledger_note records its own booking as 'Net +14 -> +15', later +16 at the 2026-08-24/30 RLC cycle and Tier-4 delta-0 bank). Zero new inputs survive the R1 fence. Zero banked-result loss (table below). The one genuine cost is non-ledger: claimed-but-unused declaration territory at ω≲H is forfeited until separately licensed — the make-the-implicit-explicit trade the program's own ethic demands, and the corpus already behaves as if it were in force (T4 refuses; rung3's closure records; kk_static_transfer answered). Currently-unpriced presuppositions SURFACED for the owner's OMISSION_STANDARD_v2 queue, per protocol, without moving this verdict: (i) the spatial S6 side (homogeneity/isotropy/flatness) is prose-carried in GRUT scope ('S6' occurs 0 times in claims.json — verified; priced only inside the sealed vacuum-cluster scope, which 'cannot bleed'); (ii) D6's ω~H coth-transport, ALREADY recorded in-corpus as 'currently unpaid' — the declaration confines it to the fenced regime and bounds it by eps_H within scope, improving but not curing it."
   },
   "scope_survival": [
    {
     "result": "kr_contract_retarded_tier4 (the Tier-4 kernel)",
     "fate": "SURVIVES VERBATIM; tier restored shown per P1A route. Its banked statement already carries the declared scope on its own face — the re-declaration is a no-op for it.",
     "evidence": "Node statement: 'Validity: omega >> H with eps_H = (104/9) H^2/omega^2; omega << H refused by the evaluator'; wall_kr_tier4_retarded.py T4-4 three-band hard gate ('eps_H >= 1 : REFUSED... extrapolation into omega <~ H is forbidden, and the refusal is itself a gated control'); banked at delta 0."
    },
    {
     "result": "calc/gw_tensor_friction.py (Γ_T closure, REFUSE verdict) and rung8 tabletop stakes",
     "fate": "SURVIVE VERBATIM. The frozen FRW-cosmic-clock declaration is untouched because no static-patch clock is declared anywhere; the scope factor is the file's own validity term.",
     "evidence": "Header verbatim: 'single FRW COSMIC clock throughout... no static-Killing or e-fold quantity is imported'; 'the flat-contract scope is carried via the [1 + (104/9)(H0/w)^2] validity term the kernel itself supplies'; hcorr prints 1+hcorr == 1.0 in double at LIGO band (H0/ω ~ 1e-20). rung8: T_dS ~ 3e-30 K 'exp(-10^21)-negligible at lab frequencies' (rung2 ledger_note)."
    },
    {
     "result": "rung4_love_kk / calc/gw_dissipation_bounds.py",
     "fate": "SURVIVES; tier restored. Banked content = worldline-EFT tidal-response structure (D3a's exact scope) + KK/retarded analyticity (causal support is background-free) + LIGO-band verdicts at H/ω ~ 1e-20. The ω→0 tail was already fenced out of its one strong step in 2026-08-20, before this gate.",
     "evidence": "Node statement: 'recovers worldline-EFT tidal-response structure'; calc/gw_dissipation_bounds.py:49-50: 'SCOPE FENCE ADDED 2026-08-20. The |Re| ~ |Im| step is true FOR THIS POWER-LAW BRANCH and is FALSE for a far-IR Lorentzian tail'; verdict 21-62 orders below sensitivity, in-band."
    },
    {
     "result": "rung2_kms_gate / gate/kms.py",
     "fate": "SURVIVES; tier restored; and the package PAYS its recorded scope debt. The gate is an ω-grid instrument; its banked equilibrium uses are lab-band (rung8) where scope holds trivially. The T=T_dS attribution rides the PAID rung5 Unruh import plus D6's derived attribution; C3's demand — 'legitimate for worldline kernels; must still name that scope' — is exactly what the declaration does.",
     "evidence": "rung2 ledger_note: horizon-temperature relation 'already paid as rung5's Unruh-T import (borrowed node entropy_area_unruh)' — node verified present; keystone C3 row (D3a: 'along a comoving worldline T_dS IS the cosmic-clock temperature'); D6 residue carried forward verbatim in the discharge note."
    },
    {
     "result": "rung3_single_pole (anchor conjecture) and its reserved tier question",
     "fate": "UNTOUCHED BY CONSTRUCTION (protocol hard fence). Nothing moves: its claim always lived OUTSIDE the flow license (the corpus closed the WKB escape at rung3 itself), so retiring the license changes its epistemic position not at all; the declaration merely writes at register level what its own boundary_condition already records.",
     "evidence": "rung3.boundary_condition verbatim: 'the WKB reading is valid in the regime the single-pole conjecture does not live in, and invalid in the one it does. NOT fatal to the framework; fatal to the soft reading'; owner scope correction: 'A POLE-FREE FREE RESPONSE IS NULL, NOT ADVERSE... Everything that bears on this node rests on the interacting self-energy Sigma.'"
    },
    {
     "result": "mu_linear (no-go export: μ=4/3 endpoint exclusion + conditional μ=Σ=η=1 leg) and the KC5 spatial-SVT frame",
     "fate": "SURVIVES VERBATIM. Not in the flow cone at all (depends_on: p_tt_ansatz, rung9a_value — verified). The exclusion legs are (a) separate-universe consistency (non-spectral) and (b) data vs CONSTANT-coefficient modified-Poisson hypotheses on the FRW clock — constant coefficients are instantaneous/contact objects needing no stationarity; the corpus's own clock audit already ruled these 'fine internally'. The GRUT-kernel-to-quasi-static bridge was ALREADY the open kk_static_transfer question before this gate and is unchanged. The KC5 frame is spatial; spatial isometries stay exact.",
     "evidence": "claims.json mu_linear depends_on = ['p_tt_ansatz','rung9a_value']; keystone row C5: 'single FRW cosmic clock throughout | identity | sub-horizon quasi-static | derived | fine internally'; kk_static_transfer statement: 'UNTIL THIS IS COMPUTED, the quasi-static x inherits NO sign from the passivity floor.'"
    },
    {
     "result": "zeta_interior_family (constant-x window x < ~0.59)",
     "fate": "SURVIVES VERBATIM: a constant-x kernel is frequency-flat (instantaneous in time domain), so the flow question is degenerate for the entire cut; the window is a data bound on that family.",
     "evidence": "S_IF §5: 'the constant-x interior family is a ONE-DIMENSIONAL CUT through a function space'; window '(DESI Sigma0 lensing, central-inputs loose-upper, F-MAP fence)'."
    },
    {
     "result": "passivity_channel_diagonal + x_no_pin_theorem (the floor and the no-pin)",
     "fate": "SURVIVE; passivity tier restored. The lemma is banked as FRAME-FREE projector algebra ('for ANY kernel decomposed on mutually orthogonal idempotent projectors') — mathematics with no background claim; it survives untouched. Its applied quantifier in S_IF §6.1 ('at any frequency') gains the scope tag — a declaration-level trim with zero banked consumers outside scope, because the only banked would-be consumer of the ω→0 end (the static transfer) was ALREADY answered REFUTED-unconditionally / chi_inf-conditional. The no-pin/no-ceiling results are negatives; narrowing scope cannot un-prove them.",
     "evidence": "passivity node sub_status: 'shown as frame-free mathematics: PSD factorizes over orthogonal idempotents'; kk_static_transfer boundary_condition: 'the whole transfer question IS the sign of chi_inf, a reactive contact datum that passivity, causality, and the KMS lock are all structurally BLIND to.'"
    },
    {
     "result": "kk_static_transfer (static-transfer question, ANSWERED at (ii)+(iii))",
     "fate": "SURVIVES VERBATIM; the declaration strengthens its provenance (the ω≲H segment of its dispersion integrand is now explicitly outside declared kernel scope — same landing, 'quasi-static x inherits NO sign', better paperwork). A negative/obstruction result cannot be lost by a fence that agrees with it.",
     "evidence": "sub_status: 'ANSWERED 2026-08-09... at outcomes (b) AND (c) jointly'; decomposition identity chi(0)=chi_inf+(2/π)∫dω Im chi/ω machine-verified."
    },
    {
     "result": "rung7_wz (w=-1 flat sourced prediction; +3 inserted for the evolving piece) and rung7_w1_wz_map",
     "fate": "SURVIVE VERBATIM (rung7_wz is to-derive +3, untouched; w1 map restored shown). The SOURCED prediction is a time-domain statement (a UV-cutoff single-pole vacuum relaxor gives w=-1 flat on the FRW clock) — no single-ω kernel is evaluated at ω~H in any banked statement; the w1 map is the standard borrowed EFT-of-DE map. The keystone §6.3 cosmic-vs-static bridge question is NOT worsened: the declaration declares NO static-clock object, so no new transport arises; w(z) work stays homed in the time domain where the corpus already homes it.",
     "evidence": "rung7_wz statement: 'The SOURCED prediction is w = -1 flat'; calc/wz_dark_energy.py time-domain relaxor on E(z), OM=0.31 flat; rung7_w1 statement: 'standard effective-field-theory-of-dark-energy (Gubitosi-Piazza-Vernizzi)'."
    },
    {
     "result": "rung7_w2_wa_sign / rung7_w3_nocrossing_export (the phantom-crossing no-go direction)",
     "fate": "UNCHANGED — both are to-derive, delta 0, default-BROKEN (NOT banked results; verified in claims.json). Their eventual graduation gains one named ingredient: either a time-domain/second-law derivation (Π=-3ζH≤0 needs no ω label) or an explicit ω~H license. Honest sharpening of an open question; no banked loss possible where nothing is banked.",
     "evidence": "rung7_w2_wa_sign tier: to-derive, 'SCREENED-PARTIAL (2026-06-29)' only; rung7_w3 'contingent on W2's no-crossing'."
    },
    {
     "result": "u1_form_universality + info_i1_renorm_as_information",
     "fate": "SURVIVE; tiers restored. Generic/borrowed motifs whose content (Feynman-Vernon/Caldeira-Leggett FORM; RG-as-information) exists in the time domain on general backgrounds.",
     "evidence": "u1 statement: 'ANY local, causal open quantum system yields it under coarse-graining'; info_i1 'GENERIC -- already in GRUT, NOT uniquely GRUT'."
    },
    {
     "result": "vacuum cluster (21 vc_* nodes) + response_lorentz_covariance + eft_operator_basis/p_tt_ansatz + rung9a_value",
     "fate": "ALL UNTOUCHED. The cluster is sealed at its own scope with no kernels ('the two vocabularies cannot bleed' — verified in VACUUM_CLUSTER_MAP.md); RLC is the retirement PRECEDENT, already shown/0; the enumeration and ansatz are to-derive/assumed question-and-input nodes over formal (ω,k²) structures in the spatial S6 frame, which stays exact; rung9a's ω→0 mention is a recorded referee annotation about an OPEN PROBLEM, not a banked evaluation (verified in-node).",
     "evidence": "VACUUM_CLUSTER_MAP.md line 5; response_lorentz_covariance ledger_note ('RETIRED 2026-08-30 per this node's own discharge clause'); RESULT-tier low-frequency sweep over all 74 claims returned only the nodes handled above."
    },
    {
     "result": "calc/static_patch_tt_response.py (the existing static-patch computation)",
     "fate": "UNCHANGED, and gains a clean home: it remains the in-corpus demonstration that the static patch exists as derived LOCAL geometry (the chart the declaration cites via D1/D2) and that its free response is null for rung3 purposes. Nothing banked lives on it, so nothing needs re-homing — which is precisely why R-scope beats R-global.",
     "evidence": "File header: PART 1b c=0 DERIVED (axial); PART 3b retraction ('B never vanishes, so these are not quasinormal modes'); rung3 owner correction 'NULL, NOT ADVERSE'."
    }
   ],
   "case_points": [
    {
     "point": "Every banked consumer of the single-ω license already sits inside the proposed scope or already refuses outside it — so the priced commitment has ZERO banked consumers at the only regime where it exceeds standard physics, and a priced input with no consuming object retires under the register's own practice.",
     "evidence": "Tier-4: ω≫H on its banked face + DomainRejected in code; GW files: LIGO band, H0/ω~1e-20, far-IR fenced 2026-08-20; rung8: lab band; constant-x window: frequency-flat kernels; w=-1 flat: time-domain; vacuum cluster: no kernels; the ω≲H would-be consumers each carry their own recorded refusal (rung3 WKB closure 'fatal to the soft reading'; kk_static_transfer 'inherits NO sign'; T4 'no omega << H'). Precedent: response_lorentz_covariance retired +1→0 on demonstration, owner go 2026-08-30."
    },
    {
     "point": "The residual license is standard physics the corpus itself records as valid, in owner-ordered text — not a new argument this gate invents.",
     "evidence": "rung3_single_pole.boundary_condition (owner-ordered, 2026-08-18): 'the quasi-stationary/WKB defense of writing this framework's kernel at fixed (omega,k) on a non-stationary FRW background is VALID FOR H << omega'; the same record confines the failure to ω→0 ('nowhere else does the approximation matter'). The Tier-4 bank then carried the license through an adversarial pre-screen (T4-BANK-A, b4a2aa2) as a banked validity domain."
    },
    {
     "point": "The flow node's own R1 membership fence, re-run at scope, dissolves: the named denying positions (FRW cosmological perturbation theory; secular/non-stationary dS programs) deny the GLOBAL flow and themselves USE the scoped license — so the discharge replaces the +1 with literally nothing priceable, defeating the K3 'relabel' pattern by the register's own pricing rule.",
     "evidence": "Flow node tier_note verbatim: 'R1 MEMBERSHIP FENCE: passes -- named positions deny it. Cosmological perturbation theory on FRW and the secular/non-stationary de Sitter programs work on a background with NO timelike Killing vector.' Sub-horizon WKB mode treatment is constitutive of the first named position; neither denies adiabatic frequency labels at H≪ω."
    },
    {
     "point": "The clause's mechanism is satisfied on the ALREADY-declared background, so no background re-declaration occurs and no banked frame can break — the entire R-global collision list (A3 prohibition, gw clock SPEC, KC5 frame, w(z) chart, D3b label geometry) is mooted by construction.",
     "evidence": "D3a (exact worldline stationarity, 12 decimals, derived from the embedding) and eps_H-controlled H≪ω stationarity are PROPERTIES of flat FRW; S_IF §1's 'flat FRW for every banked observable' stands unedited except for gaining the scope sentence; spatial translations remain exact isometries so the k label is untouched."
    },
    {
     "point": "The mechanical reversal of the eight demotions is pre-recorded owner-side law, not this advocate's construction — and it is ledger-neutral, so the package's net improvement is carried entirely by the retirement.",
     "evidence": "P1A_EDGE_ADJUDICATION_01.md: 'the flow is a named open input with a named discharge route that retires it at zero and mechanically reverses the demotions'; §6: 'the static-patch discharge mechanically reverses the demotions as a register change requiring its own re-certification'; P1B: 'a static-patch discharge re-promoting rung1 would legally re-land at shown'; OPEN_PASSES P1A stanza GOES-MOOT clause; all eight demotion deltas unchanged at 67059a8 ('tier field only, non-tier fields byte-equal')."
    },
    {
     "point": "The declaration pays two standing recorded debts at zero cost: C3's 'must still name that scope' for the T_dS/coth worldline use, and keystone §6.2's missing clock vocabulary ('the flow must be named to be discharged') — the discharge is the naming act the corpus's own audit demanded.",
     "evidence": "Keystone C3 row and §6.2 verbatim; the register's clock-vocabulary gap ('no register field mentions foliation, cosmic time, or proper time — which is how the C1/C2 mismatch passed two pre-registrations') is repaired by the discharge note's explicit clock/scope text."
    },
    {
     "point": "Net strictly improves: +16 → +15, by the same arithmetic and the same clause-execution pattern the register has already run once with owner ratification.",
     "evidence": "Verified sum of ledger_delta = +16 (GRUT scope) at HEAD; flow node ledger_note 'Net +14 -> +15' at booking; STATE.md gloss: 'net +16 is the blind SUM of the register's underived-input ledger... updated on the 2026-08 response_lorentz_covariance retirement (+17 → +16, assumed→shown per the node's own retire clause).'"
    },
    {
     "point": "The package is concrete and severable: declaration text supplied; what retires named; ripples enumerated (three S_IF quantifier tags, eight tier reversals with re-certification, two owner-relay riders for the pre-existing S6 and D6 residues); the owner can ratify the retirement alone and stage the promotions behind their P1B decision without touching the net.",
     "evidence": "Package text in the availability analysis; P1B_RATIFICATION_PACKAGE_01.md already on the owner's desk; every tier move delta-0."
    }
   ],
   "rebuttals_of_rivals": [
    {
     "rival": "DISCHARGE-UNAVAILABLE (status-quo-favorable): 'the clause names a timelike-KV background; flat FRW has none; the named candidate breaks banked frames; K2/K3 and the sealed standing prediction kill the route; the H≪ω alternative is a re-scope, and re-scoping banked observables is the protocol's named failure.'",
     "rebuttal": "Three answers. (1) K2/K3 are the pre-registered kill conditions of a DIFFERENT question: PRIMITIVE_INVERSION_SCOPE.md §6 opens 'The question is REFUTED if...' — the question being the modular-primitive INVERSION (deriving the flow as geometric/fundamental via BW/Sewell or thermal-time). This gate's question is whether a priced commitment can retire into a declaration; the inversion's failure modes were sealed against a far more ambitious target, and the corpus itself distinguishes them (keystone §6.1: what the inversion would fix 'is unchanged and stays where §5 puts it' — a question this package neither answers nor needs). Even on their own terms: K2's premise ('cannot be connected') has been PARTIALLY overtaken by the corpus's own later derivations — D1 (T=t on axis, exact), D3a (worldline stationarity, exact), D2/D4 (divergence quantified at O(1/H)) — the connection now EXISTS at exactly the scope the package declares and fails exactly where the package refuses; the standing prediction is confirmed FOR the inversion and for R-global, both of which this package rejects. K3 (one-for-one relabel, net doesn't move) is defeated arithmetically: the replacement license fails the R1 fence as an input (nothing to price — the named deniers use it), so net moves −1, which is precisely what K3 says a relabel cannot do; and §4.3's named relabel example (Connes–Rovelli thermal-time) is an ASSUMPTION-for-assumption swap, structurally unlike swapping an assumption for a derived-plus-standard-physics property. (2) The clause's literal antecedent vs mechanism: the gate's frozen protocol itself invites 'alternatives... proposed only from the corpus's own texts', and D3a + the WKB record + eps_H ARE the corpus's own texts; the clause family was never single-route (its own second route — exhibit a K_R-free formulation — proves the mechanism, not the static patch, is primary). (3) 'Silently re-scoping banked observables': the protocol's word is SILENTLY, and the operative noun is OBSERVABLES. The package re-scopes NO banked observable (per-result table: every banked verdict already sits inside scope or outside the kernel formalism); the only trims are two quantifiers in a ledger-0 CONSTRUCTION document, reported loudly with the table. UNAVAILABLE must also carry its own unpaid cost: it leaves a +1 priced on a commitment with zero banked consumers in the regime where it exceeds standard physics — pricing without an object — against the register's own demonstrated-unimposed retirement practice, and against three owner-side texts that treat the discharge as live and legal (P1A ruling, P1B risk note, the GOES-MOOT stanza)."
    },
    {
     "rival": "NEW-OMISSION-SURFACED (instrument-favorable): 'the real finding is the unpriced flat-FRW/homogeneity presupposition (S6 prose-carried; priced only in the sealed vc scope) and D6's unpaid transport — book omissions, don't discharge.'",
     "rebuttal": "NEW-OMISSION is not actually a rival: the frozen protocol says it 'may coexist with either outcome above', and this package carries both findings to the owner as relay riders WITHOUT them touching the verdict. On the merits: (i) the S6 spatial gap is real, pre-existing, and ORTHOGONAL — it is the SPATIAL half of the background declaration, while this gate's node prices the TEMPORAL flow; the flow node's own R4-atomisation lesson (compound presuppositions factor, each half booked where it lives) says exactly this. It exists identically under every verdict; the discharge neither creates nor conceals it — the package's S_IF §1 edit touches the very sentence where the gap lives, making it MORE visible. Booking it is protocol-assigned to the OWNER under OMISSION_STANDARD_v2, 'not by this gate'. And one tell cuts against booking it as a pure omission: unlike the flow ('stationar' occurred ZERO times in S_IF — invisible), S6 is VISIBLE — declared in S_IF §2's inventory and named in eft_operator_basis's statement among 'REGISTER-DECLARED symmetries' — so whether it books as omission or as a split/pricing of the frame declaration is a genuine OMISSION_STANDARD_v2 adjudication, i.e., the owner's. (ii) D6's transport is by definition not a NEW omission: the corpus already recorded it in its own words ('the transport itself is the assumption, and it is currently unpaid'), and the package IMPROVES its standing — within the declared scope the transport error is bounded by the same eps_H the corpus computes, C3's scope-naming demand is met, and the residue is carried forward verbatim, confined to the fenced regime nothing banked occupies."
    },
    {
     "rival": "UNDERDETERMINED ('the record cannot decide; name the missing computation and stop') — and, within my own verdict family, the naive R-GLOBAL static-patch re-declaration.",
     "rebuttal": "UNDERDETERMINED fails because every load-bearing determination is already computed or ruled IN the record, not extrapolated by this gate: the scope occupancy of each banked result is on its banked face (Tier-4's validity string; the GW files' bands; the constant-x cut; the time-domain w=-1); the license's validity at scope is an owner-ordered record (WKB closure) plus banked code (T4-4); the worldline bridge is a screened numerical derivation (D3a, 12 decimals); the reversal mechanics are a ruled adjudication (P1A) plus a certified legality note (P1B); the retirement pattern has an executed precedent (RLC, owner go). No new computation is required to decide availability or cost — the one genuinely open computation in the neighborhood (the assembled-Σ reduction; rung3's Σ) is explicitly NOT consumed by the package and stays frontier-reserved. As for R-GLOBAL: this package REJECTS it on the corpus's own texts (A3's frozen PROHIBITED line; gw_tensor_friction's SPEC clock; D3b's (T,l)-vs-(ω,k) label geometry; w(z) off-chart; the static patch's in-corpus record of one derived potential, one retraction, one null) — the named candidate was the WRONG EXECUTION of the RIGHT mechanism, and conceding that is not conceding unavailability: the clause names the static patch as 'the named candidate', the mechanism as the operative content, and the gate protocol explicitly admits corpus-grounded alternatives."
    }
   ],
   "honest_weaknesses": [
    "CLAUSE-LITERALISM: the discharge clause's antecedent reads 'the declared background is one carrying a timelike Killing vector', and flat FRW carries none globally. The package discharges under the clause's stated MECHANISM (property-not-commitment, retire-at-zero) with the property supplied at scope by D3a + the WKB record — a reading the gate protocol invites but the clause's author did not write. A strict-constructionist owner could rule this an AMENDED clause rather than an executed one; the package is inert until ratification either way, but the discharge is then one ratification judgment, not pure clause mechanics.",
    "THE RUNG1 LANDING TIER walks into a named standing risk on the register's own books: re-promotion re-lands rung1_inin_formalism at shown with +4 prose-priced inputs — 'the exact configuration this pass questions' (P1B, LEGISLATION-REQUIRED, 2026-09-13), with CHARTER §4's premature-graduation clause 'in direct textual tension' over the still-open fourth input. The package's severability answer (ratify the retirement, stage rung1's re-landing behind the P1B decision) is honest but concedes that the full mechanical form of 'the eight reverse' is not clean today for one of the eight.",
    "SCOPE FORFEITURE IS REAL EVEN IF UNCONSUMED: S_IF §3/§6 nominally claimed all-ω structure; after the declaration, any future ω≲H kernel statement (rung7 W2/W3 graduation, horizon-scale kernel claims, the assembled-Σ reduction) must buy its license explicitly. Nothing banked evaluates there today — verified per-result — but a rival can fairly call this 'shrinkage marketed as hygiene', and the honest answer is yes: the package trades claimed-but-unused territory for a −1, and the trade is favorable only because the corpus never cashed that territory.",
    "BIAS-DIRECTION COINCIDENCE: DISCHARGE-AVAILABLE is the protocol-named tidiness- AND framework-favorable direction, and the eight re-promotions match the builder's documented over-unification pattern. The structural mitigations are real (net −1 carried entirely by the retirement; promotions ledger-neutral and severable; every load-bearing citation is owner-ruled or machine-checked text), but the verdict coincides with the comfortable direction and that coincidence should be weighed by the referee, not by me.",
    "D6 IS CONFINED, NOT CURED: the ω~H coth/temperature transport stays exactly as unpaid as the keystone records; the declaration bounds it within scope and fences the rest. If ratification prose ever reads as 'the KMS/temperature story is now clean', that is an over-read — the package text carries the D6 pointer forward verbatim precisely to prevent it. Likewise the exact-worldline leg (D3a) rests on a screened-but-unbanked derivation (keystone map: 'may not be cited as content... banking remains the owner's'); the package banks it via the same ratification, and if the owner declines, the worldline leg falls back to the H≪ω leg alone — which still covers every banked consumer, but the declaration loses its 'exact' sector and with it the C3 debt payment.",
    "VISIBILITY TRADE AT THE FOUNDATION: a priced +1 sitting at the register's root kept the 'what licenses your ω label' question permanently loud; a shown declaration is structurally quieter. The declaration compensates by refusing ω≲H in the same sentence and by leaving rung3's WKB closure untouched at rung3, but it remains true that the discharge converts a standing irritant into settled furniture — the exact move the program's directional-bias file warns tends to precede the next uncaught over-claim. The re-certification requirement (P1A §6) and the S6/D6 owner-relay riders are the package's counterweights, and they depend on the owner actually running them."
   ]
  }
 },
 "evidence_verifier": {
  "verified": [
   {
    "item": "HEAD/protocol integrity: brief cites e76953a; actual HEAD is d4a385a with e76953a an ancestor 3 commits back",
    "evidence": "git log; git diff e76953a..HEAD --stat touches only PUBLIC_NUMBERS.md, program/P1B_*, program/P6_RATIFICATION_PACKAGE_01.md, provenance/{OPEN_PASSES.txt,TIER_BASELINE.json,emit_public_numbers.py,test_tier_provenance.py,validate.py} — zero diff on the protocol file and on every evidence file cited by the surveyor (claims.json, S_IF.md, calc/*, PHYSICS_LEDGER/*, RUNG3_KEYSTONE_MAP.md, PRIMITIVE_INVERSION_SCOPE.md, VACUUM_CLUSTER_MAP.md, gate/kms.py)"
   },
   {
    "item": "Protocol premise: S_IF.md §1 flat-FRW declaration",
    "evidence": "S_IF.md §1 verbatim: 'flat FRW for every banked observable; the linear-cosmology frame is the fixed-slicing spatial-SVT frame of mu_linear — [eft_operator_basis, KC5 fence]'; header: 'CONSTRUCTION DOCUMENT, ledger 0'"
   },
   {
    "item": "Protocol premise: flow node and its discharge clause (row 1)",
    "evidence": "provenance/claims.json background_time_translation_flow: tier=assumed, ledger_delta=+1, depends_on=[]; overturning_computation verbatim incl. 'the flow is a PROPERTY OF THE BACKGROUND rather than a commitment, and this node retires into that declaration at zero' and the WKB note 'quasi-stationarity holds for H << omega and the single-pole claim lives at omega -> 0, exactly where it fails'; R1 fence sentence verbatim in tier_note; the separate rung3 tier question flagged-not-moved in ledger_note"
   },
   {
    "item": "R5 wiring and the eight-demotion cascade (flow_usage_map)",
    "evidence": "depends_on edges to the flow node exist on exactly rung1_inin_formalism, rung1_ontology_finite_memory (2026-08-23 split, 04dc7e1 POINTER in ledger_note), rung2_kms_gate; git diff d5e9a99..67059a8 (checked field-level via both snapshots) shows exactly 8 tier changes shown->derived-pending on the 8 nodes the surveyor lists; RAI_CAPABILITY_TEST_01_CERTIFICATION.md carries 'git diff d5e9a99..67059a8 -- provenance/claims.json is the authority' verbatim; P1A_EDGE_ADJUDICATION_01.md (ruled EDGE_CORRECT, 1ad2fb9): 'the static-patch discharge mechanically reverses the demotions as a register change requiring its own re-certification'"
   },
   {
    "item": "Row 2 kr_contract_retarded_tier4",
    "evidence": "Node: domain 'open-systems-EFT / de Sitter contract scope'; statement verbatim 'Validity: omega >> H with eps_H = (104/9) H^2/omega^2; omega << H refused by the evaluator'. PHYSICS_LEDGER/wall_kr_tier4_retarded.py:10-11 G0_R = 2 kappa^2/((omega+i0)^2 - k^2); :30-36 validity domain incl. 'REFUSED (raises DomainRejected -- extrapolation into omega <~ H is forbidden, and the refusal is itself a gated control)'; :40-41 'HARD STOP ... no omega << H'. WALL_A_A3_DECLARATIONS.md:132 'positive frequency w.r.t. the BD vacuum in the far past η → −∞'; :156 static-patch thermal state 'PROHIBITED as an input for the primary run'"
   },
   {
    "item": "Row 3 calc/gw_tensor_friction.py",
    "evidence": "Lines 6-7 clock declaration verbatim ('single FRW COSMIC clock throughout ... no static-Killing or e-fold quantity is imported'); line 29-30 flat-contract validity-term sentence verbatim incl. 'no external-background transplant occurs'; line 151 'printed directly: 1+hcorr == 1.0 in double'; F_BAND=[10,100,1024] Hz; ZREF=0.5; line 175 achromatic limit w*tau_2>>1; RESULTS_gw_tensor_friction.md: 'SPEC §5 OUTCOME: REFUSE'"
   },
   {
    "item": "Row 4 mu_linear",
    "evidence": "S_IF §1 quote verbatim; RUNG3_KEYSTONE_MAP.md line 121 row C5 verbatim ('single FRW cosmic clock throughout | identity | sub-horizon quasi-static | derived | fine internally'); calc/isw_exclusion.py header 'the low-ell temperature-galaxy CROSS-CORRELATION'; node statement carries ~2.0sigma cross (1.97 central) + DESI Sigma0 ~3.5sigma; KC5 fence verbatim in eft_operator_basis.boundary_condition incl. 'exhaustive at all derivative orders WITHIN the frame' and 'Riegert-Paneitz completion NOT covered -- that portion FRONTIER-RESERVED'"
   },
   {
    "item": "Row 5 rung2_kms_gate / gate/kms.py",
    "evidence": "gate/kms.py:8-11 G_K formula and G_A=conj(G_R); :44 'T<=0: KMS/detailed-balance undefined'; docstring 'must avoid omega=0'; no k anywhere in the 95-line file; depends_on includes background_time_translation_flow; ledger_note verbatim 'on the declared background the Hadamard/KMS condition forces T = T_dS = H/2pi uniquely (any other T is singular at the horizon...)'; keystone D6 verbatim 'the transport itself is the assumption, and it is currently unpaid'"
   },
   {
    "item": "Row 6 rung3_single_pole",
    "evidence": "boundary_condition WKB CLOSURE 2026-08-18 verbatim ('valid in the regime the single-pole conjecture does not live in, and invalid in the one it does'; eta = lim_{omega->0} Im G_R(omega)/omega); tier_note tower text ('the whole infinite tower n >= l+1', 'IT NAMES A SINGLE RATE THAT DOES NOT EXIST AT FREE LEVEL') and owner scope correction verbatim ('A POLE-FREE FREE RESPONSE IS NULL, NOT ADVERSE ... Everything that bears on this node rests on the interacting self-energy Sigma'); Matsubara/static-patch campaign referenced in-node"
   },
   {
    "item": "Row 7 rung4_love_kk / calc/gw_dissipation_bounds.py",
    "evidence": "F_BAND=[10,100,1024] Hz; D_GW170817=40 Mpc, D_BBH=400 Mpc; lines 49-51 scope fence verbatim incl. 'Im/Re = omega*tau -> 6.3e20 at 100 Hz for tau_2 ~ 1/H0'; KK integral over all omega at line 70; tier_note '~10^21-22 (q=1) ... orders BELOW LIGO phase sensitivity', 'GRUT sits 21-62 orders below', 'Outcome (B) signed off -- CLOSED'"
   },
   {
    "item": "Row 8 rung7_wz / calc/wz_dark_energy.py",
    "evidence": "Lines 23-24 'the cosmological expansion forces the vacuum at frequency ~ H(z)'; line 29 'OM = 0.31 # matter density; OL = 1-OM (flat)'; w(z)=-1+eps*R(H(z)tau) relaxor; node statement 'SOURCED prediction is w = -1 flat' and tau_2~1/H0 = '+2 of the +3 total'; RUNG3_KEYSTONE_MAP §6 item 3 verbatim incl. 'w(z) is not an axis quantity'; D2 verbatim 'w(z), a quantity of the cosmological background averaged over a comoving slice, is not an origin-worldline quantity'"
   },
   {
    "item": "Row 9 calc/static_patch_tt_response.py",
    "evidence": "PART 0 computes R_mn=3H^2 g_mn on g=diag(-f,1/f,r^2,r^2 sin^2 th), f=1-H^2 r^2; header (A) 'PURELY IMAGINARY, DISCRETE'; (D) 'Gap and spacing are both O(H); their ratio is O(1) and contains no small quantity'; (A-RETRACTED)/PART 3b verbatim 'B = -A for EVERY omega. B never vanishes, so these are not quasinormal modes and nothing in the c = 0 family is' + 'every downstream part is now marked conditional'; graviton family restricted to l>=2 (lines 600-601); free-level-null owner text in-file; claims.json search: only rung3_single_pole and the flow node mention static patch/Killing — no node banks a static-patch background declaration"
   },
   {
    "item": "Row 10 kk_static_transfer",
    "evidence": "Node statement verbatim incl. the KK candidate route Re c0(0)=(2/pi) int domega Im c0(omega)/omega, 'THE OBSTRUCTION ... SUBTRACTED dispersion relations', and 'UNTIL THIS IS COMPUTED, the quasi-static x inherits NO sign from the passivity floor'; tier derived-pending, delta 0"
   },
   {
    "item": "Rows 11-12 passivity/x_no_pin, eft_operator_basis + p_tt_ansatz",
    "evidence": "S_IF §3 passivity row and §6 'Anti-passive response in either channel, at any frequency' verbatim; p_tt_ansatz 'its banked home is the 6-dim SVT frame whose KC5 fence frontier-reserves the h_0mu/4d completion' verbatim; '(on the FRW frame mu_linear uses: 5 independent form factors, GRUT keeps 1, zeroes 4)' verbatim; eft_operator_basis statement verbatim ('the FRW homogeneity/isotropy frame' among 'REGISTER-DECLARED symmetries only'), tier to-derive delta 0; calc/operator_basis.py:17-19 S6 gloss verbatim incl. 'NOTED as inherited, not chosen'; p_tt interrogation 'VERDICT = CHOSEN, unanimous' at 'full Lorentz-covariant' generality"
   },
   {
    "item": "Rows 13-16 zeta_interior_family, vacuum cluster, response_lorentz_covariance, rung8",
    "evidence": "S_IF §5 'x < ~0.59 (DESI Sigma0 lensing, central-inputs loose-upper, F-MAP fence)' verbatim + node's own 'lensing-bound window x < ~0.59'; 21 vc_* nodes counted in claims.json vs VACUUM_CLUSTER_MAP.md '13 nodes ... BANKED under its own scope (2026-08-04)' and 'GRUT's ledger is untouched at +13 ... the two vocabularies cannot bleed' verbatim (surveyor's own 13-vs-21 reconciliation note is accurate); vc_flatness_in_reduction and vc_lorentz_invariant_vacuum statements verbatim, both ledger_scope vacuum-cluster; vc_rho_lambda scheme_tag 'base-LCDM, FLAT, w FIXED = -1' verbatim; response_lorentz_covariance statement verbatim ('NOT that the background is Lorentz invariant'), +1 owner-ruled 2026-08-24, 'RETIRED 2026-08-30 per this node's own discharge clause', 'DEMONSTRATED unimposed, not assumed' present; rung8 quote verbatim from rung2 ledger_note (T_dS ~ 3e-30 K, exp(-10^21))"
   },
   {
    "item": "Flow-usage map regime partition and joint-label geometry",
    "evidence": "H<<omega consumers verified (Tier-4 hard gate raising DomainRejected; gw_tensor_friction LIGO band with hcorr below double precision; rung4 LIGO band; rung8 sub-kHz-and-up lab stakes); omega->0 / omega~H consumers verified (rung3 eta=lim_{omega->0} + WKB closure; kk_static_transfer dispersion integral to omega->0; rung7 x=H(z)tau with tau_2~1/H0 so H/omega~O(1); mu_linear low-ell ISW leg); D3a ('IS stationary in cosmic proper time ... verified numerically to 12 decimals'), D3b ('only the static patch offers a global reduction'), D2, D4 all verbatim in RUNG3_KEYSTONE_MAP.md; PRIMITIVE_INVERSION_SCOPE.md §5 verbatim ('GRUT's kernel is written in cosmological slicing at (ω, k) on an FRW background. In flat slicing de Sitter has no global timelike Killing vector'; 'different flows'; 'Standing prediction: this is where it breaks'); K2 kill verbatim"
   },
   {
    "item": "PHYSICS CRUX (marked judgment): static-patch KV does not commute with flat-slicing spatial translations; joint K_R(omega,k) label is an H<<omega effective object",
    "evidence": "Standard dS geometry: in flat/planar coordinates the static-patch time-translation KV is xi = d_t - H x^i d_i, so [xi, d_i] = H d_i != 0 — no joint eigenbasis; the obstruction is O(H/omega), entering the isotropic kernel at O(H^2), exactly the corpus's eps_H=(104/9)H^2/omega^2 control structure. Corroborated in-corpus by D3b, PRIMITIVE_INVERSION_SCOPE §5, and the Tier-4 gate. The static patch covers r<1/H (f(r)=1-H^2r^2 in the calc's own metric) and supplies a (T, multipole-l) label, not the flat-slicing (omega,k) label — verified against the calc's radial master equation/multipole decomposition. JUDGMENT: the surveyor's regime assignments per row are physically correct"
   },
   {
    "item": "Surveyor surprises 1-8 substantially verified",
    "evidence": "Item 1: §5 standing prediction + K2 verbatim (K3 truncation flagged in failures). Item 2: D3a/D3b worldline third-route verbatim. Item 3: flat G0_R + BD eta->-inf + PROHIBITED-as-primary-input line all verified (scoped-prohibition nuance flagged). Item 4: rung2 ledger_note + D6 verbatim. Item 5: clock declaration verbatim. Item 6: c=0 derived (PART 1b), PART 3b retraction, owner NULL ruling all verified; no node banks a static-patch background (claims.json search). Item 7: §6.2 vocabulary-gap quote verbatim. Item 8: components individually verified (see regime partition)"
   }
  ],
  "failures": [
   {
    "item": "Row 9 (static_patch_tt_response) evidence citation: '(B)/(C) the graviton potential c=0 derived (axial sector)'",
    "actual": "The graviton c=0 derivation is (C)/PART 1b alone ('Linearising R_mn = Lambda g_mn ... gives V = f(r) l(l+1)/r^2: c = 0 EXACTLY, and NOT the c = -2 massless-scalar member'); (B) is the c=-2 massless minimally coupled SCALAR potential. The substance (graviton c=0 derived, axial sector) is correct; the part-label conflates two distinct results.",
    "severity": "MINOR"
   },
   {
    "item": "Row 11 (passivity_channel_diagonal + x_no_pin_theorem): 'Both nodes sit in the eight-node demotion cone'",
    "actual": "passivity_channel_diagonal IS one of the eight demoted (shown->derived-pending in 67059a8). x_no_pin_theorem was NOT demoted — it was derived-pending both before and after 67059a8 (verified on both snapshots) — and therefore would NOT mechanically re-promote under the discharge; it sits downstream via depends_on [passivity_channel_diagonal, eft_operator_basis]. Matters for re-promotion arithmetic; the flow_usage_map's own eight-node list is correct.",
    "severity": "MINOR"
   },
   {
    "item": "Surprises item 1: K3 quoted as 'this outcome is the most likely one'",
    "actual": "PRIMITIVE_INVERSION_SCOPE.md reads 'this outcome is the *most likely* one after K2' — the ranking is relative to K2, which the truncation drops. Direction of the point (corpus pre-registered relabel/kill as expected outcomes) is unchanged.",
    "severity": "MINOR"
   },
   {
    "item": "Homogeneity finding (1): S1-S7 inventory 'lives only in S_IF.md §2 prose and the calc/operator_basis.py header'",
    "actual": "It also lives in calc/RESULTS_operator_basis.md (S1-S7 table, S6 row verbatim) and is referenced in provenance/prereg/RESULT_KAPPA_2026-08-08.txt ('not implied by S1–S6'). All additional homes are still prose/calc-layer with zero register pricing, so the material conclusion is unchanged (slightly strengthened: more prose homes, still no node).",
    "severity": "MINOR"
   },
   {
    "item": "Row 4 regime: ISW cross and DESI Sigma0 glossed jointly as 'i.e. k~H0'",
    "actual": "k~H0 is right for the low-ell ISW cross leg; the DESI Sigma0 lensing bound draws dominantly on sub-horizon lensing modes (the banked bound is a scale-independent amplitude), so the k~H0 gloss should not cover that leg. 'NOT a deep-subhorizon-only statement' survives via the ISW leg alone. (Physics judgment.)",
    "severity": "MINOR"
   },
   {
    "item": "Row 16 (rung8): 'H≪ω by ~39+ orders'",
    "actual": "rung8's staked cutoff is sub-kHz-scale (the '689 Hz parameter-free' claim was RETIRED into the staked omega_c), giving H0/omega ~ 1e-21, i.e. ~21 orders; ~33 orders if atomic-gap frequencies are meant. The quoted exp(-10^21) negligibility line is verbatim-correct and the direction (deeply H≪ω) is unaffected.",
    "severity": "MINOR"
   }
  ],
  "survey_row_corrections": [
   {
    "row": "calc/static_patch_tt_response.py (row 9)",
    "correction": "Cite the graviton c=0 derivation as (C)/PART 1b only; (B) is the c=-2 massless-scalar potential. Add PART 3b's named open remainder: at omega=-ik (k=1..l) the Jost pair degenerates and 'those finitely many frequencies are the only surviving pole candidates and this file does not resolve them' — so 'a null' carries a bounded unresolved set, not a clean zero."
   },
   {
    "row": "passivity_channel_diagonal + x_no_pin_theorem (row 11)",
    "correction": "Replace 'Both nodes sit in the eight-node demotion cone' with: passivity_channel_diagonal is one of the eight demoted nodes; x_no_pin_theorem was never demoted (derived-pending before and after 67059a8) and is downstream of the lemma via depends_on — it does not re-promote under the discharge."
   },
   {
    "row": "kr_contract_retarded_tier4 (row 2)",
    "correction": "The A3 alternative-states quote should carry its full clause: the static-patch thermal state is 'PROHIBITED as an input for the primary run; permitted only as a cross-check AFTER (ii) is answered' — the prohibition is scoped, which weakens (without voiding) surprises item 3's 'collision with a frozen declarations document'; the BD/eta->-inf mode-function declaration remains the harder collision."
   },
   {
    "row": "mu_linear (row 4)",
    "correction": "Scope the horizon-scale claim to the ISW-cross leg; the DESI Sigma0 lensing leg is sub-horizon-dominated. The row's conclusion (the empirical kill is not deep-subhorizon-only) stands on the ISW leg."
   },
   {
    "row": "rung3_single_pole (row 6)",
    "correction": "Two trivia: the tier_note reads 'the WHOLE infinite tower' (surveyor: 'entire'); and the owner NULL correction is not the node's last word — the tier_note continues 'AND THE NARROWING IS ITSELF NARROWED, 2026-08-19, BACK TOWARD ADVERSE' on the finite-T/coth-cancellation convention sub-question. Neither changes the row's regime claim."
   },
   {
    "row": "rung8_falsifier (row 16)",
    "correction": "Replace '~39+ orders' with '~21 orders at the staked sub-kHz omega_c (H0/omega ~ 1e-21)'; keep the verbatim exp(-10^21) negligibility quote."
   }
  ],
  "homogeneity_pricing_check": "INDEPENDENTLY CONFIRMED, with one omitted counter-precedent the gate must see. My own searches: (a) the string 'S6' occurs ZERO times in provenance/claims.json (grep, two patterns); (b) every occurrence of 'homogene*' in claims.json enumerated — 8 total: l0_r2_exact_unique_breaker, passivity_channel_diagonal, x_no_pin_theorem use it in the unrelated scaling sense (kernel/amplitude homogeneity); eft_operator_basis names 'the FRW homogeneity/isotropy frame' as a register-declared symmetry it CONSUMES (tier to-derive, delta 0 — it prices nothing); the remaining 4 are all inside vc_averaging_commutes (ledger_scope: vacuum-cluster). (c) The only priced flatness/homogeneity items are vc_flatness_in_reduction (postulate) and vc_averaging_commutes (postulate), both ledger_scope vacuum-cluster, sealed by VACUUM_CLUSTER_MAP.md ('GRUT's ledger is untouched ... the two vocabularies cannot bleed'). (d) The S1-S7 inventory including S6 is prose-carried in S_IF.md §2, calc/operator_basis.py header, and calc/RESULTS_operator_basis.md's table — no node, no delta; S_IF.md is 'CONSTRUCTION DOCUMENT, ledger 0'. (e) The flow node's +1 prices only the TIME side (its R4 atomisation explicitly factored 'stationarity' and booked only the flow); response_lorentz_covariance priced a RESPONSE property while declining the background ('NOT that the background is Lorentz invariant') and was retired 2026-08-30. So the surveyor's asymmetry finding is TRUE: in GRUT scope the temporal flow is priced (+1) and the spatial flat-FRW/homogeneity side is unpriced prose. HOWEVER — omitted and material to whether the gap converts into a bookable OMISSION: (1) vc_averaging_commutes' own sweep note records that the 2026-08-04 omission sweep RAISED the compound candidate 'homogeneous and isotropic AND averaging commutes AND one global a(t) AND one Omega_k' and DELIBERATELY booked only the averaging half, refusing to hang 'homogeneity and isotropy' off vc_ir_gravity_is_gr because that node's 'ACTUAL DENIERS (f(R), Horndeski) AFFIRM IT — manufacturing precisely the false edge the standard was frozen to prevent'; its rider adds 'timescape denies THIS while AFFIRMING statistical homogeneity'. (2) OMISSION_STANDARD_v2's fence (line 17: 'a candidate counts only if a NAMED POSITION could deny it AND the denial would change something the cluster says') carries the worked example 'FLATNESS PASSES — non-flat cosmologies are named positions AND the denial changes what the cluster says' — recorded for the CLUSTER's statement; a GRUT-scope booking must separately show the denial changes what GRUT says, and faces a double-count check against the two sealed cluster nodes. Net: unpriced-in-GRUT-scope is verified fact; whether it books as a NEW-OMISSION is a live adjudication with a recorded in-corpus precedent pulling the other way, and the gate should say both.",
  "omitted_evidence": [
   "vc_averaging_commutes (claims.json, vacuum-cluster scope): the 2026-08-04 sweep's recorded refusal to book 'homogeneity and isotropy' — 'WHOSE ACTUAL DENIERS (f(R), Horndeski) AFFIRM IT' — plus the rider 'timescape denies THIS while AFFIRMING statistical homogeneity'; the standing in-corpus precedent against a GRUT-scope homogeneity omission booking, directly relevant to the gate's NEW-OMISSION outcome.",
   "provenance/OMISSION_STANDARD_v2.txt lines 17 and 30: the fence's exact wording and the worked example 'FLATNESS PASSES -- non-flat cosmologies are named positions AND the denial changes what the cluster says' — the pass is cluster-scoped; a GRUT-scope booking needs its own 'changes what GRUT says' showing.",
   "P1A_EDGE_ADJUDICATION_01.md §6: 'the static-patch discharge mechanically reverses the demotions as a register change requiring its own re-certification' — the reversal route carries a re-certification obligation (CERTIFICATION_02 lineage) that belongs in the cost accounting; also §5's recorded counter (ii): the conditional-theorem reading of rung2 was rejected 'for the banked node as used (the gate runs against the declared background)' — an adjudication whose premise a background re-declaration would touch.",
   "POST-FREEZE MECHANICS (b9b6884 and d4a385a, after the surveyor's evidence set): (1) the tier-provenance guard — provenance/TIER_BASELINE.json pins every node's tier and provenance/test_tier_provenance.py makes any tier change legal only with a baseline update plus an append-only changelog entry carrying date/authority/rationale, so the eight re-promotions now have a mandatory mechanical route; (2) P1B adjudicated LEGISLATION-REQUIRED (stanza OPEN, owner package P1B_RATIFICATION_PACKAGE_01.md) — the booking-format question P1A recorded as 'ACUTELY LIVE' for exactly these foundation nodes is unresolved, which any bank package should disclose.",
   "PRIMITIVE_INVERSION_SCOPE.md §4.3: where no Bisognano-Wichmann/Sewell-class theorem applies, the identification falls to the Connes-Rovelli thermal-time HYPOTHESIS, 'which replaces background_time_translation_flow one-for-one with a differently-named assumption. No ledger gain. Relabeling.' — the K3 shape stated in advance for this discharge's own subject.",
   "rung3_single_pole tier_note continuation after the owner NULL correction: 'AND THE NARROWING IS ITSELF NARROWED, 2026-08-19, BACK TOWARD ADVERSE' (the node's own pre-existing finite-T/coth-cancellation text answers the convention question) — the NULL ruling is not the node's final word on the 2026-08-19 campaign.",
   "calc/static_patch_tt_response.py PART 3b open remainder: at omega = -ik, k=1..l, the Jost pair degenerates and 'those finitely many frequencies are the only surviving pole candidates and this file does not resolve them' — qualifies the 'one null' track-record framing.",
   "WALL_A_A3_DECLARATIONS.md line 156 full clause: the static-patch thermal state is 'permitted only as a cross-check AFTER (ii) is answered' — the prohibition surprises item 3 leans on is scoped to primary-run inputs.",
   "STATE.md register-sync markers: 53 GRUT-scope nodes + 21 vacuum-cluster = 74 in claims.json, nets +16 GRUT / 0 cluster — the arithmetic baseline the protocol's 'net cost to the corpus as banked' is measured against (the flow's +1 sits inside GRUT's +16).",
   "calc/RESULTS_operator_basis.md S1-S7 table (third prose home of S6, with its own 'inherited, not chosen' wording) — strengthens the unpriced-prose finding by one more calc-layer home."
  ],
  "verdict": "SOUND_WITH_CORRECTIONS"
 },
 "referee": {
  "verdict": "MIXED",
  "mixed_detail": "DISCHARGE-UNAVAILABLE (the commissioned availability question) coexisting with NEW-OMISSION-SURFACED (expressly permitted by the frozen protocol: 'may coexist with either outcome above'). UNAVAILABLE component: the named static-patch background re-declaration is not a retirement-at-zero but a priced re-foundation that breaks banked frames and costs more than it retires — the corpus's own pre-registered prediction (PRIMITIVE_INVERSION_SCOPE.md §5 'Standing prediction: this is where it breaks') confirmed with its mechanism made precise. NEW-OMISSION component: the spatial half of the very background-symmetry commitment whose temporal half was booked at +1 on 2026-08-18 — the declared background's spatial-translation/rotation invariance (S6), licensing the k label, d3k measure, SVT frame, and SO(3)-at-fixed-k enumeration — is carried as unpriced prose in GRUT scope ('S6' occurs 0 times in provenance/claims.json, machine-verified this gate; the only priced flatness/homogeneity is vacuum-cluster-scoped and sealed). Booking is the owner's under OMISSION_STANDARD_v2 per the protocol's own pre-commitment; the package below discloses the in-corpus counter-precedent. UNDERDETERMINED is rejected: every load-bearing determination is on the banked record (the bridge theorem's absence is recorded in the corpus's own words — an absent theorem is a decided fact about the corpus as banked, not an open question). The scoped/worldline alternative package (Advocate AVAILABLE's R-SCOPE) is transmitted INERT as an owner-legislation option, neither granted nor killed — it is not the clause's discharge and not this gate's question.",
  "availability": "UNAVAILABLE, on the corpus as banked plus standard physics the corpus itself cites. (1) THE CLAUSE'S ANTECEDENT IS UNSATISFIABLE BY RE-DECLARATION AT ACCEPTABLE COST. The clause (claims.json, verified verbatim) has exactly two routes: a declared background carrying a timelike Killing vector, or an exhibited K_R-free reformulation; the frozen protocol admits alternatives 'only from the corpus's own texts' as re-declarations that would SUPPLY THE KILLING VECTOR. The declared background is flat FRW for every banked observable (S_IF.md §1) — no timelike KV. (2) THE NAMED CANDIDATE FAILS ON DERIVED IN-CORPUS PHYSICS: the static-patch time-translation KV in flat slicing is xi = d_t − H x^i d_i, and [xi, P_j] = +H P_j ≠ 0 (standard dS isometry algebra, exhibited in-corpus by calc/static_patch_tt_response.py's forced (omega,l,m) multipole decomposition and derived at D3b: 'only the static patch offers a global reduction'). No joint (omega_Killing, k) eigenbasis exists; the exact label after re-declaration is (T-frequency, l, m) on a chart of extent r<1/H about one worldline, while every banked kernel statement is written in flat-slicing (omega,k) (S_IF §3 'at fixed (omega, k)', with the d3k measure). The re-declaration therefore demotes every exact banked (omega,k) statement to an H<<omega effective statement — a corpus-wide re-scope with no register field to be declared in (keystone §6.2: 'no register field mentions foliation, cosmic time, or proper time'), i.e. structurally silent, the protocol's named failure. (3) IDLE WHERE SAFE, ABSENT WHERE NEEDED: the H<<omega consumers (Tier-4 kernel with its hard eps_H=(104/9)H^2/omega^2 gate and DomainRejected at omega<~H; GW band at H0/omega~1e-20; tabletop) already carry the label through the WKB license the corpus records — the discharge buys them nothing; the consumers the +1 exists for (rung3's eta=lim_{omega->0}, kk_static_transfer's full-axis dispersion integral, rung7's w(z) forced at omega~H(z), mu_linear's low-ell ISW leg at k~H0) live exactly where the (T,l) and (omega,k) labels diverge (D2/D3b/D4) or outside the chart (w(z) 'not an axis quantity', a comoving-slice average over multi-Hubble history). (4) FROZEN-CONTRACT COLLISIONS: calc/gw_tensor_friction.py's SPEC clock ('single FRW COSMIC clock throughout... no static-Killing or e-fold quantity is imported' — verified verbatim); WALL_A_A3_DECLARATIONS.md's BD state defined at eta -> −infinity (a Poincaré-chart surface outside the static chart) with the static-patch thermal state prohibited as a primary-run input (scoped prohibition — 'permitted only as a cross-check AFTER (ii) is answered' — so the BD state declaration is the harder collision). (5) CORPUS-GROUNDED KV-SUPPLYING ALTERNATIVES ALSO FAIL: Minkowski (the Tier-4 contract's computational base) contradicts every cosmological export; Connes-Rovelli thermal-time is pre-registered at PIS §4.3 as a one-for-one relabel ('No ledger gain. Relabeling.'). (6) THE NON-KV SCOPED/WORLDLINE PACKAGE IS NOT THIS DISCHARGE: the phrase Advocate AVAILABLE quoted as the gate question's license for an H<<omega-scope alternative does NOT appear in the frozen protocol (verified by reading it); the clause's WKB escape was owner-ordered CLOSED in the node's own booked text on the booking date ('closes an escape that was nearly ruled through' — rung3.boundary_condition, verified verbatim), and the +1 prices exactly the flow-content the H<<omega license does not supply; the package's exact-stationarity leg (D3a) is keystone-map content marked 'NOTHING BANKED... may not be cited as content by any other artifact... banking remains the owner's' (AMBER-screened); and its register-level omega<~H refusal would decide the premise of rung3_single_pole's reserved tier question — reserved by the owner on the same date 'so that neither hides the other' and hard-fenced by this protocol. It is transmitted inert as an owner rewrite proposal, not ruled available. NOT UNDERDETERMINED: the cosmic/static bridge is recorded in-corpus as absent and priced ('Bridging them is either a theorem (derived) or a new priced input' — keystone §6.3; 'the transport itself is the assumption, and it is currently unpaid' — D6; the assembled reduction 'has never been performed' — D3b; C7: 'unperformed reduction, not a contradiction'). An available discharge needs the bridge to exist; the corpus contains, in its place, the bridge's absence plus its price tag. That decides availability today; the named deciders are re-open conditions, not indeterminacy.",
  "cost_accounting": {
   "retires": "BY THIS GATE: nothing (CHARTER 5.3 pre-screen; banks nothing, edits nothing; verdict UNAVAILABLE means no retirement occurs). UNDER THE HYPOTHETICAL STATIC-PATCH EXECUTION (priced to show why it fails): background_time_translation_flow's +1 retires (GRUT net +16 -> +15; net verified this gate by summing ledger_delta at HEAD: +16 GRUT / 0 vacuum-cluster / 74 nodes), and the eight demotions of commit 67059a8 mechanically reverse (verified by two-snapshot diff this gate: exactly rung1_inin_formalism, rung2_kms_gate, rung4_love_kk, kr_contract_retarded_tier4, info_i1_renorm_as_information, passivity_channel_diagonal, rung7_w1_wz_map, u1_form_universality, shown -> derived-pending; deltas unchanged by the tier moves — rung1 +4, rung2 −1, six at 0 — so the reversal is ledger-inert tier bookkeeping). The reversal is 'a register change requiring its own re-certification' (P1A §6, verified), now also subject to the post-freeze tier-provenance guard (TIER_BASELINE.json + append-only changelog), and rung1's re-landing at shown with +4 prose-priced inputs is booked by P1B (OPEN, LEGISLATION-REQUIRED) as an EXPOSURE — 'the exact configuration this pass questions' — not a license. NOTE: x_no_pin_theorem was never demoted (verified: derived-pending at both snapshots) and does not re-promote.",
   "new_inputs": [
    "INPUT 1 — the static-patch chart+state background declaration itself (observer-dependent chart, extent r<1/H, Killing time T). R1 fence: PASSES as an input — the flow node's own fence names the denying positions (FRW cosmological perturbation theory; the secular/non-stationary dS programs), and the corpus's entire flat-FRW data-reduction side denies it in use. Strictly stronger than the +1 it replaces (flow + chart + foliation + horizon). Cost >= +1, plus supersession of the frozen A3 BD/eta->-infinity state declaration (an owner act outside pricing).",
    "INPUT 2 — the D6 clock-transport bridge (Killing-clock kernel statements transported to comoving-clock observables: w(z), mu_linear's legs, all d/dt_cosmic rates). Corpus-recorded as absent-and-priced ('either a theorem (derived) or a new priced input'; 'the transport itself is the assumption, and it is currently unpaid'). CAVEAT ADOPTED from Advocate UNAVAILABLE's own honest weakness: this partially surfaces an existing latent debt (rung2's ledger_note already runs the horizon-regularity argument under the flat-FRW declaration, with the horizon-temperature RELATION separately paid via rung5's Unruh import), so the strictly NEW cost is the scope-widening from one attribution note to every cosmological export. Scored at +1 or at the discount, the availability bar still fails on the other prong.",
    "INPUT 3 — the S6 frame conversion for the operator-basis/passivity/no-pin chain: either an uncomputed re-derivation of the two-survivor enumeration and applied floors in (omega, l, m) (the static chart supplies SO(3)-about-a-worldline + T-translations, NOT translations-at-fixed-k), or a priced effective-frame declaration for retaining (omega,k) at H<<omega. Either horn >= +1 or an unpriced re-derivation debt; taking neither is the silent re-scope the protocol pre-defines as failure. This input's exposure is the coexisting omission finding surfacing under load: the spatial license was free only because it is unpriced prose."
   ],
   "net": "TODAY: no ledger movement (gate banks nothing; verdict UNAVAILABLE). HYPOTHETICAL STATIC EXECUTION: −1 (flow) +2-to-+3 (Inputs 1–3) = GRUT net +16 -> +17..+18, strictly worse, PLUS nonzero banked-result loss (rung7_wz de-homed from the declared chart; mu_linear's frame and ISW kill leg unstatable on it; gw_tensor_friction's frozen SPEC contradicted; kk_static_transfer's route re-posed) PLUS a re-certification bill and two frozen-document supersessions. The protocol's DISCHARGE-AVAILABLE bar — 'ledger net strictly improves or holds with zero banked-result loss' — fails on BOTH prongs; even the most charitable accounting (retire 1, add 1) realizes the pre-registered K3 shape ('Net ledger does not move') while the zero-banked-loss prong fails independently. Stated plainly per the anti-optimization clause: the coexisting omission finding, if the owner books Omega-A, moves the ledger UP (+1) — the instrument-favorable direction; the arithmetic is forced by verified strings, not preferred."
  },
  "scope_survival": [
   {
    "result": "kr_contract_retarded_tier4 (Tier-4 retarded kernel, banked delta 0; one of the eight demoted)",
    "fate": "SURVIVES UNDER EVERY BRANCH BY ITS OWN PRIOR FENCING, AND GAINS NOTHING FROM THE DISCHARGE: its banked statement already carries 'Validity: omega >> H with eps_H = (104/9) H^2/omega^2; omega << H refused by the evaluator', hard-wired as DomainRejected in PHYSICS_LEDGER/wall_kr_tier4_retarded.py — it is already the H<<omega effective object a re-declaration would produce. Under the static re-declaration its provenance becomes alien (computed on a THIRD background: flat Minkowski contract G0_R=2kappa^2/((omega+i0)^2−k^2) with O(H^2) BD/Poincare-chart corrections; A3 declares the state at eta->−infinity, outside the static chart). The discharge is idle for it."
   },
   {
    "result": "calc/gw_tensor_friction.py (Gamma_T closure, REFUSE verdict) + rung8 tabletop stakes",
    "fate": "SURVIVE VERBATIM ONLY IF NOT RE-DECLARED. The SPEC-frozen clock declaration ('single FRW COSMIC clock throughout... no static-Killing or e-fold quantity is imported' — verified on the file) is contractually incompatible with a static-patch background whose own time IS a static-Killing quantity: re-scoping it is the protocol's named failure, leaving it is a standing contradiction. At its working point the distinction is numerically invisible (H0/omega ~ 1e-20; 1+hcorr == 1.0 in double). rung8: lab band, T_dS ~ 3e-30 K exp(−10^21)-negligible (~21 orders H<<omega at the staked sub-kHz omega_c, per the verifier's correction); unaffected and probative of nothing."
   },
   {
    "result": "rung4_love_kk / calc/gw_dissipation_bounds.py (real-but-unobservable verdict)",
    "fate": "SURVIVES UNCHANGED on every branch: LIGO band (H<<omega by ~20 orders), worldline-EFT tidal structure, KK/retarded analyticity background-free; the omega->0 Lorentzian tail was already fenced out of its one strong step (scope fence 2026-08-20) before this gate. Its survival is the idle sector, not evidence of availability."
   },
   {
    "result": "rung2_kms_gate / gate/kms.py (one of the eight demoted; delta −1)",
    "fate": "GATE CODE SURVIVES (pure omega-grid, no k label in the file). It is the one partial would-be beneficiary — its T=T_dS=H/2pi attribution would finally match the declared background — but the benefit is already available without re-declaration (D3a-style worldline reduction at the same temperature, unbanked) and the D6 transport debt is relocated and WIDENED, not paid: every (omega,k^2) application of the KMS lock in S_IF §3 stays comoving-frame. Its ledger_note is the in-corpus instance of the unpaid transport doing ledger-affecting work under the flat-FRW declaration (horizon-regularity argument; T_SUBSUMED adjudication 2026-08-02; the horizon-temperature RELATION separately paid via rung5's Unruh import). Flagged into the omission package as rider Omega-B."
   },
   {
    "result": "rung3_single_pole (anchor conjecture; derived-pending, delta 0) and its reserved tier question",
    "fate": "UNTOUCHED BY THIS GATE (hard fence; used as evidence only). Under the static re-declaration it is NOT rescued but RE-POSED in (omega, l>=2) where the corpus's free-level record is null-with-a-bounded-unresolved-set (PART 3b: B=−A for every omega, QNM reading retracted; the finitely many omega=−ik candidates unresolved; owner ruling 'NULL, NOT ADVERSE', itself narrowed 'BACK TOWARD ADVERSE' 2026-08-19 on the finite-T sub-question) — everything rests on the uncomputed interacting Sigma. Under the transmitted R-SCOPE rewrite its position WOULD change, contra Advocate AVAILABLE: the flow node's own R1 fence names the omega->0 transport coefficient as the +1's cash value ('without the flow there is no single-omega kernel and no omega->0 transport coefficient to conjecture about'), so a register-level omega<~H refusal de-licenses the conjecture's defining object — deciding the reserved adjudication's premise. That is a reason the rewrite is owner legislation, not gate-grantable."
   },
   {
    "result": "mu_linear (no-go export: mu=4/3 exclusion + conditional mu=Sigma=eta=1 leg) and the KC5 spatial-SVT frame",
    "fate": "SURVIVES ONLY UNDER THE STATUS QUO. Internally 'fine' (keystone C5: single FRW cosmic clock, identity, sub-horizon quasi-static). Its fixed-slicing spatial-SVT frame is defined by comoving-slice translations the static chart lacks; its empirical kill legs (~2.0-sigma low-ell ISW t-g cross at k~H0 — horizon-scale; ~3.5-sigma DESI Sigma0, sub-horizon-dominated per the verifier's correction, but still a comoving-k-space data reduction) are unstatable on an r<1/H chart. Keeping them keeps flat FRW — i.e. the discharge does not happen for this node. Not in the flow node's R5 cone; not demoted; nothing to re-promote."
   },
   {
    "result": "rung7_wz (SOURCED w=−1 flat; +3 inserted for the evolving piece) + rung7_w1_wz_map (demoted, delta 0)",
    "fate": "w=−1-flat survives as a time-domain statement on the FRW clock; the OBSERVABLE w(z) is a comoving-slice average over multi-Hubble history, 'not an axis quantity' (D2, keystone §6.3), outside the static chart's r<1/H — DE-HOMED under re-declaration absent the unexhibited bridge theorem. The vacuum is forced at omega ~ H(z) (the opposite of H<<omega), so no WKB substitute reaches it either. w1 map re-promotes mechanically under a ratified reversal; survives verbatim under the status quo."
   },
   {
    "result": "kk_static_transfer (ANSWERED 2026-08-09 at outcomes (b)+(c): no sign inherited; whole question = sign of chi_inf)",
    "fate": "SURVIVES VERBATIM under the status quo — a negative/obstruction landing that any omega<~H fence AGREES with (its own text: 'UNTIL THIS IS COMPUTED, the quasi-static x inherits NO sign from the passivity floor'). Under re-declaration its KK route (integral reaching omega->0) would need re-posing per static multipole with unknown subtraction structure — re-opened, not preserved."
   },
   {
    "result": "passivity_channel_diagonal (demoted; the general lemma) + x_no_pin_theorem (NEVER demoted — verified) + eft_operator_basis/p_tt_ansatz",
    "fate": "THE LEMMA SURVIVES ANYWHERE (frame-free projector mathematics, banked as such in its sub_status). Its APPLICATION (x_no_pin's floors on c_i(omega,k^2)) and the two-survivor enumeration are S6-frame-bound: the static chart's decomposition is (omega,l,m) with l>=2 kinematics and no translations-at-fixed-k, so under re-declaration the applied chain owes a full re-derivation with unknown survivor structure (Input 3). Under the status quo all survive at their banked scopes; the S_IF §6 'at any frequency' quantifier is a ledger-0 construction-document application whose omega->0 reach has exactly one banked would-be consumer, already answered (kk_static_transfer)."
   },
   {
    "result": "zeta_interior_family (constant-x window x < ~0.59)",
    "fate": "A constant-x kernel is frequency-flat, so the flow question is degenerate for the cut itself — but the window's source (DESI Sigma0 lensing through the SVT frame) is a comoving-slice data reduction: rides mu_linear's frame. Survives status quo; re-scoped-or-lost under re-declaration."
   },
   {
    "result": "vacuum cluster (21 vc_* nodes, own sealed ledger at 0)",
    "fate": "UNAFFECTED on every branch (no kernels, no frequency labels; 'the two vocabularies cannot bleed'). The seal cuts both ways: its priced flatness/homogeneity cannot be cited as GRUT-scope pricing, and per OMISSION_STANDARD_v2 R3 neither its pass (FLATNESS PASSES) nor its recorded refusal (homogeneity off vc_ir_gravity_is_gr) inherits across scopes — the GRUT-scope fence must be run fresh (omission package, below)."
   },
   {
    "result": "response_lorentz_covariance (retired +1, 2026-08-30)",
    "fate": "UNTOUCHED — and it is the register's only executed retirement precedent, and it retired by exhibited DEMONSTRATION (membership computed, countersigned), while explicitly declining to price the background. There is no precedent for retirement by re-DECLARATION; the contrast is part of why the named route fails and why the R-SCOPE package is legislation rather than clause execution."
   },
   {
    "result": "calc/static_patch_tt_response.py (the candidate background's resident computation; calc-layer, banks nothing)",
    "fate": "UNCHANGED. It establishes the static patch as derived LOCAL geometry (metric check; c=0 axial graviton potential per PART 1b — the verifier's part-label correction adopted) with a retracted QNM headline and an owner-ruled null free response carrying a bounded unresolved set. No banked result lives on the chart; the re-declaration would move the corpus's foundation onto a chart whose only occupant is working material with a retraction in its header."
   }
  ],
  "physics_finding": "At H does not equal 0 the framework can hold exactly one of its two exact spectral labels, never both: a global single-frequency label requires a timelike Killing flow, which de Sitter supplies only as the static-patch boost xi = d_t − H x^i d_i on a chart of extent r < 1/H about one worldline, and that flow fails to commute with the comoving spatial translations that define k ([xi, P_j] = H P_j — standard dS isometry algebra the corpus itself exhibits by being forced into an (omega, l, m) multipole decomposition the one time it computed on the static patch). An exact omega therefore costs the global k label, and the exact k label of flat FRW slicing costs any global omega — stationarity and homogeneity trade off at order H, and the joint (omega, k) label every banked kernel statement is written in is an effective object whose leading obstruction is exactly the corpus's own computed control term eps_H = (104/9) H^2/omega^2, with only two exact reductions, both already derived in the corpus's working papers: along a single comoving worldline in cosmic proper time (D3a, at T_dS = H/2pi), and globally in the static-Killing decomposition (D3b) — and no theorem bridging either reduction to the comoving-slice observables (the bridge is recorded in-corpus as 'either a theorem (derived) or a new priced input', with the transport 'currently unpaid'). The load distribution is the sharp part: every banked verdict sits where the trade-off is numerically invisible (H << omega by 20-plus orders, time-domain, or frequency-flat), while the framework's defining open objects — the omega -> 0 transport limit of the anchor conjecture, w(z) forced at omega ~ H(z), the static modulus reached through a full-axis dispersion integral — live exactly where the two labels diverge. So the +1 flow assumption is not paying for anything currently banked; it is paying for the framework's future claims in the very regime where no background re-declaration can supply the license, because the missing license IS the trade-off. That is the obstruction, and per the protocol the obstruction is the finding.",
  "inert_package": "MY VERDICT LICENSES NO BANK PACKAGE (nothing is available to bank). It licenses (A) the obstruction record and (B) an omission-booking package for owner relay, and it transmits (C) without license. ALL INERT UNTIL OWNER ACTION. — (A) OBSTRUCTION RECORD (the UNAVAILABLE consequence), to be filed as STATIC_PATCH_DISCHARGE_GATE_01.{md,json}: the physics-finding paragraph verbatim; the commutator [xi, P_j] = H P_j with its in-corpus exhibitions (static calc's (omega,l,m) decomposition; D3b; PIS §5) and its identification with the eps_H control structure; the two frozen-contract collisions (A3 BD state at eta->−infinity — the harder one; gw_tensor_friction SPEC clock — with the A3 static-thermal prohibition quoted at its full scoped clause); the chart-coverage failures (w(z) off-chart; mu_linear frame); the vocabulary gap (§6.2); the per-result scope table; the cost arithmetic (retire 1, acquire >=2 R1-passing inputs + the frame-conversion debt, nonzero banked-result loss); the statement that this confirms the sealed standing prediction with K2's mechanism at at-least-K3 cost, noting K2/K3 are the inversion question's kill conditions, used here as the corpus's prediction, not as this gate's rules. — (B) OMISSION-BOOKING PACKAGE (owner adjudication under OMISSION_STANDARD_v2; this gate books nothing): CANDIDATE Omega-A, one atom only: 'background_spatial_translation_flow' — the declared background carries spatial translations + rotations of the comoving slicing under which the setup is invariant, so kernels depend on x−x' alone and the single-k label, the d3k measure, the SVT frame, and the SO(3)-at-fixed-k enumeration are definable; proposed tier assumed, +1, GRUT scope, the exact spatial twin of the booked flow node. Conjunct-1 evidence: suppose it false — kr_contract's G0_R(omega,k), mu_linear's k^2-Psi bookkeeping, x_no_pin's c_i(omega,k^2), zeta's x(omega,k^2), S_IF §3's d3k integral can none of them be established, stated, or used. Conjunct-2 evidence: 'S6' = 0 occurrences in provenance/claims.json (machine-verified 2026-09-13); prose homes only (S_IF §2; calc/operator_basis.py header; calc/RESULTS_operator_basis.md); the only priced flatness/homogeneity is vacuum-cluster-scoped and sealed. R1 fence to be run by the owner ON GRUT-SCOPE PHYSICS SPECIFICALLY (R3 bars inheriting the cluster's FLATNESS-PASSES example AND its homogeneity-refusal across scopes): candidate deniers = inhomogeneous/backreaction programs (in-register instance at vc_averaging_commutes, other scope; timescape rider noted). MUST-DISCLOSE ADJUDICATION INPUTS CUTTING AGAINST BOOKING, verified verbatim this gate: (i) vc_averaging_commutes' sweep note — the 2026-08-04 sweep raised the compound candidate and deliberately did NOT book 'homogeneity and isotropy', because hanging it off vc_ir_gravity_is_gr would manufacture a false edge (that node's actual deniers, f(R)/Horndeski, AFFIRM it); (ii) S6 IS visible in eft_operator_basis's statement (a delta-0 to-derive question node) — the S4-precedent adjudication (entailment-of-a-named-concept vs appearance; split-question vs omission) is genuinely open and is the owner's; (iii) the declaration-as-booking reading of S_IF §1 — countered but not closed by the fact that the identical reading was available for the temporal half on 2026-08-18 and the program booked +1 anyway. R4 atomisation: book at most the one atom; state-half ABSORBED (A3/BD declaration + the acknowledged vc_state_expectation_functional shared input); GRUT-scope spatial FLATNESS = separate split/atom question; parity-evenness = rider ('inherited, not chosen'); the fixed-slicing choice = convention, fails the fence. R5 WIRING PLAN if booked: depends_on edges from eft_operator_basis, x_no_pin_theorem, mu_linear, kr_contract_retarded_tier4, zeta_interior_family; dated cross-pointer on background_time_translation_flow (two halves of one background-symmetry commitment). RIDER Omega-B: the D6 clock transport — already prose-recorded as 'currently unpaid', so 'new' is contestable; owner disposition as omission vs split of rung2's T_SUBSUMED adjudication; note rung2's ledger_note currently runs a static-Killing horizon-regularity argument under the flat-FRW declaration, with the horizon-temperature RELATION separately paid (rung5 Unruh import) and only the cross-clock TRANSPORT unpaid. INSTRUMENT ITEM: the register lacks any field for background/foliation/clock (keystone §6.2, 'how the C1/C2 mismatch passed two pre-registrations') — forward to the owner's legislation queue alongside P1B; any future re-declaration is structurally silent until this exists. — (C) TRANSMITTED WITHOUT LICENSE (not a discharge; owner legislation only): Advocate AVAILABLE's scoped/worldline rewrite (retire the +1 into a declared-scope stationarity declaration: D3a banked + H<<omega WKB + register-level omega<~H refusal; S_IF §1/§3/§6 scope tags; eight tier reversals). Recorded with its true costs named: it must BANK D3a (keystone map: 'may not be cited as content'; AMBER; banking the owner's); it rides the WKB escape the owner ordered CLOSED in the node's own text on booking day; its refusal de-licenses the omega->0 transport coefficient the flow node's R1 fence names as the +1's cash value, deciding the premise of rung3's reserved tier question (hard-fenced here); the eight reversals require their own re-certification (P1A §6) under the new tier-provenance guard, with rung1's re-landing exposed on P1B (OPEN, LEGISLATION-REQUIRED). The gate neither grants nor kills it.",
  "rejected_advocate_points": [
   {
    "advocate": "AVAILABLE",
    "point": "The gate question itself licenses the R-SCOPE alternative ('dS static patch, or a corpus-grounded alternative such as declaring the flow at the H<<omega scope every banked kernel result already occupies').",
    "reason": "The quoted licensing phrase does not appear in the frozen protocol (read in full this gate). Sub-question 1 admits alternatives 'only from the corpus's own texts' as re-declarations that would SUPPLY THE KILLING VECTOR; R-SCOPE supplies none. The advocate argued from a paraphrase of its own tasking, not the frozen text."
   },
   {
    "advocate": "AVAILABLE",
    "point": "The clause's MECHANISM (property-not-commitment) is primary and is satisfied at scope by D3a + the WKB license, so the node retires at zero under its own clause.",
    "reason": "Three independent blocks. (1) The clause's antecedent is a KV background and its only other route is a K_R-free reformulation; R-SCOPE is neither. (2) The WKB escape is recorded CLOSED inside the clause itself, owner-ordered on the booking date ('closes an escape that was nearly ruled through') — the +1 was booked WITH the H<<omega license already in hand, so the +1 prices exactly what that license does not supply; retiring it on that license plus a refusal is the closed escape plus scope forfeiture, not a discharge at zero. (3) The D3a leg is keystone-map content marked NOTHING-BANKED / may-not-be-cited; a discharge available 'on the corpus as banked' cannot rest on it, and without it the package IS the closed escape."
   },
   {
    "advocate": "AVAILABLE",
    "point": "rung3_single_pole is UNTOUCHED BY CONSTRUCTION — the declaration merely writes at register level what its boundary_condition already records.",
    "reason": "The flow node's own R1 fence names the flow's cash value: 'without the flow there is no single-omega kernel and no omega->0 transport coefficient to conjecture about.' A register-level omega<~H refusal executed in the same act that retires the +1 de-licenses the anchor conjecture's defining object — deciding the premise of the tier adjudication the owner reserved the same day 'so that neither hides the other', which this protocol hard-fences. Recording that the WKB defense fails at omega->0 (status quo: the assumption covers it) is not the same as refusing the label there (post-package: nothing covers it)."
   },
   {
    "advocate": "AVAILABLE",
    "point": "The eight demotion reversals are pre-recorded owner-side law, mechanical and ledger-neutral; OPEN_PASSES even pre-clears the migration's first commit.",
    "reason": "PARTIALLY REJECTED. Ledger-neutral: verified (all eight deltas unchanged by the tier moves). Mechanical-and-clean: no — P1A §6 makes the reversal 'a register change requiring its own re-certification'; the post-freeze tier-provenance guard adds mandatory baseline+changelog mechanics; and P1B (OPEN, LEGISLATION-REQUIRED) books rung1's re-landing at shown-with-+4-prose-priced-inputs as an EXPOSURE ('the exact configuration this pass questions'), not a license. The OPEN_PASSES stanza pre-declares instrument false-positives, which is diagnostic hygiene, not authorization."
   },
   {
    "advocate": "AVAILABLE",
    "point": "N1 (the scoped WKB license) prices zero under the R1 fence because the named denying positions themselves use sub-horizon WKB labels — so the discharge replaces the +1 with literally nothing priceable.",
    "reason": "True of the H<<omega license in isolation, but it answers the wrong question: the +1 does not price the H<<omega license (which was free on booking day); it prices the global flow — whose surviving cash value is the omega->0 / omega~H territory (the node's own fence text). The package handles that territory by refusal, i.e., by forfeiting what was priced rather than discharging it, and the forfeit lands on the framework's anchor. 'Nothing priceable remains' is achieved by no longer claiming the thing that was priced — the clause's 'retires at zero' promise is conditioned on a background whose flow is globally exact, which this is not."
   },
   {
    "advocate": "UNAVAILABLE",
    "point": "K2/K3 kill the discharge (used as governing standards).",
    "reason": "QUALIFIED, not adopted as rules: PIS §6 opens 'The question is REFUTED if' — K2/K3 are the primitive-inversion question's kill conditions, not this gate's. They enter this verdict as the corpus's sealed prediction about the same flow mismatch, which the verdict CONFIRMS (K2's mechanism at at-least-K3 cost). The advocate's own rebuttal conceded the distinction; the verdict adopts it at that strength only."
   },
   {
    "advocate": "UNAVAILABLE",
    "point": "INPUT 2 (the D6 transport) scores as a full new +1 against the discharge.",
    "reason": "QUALIFIED per that advocate's own honest weakness: D6 is an EXISTING recorded-unpaid latent debt (rung2's ledger_note already runs the horizon argument under flat FRW; the temperature RELATION is separately paid via rung5's Unruh import), so the strictly new cost is the scope-widening to every cosmological export. The verdict's arithmetic is stated at both scorings; the availability bar fails either way on the zero-banked-loss prong."
   },
   {
    "advocate": "UNAVAILABLE",
    "point": "The A3 PROHIBITED row bars the static-patch re-declaration.",
    "reason": "QUALIFIED (verifier's correction adopted, confirmed on the file): the prohibition is scoped — 'PROHIBITED as an input for the primary run; permitted only as a cross-check AFTER (ii) is answered.' The harder, unscoped collision is the BD mode-function declaration at eta -> −infinity, a surface outside the static chart. The advocate's own honest-weakness list conceded the coherence argument was self-constructed; the verdict rests the state-collision limb on the BD declaration."
   },
   {
    "advocate": "OMISSION/UNDERDETERMINED",
    "point": "Availability is UNDERDETERMINED with named deciders (U1 assembly reduction, U2 bridge theorem, U3 vocabulary legislation, U4 interacting Sigma).",
    "reason": "REJECTED. The frozen standard is what the corpus AS BANKED forces, and the corpus records the deciding facts: the bridge is absent-and-priced in its own words (§6.3, D6), the assembled reduction 'has never been performed' (D3b), the label divergence is derived (D2/D4), and every banked result's scope is on its banked face. An absent theorem is a decided fact about the corpus as banked. The named deciders are RE-OPEN conditions for a future gate — preserved in this record — not present indeterminacy; ruling UNDERDETERMINED would misreport a decided question as open."
   },
   {
    "advocate": "OMISSION/UNDERDETERMINED",
    "point": "NEW-OMISSION-SURFACED as the PRIMARY verdict.",
    "reason": "REJECTED AS PRIMARY, ADOPTED AS COEXISTING: the gate was commissioned to answer availability and the record answers it; the protocol pre-commits the omission to owner booking and expressly allows coexistence. The advocate's own rebuttal of UNAVAILABLE conceded the correct composite preserves the obstruction inventory — which is what this verdict does."
   },
   {
    "advocate": "OMISSION/UNDERDETERMINED",
    "point": "The D3a worldline route makes the discharge's availability live (a flow-property of the currently declared background at worldline scope), so banking unavailability now is predicting the outcome.",
    "reason": "REJECTED as an availability ground, ADOPTED as an owner-relay flag: the route carries no timelike Killing vector (the clause does not fire), licenses along-worldline kernels only (not the assembled (omega,k) corpus — D3b), rests on unbanked may-not-be-cited content, and is a node REWRITE narrowing what is priced — owner legislation outside the clause's two routes. Transmitted inert in package item (C); the verdict on the clause's discharge is not thereby predicted but decided on the banked record."
   }
  ],
  "decisive_evidence": [
   "THE FROZEN PROTOCOL, read in full this gate: sub-question 1 admits alternatives 'only from the corpus's own texts' as re-declarations that would SUPPLY THE KILLING VECTOR; the DISCHARGE-AVAILABLE bar is 'ledger net strictly improves or holds with zero banked-result loss'; NEW-OMISSION 'may coexist with either outcome above' and is 'booked by the owner under OMISSION_STANDARD_v2, not by this gate'; hard fence on rung3's reserved tier question. Advocate AVAILABLE's quoted license for an H<<omega-scope alternative does not appear in it.",
   "provenance/claims.json, background_time_translation_flow (extracted whole this gate): the two-route overturning_computation with the WKB escape recorded CLOSED inside the clause; the R1 fence naming the omega->0 transport coefficient as the flow's cash value; the ledger_note reserving rung3's tier question the same day 'so that neither hides the other (owner ruling, same date)'.",
   "rung3_single_pole.boundary_condition, verified verbatim: 'WKB CLOSURE RECORDED 2026-08-18 (owner-ordered; closes an escape that was nearly ruled through)... VALID FOR H << omega -- and this node's claim is a statement about the OPPOSITE limit... H << omega fails exactly at omega -> 0, and nowhere else does the approximation matter.'",
   "S_IF.md (read in full): §1 'flat FRW for every banked observable' (CONSTRUCTION DOCUMENT, ledger 0); §3 the action 'at fixed (omega, k)' with the d3k measure; §6 the exclusion rule and §7's pre-existing no-unconditional-static-statement fence.",
   "RUNG3_KEYSTONE_MAP.md (read in full): header 'NOTHING BANKED... may not be cited as content by any other artifact... banking remains the owner's' (AMBER-screened) — disqualifying D3a as banked content; D1-D6 (worldline stationarity exact; no global one-clock reduction except static-Killing; exponential-to-power-law clock divergence; D6 'the transport itself is the assumption, and it is currently unpaid'); §6.2 the register vocabulary gap; §6.3 'Bridging them is either a theorem (derived) or a new priced input'; §6.4 'K3 remains the presumed outcome'; C5 mu_linear 'fine internally'; C7 'unperformed reduction, not a contradiction'.",
   "PRIMITIVE_INVERSION_SCOPE.md §§5-6, verified verbatim: 'In flat slicing de Sitter has no global timelike Killing vector... different flows'; 'Standing prediction: this is where it breaks'; K2/K3 as the inversion question's pre-registered kills ('The question is REFUTED if...'), K3 'the most likely one after K2'; §4.3 thermal-time = 'No ledger gain. Relabeling.'",
   "THE COMMUTATOR (standard physics the corpus cites-by-use): xi = d_t − H x^i d_i, [xi, P_j] = +H P_j != 0 — no joint (omega_Killing, k) eigenbasis; exhibited in-corpus by calc/static_patch_tt_response.py's forced (omega, l, m) radial master equation (f(fpsi')' + (omega^2 − V)psi = 0, chart f = 1 − H^2 r^2) and matching the Tier-4 kernel's eps_H = (104/9) H^2/omega^2 as the obstruction's leading isotropic footprint.",
   "Frozen contracts, verified on the files this gate: calc/gw_tensor_friction.py lines 6-7 'single FRW COSMIC clock throughout... no static-Killing or e-fold quantity is imported'; PHYSICS_LEDGER/WALL_A_A3_DECLARATIONS.md — BD mode functions 'positive frequency w.r.t. the BD vacuum in the far past eta -> −infinity' and the static-patch thermal state 'PROHIBITED as an input for the primary run; permitted only as a cross-check AFTER (ii) is answered' (full scoped clause).",
   "MACHINE CHECKS run this gate: git diff d5e9a99..67059a8 tier changes = exactly the eight named nodes, all shown -> derived-pending, deltas unchanged (rung1 +4, rung2 −1, six at 0); x_no_pin_theorem NOT demoted; HEAD (d4a385a; briefed e76953a a verified ancestor, zero diff on any evidence file) GRUT-scope net = +16, vacuum-cluster = 0, 74 nodes; 'S6' = 0 occurrences in claims.json.",
   "P1A_EDGE_ADJUDICATION_01.md (ruled EDGE_CORRECT): 'the static-patch discharge mechanically reverses the demotions as a register change requiring its own re-certification'; 'the flow node's two overturning routes stay live' — two, not three. P1B_ADJUDICATION_01.md (LEGISLATION-REQUIRED, stanza OPEN): 'a static-patch discharge re-promoting rung1 would legally re-land at shown with +4 prose-priced inputs before any ruling — the exact configuration this pass questions.' provenance/OPEN_PASSES.txt P5 stanza: the migration's first commit meets two pre-declared instrument false-positives.",
   "vc_averaging_commutes.statement + provenance/OMISSION_STANDARD_v2.txt (read this gate): the deliberate half-booking and the recorded refusal to book 'homogeneity and isotropy' off vc_ir_gravity_is_gr (actual deniers AFFIRM it); the R1 fence text and the cluster-scoped 'FLATNESS PASSES' worked example; R3's bar on cross-scope inheritance — the disclosed counter-precedent inside the omission package.",
   "rung2_kms_gate.ledger_note, verified verbatim: T_SUBSUMED adjudication 2026-08-02; 'on the declared background the Hadamard/KMS condition forces T = T_dS = H/2pi uniquely (any other T is singular at the horizon...)'; the horizon-temperature RELATION 'already paid as rung5's Unruh-T import' — the in-corpus instance of the unpaid D6 transport doing ledger-affecting work under the flat-FRW declaration.",
   "kk_static_transfer (node extracted whole): ANSWERED 2026-08-09 at (b)+(c); 'UNTIL THIS IS COMPUTED, the quasi-static x inherits NO sign from the passivity floor' — the omega->0 fence agrees with the landed answer, and the whole transfer question is the sign of chi_inf, which passivity/causality/KMS are structurally blind to.",
   "response_lorentz_covariance: the register's only executed retirement, by DEMONSTRATION ('DEMONSTRATED unimposed, not assumed'), explicitly declining to price the background — precedent for demonstration-based retirement, none for declaration-based retirement."
  ],
  "not_decided": [
   "rung3_single_pole's separately-reserved tier question (hard-fenced; its content used as evidence only; the 2026-08-18 reservation stands untouched).",
   "Whether omission candidate Omega-A books as a +1 omission, as a split question of eft_operator_basis (the S4-precedent adjudication — S6 is visible in that statement), or as declaration-carried at ledger 0 (the S_IF §1 reading) — the owner's adjudication under OMISSION_STANDARD_v2, with the vc_averaging_commutes counter-precedent and the R3 no-cross-scope-inheritance rule disclosed in the package.",
   "The merit of the transmitted scoped/worldline rewrite (package item C): whether the owner amends the flow node's clause, banks D3a, accepts the S_IF scope tags and the omega<~H refusal, and re-adjudicates rung3's reserved question in that light — owner legislation, outside the clause's two routes and outside this gate's question.",
   "Omega-B's classification (new omission vs split/completion of rung2's T_SUBSUMED adjudication) — already prose-recorded as unpaid, so its 'newness' is itself part of the owner's call.",
   "P1B's booking-format legislation (OPEN): what tier rung1_inin_formalism legally re-lands at on any future re-promotion with its fourth input still open — this gate's verdict removes the immediate trigger but does not touch the question.",
   "The physics deciders named for any future re-opening: whether the wall-A assembly reduces the gauge-invariant TT response to any one-time kernel (D3b's open reduction); whether the cosmic/static bridge exists as a theorem (§6.3); the interacting static-patch self-energy Sigma (rung3's owner correction); a K_R-free reformulation (the clause's own second route, untested by anyone).",
   "Whether the register should acquire background/foliation/clock vocabulary fields (§6.2) — forwarded to the owner's legislation queue; this gate edits nothing.",
   "The demotions' standing paperwork and CERTIFICATION_02 lineage (frozen per the protocol's hard fences; the hypothetical reversal mechanics are priced in the cost accounting, not executed).",
   "Which vocabulary the resident enforces per scope (OPEN_PASSES P5) and the four stale-net standing documents (P6) — pre-existing owner items noted only because the hypothetical migration's first commit would meet them."
  ],
  "what_would_decide_if_underdetermined": "The verdict is not UNDERDETERMINED — but per the protocol's discipline of naming deciders, the re-open conditions are recorded: (1) U1, the wall-A assembly reduction — derive whether the gauge-invariant assembled TT response Sigma(x;x') reduces to an along-worldline one-time kernel, to the static-Killing form, or to neither (D3b names this as never performed; C7 classifies it 'unperformed reduction, not a contradiction'); a derived reduction would re-pose the discharge question with the label geometry settled. (2) U2, the cosmic/static bridge exhibited as a THEOREM (keystone §6.3's dichotomy) — this would retire the D6 transport input and remove the largest cost line, re-opening cost accounting. (3) Owner ratification decisions that change the corpus this gate ruled on: banking D3a (making exact worldline stationarity citable content), legislating register background/foliation/clock vocabulary (§6.2 — giving any re-declaration a field to live in, curing the structural-silence objection), or ratifying the transmitted scoped/worldline rewrite (which retires the +1 by amendment and moots the discharge question in a different way — at the named costs, including deciding rung3's reserved premise). (4) The clause's own second route, untested: exhibit a formulation of every banked kernel result with no K_R(omega,k) anywhere — e.g. fully time-domain — which would show the flow was never used and retire the node without any background question. (5) U4, the interacting static-patch Sigma (rung3's owner correction: everything bearing on the anchor rests on it) — not a discharge decider per se, but the computation that would tell the framework whether the omega->0 territory the +1 currently pays for is worth paying for. Any one of (1)-(4) landing warrants a fresh gate; none existing today is why today's answer is UNAVAILABLE rather than open."
 },
 "inert_outputs": {
  "A_obstruction_record": "STATIC_PATCH_DISCHARGE_GATE_01.md",
  "B_omission_package": "OMEGA_OMISSION_PACKAGE_01.md (candidate Omega-A background_spatial_translation_flow +1 assumed; rider Omega-B D6 clock transport; instrument item: background/foliation/clock register fields)",
  "C_transmitted_without_license": "the scoped/worldline rewrite, costs named, neither granted nor killed"
 },
 "flow_node_status": "UNCHANGED -- assumed, +1, now with its cash value stated: it prices the license for future claims in the label-divergence regime",
 "reopen_conditions": [
  "U1: wall-A assembly reduction (worldline / static / neither)",
  "U2: cosmic/static bridge exhibited as a THEOREM",
  "owner ratifications changing the ruled-on corpus (bank D3a; book Omega-A/B; keystone 6.2 vocabulary fields)"
 ],
 "canonical_GRUT_RAI_modified": false
}