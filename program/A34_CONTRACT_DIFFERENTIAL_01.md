# A34_CONTRACT_DIFFERENTIAL_01 — RESULT

**OUTCOME: OBJECTS_NOT_COMPARABLE — by computation, on the frozen sources, and the
protocol's own premise was the error.**

The frozen A3-4 TT response and the frozen Tier-3 contract loop are **two different
one-loop diagrams**, not one physical content in two representations. They share an
external-slot frame and nothing else. The comparison the charter posed cannot be made;
nothing is earned toward the owner's ceiling. Per condition 10 the deriver stopped on
comparability *first*; a supplementary partial comparison on the one shared sub-object
was executed scratch-only and confirms Wall-A's contract-loop statement one-sidedly.

Executed 2026-09-16 under `A34_CONTRACT_DIFFERENTIAL_01_PROTOCOL.md` (frozen `e8044bb`).
Surveyor → deriver → adversarial checker (CONFIRMED_WITH_CORRECTIONS; no step refuted;
ΔO and the sector isolation re-implemented independently, matching the frozen cache to
4e−14; forbidden-inference scan clean). Zero register edits; both artifacts read-only.

## 1. The premise that was false — a builder error in the frozen protocol

I wrote into the protocol: *"The same vertex feeds both — this is what makes a
comparison conceivable"* and described A3-4 as *"built from the Tier-1 vertex through
the A1–A3 assembly (source vertex + observer vertex + external-mode corrections)."*
**Both statements are false on the record**, computed from the frozen sources:

- **A3-4** is built from the **A1 graviton–matter vertex** — one graviton slot, two
  **massive scalar** legs (L = √−g[g^{mn}∂φ∂φ/2 − m²φ²/2]; symbols {a, κ, m, p, q}),
  a "fish + seagull" with **flat Minkowski Feynman propagators** dressed by polynomial
  Option-B insertions, in-out contour with the signed retarded branch law, MS-subtracted,
  **fixed-ω representation only**. Its "declared TT object" is the h_μν self-energy
  contracted on TT plane-wave polarizations at finite (ω, k) — **not Bardeen variables,
  not a dressed field, not a gauge-invariant assembly**. The "source vertex + observer
  vertex + external-mode corrections" phrase is the corpus's description of the
  scalar-probe Class-C assembly, which the corpus records as *"does not exist"* for the
  graviton probe (2026-09-08 precondition gate). The A-stage record never calls the A1
  vertex "Tier-1" (grep: zero hits).
- **Tier-3** is built from the **Tier-1 pure-gravity cubic vertex** (hhh, 26,032 terms;
  symbols {H, u, e_i, p_i}; no m, no scalar), with **massless TT-gauge BD graviton**
  Wightman legs (q³W → κ²H²), in-in contour, unsubtracted, divergent **only in the
  fixed-Δ / noise representation**.

**Six obstructions, each computed or corpus-declared:** (1) different vertex; (2)
different internal field — the sector to be isolated (the constant TT mode, PATCH-LOCAL
gauge) is a property of the Tier-3 graviton line that the A3-4 scalar line **does not
possess** (a scalar carries no gauge orbit; its dressed line is finite at l → 0, no 1/q³
term) — so "removing the sector" from ΔO has no meaning on the A3-4 side; (3) no shared
representation in which the contract divergence and the A3-4 object both live; (4)
in-out+branch-law vs in-in Wightman — their retarded equivalence is the corpus's open
Wall-C prerequisite; (5) diagram class (judgment grounded in 1–2): in any theory with
both fields these are *additive* contributions (matter loop + graviton loop) to one
self-energy, not two representations of one contribution — ΔO is a difference of
unrelated diagrams; (6) the corpus's own rulings — Ruling 2 "mathematically distinct";
K_R charter STEP 1 "NOT derivable from the matter object," STEP 10 "no analytic-structure
conclusion transfers."

## 2. The partial comparison (supplementary; scratch-only; changes nothing)

On the only legitimately shared sub-object — the external-slot frame, e₊ ⊗ e₊,
K = (ω, 0), O(H²), fixed-ω retarded — the deriver executed ΔO. The divergent part of ΔO
is exactly the contract loop's own constant-TT-mode sector (PATCH-LOCAL gauge), re-derived
at loop level with the frozen instrument (const-sector pole coefficients 4ω³(−iΔω−3)/15
and 4ω⁴/15 equal the full cache's; remainder pole-free; the fixed-ω log −8ω²/15 is the
sector's retarded transform) — **but vacuously on the A3-4 side**: A3-4 has no divergent
part and no internal graviton line, so ΔO_div = O_contract,div. **This is Wall-A's
statement about the contract loop, confirmed at loop level by an independent
implementation; it is not a relation between the two objects and supports no
DIFFERENCE_* outcome.** The finite O(H²) content is mismatched in dictionary-free terms
(gapped vs gapless; m-dependent vs scale-free; NO_DIFFERENCE excluded already at H⁰ by a
rational-factor mismatch in the exactly shared massless representation).

**The standing negative result stands, now by computation:** A3-4's finiteness is no
evidence about the contract loop. The checker's framing correction is carried: the
owner's ceiling sentence was what a *successful* comparison would earn; nothing is
earned; the available sentence is only that the contract loop's O(H²) divergence at
k_ext = 0 is carried by the constant-TT-mode leg weight (PATCH-LOCAL gauge), as Wall-A
already said.

## 3. Yields that came free (for the owner; none acted on)

- **CORPUS FLAG — a latent sign defect in two frozen A3-4 instruments** (read-only
  here, not fixed): `wall_a3_4_tt_layer.py` and `wall_a3_4_adjudication.py` `quad_atom`
  with default `branch=−1` evaluate the R-atom branch as D + iη (+i0) while the G-branch
  is −i0 — the **reverse** of the A3-2 referee's frozen −i0 law. **INERT on the frozen
  record** because every executed gate evaluates only the flat P₂ channel whose sole atom
  is G[0,0,2]; any future reuse on R[n,np,e ≤ −1] atoms above threshold would be wrong.
  Demonstrated by the checker on R[3,1,−1] (sign flip) and on a₂ at ω = 3 (correct
  1.35618 vs 1.66839). The deriver's and surveyor's finite-m above-threshold numbers in
  the .json are affected and are superseded by the checker's corrected values; no
  conclusion changes.
- **Strengthening:** the Tier-3 H¹ sector vanishes as an *exact rational identity*
  in (e^{iqΔ}, e^{iqu_b}) in both combinations; H⁰ and H² retarded integrands are exactly
  u_b-free, so the base-time alignment is an identity at this order (the record's
  "accidental to the H² budget" is a theorem for this object).
- A3-4's H² TT coefficient is regular and continuous at k → 0; the k_ext = 0 locus that
  hosts the Tier-3 power class is not a distinguished point of the A3-4 object.
- The survey's a₁ ≠ 0 was wrong; a₁ ≡ 0 for the TT view (checker-confirmed to 1e−29).

## 4. What this establishes, and what it does not

Establishes: the cheap bridge does not exist on the record — the two objects are not
the same physical observable and their difference is not accountable, because they are
different diagrams. The "three possibilities" the owner named (gauge contamination /
genuine IR piece / not the same object) resolve to the third, by construction.

Does NOT establish: anything about whether the gauge-invariantly assembled graviton loop
is IR-finite (condition 6); nothing about Ward (7); nothing about s ≥ 2 (8). **The
question the owner boxed — does the fully gauge-invariant loop remain IR-finite? — is
untouched by this result, and the Ward/gauge-invariant construction is now justified as
the next step on the owner's own terms** ("if it fails, you've lost almost nothing …
then the Ward/gauge-invariant loop construction becomes justified"). It remains
HARD-STOPped pending charter.

## 5. On the face

The comparison failed on a premise I wrote into the frozen protocol without checking the
frozen sources — the second protocol-freeze error in two days (the first: the
"discrete Killing tower" in the Wall-A protocol). Both were caught by the surveyor on
the record, not by me. The pattern is the same both times: a plausible characterization
of a frozen object, written from memory of the *program's language* rather than from
the object's *source*. On the ledger as its own entry: **freeze protocols only against
the sources, never against the vocabulary.**

---

*Filed 2026-09-16. Surveyor, deriver, checker filings in `A34_CONTRACT_DIFFERENTIAL_01.json`;
scratch under scratchpad; repo verified clean at `e8044bb`. Fork gated; nothing banked;
Ward HARD-STOPped; Born 2A/2B unchanged.*
