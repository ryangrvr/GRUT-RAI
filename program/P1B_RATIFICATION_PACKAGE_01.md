# P1B_RATIFICATION_PACKAGE_01 — the owner's decision on result-tier semantics

**Status: RECOMMENDATION, NOT LAW. Nothing here is enacted.** P1B_ADJUDICATION_01 ruled
LEGISLATION-REQUIRED: booked law does not define what a result tier (`shown`/`derived`)
asserts about a node's *declared entry-price inputs*, and the register has operated two
incompatible semantics with constitutional footprints on both sides. The choice is new
owner law. This package frames it principle-first with the mechanics and sub-decisions
enumerated, so ratification can be one selection plus (for option A) its sub-choices.

## The principle to rule on

**Option A — input-carrying result tiers (RECOMMENDED).** A result tier is a predicate
over inputs: named open inputs of result-tier nodes must be machine-visible as
`depends_on` edges to booked nodes, whereupon the ruled tier rule (P1A) caps the node at
`derived-pending` until discharge. P1A's semantics, generalized past booking format.

**Option B — verification-grade result tiers.** The tier grades verification against
primary literature; the ledger triad (`ledger_delta` + `laundering_ok` +
`stance_justification`) carries declared inputs. CHARTER §1's own settled-state gloss at
rung1; the 2026-08-09 waiver instrument's apparent design intent.

Neither is derivable from booked text — that is the adjudicated finding. The referee
recommends **A** because the program's every recorded defect-shape points the same way
(CHARTER §7: prose the machine can't see "banks as a silent PASS"; R5's rationale:
"the register must be able to COMPUTE this, not merely assert it in prose"; the P1B
stanza's own warning: "A GREEN SUITE WOULD THEN BE THE ANSWER, AND NOBODY WOULD HAVE HAD
TO GIVE ONE") — while stating plainly that this is direction, not derivation.

## Option A's sub-decisions (each is the owner's, enumerated by the referee)

1. **Result-tier vocabulary.** Quantify the rule over resident `RESULT_TIERS`
   ({shown, derived} — genuinely prospective, zero present-day effect) or over the
   auditor's `LAUNDERABLE_TIERS` (state the rung1-today consequence explicitly before
   choosing). RECOMMENDED: resident vocabulary, prospective.
2. **Input-node booking.** rung1's four inputs (system/bath split; Gaussian truncation;
   background Lorentzian causal structure; 4d-covariant Ward gauge-orbit zero) do not
   exist as nodes. Each must pass its own R1 membership fence before booking ("an input
   is something a named position denies" — the Ward zero and Gaussian truncation
   plausibly pass; anything calibration-shaped is a rider, not a node). C3-class
   per-candidate judgments, never run in the adjudication.
3. **Delta placement.** New input nodes at zero delta with the price staying home on
   the host (preserves the 2026-08-04 n-sided double-count arithmetic and the auditor's
   blind-sum semantics). RECOMMENDED over relocation.
4. **Guard semantics.** FLAG-FOR-FIREWALL, never BLOCK (consistent with the resident's
   booked flag discipline). Enacted as a dated successor standard, never an edit to
   OMISSION_STANDARD_v2.
5. **The waiver instrument.** Whether `laundering_ok`'s shown/derived domain is retired
   (a repeal only the owner can enact) or left as a dead letter under A.

## Option B's obligation (if chosen)

Disclosure tightening: the gate's WAIVED-BY-STANCE block prints prose-priced inputs as
*ruled-sufficient* with the ruling date, so the asymmetry is worn openly as law rather
than carried as an unscreened stance.

## On the face, either way

- **The §4 tension (the strongest surviving counter, unresolved):** CHARTER §4's
  "Awaiting a computation ⇒ derived-pending, not derived" is format-blind in its own
  text, and rung1's fourth input awaits a named computation (KC5-reserved). Any future
  re-promotion of rung1 to `shown` with it open sits in direct textual tension with a
  booked CHARTER clause — under BOTH options until the owner addresses it.
- **The unguarded window:** until this ruling lands, a static-patch discharge reversing
  the eight demotions would legally re-land rung1 at `shown` with +4 prose-priced
  inputs — the exact configuration P1B questions.
- The adjudication's verdict was the deference-favorable direction; the demonstration
  was carried from booked text, and the coincidence is stated, not hidden.

## On ratification (mechanics, pre-stated)

Option A: book the fenced input nodes (owner-relayed, per candidate), wire the edges,
enact the successor standard + FLAG guard, close P1B with the ruling appended.
Option B: apply the disclosure tightening, close P1B with the ruling appended.
Either: the stanza's history stays byte-identical; suite + expected_red re-run; commit
citing this package and the ruling; push.

*Prepared 2026-09-13 from P1B_ADJUDICATION_01 (LEGISLATION-REQUIRED). Nothing enacted;
rejection costs nothing — the stanza is already correctly OPEN.*
