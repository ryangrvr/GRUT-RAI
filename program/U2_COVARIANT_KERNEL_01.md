# U2_COVARIANT_KERNEL_01 — RESULT: can a covariant kernel replace the joint (ω,k) representation?

**RESULT: MIXED = B, both sectors — with Result A WITHDRAWN on the record.**

Executed 2026-09-14/15 on the U1 specification (B1 assembly reduction; B2 bridge; K1
two-time / K2 z-kernel candidates; the wavepacket residue). Method, lean per the owner's
economy directive: one DERIVER (computations run, not asserted — 8 sympy/mpmath
computations C1–C8), one ADVERSARIAL CHECKER (independent re-derivation; verdict
REFUTED on one load-bearing step), one RESOLVER (ruled the dispute step by step with
its own computations). Zero register edits; rung3's tier untouched. Full filings in
`U2_COVARIANT_KERNEL_01.json`.

## The result, by sector

**(B, matter-FRW — derived, undisputed, checker-confirmed.)** On FRW with Ḣ ≠ 0 there is
no timelike Killing vector *and no dilatation*, so **no frequency label is exact at any
k, k = 0 included**. The only exact structure is the E(3)-covariant two-time retarded
kernel K(t,t′;k) with k exact (S6). The cosmological consumers — w(z)'s evolving route,
μ's kernel inference at k ~ H₀, χ_static's IR segment on the actual background — need
the two-time kernel over memory times ~1/H₀ as a **NEW priced input with O(1) observable
freedom** (C7: three natural two-time extensions of the same Debye τ₂ = 1/H kernel
differ by a factor ~2 in the response at every z; the adiabatic ω-space substitution by
another O(1); the adiabatic license Hτ₂ ≪ 1 is negated by the route's own τ₂ ~ 1/H₀).
No covariant completion can supply that license, because none exists there. The banked
frequency-flat / time-domain / H≪ω pieces are untouched.

**(B-conditional, exact dS at k = 0 — derived kinematics with an OPEN antecedent.)** On
exact dS every ξ-covariant kernel (ξ = ∂_t − H x·∂_x, the dilatation; [ξ,P_j] = H P_j;
ξ = ∂_t *exactly* on the worldline AND on the k = 0 sector — C1) has the form
K(t,t′;k) = (aa′)^{3/2} G(τ, k e^{−H t̄}) — verified exactly on the corpus's own Tier-2
BD modes (C2). Its **k = 0 sector is exactly stationary in cosmic time — "D3c", derived
here** — and is reached in the Kubo order, so it is the natural exact host of
η = lim Im σ̃(ω)/ω. The banked Tier-4 K_R(ω) is the O(H²) conformal-Wigner truncation of
that form for its absorptive part (C3; ε_H = 104/9·H²/ω² is exactly the ratio of the H²
and H⁰ absorptive coefficients; the H²/ω² *scaling* is the forced content, the 104/9
coefficient is chart-dependent — 253/18 in the cosmic-symmetric normalization).
**BUT the antecedent — ξ-invariance of the assembled object — is OPEN ON THE RECORD**:
for the corpus's own one-loop k_ext = 0 contract object, the pre-registered **fork (ii)
FIRED at O(H²)** (WALL_KR_TIER3_LOOP_RESULT.json `fork_fired_sectors = [2]`; owner
ruling IR-B; 2026-09-01 decision: H² local sector left fork-gated, "introducing an IR
scale later would constitute a NEW DECLARED INPUT"). The deriver's condition ("fork (ii)
does not fire; no new input") was **false on the record**, and its cited evidence (the
u_b-free cone coefficients, Im Σ^{H1} = 0) is provably blind to the failure mode — both a
covariant and a comoving IR completion pass those gates identically and differ only at
O(H³). Resolver-verified criterion: **D3c and cosmic-time KMS exist for the completed
object iff the fork-(ii) IR completion is dilatation-covariant** (physical scale
εH a(t̄) → ln(2ε tanh(Hτ/2)): stationary, KMS-periodic; a comoving scale gives a secular
−H t̄ and breaks both). **The exact host is a theorem conditional on a NEW priced input,
and that input RELOCATES the stationarity/+1-flow assumption into the covariance of the
prescription — it does not retire it.** The +1 at k = 0 on dS now pays for exactly that.

**A — WITHDRAWN.** Criterion 3 (a controlled ω ~ H prediction) fails on the record: A₀,
the ω⁰ coefficient deciding Ohmic vs super-Ohmic, sits in the owner-ruling's
prescription-fed slot; an exact-chart or O(H⁴) re-run of the frozen vertex does not
converge (the exact unexpanded kernel diverges: noise 2H⁴κ⁴/q⁴, retarded −4iΔH²κ⁴/q).
Selecting A would have promoted a possibility to a result — the exact move the
2026-08-21 constraint forbids, and the checker caught it.

**C — not selected.** Every failure found is a domain boundary or a named input; no
contradiction was derived (checker concurs).

**D-class working material — real, but NOT forced for the corpus's object.** For
IR-safe polynomial vertices (α > −1 at every H order — fully confirmed on toys, the
disputed coefficients resolved in the deriver's favor: H² cross term −18, third toy
(9H²+4ω²)(25H²+4ω²)/32): the exact spectral function is ρ(ω) = tanh(πω/H)·P(ω) with P a
terminating even polynomial; a meromorphic ladder at ω = −i(n+½)H; exact cosmic-time
KMS; the universal +5/2 chart shift; an A₀ Ohmic criterion. Correct theorems for that
class. The corpus's frozen contract vertex is IR-unsafe at O(H²) on the record and is
not in that class without the new input.

## B2 — the cosmic/static bridge

Retired **only as the kinematic identity bridge**: ω_T = ω_cosmic on the k = 0 / l = 2
sector and on the worldline (T = t on the axis) — unconditional, verified kinematics.
The **thermality/KMS transfer** at k = 0 is conditional on (i) a dilatation-covariant IR
completion and (ii) wall-A Q4 at k = 0. For k ≠ 0 dS-slice objects, D6 remains an
effective statement priced by (H/ω)(k_phys/ω)²; on matter-FRW it is void (T_dS
undefined). **D6 is not retired.**

## The wavepacket residue (stress-(ii)) — SETTLED, two parts (C6)

Δω ~ H forces a drive of duration T ≳ 1/(2H), over which ΛCDM's background changes by
Δln H = 0.23 (z=0) to 0.69 (z=2) — the symbol is sampled over an O(1) background change.
A band-limited drive at ω₀ ~ few×H **does** operationally determine the local in-band
exponent (Ohmic / super-Ohmic / Debye cleanly distinguishable: effective slopes
0.98 / 2.47 / −0.68), but **not the ω → 0 class**: an explicit passive pair with
identical band averages to 10⁻⁵ has η finite in one and η → ∞ in the other. η's
boundary is therefore the sharp form: *no exact-label operationalization; class-probe
closed; in-band exponent open.*

## Observable consequences (the four divergent rows)

- **η (rung3):** UNSUPPORTED_AS_STATED → **CONDITIONALLY DEFINABLE**: the host exists
  iff a dilatation-covariant fork-(ii) completion is declared (new priced input); its
  absorptive O(H²) part is exhibited consistent with covariance; its local sector is
  IR-log-divergent on the record; A₀ is not a finite in-pipeline computation. U1's
  operational judgment (no apparatus realizes the superhorizon-coherent k = 0 shear
  drive) stands, separated from definability. rung3's reserved tier: untouched.
- **w(z):** the w = −1 flat route untouched; the evolving route is **sharpened to
  UNSUPPORTED_AS_STATED for a derived reason** — no exact label on matter-FRW at any k;
  the route needs the two-time kernel as a new input with a factor-~2 freedom at every z.
- **χ_static's IR segment:** the H-free refutation leg untouched; leg (c) at k → 0 on
  exact dS is finite and sign-definite (≥ 0) on the k = 0 host under the same two
  conditions; on matter-FRW it inherits the two-time input.
- **μ kernel-inference at k ~ H₀:** the measured numbers untouched; the inference of a
  genuine x(ω,k²) at k ~ ω ~ H has no exact host and no small parameter — label drift
  (H/ω)(k_phys/ω)² → O(1) there (C5), and on ΛCDM the symbol is smeared over
  Δln H = 0.2–0.7.
- **Cross-cutting refinement of the finding:** "leading obstruction = ε_H" splits into
  **three distinct H-orders that must not be merged** — label drift (H/ω)(k_phys/ω)²
  (zero at k = 0), the even-H curvature correction H²/ω² (ε_H; forced scaling,
  chart-dependent coefficient), and the O((H/ω)·log) normalization of the physical-h
  kernel. The **DomainRejected fence at k = 0 is a truncation fence AND an IR-fork
  fence.**

## Costs on the face

The Result A the deriver reached — and I would have wanted — was withdrawn by the
adversarial checker on a record citation the deriver missed (the fired fork), with its
evidence shown blind by construction: the recorded bias direction, caught by the
instrument, in the physics itself this time. Two deriver over-statements corrected
("forced" → "exhibited on the free propagator and IR-safe toys"; "D6 RETIRED at k = 0" →
identity bridge only). One checker error corrected the other way (the C4 coefficient
dispute — the deriver's numbers were exact). One convention slip (the ρ = 2 Im G_R
factor of 2). AMBER keystone content used throughout as evidence; nothing here is banked.

**What the owner now holds:** the fork-(ii) IR completion is no longer a bookkeeping
item — it is the single declared input on which the exact k = 0 host, cosmic-time KMS,
η's definability, and B2's thermality transfer all hang, with a derived criterion for
which prescriptions preserve them (dilatation-covariant) and which destroy them
(comoving). That is where the physics is.

---

*Filed 2026-09-15. Source record: WALL_KR_TIER3_LOOP_RESULT.json (fork_verdicts),
WALL_KR_TIER3_FORK_INVOCATION.md, WALL_KR_TIER3_FORK_ADJUDICATION.md §4,
WALL_KR_H2_IR_OWNER_RULING.md, RUNG3_BRIDGE_SCOPE, RUNG3_KEYSTONE_MAP D1–D6 (AMBER),
S_IF.md, WALL_A_A3_DECLARATIONS.md, calc/static_patch_tt_response.py.*
