# OMEGA_OMISSION_PACKAGE_01 — owner adjudication under OMISSION_STANDARD_v2

**Status: INERT — NOTHING BOOKED.** Surfaced by STATIC_PATCH_DISCHARGE_GATE_01
(NEW-OMISSION-SURFACED component). Booking an omission is the owner's act; this package
presents the candidate, its conjunct evidence, **and the adjudication inputs that cut
against booking**, so the ruling is made with both directions on the table. Rejection
costs nothing — the prose record stands as it is.

## Candidate Ω-A — `background_spatial_translation_flow` (one atom)

**Proposed node:** the declared background carries spatial translations + rotations of
the comoving slicing under which the setup is invariant, so kernels depend on x − x′
alone and the single-k label, the d³k measure, the SVT frame, and the SO(3)-at-fixed-k
enumeration are definable. Proposed tier `assumed`, ledger +1, GRUT scope — **the exact
spatial twin of the booked flow node**, whose temporal half was booked at +1 on
2026-08-18 as an omission with mandatory R5 wiring.

**Conjunct 1 (discharge-blocking — established, stated, AND used):** suppose it false —
kr_contract's G₀_R(ω,k), mu_linear's k²Ψ bookkeeping, x_no_pin's c_i(ω,k²), zeta's
x(ω,k²), and S_IF §3's d³k integral can none of them be established, stated, or used.

**Conjunct 2 (invisible in the register; multiply presupposed):** 'S6' occurs ZERO times
in `provenance/claims.json` (machine-verified 2026-09-13, twice independently). Prose
homes only: S_IF.md §2, `calc/operator_basis.py` header, `calc/RESULTS_operator_basis.md`
(and a prereg reference). The only priced flatness/homogeneity in the repository is
vacuum-cluster-scoped and sealed — R3 bars citing it across scopes in either direction.

**R1 membership fence (to be run by the owner, on GRUT-scope physics specifically):**
candidate deniers = inhomogeneous/backreaction programs (in-register instance:
`vc_averaging_commutes`, other scope; timescape rider noted). The fence is the owner's
to run; this package does not pre-certify a pass.

**MUST-DISCLOSE — adjudication inputs cutting AGAINST booking (verified verbatim):**
1. The 2026-08-04 sweep raised the compound candidate and **deliberately did NOT book
   "homogeneity and isotropy"**, because hanging it off `vc_ir_gravity_is_gr` would
   manufacture a false edge (that node's actual deniers, f(R)/Horndeski, AFFIRM
   homogeneity). A deliberate past refusal is a precedent the owner may re-affirm.
2. **S6 IS visible in `eft_operator_basis`'s statement** (a delta-0 to-derive question
   node) — so the S4-precedent adjudication (entailment-of-a-named-concept vs
   appearance; split-question vs omission) is genuinely open: Ω-A may be a SPLIT of
   eft_operator_basis rather than an omission, or declaration-carried at ledger 0.
3. The declaration-as-booking reading of S_IF §1 ("flat FRW for every banked
   observable") — countered but not closed by the fact that the identical reading was
   available for the temporal half on 2026-08-18 and the program booked +1 anyway.

**R4 atomisation (if booked, book at most the one atom):** the state half is ABSORBED
(A3/BD declaration + the acknowledged `vc_state_expectation_functional` shared input);
GRUT-scope spatial FLATNESS is a separate split/atom question; parity-evenness is a
rider ("inherited, not chosen"); the fixed-slicing choice is a convention and fails the
fence.

**R5 wiring plan (mandatory if booked, in the same edit):** `depends_on` edges from
`eft_operator_basis`, `x_no_pin_theorem`, `mu_linear`, `kr_contract_retarded_tier4`,
`zeta_interior_family`; dated cross-pointer on `background_time_translation_flow` (two
halves of one background-symmetry commitment). Note the tier consequences under the
ruled P1A law: wired result-tier dependents cap at derived-pending — the wiring plan's
tier ripple must be priced in the booking decision, not discovered after (mu_linear is
already derived-pending; x_no_pin_theorem's current tier governs).

## Rider Ω-B — the D6 clock-transport bridge

Already prose-recorded as **"currently unpaid"** (RUNG3_KEYSTONE_MAP.md D6), so its
"newness" is itself part of the owner's call: dispose as a new omission OR as a
split/completion of rung2's T_SUBSUMED adjudication (2026-08-02). Facts on the record:
rung2's ledger_note runs a static-Killing horizon-regularity argument (T = T_dS = H/2π
"singular at the horizon" otherwise) under the flat-FRW declaration; the
horizon-temperature RELATION is separately paid (rung5's Unruh import); only the
cross-clock TRANSPORT is unpaid — and the clock audit's own refinement narrows the
unpaid part to non-worldline (assembled/tower) objects, D3a having licensed the
worldline case.

## Instrument item (owner legislation queue, alongside P1B)

The register has **no field for background / foliation / clock** (keystone §6.2 — "how
the C1/C2 mismatch passed two pre-registrations"). Any future re-declaration is
structurally silent until this exists. Forwarded, not enacted.

---

*Prepared 2026-09-14 from STATIC_PATCH_DISCHARGE_GATE_01 (referee's package (B),
transmitted with its counter-precedents intact). Nothing booked; no node, tier, edge,
or stanza touched.*
