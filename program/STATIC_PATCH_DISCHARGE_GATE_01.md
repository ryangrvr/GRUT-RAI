# STATIC_PATCH_DISCHARGE_GATE_01 — RESULT

**VERDICT: MIXED — DISCHARGE-UNAVAILABLE, coexisting with NEW-OMISSION-SURFACED** (the
coexistence expressly permitted by the frozen protocol). UNDERDETERMINED was **rejected**:
every load-bearing determination is on the banked record — an absent theorem is a decided
fact about the corpus as banked, not an open question.

Executed 2026-09-13/14 under `STATIC_PATCH_DISCHARGE_GATE_01_PROTOCOL.md` (frozen
`e76953a` before execution). Method as frozen: corpus surveyor → three assigned-verdict
advocates + independent evidence verifier → mandatory referee; builder executed, did not
vote. This gate is a CHARTER §5.3 pre-screen: **it banked nothing and edited no register
node, tier, or edge** — the flow node, every stanza, and every frozen artifact are
byte-identical. One mid-run note: the first execution lost two roles to a session limit
and the harness refused to let the referee rule on an incomplete panel; the run was
resumed with the completed roles cached and the dead roles re-run live.

## 1. THE PHYSICS FINDING (the referee's paragraph, verbatim — the gate's yield)

> At H ≠ 0 the framework can hold exactly one of its two exact spectral labels, never
> both: a global single-frequency label requires a timelike Killing flow, which de
> Sitter supplies only as the static-patch boost ξ = ∂_t − H x^i ∂_i on a chart of
> extent r < 1/H about one worldline, and that flow fails to commute with the comoving
> spatial translations that define k ([ξ, P_j] = H P_j — standard dS isometry algebra
> the corpus itself exhibits by being forced into an (ω, l, m) multipole decomposition
> the one time it computed on the static patch). An exact ω therefore costs the global k
> label, and the exact k label of flat FRW slicing costs any global ω — stationarity and
> homogeneity trade off at order H, and the joint (ω, k) label every banked kernel
> statement is written in is an effective object whose leading obstruction is exactly
> the corpus's own computed control term ε_H = (104/9) H²/ω², with only two exact
> reductions, both already derived in the corpus's working papers: along a single
> comoving worldline in cosmic proper time (D3a, at T_dS = H/2π), and globally in the
> static-Killing decomposition (D3b) — and no theorem bridging either reduction to the
> comoving-slice observables (the bridge is recorded in-corpus as "either a theorem
> (derived) or a new priced input", with the transport "currently unpaid"). The load
> distribution is the sharp part: every banked verdict sits where the trade-off is
> numerically invisible (H ≪ ω by 20-plus orders, time-domain, or frequency-flat),
> while the framework's defining open objects — the ω → 0 transport limit of the anchor
> conjecture, w(z) forced at ω ~ H(z), the static modulus reached through a full-axis
> dispersion integral — live exactly where the two labels diverge. So the +1 flow
> assumption is not paying for anything currently banked; it is paying for the
> framework's future claims in the very regime where no background re-declaration can
> supply the license, because the missing license IS the trade-off. That is the
> obstruction, and per the protocol the obstruction is the finding.

This confirms the corpus's own sealed standing prediction (PRIMITIVE_INVERSION_SCOPE.md
§5: *"Standing prediction: this is where it breaks"*) with the mechanism made precise —
K2's mechanism at at-least-K3 cost (K2/K3 used as the corpus's prediction, not as this
gate's rules).

## 2. Why UNAVAILABLE (six blocks, each verified on the record)

1. **The clause's antecedent is unsatisfiable by re-declaration at acceptable cost** —
   its two routes are a KV-carrying declared background or an exhibited K_R-free
   reformulation; the declared background is flat FRW for every banked observable
   (S_IF.md §1), no timelike KV.
2. **The named candidate fails on derived in-corpus physics** — [ξ, P_j] = H P_j: no
   joint (ω_Killing, k) eigenbasis; the exact post-re-declaration label is (T-frequency,
   l, m) on a chart of extent r < 1/H, while every banked kernel statement is written at
   fixed (ω, k) with the d³k measure. The re-declaration demotes every exact banked
   (ω,k) statement to an H≪ω effective statement — a corpus-wide re-scope with **no
   register field to be declared in** (keystone §6.2: no field mentions foliation,
   cosmic time, or proper time) — structurally silent, the protocol's named failure.
3. **Idle where safe, absent where needed** — the H≪ω consumers (Tier-4 kernel with its
   hard ε_H gate and DomainRejected at ω ≲ H; the GW band at H₀/ω ~ 10⁻²⁰) already
   carry the label through the recorded WKB license: the discharge buys them nothing.
   The consumers the +1 exists for (rung3's η = lim_{ω→0}; kk_static_transfer's
   full-axis dispersion integral; rung7's w(z) at ω ~ H(z); mu_linear's low-ℓ ISW leg
   at k ~ H₀) live exactly where the labels diverge or off-chart (w(z) is "not an axis
   quantity").
4. **Frozen-contract collisions** — `gw_tensor_friction.py`'s SPEC clock ("no
   static-Killing or e-fold quantity is imported") and, harder, WALL_A_A3's BD state
   defined at η → −∞ (a Poincaré-chart surface outside the static chart), with the
   static-patch thermal state prohibited as a primary-run input (scoped prohibition,
   verified at its full clause).
5. **Corpus-grounded KV alternatives also fail** — Minkowski contradicts every
   cosmological export; Connes–Rovelli thermal time is pre-registered at PIS §4.3 as a
   one-for-one relabel ("No ledger gain. Relabeling.").
6. **The scoped/worldline package is not this discharge** — the licensing phrase its
   advocate quoted does not appear in the frozen protocol (verified by reading it);
   transmitted separately, without license (§5 below).

**Cost accounting (hypothetical execution, priced to show why it fails):** retire −1
(flow) but acquire +2 to +3 R1-passing inputs (the chart+state declaration itself; the
D6 clock-transport bridge; the S6 frame conversion) → net +16 → +17..+18, strictly
worse, PLUS nonzero banked-result loss (rung7's w(z) de-homed; mu_linear's frame and
ISW kill leg unstatable; the Γ_T SPEC contradicted) PLUS the eight tier reversals'
re-certification burden (P1A §6; tier-provenance guard; rung1's re-landing exposed on
OPEN P1B).

## 3. NEW-OMISSION-SURFACED (owner adjudication; this gate books nothing)

**The spatial half of the very background-symmetry commitment whose temporal half was
booked at +1 on 2026-08-18 is carried as unpriced prose in GRUT scope**: 'S6' occurs
ZERO times in `provenance/claims.json` (machine-verified, twice independently — surveyor
and verifier); the only priced flatness/homogeneity is vacuum-cluster-scoped and sealed;
yet S6 licenses the k label, the d³k measure, the SVT frame, and the SO(3)-at-fixed-k
enumeration corpus-wide. The candidate booking (Ω-A, `background_spatial_translation_flow`,
one atom, tier assumed, +1) — **with the counter-precedents that cut against booking
disclosed on its face** — is `OMEGA_OMISSION_PACKAGE_01.md`, INERT until the owner rules
under OMISSION_STANDARD_v2. Rider Ω-B (the D6 clock transport, already prose-recorded
"currently unpaid" — new omission vs split/completion of rung2's T_SUBSUMED
adjudication) rides in the same package, as does the instrument item (the register lacks
any background/foliation/clock field — forwarded to the owner's legislation queue
alongside P1B).

## 4. Scope survival (per banked result; full table in the .json)

Everything banked SURVIVES UNDER THE STATUS QUO — that is the point: kr_contract (its
own ε_H gate already fences it), gw_tensor_friction (verbatim only if NOT re-declared),
rung4/GW bounds (background-free at ~20 orders H≪ω), rung2's gate code (pure ω-grid),
mu_linear (only under the status quo — its frame is comoving-slice-defined),
kk_static_transfer (verbatim; any ω ≲ H fence agrees with it), the passivity lemma
(frame-free; its application is S6-frame-bound), the vacuum cluster (sealed both ways),
response_lorentz_covariance (the register's only executed retirement — by exhibited
demonstration, a precedent the discharge could not match). rung3_single_pole: untouched
by hard fence; under re-declaration it would be re-posed in (ω, l ≥ 2), not rescued.

## 5. Transmitted WITHOUT license (owner legislation only)

Advocate AVAILABLE's scoped/worldline rewrite — retire the +1 into a declared-scope
stationarity declaration (bank D3a + the H≪ω WKB license + a register-level ω ≲ H
refusal; S_IF scope tags; eight tier reversals). Recorded with its true costs named: it
must BANK D3a (keystone map: "may not be cited as content"; AMBER; banking is the
owner's); it rides the WKB escape the owner ordered CLOSED in the node's own text on
booking day; its refusal de-licenses the ω→0 transport coefficient the flow node's R1
fence names as the +1's cash value — deciding the premise of rung3's reserved tier
question (hard-fenced here). The gate neither grants nor kills it.

## 6. Re-open conditions (named deciders, recorded though the verdict is not UNDERDETERMINED)

- **U1 — the wall-A assembly reduction**: derive whether the gauge-invariant assembled
  TT response Σ(x;x′) reduces to an along-worldline one-time kernel, to the
  static-Killing form, or to neither (D3b: never performed; C7: "unperformed reduction,
  not a contradiction"). A derived reduction re-poses the discharge with the label
  geometry settled.
- **U2 — the cosmic/static bridge as a THEOREM** (keystone §6.3's dichotomy): would
  retire the D6 transport input and remove the largest cost line.
- Owner ratifications that change the corpus this gate ruled on (banking D3a; booking
  Ω-A/Ω-B; the §6.2 vocabulary fields).

## 7. Verifier and referee dispositions of note

Verifier: SOUND_WITH_CORRECTIONS — six MINOR (part-label on the static calc's c=0;
x_no_pin_theorem was NOT in the demotion cone; a K3-ranking truncation; two extra prose
homes for S1–S7; the DESI-Σ0 scale gloss; rung8's order count ~21 not ~39) — all
adopted. Referee rejections of note: the AVAILABLE advocate's four load-bearing points
(including its reading of the protocol's licensing phrase — refuted by reading the
protocol) and, against the UNAVAILABLE side, K2/K3-as-rules and the D6-as-full-new-+1
scoring (qualified: an existing recorded-unpaid latent debt, per that advocate's own
honest-weakness note).

---

*Result recorded 2026-09-14. Protocol frozen at `e76953a`, not edited. Zero register
edits; the flow node's +1 STANDS, now with its cash value stated: it prices the license
for the framework's future claims in the regime where the two labels diverge. Full
survey, advocate filings, verifier report, and referee ruling preserved in
`STATIC_PATCH_DISCHARGE_GATE_01.json` (workflow wf_3716ed7d-f25).*
