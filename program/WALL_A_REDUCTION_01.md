# WALL_A_REDUCTION_01 — RESULT: one kernel or two?

**OUTCOME: MIXED, by derivation — and the decomposition is the finding.** ROUTE-A is
REFUTED for the fork's *firing* and holds only for its *pricing*; the owner's question is
answered **TWO KERNELS — differing by their internal-leg operator, and the difference is
exactly the sector carrying the fired divergence: the pure-gauge constant TT mode.**
Candidate 6's IR-finiteness cannot be imported. The fork stays fired under every
projection.

Executed 2026-09-16 under `WALL_A_REDUCTION_01_PROTOCOL.md` (frozen `f77e2d1`; the
owner's 2026-09-16 message as the charter past the wall-A HARD STOP, this question
only). Surveyor → deriver → adversarial checker (CONFIRMED_WITH_CORRECTIONS; the crux
computation re-run end to end with an independent implementation, agreeing; three
corrections, one a labeling correction to the deriver's headline, one a numeric, one to
the protocol's own characterization of O_static). Zero register edits; frozen caches
read-only (sha verified); no K_R build; no Ward repair; rung3 untouched.

## 1. The locus test — the crux, and its split answer (deriver + checker, agreeing)

From the frozen Tier-2 BD legs, the k_ext-dependence of the two-internal-leg
superhorizon region at O(H²) was derived (momentum conservation with one leg carrying
k_ext), then a horizon-localized TT l = 2 test function of physical width R was applied
to the *same* flat-slicing loop at both external times.

- **The noise q⁻² POWER class (α = −2, coefficient 4ω⁴/15) requires k_ext = 0 EXACTLY.**
  It is the equal-|q| kinematics of both legs going superhorizon together. At any
  k_ext ≠ 0 the noise combination is LOG (α = −1) with coefficient
  B_noise(k) → 4ω⁴/(15k) + O(k): **the recorded power coefficient is exactly the numerator
  of a 1/k_ext singularity** — the k = 0 power class is the k → 0 accumulation of a
  finite 1/k piece. Under every localized window it reappears as a finite term
  proportional to R.
- **The retarded q⁻¹ LOG class (α = −1) does NOT require k_ext = 0.** It persists at
  every k_ext ≠ 0 with a closed-form coefficient whose k → 0 limit reproduces the record
  exactly (4ω³(−iΔω−3)/15), and under every localized window of every width
  (R = 0.25…8/H: coefficients nonzero, converging to the k = 0 record as R → ∞:
  −795.6−1865.0i at R = 8 vs −800−1866.7i). The checker confirmed this in BOTH the
  fixed-Δ and fixed-ω representations (dispersive PV part → −8ω²/15 as R → ∞ — exactly
  the owner-ruling's both-branch retarded log coefficient, an independently extracted
  corpus number; absorptive part −(πω³/30)·δ(ω − k_ext), the single-soft-leg cut).
  **The localized projection is NOT finite.**

So: the fork's *firing* (the scaleless log, Tier-2's armed class) is
**projection-invariant** — a property of the kernel's internal construction (the
single-leg superhorizon term κ²H²/q³ × the ν-bare vertex coupling V₀ = 8ω⁴/15 ≠ 0,
closed form confirmed end to end to 4e−5). The fork's *pricing* at the power class is
the property of the literal-k_ext = 0 (Option 1b) evaluation — **the class the D1
ruling itself rejected** as "the convenience option that silently resolves the order of
limits." Under Option 1a (controlled k → 0) the fork fires in both combinations at the
log class. **Nothing is un-fired; the owner holds a re-pricing question, not a
de-firing.**

## 2. The owner's question — two kernels, by operator content, not by scale

The protocol's own characterization of O_static was **wrong on the record** and is
corrected here for all downstream use: there is no discrete Killing tower — ω_T is a
CONTINUUM for every (l,m) (exact Jost ladder, |R_l| = 1, spectral power ω^{1.00} for
l = 1,2,3; the candidate set −i(l+2n+Δ)H is a retracted non-QNM set); only (l,m) is
discrete. ROUTE-B's pre-registered *mechanism* ("discreteness is a completion") is
refuted. What differs is the **internal-leg OPERATOR**:

- **All of the IR-divergent weight of O_flat's TT-gauge BD legs restricted to any patch
  is the constant TT mode** (the x-dependent remainder is O(q), IR-finite — derived) —
  and that mode is **EXACTLY a linearized diffeomorphism**, L_ζ g for ζ^i = ε_ij x^j/2
  (all components, computed), Killing-time independent (ω_T = 0), pure l = 2 polar,
  regular on r < 1/H.
- **O_static's legs are the gauge-invariant static master variable** (c = 0 exactly for
  the graviton; axial invariance derived in-corpus, polar via the Zerilli M = 0 collapse
  taken on authority), **identically blind to that mode**: its computed spectral function
  has no zero mode. The physical scalar zero mode (c = −2, l = 0) diverges in the static
  thermal representation exactly as the flat log does — so the static description is
  not IR-finite by magic; it is finite for the graviton because the divergent sector is
  gauge.

**Checker's labeling correction, carried:** O_static on the record is a *free* classical
scattering object — no state, no loop; no static loop exists at any H. "Two kernels" is
therefore a statement about **field variables and their coupling** (exact at the
free-leg level; V₀ ≠ 0 is loop-level with its gauge status undecided — Ward repair
fenced, T3-8 records the loop non-transverse), **not a computed loop-vs-loop
difference.** Candidate 6 is connected to the computed physics only through the
gauge-invariant sector; its finiteness is not importable into O_flat as assembled.

## 3. The (O, D, L, R) statement for the fired divergence

- **O:** the Tier-3 K_R-contract object only — the TT-gauge one-loop pair Σ_> ∓ Σ_<
  from the frozen Tier-1 dS cubic vertex and frozen Tier-2 TT-gauge BD legs on the
  q-continuum with no lower limit. Not the A3-4 assembled TT response (different
  object), not any static loop (none exists).
- **D:** flat-slicing conformal chart a = 1/(1−Hu), BD at η → −∞, Wigner frame,
  fixed-Δ bi-time integrand after angular averaging; u_b-free at O(H²) (re-verified 1e−28).
- **L:** two classes, two loci. LOG (α = −1): locus-INDEPENDENT — k_ext = 0, every
  k_ext ≠ 0, every window. POWER (α = −2): the exactly-k_ext = 0 locus only; absent from
  every localized projection; a limit-locus statement about O, **not a sectorization**.
- **R:** projection equivalence under horizon-localized TT l = 2 windows: HOLDS for the
  log class (continuously related coefficients); FAILS for the power class.
- **Projection-invariant distinctions (the only ones that count):** (i) the log class,
  a property of the kernel's internal construction; (ii) the O_flat/O_static difference,
  a property of the internal-leg operator (gauge-variant h_ij carrying the pure-gauge
  constant-TT sector vs. the gauge-invariant master variable). Neither is a "zero
  sector"; both are statements about specified objects.

## 4. Consequences

1. **Wall-A cannot dissolve fork (ii) into a projection artifact.** ROUTE-A's main prize
   is denied by computation.
2. **Re-pricing question for the owner (derived, not ruled):** the fork's price at the
   power class rests on the Option-1b evaluation D1 rejected; under Option 1a and every
   localized window the noise class is LOG with a finite 1/k_ext piece. The record's
   pricing text may need to say so.
3. **U1's operational judgment is now split:** "no apparatus realizes the k = 0 shear
   drive" is DERIVED for the power class (no localized apparatus sees it) and REFUTED
   for the log class (every localized TT apparatus coupling through this kernel sees
   it). Only the projection-invariant half is a physics statement.
4. **S6/U3 sharpened — the completion the dynamics does not fix has a derived candidate
   that is NOT a scale: gauge invariance of the internal-leg operator.** The RW/Zerilli
   master variable removes the divergent sector identically. Whether the corpus's loop,
   assembled gauge-invariantly, is IR-finite is the standard dS-IR controversy — Ward
   repair is HARD-STOPped and a new owner charter, not a computation available here.
   The static→flat reduction theorem U3 named is thereby *re-posed*: not
   "static ↔ flat" but "gauge-variant ↔ gauge-invariant internal legs."
5. **Checks that came free:** the H¹ graviton-loop sector vanishes identically at finite
   k_ext too (1e−19; consistent with U2's Λ = 3H² covariance argument); the H⁰ kernel is
   continuous at k_ext → 0 (1 + 9.6(k/ω)² + …), so the order-of-limits issue is confined
   to the O(H²) superhorizon sector, as Tier-2's T2-3 split anticipated.

## 5. On the face

The framework-favorable trap (ROUTE-A) was reached by neither deriver nor checker: the
deriver derived its refutation, the checker re-derived it independently in two
representations. One deriver numeric corrected (a mirror-point limit; not
load-bearing). The deriver's headline "two kernels" was over-labeled and is carried
here with the checker's qualification. The protocol's own O_static characterization was
wrong on the record — a builder error in the freeze, corrected on the result, not
rewritten in the protocol.

---

*Filed 2026-09-16. Deriver, checker, and surveyor filings in `WALL_A_REDUCTION_01.json`;
checker scratch under scratchpad/chk/ (repo clean at `f77e2d1`, caches sha-verified).
Fork (ii) stays gated; nothing banked; Born 2A/2B unchanged.*
