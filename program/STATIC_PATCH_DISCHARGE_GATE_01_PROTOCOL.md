# STATIC_PATCH_DISCHARGE_GATE_01 — PROTOCOL (frozen before execution)

**Commissioned:** owner directive 2026-09-13 ("perfect the program, find the physics.
go"), ratifying the builder's recommendation to take the static-patch discharge question
seriously. This gate is an ADVERSARIAL PRE-SCREEN under CHARTER §5.3 ("any claim that
resolves a fork must pass an adversarial pre-screen and survive, with the result relayed
up before it's banked"): **it banks NOTHING and edits NO register node, tier, or edge.**
Its output is a verdict plus, where applicable, a bank package for owner ratification.

## The question

`background_time_translation_flow` (assumed, +1 — the foundation input all eight
demotions cascade from) carries its own named discharge route:

> "OVERTURNED / DISCHARGED IF: the declared background is one carrying a timelike
> Killing vector — the de Sitter static patch is the named candidate — in which case the
> flow is a PROPERTY OF THE BACKGROUND rather than a commitment, and this node retires
> into that declaration at zero."

But the register's actually-declared background is **flat FRW for every banked
observable** (S_IF.md §1), which carries NO timelike Killing vector. So the route is a
background RE-DECLARATION, not a free retirement, and the gate must answer:

**Is the static-patch discharge available, and at what net cost to the corpus as banked?**

## Sub-questions (each answered explicitly)

1. **AVAILABILITY.** What re-declaration would supply the Killing vector (dS static
   patch is the named candidate; alternatives may be proposed only from the corpus's own
   texts)? Is it compatible with the banked corpus — specifically: `mu_linear`'s
   fixed-slicing spatial-SVT frame (KC5 fence, eft_operator_basis); the S6
   homogeneity/isotropy declaration and the meaning of the k label (the static-patch
   time-translation Killing vector does not commute with flat-slicing spatial
   translations — is the joint K_R(ω,k) label then an H≪ω effective object, and is that
   already each banked result's scope, e.g. the Tier-4 kernel's [1+(104/9)(H0/ω)²]
   structure?); rung7_wz's FRW cosmology; the ω→0 boundary where rung3's single-pole
   claim lives ("quasi-stationarity holds for H ≪ ω and the single-pole claim lives at
   ω → 0, exactly where it fails" — the flow node's own WKB-closure note); the
   vacuum-cluster's 21 nodes' scope; `calc/static_patch_tt_response.py` (the program has
   already computed on a static patch — what does that calc's scope establish?).
2. **COST ACCOUNTING.** Full ledger arithmetic of the re-declaration: what retires (the
   flow's +1; the eight demotions mechanically reverse per the P1A ruling's recorded
   route); what NEW inputs appear (each candidate priced under the R1 fence — "an input
   is something a named position denies"); whether currently-unpriced presuppositions
   surface (is flat-FRW/homogeneity itself priced anywhere, or prose-carried? an
   unpriced presupposition found here is an OMISSION finding in its own right, booked by
   the owner under OMISSION_STANDARD_v2, not by this gate).
3. **SCOPE SURVIVAL.** Which banked results survive the re-declaration at which scopes,
   and which are re-homed, re-scoped, or lost. A discharge that "succeeds" by silently
   re-scoping banked observables is a failure reported as one.

## Pre-registered outcomes

- **DISCHARGE-AVAILABLE-NET-FAVORABLE** — a coherent re-declaration exists; ledger net
  strictly improves or holds with zero banked-result loss. Consequence: bank package
  prepared (declaration text, node retirement, demotion reversals, ripple list) for
  OWNER ratification; nothing banked here.
- **DISCHARGE-UNAVAILABLE** — the re-declaration breaks banked frames or costs more
  than it retires. Consequence: the obstruction recorded precisely — that obstruction is
  itself the physics finding (what the stationarity/homogeneity trade forces at H).
- **NEW-OMISSION-SURFACED** — the investigation exposes an unpriced presupposition
  (e.g. spatial homogeneity/flatness). May coexist with either outcome above.
  Consequence: omission-booking package for owner relay (R5 wiring plan included).
- **UNDERDETERMINED** — the record cannot decide; the gate names the specific
  computation or declaration that would decide it, and stops.

## Anti-optimization commitments

Named bias directions: DISCHARGE-AVAILABLE is tidiness- AND framework-favorable (retires
the foundation assumption, re-promotes eight nodes toward `shown` — the exact
directional-bias pattern on the builder's ledger); DISCHARGE-UNAVAILABLE is
status-quo-favorable; NEW-OMISSION is instrument-favorable (more bookkeeping to own).
None is preferred. The verdict standard: what the corpus as banked, plus standard
physics the corpus itself cites, forces. Suite color and ledger direction are outputs,
never inputs.

## Hard fences

Zero register/tier/edge/node edits by this gate. rung3_single_pole's separately-reserved
tier question is NOT moved (its 2026-08-18 reservation stands); its content is used as
evidence only. No CHARTER edits. The frozen RAI pair, dc110ba, CERTIFICATION_02
untouched. Born 2A/2B unauthorized. Any bank package produced is INERT until the owner
ratifies.

## Post-verdict mechanics (pre-committed)

Record result in `STATIC_PATCH_DISCHARGE_GATE_01.{md,json}` (+ bank/omission packages as
separate INERT files where applicable); flow node and P-pass stanzas untouched (the
owner's ratification, not this gate, would move them); suite + expected_red re-run;
commit; push. Owner ratifies, amends, or rejects the package.

*Frozen 2026-09-13 before any advocate ran. Evidence pointers assembled pre-freeze:
S_IF.md §§1–2 (background + S1–S7 inventory), claims.json (flow node, rung3, rung7,
mu_linear, eft_operator_basis, p_tt_ansatz), calc/static_patch_tt_response.py,
calc/gw_tensor_friction.py (H0/ω structure), calc/wz_dark_energy.py,
PHYSICS_LEDGER/WALL_A_A3_DECLARATIONS.md, the vacuum-cluster scope docs. Advocates are
charged to attack this selection and extend it.*
