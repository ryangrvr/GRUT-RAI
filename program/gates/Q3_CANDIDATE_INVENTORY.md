# Q3 POST-GATE CANDIDATE INVENTORY

> **2026-09-07 · Inventory only.** No simulation, no new primitive, **no dimensional-
> transmutation derivation attempted**. Q3 is **not** declared answered. The Q3 gate
> (`e668b72`) is not modified. Q1, Q2, Q2-SY, Q2-BRIDGE, the register and the corpus are
> untouched. Sweep: `wf_e35565a5-cef`.
>
> **THIS DOCUMENT IS ITS OWN SECOND DRAFT.** A first draft was destroyed by the hostile
> anti-fake-running refuter, whose central objection I verified and accepted. **The first
> draft's load-bearing claim was FALSE**, and the corrected picture points in the *opposite*
> direction from it. §7 records the whole failure.

## 1 · The closed candidate

**r = c₀/c₂ — CLOSED / REJECTED FOR THIS ROUTE**: β_r = 0 under the frozen μ-shift law; the
apparent running was kinematic dependence misread as RG flow; the proposed μ* collapsed to μ;
r is a form-factor ratio, not a Callan–Symanzik coupling. Not re-proposed.

## 2 · What the record actually contains — corrected

**A working transmutation construction, already executed and machine-gated.**

    c₄ runs:  β_c₄ = dc₄/dlog μ = −2A = +3/(640π²)   (nonzero; detecting negative control)
    ⇒  Λ_R := μ·exp(c₄/2A)  is EXACTLY μ-INVARIANT

Verified numerically across six decades: Λ_R = 0.79483456354 at μ = 0.01, 1, 100, 10⁶ —
**constant**. The record certifies it: *"the single RG-invariant Λ_R = μ·exp(c₄/2A) …
RG-INVARIANT (gated, with a teeth-control that breaks it under a perturbed shift)."* And
Λ_R has physical content: it is **the frequency at which Re Σ^{H⁰} vanishes**
(Re Σ^{H⁰} = 2Aω⁴log(Λ_R/ω)).

The H² sector carries the partner Λ′ := μ·exp(c2p/2A′), and the two are linked exactly:

    c2p − (104/9)·c₄  =  −(104/9)·(2A)·log(Λ_R/Λ′)

**— i.e. the "μ-invariant combination" is the log of the ratio of two independently generated
scales.** *(Constructed here; not banked. W-0.)*

**And 104/9 is not inert.** With both slots running affinely, the dimensionless ratio
y := c2p/c₄ flows to **104/9 = A′/A** from any initial condition (verified: y = 5.97 → 8.48 →
9.43 → 9.93 → 10.24 → … → 11.5556). **104/9 is the attractor of that flow**, not merely a
datum about it.

## 3 · Why transmutation is nevertheless not established — the correct reason

**Not because the flow is affine.** *(That was the first draft's claim and it is false — §7.)*

The construction produces μ-invariant scales; **what it does not do is determine their
values.** Λ_R's value is fixed by c₄ at a reference point — a **renormalization condition**.
The record prices exactly this: mu-RULING-C, *"a numerical μ WOULD BE A NEW INPUT"*; and the
Λ_R owner ruling permits fixing it *"only through an independently justified
renormalization/matching condition."*

**The honest comparison, stated without flattery in either direction.** QCD's Λ is *also*
fixed by an input — a measured α_s at a chosen scale. Structurally the two constructions are
the same trade: a dimensionless condition at a free reference point, for a reference-invariant
scale. **So the difference is not the mechanism.** What QCD has and this does not is
(i) a coupling whose flow reaches a *physically distinguished* regime (strong coupling) that
makes Λ meaningful independently of the fitting convention, and (ii) an **independent
determination** of the condition that fixes it. GRUT has neither: c₄ is a subtraction
constant, and its determining condition is, by the record's own ruling, **an unresolved
input**.

**What is NOT claimed here.** The first draft asserted Λ_R/μ = 0.795 is "O(1), no hierarchy
generated." **That is withdrawn.** Λ_R/μ is μ-dependent and spans all positive reals
(79.5 at μ = 0.01; 7.9×10⁻⁷ at μ = 10⁶); the number 0.795 is Λ_R read at μ = 1 — **precisely
the numerical-μ convention mu-RULING-C prices as a new input.** No hierarchy statement is
available without first fixing that input.

## 4 · The screens, stated explicitly (they were used but never listed)

A candidate must be: **(1)** dimensionless; **(2)** not merely a kinematic ratio of two scales;
**(3)** not merely a μ-slot; **(4)** not merely an unpinned coefficient; **(5)** carrying an
actual transformation law; **(6)** capable of a nonzero β; **(7)** inserting no new physical
scale; **(8)** requiring no post-hoc renormalization condition; **(9)** hiding no new
primitive.

**c₄ passes 1, 5, 6.** It fails **3** (it is the μ-slot's partner: *"the surviving (μ, c₄)
pair is redundant by exactly one function's worth"*) and fails **8** at the terminus. **That
failure — screen 8, the renormalization condition — is the operative one**, and it is the
same wall the record reached independently.

## 5 · The inventory by kind (22 objects listed here, from a 31-object sweep)

- **RUNNING, μ-absorbing (β ≠ 0):** c₄, c2p. Derived invariants: Λ_R, Λ′, log(Λ_R/Λ′).
- **RG ATTRACTOR:** 104/9 = A′/A, the fixed point of y = c2p/c₄. *(Reclassified — the first
  draft called it "never a beta-function candidate," an over-correction.)*
- **STRUCTURAL INVARIANT (β ≡ 0):** α = a/c (cohomological, one-loop-exact, state-independent);
  the spectral index s; the frozen residues A, A′; the GR lock.
- **KINEMATIC (screen 2):** ε_H = (104/9)H²/ω²; λ ∼ (κω)²; ω/T; m_eff²/H²; ωτ; w(z).
- **BOOKKEEPING RATIO:** the register's declared x; μ(x) = 1 + xα; n₀/n₂; η; Σ.
- **UNPINNED, no transformation law:** c_R^tot; κ; B.

**On the register's declared x:** the r rejection transfers on two limbs, its numbers do not.
x carries **computed** bounds — a DESI-Σ₀ lensing window x ≲ 0.59, **central-inputs/loose-upper
per the F-MAP fence and conditional on "x approximately constant over the observational band,"
a declared unsupported restriction every downstream quote must carry**; x = 1 excluded;
μ(x) = 1 + xα. The naming trap is preserved.

## 6 · Classification: **D — ambiguity remains and requires an explicit gate**

Not B: a candidate with a genuine nonzero β **does** exist and its invariant **is** gated.
Not A: nothing determines that invariant's value from within. **The live question is exactly
whether an input-fixed μ-invariant scale counts as dimensional transmutation — and my first
two attempts answered it wrongly in opposite directions** (first "it runs and generates a
scale," then "nothing runs and no scale is possible"). That is the signature of a question
needing its own gate, not another inventory verdict.

**The gate that D calls for** would ask one thing: *is there any independent determination of
the renormalization condition fixing c₄ — or is screen 8 a permanent wall for this route?*
**Not authorized here.**

## 7 · Disclosed: this document's first draft was wrong

The hostile refuter falsified the first draft's central claim; I verified each point before
accepting it.

1. **THE LOAD-BEARING CLAIM WAS FALSE.** The draft argued *"a constant β is a free transitive
   translation action … affine flow cannot single out a scale."* **Constant-β is a property of
   the CHART, not the flow.** For any 1-d flow with β ≠ 0, u = ∫dg/β gives du/dt = 1. **QCD at
   one loop is exactly affine in u = 1/(2b₀g²) — which is why 1/α_s is a straight line in
   log μ — and Λ_QCD = μ·exp(−u) is literally "exp of the free initial condition."** Applied
   consistently, my criterion **abolishes Λ_QCD**.
2. **SELF-CONTRADICTION.** §2 said c₄ runs (β ≠ 0); §3 said Λ_R is "exactly proportional to μ."
   Both cannot hold: if c₄ runs, Λ_R is μ-**invariant** — which is the machine-gated fact.
3. **A FORBIDDEN INPUT.** "Λ_R/μ = 0.795, O(1), no hierarchy" read Λ_R at μ = 1 — the numerical
   convention mu-RULING-C prices as a new input.
4. **THE CONFUSION I CREDITED MYSELF WITH CATCHING.** Two of the three rows offered as proof
   that "something runs" were **dilatation dependences** (frequency; dS scale factor), not
   μ-flows — the same move that killed r. *(One was additionally a declared toy: one mode,
   single pole.)*
5. **AN INVERTED DISQUALIFIER.** The draft rejected c2p − (104/9)c₄ *for being an RG
   invariant* — while its own chain requires one, and Λ_QCD **is** one. The object is the log
   of a ratio of two generated scales, and the draft dismissed it in a subordinate clause.
6. **AN OVER-CORRECTION.** 104/9 "can never be a beta-function candidate, no transformation
   law" — it is the **attractor** of y = c2p/c₄.
7. **Minors:** a headline count of 31 the document did not reproduce (now stated as 22 of 31);
   nine screens used but never listed (now §4); the x bounds quoted without the register's
   mandatory F-MAP fence (now carried); novelty over-claimed against the gate, whose §3 table
   already had the affine shift and the linear-vs-nonlinear contrast; the class vocabulary's
   provenance and its collision with the gate's two other A/B/C schemes; "the MS scheme's own
   signature" omitting that the dominant term −6841/2835 is genuine loop content; and a
   two-sentence quotation spliced with a semicolon.
