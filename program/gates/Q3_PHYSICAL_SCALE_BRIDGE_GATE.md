# Q3 — PHYSICAL SCALE BRIDGE GATE

> # ⛔ FAILED AUDIT — NOT A RESULT. DO NOT CITE.
>
> **This gate did not pass and is NOT committed.** Four drafts were written and all four were
> destroyed by the two-audit protocol: draft 1 = **B**, draft 2 = **C**, draft 3 = **E**,
> draft 4 = **C, scoped** (the text below).
>
> **The two audits SPLIT on draft 4's deciding result (§4, N1–N3).** AUDIT A: the physics is
> earned, the document fails on provenance (16 defects, 3 high). AUDIT B: the deciding result
> is definitional and the classification should be **E**. Adjudicated in-session, symbolically:
>
> 1. **N2 proves nothing — AUDIT B is right.** Since `Λ_R := μ·exp(c₄/2A)`, the shift
>    `Λ_R → Λ_R·e^δ` *is* `c₄ → c₄ + 2Aδ`; and the counterterm solved for equals `2Aδ`
>    **exactly**. N2 therefore says only that a c₄ shift is undone by shifting c₄ back — true
>    of *any* parameter. It is not a redundancy demonstration. This is the register's own
>    flagged shape — *a gate whose identity is definitional proves nothing* — reproduced here
>    while arguing AGAINST physical content.
> 2. **N3 tested the wrong operation — but AUDIT B overstates the kill.** A counterterm is
>    *added*; a field redefinition `h → h + a□h` is a change of variables sending
>    `D → (1−aω²)²·D`, which does move the imaginary part (`O(a)` shift of
>    `Im D = −3ω⁶/(640π)`). However that induced term sits at `O(a·Σ)`, **one loop order
>    beyond** the removal at `O(a·D₀)` — standard field-redefinition bookkeeping, not an
>    immediate refutation. Absorbing it requires next-order counterterm structure the record
>    does not have (Σ is one-loop, O(k²) uncomputed, the assembly does not exist).
> 3. **AUDIT A's defence names the right route and cannot complete it.** "□² vanishes on the
>    free shell" is the correct redundancy argument — and it is exactly the on-shell/assembled
>    statement `SPECIALIST_BRIEF_rung3_spine.md:43` and `CLASS_C_WALL_CONTRACTS.md:22-25`
>    record as not existing.
>
> **ADJUDICATION: removability is PLAUSIBLE AND NOT ESTABLISHED.** The field-redefinition leg
> is incomplete, not refuted. So §4 does not carry §13, and **C is not earned on the argument
> given** — but neither is the argument dead in the way AUDIT B concludes.
>
> **Therefore §4, §8 screen 5, §9 B1, and the classification in §13 do not stand.**
> §8 screen 7 also fails: "form-factor feature" has no definition in the record, and under the
> reading used here it would exclude ρ_TT = 2 Im G_R^TT — the designated Class-C observable —
> making this gate's own reopening condition 1 unreachable.
> §10 excludes B by the modal misreading `Q3_LAMBDA_PHYSICAL_CONTENT_GATE.md:167-170`
> identified and withdrew: **B describes a structure, not a discharged state.**
>
> **What survived all eight audit passes** (and is the only material here fit to reuse):
> §3.1 (the certification apparatus is Λ_R-blind by construction); the exclusion of **A**
> (nothing determines Λ_R — count 0 at contract scope, both number-supplying routes barred);
> §10.1's exclusion of **D** (the μ election is *"equivalently declare Λ_R"* — stipulation);
> §5.3 (the pole lever is out of band); §7.1's withdrawal of a manufactured β_c₄ conflict; and
> the arithmetic, independently reproduced eight times.
>
> **B, C and E each remain unestablished**, each having failed for a different stated reason.
> Retained as a record of the attempt, per the program's disclosure discipline. Register
> untouched, ledger delta 0, W-0 held. — 2026-09-08

**Status:** COMPLETE — classification **C, scoped** (§13)
**Date:** 2026-09-08
**Draft:** 4. Drafts 1 (**B**), 2 (**C**, wrong argument), 3 (**E**) each failed both audits.
Full disclosure in §14.
**Scope:** symbolic analysis and record reconstruction only. No numerical experiment, no
parameter fitting, no new coupling, no new scale, no new renormalization condition.
**Register:** untouched (74 nodes, sha256 `beaeb84e8a6f8468`). **Ledger delta: 0.** W-0 held.
**Q3 disposition:** **OPEN** (`program/QUESTION_LEDGER.md:163`).
`Q3_DIMENSIONAL_TRANSMUTATION_GATE.md:181` proposes OPEN → GATED and marks it owner-owed;
`Q3_LAMBDA_PHYSICAL_CONTENT_GATE.md:191` asserts *"**Q3 remains GATED.**"* **The ledger is the
authority and reads OPEN.** Flagged for the owner; nothing applied.

---

## 1. EXACT QUESTION

> Can an already-declared physical observable, or an independently motivated physical
> matching condition, determine Lambda_R without merely defining that observable to be
> Lambda_R?

### 1.1 Vocabulary provenance

**A–E are the owner's PHASE 7 outcome set for this gate**, quoted from the issuing instruction.
**That instruction is not archived in the repo**, so the outcome set is unverifiable against a
repo artifact; it is reproduced here in full so the verdict is at least self-contained:

> A. An existing physical observable independently fixes Lambda_R.
> B. A physical observable depends on Lambda_R but its normalization/fixing condition remains unresolved.
> C. No physical observable in the current structure depends on Lambda_R.
> D. A candidate appears, but only by adding a new primitive or new empirical input.
> E. The question remains ambiguous.

These letters collide with other A/B/C schemes in the corpus. **This gate's C is not
`Q3_LAMBDA_PHYSICAL_CONTENT_GATE.md:14`'s C** — see §7.1, where that sibling's "Not C" is shown
to be a statement about a different proposition.

**The seven screens** (§2, §8) are this gate's own. `Q3_CANDIDATE_INVENTORY.md:95-107` uses a
different nine-screen list. Screens: (1) independently defined, (2) observable at declared
scope, (3) gauge-invariant, (4) scheme-independent, (5) nonlocal rather than local/contact,
(6) independent of c4, (7) constrains rather than defines. **Screen 6 is malformed and is
retired — §8.1.**

### 1.2 The premise sentence

The instruction's premise — *"no known physical observable has been shown to depend on
Lambda_R"* — **stands, and is corroborated by the record**, which books the same negative:
`Q3_LAMBDA_PHYSICAL_CONTENT_GATE.md:160-161`, *"**Observable invariance: no candidate exists**,
and the one nominated was definitional (§4). That is the gate's sharpest negative and it is
evidential, not graded"*; and :165-166, *"no observable is known to depend on Λ_R at all."*

What the record separately banks at `PHYSICS_LEDGER/WALL_KR_LAMBDA_R_OWNER_RULING.md:50-52` is a
*parameter-counting* fact: *"the response genuinely **depends** on Λ_R (∂ReΣ/∂Λ_R ≠ 0, so the
count is not zero)"*. **Dependence of the response is banked; dependence of an observable is
not.** This gate's result is that the gap between them is closed in the negative direction, at
the declared scope, for a stated structural reason.

---

## 2. COMPLETE CANDIDATE INVENTORY

### 2.1 H⁰ sector

| # | Candidate | Carries Λ_R? | Disposition |
|---|---|---|---|
| 1–6 | Im χ; branch point; the cut; s-class; Axis-1 convergence; log coefficients A, A′ | **No** | slot-invariant *by the mechanism of their certification* — §3.1 |
| 7 | Zero of Re Σ | Yes | **WITHDRAWN** — definitional (`program/gates/Q3_CANDIDATE_INVENTORY.md` §2). Not reopened. |
| 8 | Pointwise sign of Re χ (Axis-2) | Yes | **Refuted, and it is candidate 7 in another guise.** With A < 0, `Re Σ^{H⁰} = 2Aω⁴log(Λ_R/ω)` makes the window sign a pure function of ω ≷ Λ_R — the sign structure and the zero are the same fact. Independently refuted in the published corpus: `books/BOOK_II_CONSTITUTIVE_FRAMEWORK.md:578` — *"no scheme-invariant Re χ sign statement exists."* `PHYSICS_LEDGER/WALL_KR_MU_OWNER_DECISION_PACKAGE.md:106-107` marks it *"**Blocked by μ**"*, which concerns its classification, not its admissibility. **Draft 3 rehabilitated this; withdrawn.** |
| 9 | Resummed-denominator pole | Yes | register node `kr_contract_retarded_tier4`, field `statement`: "NO pole claim is made"; lever out of band (§5.3) |
| 10 | GW dephasing Δφ(ω) | Yes | never computed from the Tier-4 kernel; 21–62 orders below threshold (`calc/RESULTS_gw.md:26`) |
| 11 | v_g(ω) ≠ c | Yes | `calc/RESULTS_gw.md:12` *"it is not binding"*; :14 *"26–66 orders to spare"* |
| 12 | QNM / ringdown | Yes | inherits 10–11; not computed |
| 13 | Γ_T | **No** | built from Im Σ alone — §5.2 |
| 14 | Siren / Ξ(z) | **No** | staked B; two live values 3.2 orders apart (`GRUT_PREDICTION_GATE_GAMMA_T.md:94`) |
| 15 | USL / rung8 | **No** | `PHYSICS_LEDGER/WALL_KR_NOISE_IR_AUDIT.md:99-100`: *"H⁰ = 1 (Λ_R); c0′/c2′ unresolved, fork-gated; zero new inputs; **no scale hidden in Λ_R**."* |
| 16 | ρ_TT(ω→0), η | see §5.4 | the designated object; **not constructed** |
| 17 | Im G_R(ω), finite ω, in band | **Yes, exactly** | the sole live carrier — and it **fails** — §4, §8 |
| 20 | Gate-E / FDT-KMS channel | **No**, structurally | `PHYSICS_LEDGER/GATE_E_H2_FDT_KMS_AUDIT.md:40-43`: the local real terms *"(c0′, c2′, the Λ_R slot) … **cannot enter either side**. Excluded by the structure of the registered relation, not by assumption"*; :128 — *"A Gate-E pass does **NOT**: fix c0′; fix c2′; fix Λ_R"*. |

### 2.2 H² sector

`program/gates/Q3_CANDIDATE_INVENTORY.md:37-39`: `Λ′ := μ·exp(c2p/2A′)`, with
`c2p − (104/9)·c₄ = −(104/9)·(2A)·log(Λ_R/Λ′)`.

| # | Candidate | Disposition |
|---|---|---|
| 18 | Λ′ (H² partner) | **Cannot fix Λ_R, for a reason both prior drafts missed.** c₄ is already CALCULATED (§3.2), so Λ_R is already pinned by its definition; determining c2p fixes **Λ′**, not Λ_R. The link is moreover an **algebraic identity** — substituting the two definitions returns `c2p − (104/9)c₄` — so it carries no independent content. `PHYSICS_LEDGER/WALL_KR_H2_LOCAL_FORK.md:104-108` does name a conditional route to determining c2p, recorded there *"as conditional only. It is not claimed, because the extraction is not currently licensed."* Draft 3 called this a live route to fixing Λ_R; **withdrawn**. |
| 19 | 104/9 = A′/A | **No** — the slope of a constant translation vector in (c₄, c2p); withdrawn over-claim (`Q3_CANDIDATE_INVENTORY.md:44-45`) |

### 2.3 Machine checks, correctly scoped

- `Λ_R`/`c4`: **0 of 44** scripts in `calc/`. **This null is directory-scoped**:
  `PHYSICS_LEDGER/` holds **24 of 123** carrying them. `calc/` is the empirical-contact
  directory by convention; the null shows the object has not reached empirical contact, not
  that it is absent from the program. Draft 2 filed it under "NOTHING SUPPLIES A NUMBER";
  that framing stays withdrawn.
- **33** `RESULTS*` files repo-wide (32 excluding the `release/zenodo-v1.0/` duplicate); none
  carries the object.
- `Λ_R` in **1 of 74** register nodes (`kr_contract_retarded_tier4`, field `statement`), with
  nothing downstream — no `depends_on` reference, no textual reference.

### 2.4 The numerical value — corrected

`Q3_CANDIDATE_INVENTORY.md:26-27` records Λ_R constant at μ = 0.01, 1, 100, 10⁶ — **eight
decades**, as `Q3_LAMBDA_PHYSICAL_CONTENT_GATE.md:29` already corrected ("six" is superseded).

With β_c₄ = −2A, `Λ_R = μ·exp(c₄(μ)/2A) = exp(c₄(1)/2A) = 0.7948345635404438` — **a μ-free
constant**. It is **Λ_R/μ that is μ-dependent**, spanning all positive reals — a withdrawal
already logged at `Q3_CANDIDATE_INVENTORY.md:89-93`. **Draft 3 inverted this label; corrected.**

The number is in the kernel's master units. What is unregistered is the renormalization
**point** that sets those units (`PHYSICS_LEDGER/WALL_KR_MU_CONVENTION_RULING.md:82`: *"**What
is NOT registered: the renormalization POINT.**"*), which is why the record can hold both
"numerically constant" and *"remains **undetermined**"*
(`WALL_KR_MU_OWNER_DECISION_PACKAGE.md:118-120`) without contradiction — and why
`WALL_KR_NOISE_IR_AUDIT.md:99-100` books *"no scale hidden in Λ_R."*

---

## 3. DEFINING VS CONSTRAINING

### 3.1 The certification apparatus is Λ_R-blind by construction

`PHYSICS_LEDGER/WALL_KR_D5_RENORMALIZATION_RESULT.json` (`invariant_3`) certifies the log
coefficients as slot-invariant as **"the 5th/3rd polynomial-free derivatives"** — invariance
established *by differentiating the local quartic away*:

    d^5/dω^5 [ 2A ω^4 log(Λ_R/ω) ] = −48A/ω        (no Λ_R)
    d/dΛ_R   [ 2A ω^4 log(Λ_R/ω) ] = 2A ω^4/Λ_R    (nonzero)

Candidates 1–6 could not have carried Λ_R. **Uncontested across all six audit passes**, and it
does more work than either prior draft allowed: it establishes that Λ_R is a **bijective
relabelling of the single local ω⁴ slot** (c0 = c2 = 0 exactly), which settles screen 5 (§8).

### 3.2 Where the openness lives

c4 is **CALCULATED**: `PHYSICS_LEDGER/WALL_KR_D5_EXECUTION_VERDICT.md:46`,
`c4 = A·(−6841/2835 − γ_E + log 4π) ≈ +1.0906×10⁻⁴ (μ = 1)`, under the rule at :11-12 — *"THE
SCHEME MAY BE DECLARED. THE FINITE LOCAL NUMBERS MAY ONLY BE CALCULATED."*

It is settled **given μ**: `WALL_KR_MU_OWNER_DECISION_PACKAGE.md:118-120` — *"c4 is settled
**given μ** … The μ-invariant content of the local sector is the single constant Λ_R, which
remains **undetermined**."* Draft 2's "Λ_R admits no admissible variation" was false and stays
withdrawn: the unregistered renormalization point is exactly the admissible variation.

---

## 4. NONLOCAL / LOCAL — THE DECIDING RESULT

**The Λ_R direction is exactly removable by a real local field redefinition at the contract's
declared evaluation point.**

`D₀ = ω²/(2κ²)` is pinned at k → 0 (`PHYSICS_LEDGER/wall_kr_tier4_retarded.py:296`, :513; the
k → 0-first ordering is declared at :297). Verified in `program/gates/q3_bridge_symbolic_check.py`:

| | Result |
|---|---|
| R1 | `d(ReΣ)/dlog Λ_R = −3ω⁴/(640π²)` — a real local polynomial |
| **N1** | `Λ_R → Λ_R·e^δ` shifts D by `+3δω⁴/(640π²)` — **purely real** |
| **N2** | removed **exactly** by the real counterterm `a* = −3δ/(640π²)`, ω-independent, at O(δ) |
| **N3** | `Im Σ = −3ω⁴/(1280π)` is **imaginary**: no real `a` can produce or remove it |
| R2 | `ω⁴/D₀² = 4κ⁴` — ω-independent |
| K4 | `(ω²−k²)²/D₀(k)² = 4κ⁴` — the same proportionality at **all** k |

**N3 is the reality condition, and it is what makes the argument valid rather than
overreaching.** A field redefinition `h → h + a□h` carries a *real* parameter. The correct
criterion is therefore "a **real** local term ∝ D₀²". Under it the Λ_R direction is removed
exactly, and Im Σ — with the cut, ρ_TT and Γ_T — is untouched. Draft 3's objection that the
criterion "proves too much" omitted the reality condition and is **withdrawn**.

This is the record's own doctrine, not an import: `SIGNATURE_AUDIT.md:60` fences α_M as coming
*"from a **Hermitian action**: **removable by field redefinition** … A genuine dissipative
Im Σ_R is none of those."* Λ_R lives entirely in the Hermitian part — Im Σ is Λ_R-free by
construction (F3). The record's verdict on that class is that a detection *"could **never
confirm**"* the framework.

**On the covariance question.** `PHYSICS_LEDGER/WALL_KR_D5_RENORMALIZATION_AUDIT.md:80-84` books
as *"Executable span content (review-corrected)"*, symbolically verified and **unflagged**, that
*"the ω⁴ slot is carried by the Ricci²/Riemann² classes"*. The `CONDITIONAL` at :84-88 governs
**the H² linkage of (c0p, c2p) to the flat constants**, a different sentence and a different
object. **Draft 3 spliced the two across a sentence boundary and manufactured a two-horn fork
from it; the fork is withdrawn.** F7 (`PHYSICS_LEDGER/WALL_A_A3_DECLARATIONS.md:62-66`) is real
but is scoped to the wall-question (i) placement verdict, and draft 3 restated it as an
unrestricted bar; also withdrawn. The register additionally carries
`response_lorentz_covariance` (tier `shown`, *"DISCHARGED 2026-08-30 … demonstrates the response
lies in the 3-dimensional Lorentz-covariant subspace **WITHOUT imposing it as an input**"*),
whose discharge covers the nonlocal block while *"c0 = 0 remains separately unlicensed"*.

**Consequence.** By K4 the proportionality holds at all k, so the removal is not an artifact of
the k → 0 slice — and in any case k → 0 **is** the contract's entire declared evaluation domain
(:297). Draft 2's "reopens at finite external momentum" escape was empty, and draft 3's
elevation of the same point to a fork was doubly so.

### 4.1 What remains true and does not rescue the carrier

Off-shell Green's functions are **not** invariant under field redefinitions. So
`d(Im G_R)/dlog Λ_R = 9κ⁶ω²/(51200π³) + O(ω⁴) ≠ 0`, and the O(Σ²) real part carries
`ω²·log(Λ_R/ω)` — both verified — and **neither is evidence against redundancy**; they are what
redundancy predicts for an off-shell object. Since the Λ_R shift and the real counterterm act
identically on D (N2), they act identically on `G = 1/D` and on every term of its expansion,
the log term included. Draft 3 read these as refutations; **withdrawn**.

Likewise the ratio `dlog|Im G_R|/dlogΛ_R ÷ (Im Σ/D₀) = 4/π` is an **analytic identity** — the 4
is the log-slope, the π the branch discontinuity, both descending from `log(Λ_R/(−iω))`; A and
ω cancel. It is a constant by construction and carries no magnitude information. Draft 3
promoted it to a headline; **withdrawn**. What survives is only the qualitative point that
`(κω)²`-suppression alone cannot disqualify anything the register banks — draft 2's smallness
argument stays withdrawn on that ground.

---

## 5. SCHEME ANALYSIS

### 5.1 Status
Scheme invariance is **UNRESOLVED** on the record: `Q3_LAMBDA_PHYSICAL_CONTENT_GATE.md:30`,
*"menu γ — the scheme-independence demonstration — is open and never executed"*;
`CLASS_C_MANIFEST.json` (repo root), `"renormalization": "UNDECIDED-DISPATCH"`. **This gate
exhibits no transformation and claims none.** The three invariances are kept distinct per
`Q3_LAMBDA_PHYSICAL_CONTENT_GATE.md:33`: *"These are three different properties and the gate
turns on not collapsing them."*

### 5.2 What Γ_T's blindness does and does not show
`calc/RESULTS_gw_tensor_friction.md:31-32`: *"Im L = π ⇒ μ-independent — no scheme slot enters
the friction."* Γ_T is built from Im Σ alone and is therefore blind to the **entire real part**
— so its blindness is not Λ_R-specific evidence. It does, however, establish that the one
channel carried to a number cannot carry Λ_R.

*(The 62.7-order margin at :88 is computed and re-verified. The register's fence — `provenance/claims.json`,
node `rung3_single_pole`, field `boundary_condition`: "ORDER-OF-MAGNITUDE INFERENCE ONLY; do not
quote as a bound, do not consume downstream, …" — attaches to the **few × H₀ bound**, not to the
margin. Draft 1 quoted the margin as a bound; draft 2 misattributed the fence; both corrected.)*

### 5.3 The non-perturbative lever is out of reach
Re D = 0 gives `log(Λ_R/ω) = −320π²/(3κ²ω²)`, i.e. `|log(Λ_R/ω)| ~ 1/(κω)² ≫ 1` — outside the
EFT domain, consistent with "NO pole claim is made."

### 5.4 ρ_TT and η
`ρ_TT(ω→0) = 2 Im G_R^TT(ω)`, `η = lim Im G_R^TT/ω` (`CLASS_C_WALL_CONTRACTS.md:18`,
`DISPATCH_ONE_PAGE.md:22`) sit at ω → 0, outside the Tier-4 band ω ≫ H (node `statement`:
*"omega << H refused by the evaluator"*). **This gate does not evaluate there** — the H² term is
the same order in ω as D₀ in that limit, so the H⁰ truncation is invalid exactly where draft 2
used it. The record also refuses to presume the object: `CLASS_C_MANIFEST.json`, key
`clock.declaration` — *"no global rho(omega) presumed; any one-clock spectral reduction requires
its own proof for the assembled object."*

---

## 6. MATCHING-CONDITION ANALYSIS

`PHYSICS_LEDGER/WALL_KR_MU_CONVENTION_RULING.md:61`: **"Count supplying a number at contract
scope: 0."** The two number-supplying rows are independently barred — **(ii)** matter-stage
MS-bar with μ = 1 (:56, *"STEP 10 bars parameter transfer"*); **(iii)** μ = WC (:57, *"**BARRED
BY CONSTRUCTION**"*). The only valid-type row **(v)** has provenance cell *"nowhere declared for
the contract H⁰ sector"* and status cell *"the H⁰ sector contains no H by construction; m is
matter scope"* (:59, two cells).

**Scope.** This is a count of what has been **declared**, not a proof of what can exist. Draft 2
wrote *"none exists even weakly"*; that stays withdrawn. It suffices to exclude A.

---

## 7. QCD COMPARATOR

Λ_QCD = μ·exp(−1/(2b₀g²(μ))) is exactly μ-invariant — the same RG structure as Λ_R, and it too
needs an input. **The presence of an input does not disqualify Λ_R**, and a subtraction constant
*can* be physical (α(0) fixes the QED vacuum-polarization subtraction). Both drafts' criteria
are withdrawn: draft 1's "fix-one-predict-many" (it indexes physicality to program maturity),
and draft 2's "coupling vs subtraction constant" (it smuggles in its own conclusion).

**What actually separates them here** is not the RG structure but the operator: Λ_QCD's
parameterizes an interaction that controls amplitudes at every order; Λ_R's is, at this
contract's declared evaluation point, removable by a real local field redefinition (§4). The
comparator carries no rebuttal work in either direction.

### 7.1 Sibling alignment — and the one that is not a conflict

`Q3_LAMBDA_PHYSICAL_CONTENT_GATE.md:165-166` reads *"**Route taken: B.** Not A … **Not C** — the
subtraction is registered with zero finite discretion."* **That "Not C" is about that gate's own
outcome set**, whose C concerns the *subtraction's discretion*, not this gate's C (*no physical
observable depends on Λ_R*). On this gate's proposition the same sibling is **aligned**, twice
over, at :160-161 and :165-166. Draft 3 cited that file eleven times without disclosing this.

**The β_c₄ "conflict" draft 3 routed to the owner does not exist.**
`Q3_DIMENSIONAL_TRANSMUTATION_GATE.md:81-88` carries a SUPERSEDING NOTE (2026-09-07): *"Something
does run: the subtraction constant c₄, with β_c₄ = −2A ≠ 0, machine-gated … **Failure 1 below
stands as stated for r** — β_r = 0."* The ":186 candidate that does not run" is
**r := c₀/c₂**, a different object. **Withdrawn.** Draft 3 also inverted which β_r claim was
retracted: `β_r = 0` (:79) is the **standing corrected result**; what was withdrawn was the
nonzero ∂r/∂L computed at fixed slots.

---

## 8. SURVIVING CANDIDATES — NONE

Candidate 17 (Im G_R at finite ω) is the sole live carrier, and it fails:

| Screen | Outcome |
|---|---|
| 1 — independently defined | **Passes.** |
| 2 — observable at declared scope | **FAILS.** Not shown to be an observable is not passed. `SPECIALIST_BRIEF_rung3_spine.md:43`: the low-ω TT class is *"not extractable from the existing computed object"*; the manifest presumes no global ρ(ω). |
| 3 — gauge-invariant | **FAILS**, on an established result. `CLASS_C_WALL_CONTRACTS.md:31-33`: the individual self-energy pieces *"are **GAUGE-DEPENDENT** — established for the dS graviton self-energy by arXiv:1205.4468"*; :41 — *"**Only** the FULLY ASSEMBLED observable"* is expected gauge-invariant. Candidate 17 is a Dyson resummation of those pieces. The manifest's `"gauge": "UNDECIDED-DISPATCH"` is a dispatch-status field (:72, it stays so *"until the dispatched computation exists"*), not an un-establishing of the pieces' gauge-dependence. **Draft 3 scored this "undecided"; withdrawn.** |
| 4 — scheme-independent | **Open** — §5.1, menu γ never executed. |
| 5 — nonlocal vs local/contact | **FAILS**, decided by §3.1: Λ_R is a bijective relabelling of the single local ω⁴ slot, and §4's removal is exact. |
| 6 — independent of c4 | **Retired** — §8.1. |
| 7 — constrains rather than defines | **FAILS** by inheritance: the record's own reopening condition (iii) excludes *"a form-factor feature"*, and the register types the response's coefficients as **form factors** of external kinematics (`Q3_DIMENSIONAL_TRANSMUTATION_GATE.md:103-104`). Candidate 17 is the imaginary part of a dressed two-point form factor. **Draft 3 applied this clause to draft 1's carrier and exempted the identical carrier; corrected.** |

### 8.1 Screen 6 is malformed and is retired
Any candidate must carry Λ_R; screen 6 demands independence from c4; Λ_R is a bijection of c4 at
fixed μ. No carrier can pass. Retired and excluded from the decision — **and its content is not
lost**: that no Λ_R-carrier can pass *is* the statement that Λ_R is a local subtraction constant
relabelled, which §3.1 and §4 establish directly.

---

## 9. BLOCKERS

- **B1 — THE Λ_R DIRECTION IS REMOVABLE.** §4: exact removal by a real, ω-independent local
  counterterm at the contract's declared evaluation point, with the reality condition (N3)
  preserving Im Σ and everything the register banks.
- **B2 — THE SOLE CARRIER FAILS FOUR SCREENS**, three of them on sourced record results (§8).
- **B3 — EVERY CONSTRUCTED CHANNEL IS Λ_R-BLIND.** Gate-E *"by the structure of the registered
  relation, not by assumption"*; Γ_T carries no scheme slot; the noise/IR sector books *"no
  scale hidden in Λ_R"*.
- **B4 — NOTHING PRESENTLY SUPPLIES A NUMBER** (§6) — scoped to declarations.

**Not blockers:** smallness (§4.1); the `calc/` null (§2.3, directory-scoped).

---

## 10. DECISION TREE

    Does the record's response depend on Lambda_R?  YES, exactly (banked).
      |
      +-- Is the dependence carried by a PHYSICAL OBSERVABLE?
            |
            +-- The sole carrier is Im G_R at finite omega. It FAILS screens 2, 3, 5, 7:
            |   not shown to be an observable; gauge-dependent on an ESTABLISHED result;
            |   local/contact by Sec. 3.1; and a form-factor feature, which the record's
            |   own reopening condition (iii) excludes by name.
            |
            +-- Is the Lambda_R direction physically removable?
            |     YES -- exactly, by a REAL local field redefinition, at the contract's
            |     declared evaluation point, with Im Sigma untouched (Sec. 4, N1-N3).
            |
            +-- Does anything DETERMINE Lambda_R?
                  NO -- count 0 at contract scope; both number-supplying routes barred.
                    |
                    +--> A excluded.
                         B excluded: B needs a physical observable. None is constructed,
                           and the one carrier fails four screens. An empty domain makes
                           the existential FALSE.
                         D excluded: see Sec. 10.1.
                         E excluded: see Sec. 10.2.
                    |
                    +--> **C, scoped** (Sec. 13).

### 10.1 Why not D — adjudicated, not dismissed
D requires *a candidate* to appear on adding a new primitive or empirical input. The only
route that would supply a number is the μ election, which
`PHYSICS_LEDGER/WALL_KR_MU_CONVENTION_RULING.md:75-77` prices as *"a **new free input**: a
renormalization-point declaration"*, and :86-88 as *"priced as a **new register input**."* But
`WALL_KR_MU_OWNER_DECISION_PACKAGE.md` records that election as *equivalently declaring Λ_R* —
i.e. **stipulation, not a candidate observable that determines Λ_R.** That is exactly what this
gate's question excludes ("without merely defining"). D is therefore not reached. *(Draft 3
dismissed D in one false line — "no candidate appears at all" — while its own text called two
candidates live; that is corrected here by adjudicating D rather than asserting it away.)*

### 10.2 Why not E — the symmetry claim was a logic error
Draft 3 held that the absence of a constructed observable *"blocks B and C symmetrically."*
**It does not.** Over an empty domain the existential (B) is false and the universal negative
(C) is satisfied; emptiness decides both, in opposite directions. E was reached by treating a
decided asymmetry as a block. Withdrawn.

---

## 11. NEW STRUCTURE, ROUTED NOT ADDED (PHASE 8)

Nothing is added. The computation that would move this result is the record's own:

    Q5 (PROPOSED, not opened): construct the gauge-invariant, assembled, low-omega TT
        observable for the pure-graviton case -- already the record's named blocking
        computation (SPECIALIST_BRIEF_rung3_spine.md:43). Screens 2, 3 and 7 are
        adjudicable only after it exists.

Owner item (not a question): the Q3 disposition discrepancy (ledger OPEN vs
`Q3_LAMBDA_PHYSICAL_CONTENT_GATE.md:191` GATED).

No new primitive, coupling, scale, or renormalization condition is introduced.

---

## 12. REOPENING CONDITIONS

1. **The assembled observable is constructed** (Q5) and carries Λ_R — this is the only route
   that produces B.
2. **B4 falls** — an admissible matching condition supplies a number at contract scope, passing
   the `WALL_KR_MU_CONVENTION_RULING.md` fence rather than being barred by it.
3. **Σ's O(k²) is computed** (`wall_kr_tier4_retarded.py:300-301`: *"The O(k^2) dependence of
   Sigma was not computed at T3 (scope-disclosed)"*) and breaks the ∝ D₀² proportionality
   off the declared domain.
4. **Menu γ is executed** and the scheme-independence result bears on the removal.

**Reopening condition (iii) of the sibling gate** (`Q3_LAMBDA_PHYSICAL_CONTENT_GATE.md:183`) —
*"any observable — **not a form-factor feature** — shown to depend on Λ_R"* — is **not met**,
and its exclusion clause is why candidate 17 fails screen 7.

---

## 13. FINAL CLASSIFICATION

> ## **C, scoped**
>
> **No physical observable in the current structure depends on Lambda_R** — because the Λ_R
> direction is exactly removable by a real local field redefinition at the contract's declared
> evaluation point, and because the sole carrier fails four screens, three on sourced record
> results.

**What C does not say.** It is not a proof that no observable *could* depend on Λ_R. Three
residuals are live and are the reopening conditions: Σ's O(k²) was never computed (scope-
disclosed); menu γ is open; and §6's count-0 is a count of **declarations**, not a proof of
nonexistence. Draft 2 dropped these caveats and its C was unscoped; this one carries them.

**Firmly established, and excluding A:** nothing determines Λ_R. Count 0 at contract scope, both
number-supplying routes barred, Λ_R a μ-free constant in master units whose unit-setting
renormalization point is unregistered.

**This does not invalidate the mechanism.** β_c₄ = −2A ≠ 0 is machine-gated and settled (§7.1),
Λ_R is genuinely μ-invariant, and the reparameterization stands as the owner ruling framed it —
*"REPARAMETERIZED, not removed"* (`WALL_KR_LAMBDA_R_OWNER_RULING.md:56`). What this gate adds is
that the surviving constant, at the current scope, parameterizes a removable direction.

**Q3 is not marked answered** and is not marked impossible; it remains **OPEN** in the ledger.
Ledger delta 0.

---

## 14. DISCLOSURE — three prior drafts, three classifications

**Draft 1 = B, draft 2 = C, draft 3 = E. All three failed both audits.**

- **Draft 1's defects all pointed positive:** a "headline advance" that was a banked fact with
  its meaning inverted; reopening condition (iii) quoted **without the clause excluding its own
  candidate**; a real file (`CLASS_C_MANIFEST.json`) declared a phantom on a wrong prefix; a
  false "normalization discipline" disclosure withdrawing correct record-backed numbers; a
  central result ("first-order Dyson is Λ_R-free") false and obtained by computing only the
  imaginary part; four false screen passes.
- **Draft 2's defects all pointed negative:** the redundancy argument stated **without the
  reality condition**, so it appeared to delete Im Σ; "Λ_R admits no admissible variation,"
  which was false and self-refuted its own deciding section; a fence misattributed; a null
  obtained by surveying the wrong directory; `η = −∞` from an invalid truncation.
- **Draft 3's defects all pointed toward openness:** a **logic error** — treating an empty
  domain as symmetric between an existential and a universal negative; a **manufactured
  covariance fork**, produced by splicing a `CONDITIONAL` across a sentence boundary from the
  H² linkage onto the ω⁴-slot identification; an **analytic identity (4/π) promoted to a
  magnitude**; a settled β_c₄ result re-opened as an owner item; the β_r direction inverted;
  candidate 8 rehabilitated against a published refutation; the ratio Λ_R/μ inverted; and the
  reopening-condition (iii) exclusion applied to draft 1's carrier while exempting the
  identical carrier.

**The pattern, corrected.** §14 of draft 3 named the failure as *"reaching a verdict before the
structure is derived, then erring in whichever direction the last evidence pointed."* That was
right about the mechanism and wrong to think E escaped it: openness is a third direction, not
neutrality. The specific fault in each draft was that **the load-bearing physics step was never
actually performed** — draft 1 never asked whether the direction was removable, draft 2 asked
but omitted the reality condition, draft 3 replaced the question with a fork. Draft 4 performs
it: N1–N3, exact, with the reality condition supplied.

**What survives from all four:** §3.1 (uncontested through six audit passes), §5.3, the machine
counts (now correctly scoped), and the arithmetic, independently reproduced six times.
