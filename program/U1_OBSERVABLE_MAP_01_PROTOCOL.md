# U1_OBSERVABLE_MAP_01 — PROTOCOL (frozen before execution)

**Commissioned:** owner directive 2026-09-14, verbatim priorities: *"U1 first, then U2;
Ω-A as an explicitly bounded parallel investigation"* — and the standing instruction that
governs everything below: *"treat the (ω,k) incompatibility at H≠0 as a physics finding
to be stress-tested, not as a defect to be repaired."*

**The question (owner's, boxed):**

> **What physical observable does the banked K_R(ω,k) actually describe?**
> Not what coordinates label it. Not whether the algebra can be written.
> **What does an experiment measure?**

**Binding constraint carried forward** (owner 2026-08-21, keystone §0, still in force):
*"Do not search for a way to make GRUT work. Search for the mathematically necessary
bridge between the already-banked responsive-vacuum structure and the required
low-frequency gravitational response. If the bridge cannot be derived from the existing
priced inputs, identify the exact new input required and whether introducing it merely
relocates the assumption. Do not promote a possibility to a result."*

## The campaign's pre-registered outcome space (owner's, 2026-09-14 — for U1+U2 jointly)

- **Result A — Covariant completion:** a response-kernel representation not requiring
  simultaneous global (ω,k), recovering the existing limits, with a controlled ω ~ H
  prediction.
- **Result B — Effective-only:** the existing kernel is legitimate but only as a
  local/sub-Hubble effective description; GRUT's claim shrinks to "responsive-memory
  physics works in regime X; the cosmological extension isn't established."
- **Result C — Genuine contradiction:** the required cosmological response cannot
  consistently be constructed; the cosmological GRUT route is dead.
- **Result D — Something new:** the non-diagonalizability forces a new response
  structure absent from the original architecture.

**U1 does not select among A–D.** U1 builds the OBSERVABLE MAP that U2 needs; premature
selection is the named failure.

## Anti-optimization — all four pulls named

A is framework-favorable (the recorded bias direction). B is status-quo-comfortable.
**C is deflation-comfortable — the program's recent groove, and over-deflation is on the
bias ledger too** ("do not replace an overgeneralization with its mirror"). **D carries
the seduction of the owner's own "jackpot" label** — novelty-hunger is a bias, not a
method. None is preferred; the map records what the corpus and standard physics force.

## U1 deliverable — THE OBSERVABLE MAP

For EACH item below, four answers with file-level evidence:

(a) **Operational definition** — what apparatus, along what worldline or over what
    region/ensemble, in what clock, produces what NUMBER;
(b) **Which descriptions can state it** — comoving / static-patch / worldline /
    covariant (functions of the dS invariant z; D3a/D3b are the corpus's own derived
    reductions, AMBER working-paper status: usable as evidence, not citable as banked);
(c) **The banked K_R(ω,k) statement's status for it** — exact-in-a-limit (which limit,
    what controls the error — ε_H or otherwise) / effective-with-controlled-error /
    unsupported as stated;
(d) **The H-regime it occupies** — and specifically whether it lives where the two
    labels diverge (ω ≲ H) or where the trade-off is invisible (H ≪ ω).

**The observables** (fan-out; each gets its own analysis):
1. GW dephasing/attenuation bounds (rung4_love_kk; calc/gw_dissipation_bounds.py) —
   accumulated phase along a null geodesic, LIGO band.
2. Standard-siren amplitude Γ_T (calc/gw_tensor_friction.py) — d_L^GW/d_L^EM over
   lookback, frozen FRW cosmic clock.
3. mu_linear's two kill legs — low-ℓ ISW×galaxy cross (k ~ H₀) and DESI Σ₀ lensing
   (comoving-slice ensemble statistics through the SVT frame).
4. w(z) (rung7_wz) — comoving-slice average over multi-Hubble history ("not an axis
   quantity", D2).
5. rung3's transport coefficient η = lim_{ω→0} Im G_R(ω)/ω — WHAT operation on WHAT
   system measures this, on an expanding background? This is the campaign's hardest
   operational question; "the ω → 0 limit of a label that fails at ω ~ H" must be
   confronted, not assumed away.
6. kk_static_transfer's static modulus (the full-axis dispersion-integral route,
   answered 2026-08-09 at outcomes (b)+(c)) — what would measure χ_static?
7. rung8's tabletop decoherence falsifier — laboratory frame, H-irrelevant?
   (The control case: an observable where the incompatibility should do NOTHING.)
8. The Tier-4 kernel itself as consumed — every banked consumer, with the ε_H fence
   status per consumer.

**Stress-test track (one hostile agent):** attack the finding itself, per the owner's
instruction. Strongest attack lines to pursue honestly: (i) observables are covariant
scalars, labels are charts — if every banked NUMBER is label-independent, the
incompatibility never had physical content ("representation-trivial" resolution); (ii)
[ξ, P_j] = H P_j concerns exact global eigenbases, but no experiment measures a global
eigenbasis — finite-resolution wavepackets may make the obstruction operationally empty
at all accessible scales; (iii) the BD state is dS-invariant, so the two-point function
is a function of z alone and both spectral decompositions are integral transforms of ONE
function — the "incompatibility" may be a statement about transforms, not physics. The
finding survives only if it survives these.

## Ω-A bounded parallel track (one agent; reports to the referee; NO booking recommendation)

**The owner's sharpened question:** *"Is the spatial translation structure merely an
implicit representation choice, or is it an unacknowledged physical input to GRUT's
constitutive kernel?"* Operational test: does any banked NUMBER change under a change of
spatial label (plane waves ↔ multipoles ↔ position space)? Where does the corpus use k
load-bearingly in the CONSTITUTIVE structure (the x(ω,k²) family; x_no_pin's c_i(ω,k²)
floors; μ_linear's k²Ψ; the d³k measure in S_IF §3) versus merely as a chart? BOUNDED:
evidence and analysis only; the booking question stays with the owner
(OMEGA_OMISSION_PACKAGE_01, untouched).

## Hard fences

Zero register/tier/edge/node/stanza edits. rung3's reserved tier question untouched
(its observable is analyzed; its tier is not). AMBER keystone content (D1–D6) used as
working-paper evidence, never cited as banked. Frozen artifacts, dc110ba,
CERTIFICATION_02, the flow node — untouched. Nothing promoted to a result without the
derivation exhibited. Born 2A/2B unauthorized. Suite color is an output.

## Method

Fan-out: eight observable analysts + one stress-tester + one Ω-A analyst, independent.
One evidence verifier over all ten reports (file-level claims checked). Mandatory
referee synthesizes THE OBSERVABLE MAP, rules on the stress-test (does the finding
survive, and in what precise form), answers Ω-A's bounded question on the evidence, and
states what U2 must now derive — WITHOUT selecting among Results A–D. Builder executes,
does not vote.

## Post-verdict mechanics (pre-committed)

Record in `U1_OBSERVABLE_MAP_01.{md,json}`; no register changes; suite + expected_red;
commit; push. U2's protocol is drafted only after U1 lands (owner: "U1 first, then U2").

*Frozen 2026-09-14 before any agent ran.*
