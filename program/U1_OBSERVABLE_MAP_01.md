# U1_OBSERVABLE_MAP_01 — RESULT: the observable map

Executed 2026-09-14 under `U1_OBSERVABLE_MAP_01_PROTOCOL.md` (frozen `f3c5b6a`). Eight
observable analysts + hostile stress-tester + bounded Ω-A analyst, independent; evidence
verifier over all ten (SOUND_WITH_CORRECTIONS — 10 MINOR citation/figure corrections,
applied; 5 cross-analyst disagreements, referee-resolved); mandatory referee synthesis.
Zero register edits; rung3's reserved tier untouched; AMBER keystone content used as
evidence only. Full analyst filings, verifier report, and referee synthesis in
`U1_OBSERVABLE_MAP_01.json`. (Execution note: the run crossed two session-limit outages;
all cached results replayed, referee completed on the third leg.)

## THE MAP

| # | Observable | Banked status | Diverges (lives at ω≲H / k~H)? |
|---|---|---|---|
| 1 | GW dephasing/attenuation (rung4; LIGO band) | EFFECTIVE_CONTROLLED (ε_H ~ 1e-40) | **no** |
| 2 | Standard-siren Ξ(z) = d_L^GW/d_L^EM (Γ_T) | MIXED — licensed horn (a) controlled; horn (b)'s τ₂ ~ 1/H₀ source is the tendril | **no** (tendril noted) |
| 3 | mu_linear's kill legs (low-ℓ ISW ×gal; DESI Σ₀) | MIXED — the measured numbers are chart-invariant; the KERNEL-INFERENCE at k ~ H₀ is the divergent piece | **yes** |
| 4 | w(z) (rung7) | MIXED — w=−1-flat time-domain survives; evolving w(z) is forced at ω ~ H(z) | **yes** |
| 5 | rung3's η = lim_{ω→0} Im G_R(ω)/ω | **UNSUPPORTED_AS_STATED** — the ω→0 limit of a label whose exactness fails at ω ~ H; no exact host exhibited; no measurement operationalization on this background as banked | **yes** |
| 6 | kk_static_transfer's χ_static (full-axis dispersion integral) | MIXED — the H-free refutation leg survives verbatim; the 1/ω-weighted IR segment is the divergent piece | **yes** |
| 7 | rung8 tabletop falsifier (the control case) | EXACT_IN_A_LIMIT — confirmed as the control: no leg feels the incompatibility (~21 orders) | **no** |
| 8 | Tier-4 kernel as consumed | EFFECTIVE_CONTROLLED — every banked consumer inside the ε_H fence; k→0-first object, joint rays not verdict-bearing | **no** |

## THE PATTERN (referee synthesis; the map's value over its rows)

1. **The 4/4 partition coincides exactly with banked-vs-defining-open, 8/8, no
   exception.** The four diverges=false rows are precisely the EFFECTIVE_CONTROLLED /
   EXACT rows; the four diverges=true rows are precisely the MIXED/UNSUPPORTED rows —
   and in every MIXED row the internal split falls on the same line: the
   measured/banked piece is frequency-flat, time-domain single-clock, or H≪ω; the
   unsupported piece is the same observable's ω ≲ H continuation. The discharge-gate
   finding's load-distribution clause, certified at per-observable resolution.
2. **One fence, four doors.** The divergent rows reach ω ≲ H by four structurally
   different routes — a LIMIT (η's ω→0), a FORCING frequency (w(z), the μ kernel
   inference at ω ~ H(z)), an INTEGRAL (χ_static's 1/ω-weighted full axis), and a
   SOURCE tendril (τ₂ ~ 1/H₀ read out at ω ≫ H) — all terminating at the SAME corpus
   object: ε_H = (104/9)H²/ω² and its DomainRejected. One obstruction, instantiated
   four ways.
3. **Column structure.** COMOVING hosts all eight (natively for the safe four,
   effectively-only for the divergent four). WORLDLINE hosts every READOUT — all eight
   apparatuses read on one worldline, each D3a-licensable — but no assembled observable
   except rung8, whose observable IS its readout. STATIC offers *different objects*
   ((ω_T, l, m) statics) for η and χ_static, and hosts no cosmological ensemble
   quantity. COVARIANT is empty — nothing exhibited (absence of a covariant statement
   is not an impossibility proof; recorded as a limit).

## Stress-test ruling: THE FINDING SURVIVES AS STATED

Eight attacks (three protocol-named + five self-found), none sustained — several
strengthening the finding: no banked number requires joint-label exactness *because the
corpus engineered it away* (Tier-4 is k→0-first with a hard gate); the operational
wavepacket floor √(H/ω) ~ 1e-11 at band quantifies the load-distribution clause; the
transform-triviality attack fails on FRW ≠ dS and where it holds it derives the finding;
the Minkowski parity fails because Minkowski possesses a commuting pair and dS does not.
One incidental yield, recorded: **AMBER keystone D4 contains an internal defect**
('HT = e^{Ht} (axis)' contradicts D1's exact axis identity T = t, verified from the
embedding) — not load-bearing for the finding; working-paper erratum for the owner's
queue, no register consequence. One residue left open (deliberately): whether a
finite-bandwidth wavepacket drive at ω ~ few×H with Δω ~ H operationally probes the
low-ω class without an exact label — assigned to U2 (it decides the sharpness of η's
boundary: "no operationalization" vs "no exact-label operationalization, class-probe
open").

## Ω-A ruling (bounded; no booking recommendation): MIXED_BY_USE

Three separable things: (1) **k as a label** (basis on a fixed slicing) —
REPRESENTATION CHOICE; no banked number changes under plane waves ↔ multipoles ↔
position space on the same slicing (analytic argument: SO(3)-scalar kernel pairs
c_i(ω,k²) + unitary regrouping; marked as an analyst's mathematical judgment — no calc
re-ran a banked number end-to-end in a second basis). (2) **k-dependence of the
constitutive kernel** — physical content where asserted (the x(ω,k²) family, c_i(ω,k²)
floors, k²Ψ). (3) **The slicing choice itself** — the load-bearing commitment; this is
where Ω-A's booking question genuinely lives, and it stays with the owner
(OMEGA_OMISSION_PACKAGE_01, untouched).

## U2 SPECIFICATION (fixed by this map; U1 selects no Result)

- **B1 — the wall-A assembly reduction (the precondition of everything):** derive
  whether the gauge-invariant assembled TT response Σ(x;x′) reduces to an
  along-worldline one-time kernel (D3a form), to the static-Killing (ω_T,l,m) form
  (D3b), to a covariant form, or to neither. Needed for η (row 5). Failure condition:
  record a domain-of-validity boundary — η-as-banked is a formal extrapolation
  parameter; its surviving operational content is the boundedness/class statement over
  the accessible band. Also settle the stress-(ii) wavepacket residue.
- **B2 — the cosmic/static bridge as a theorem** (keystone §6.3's dichotomy): a derived
  transport between static-Killing-clock objects and cosmic-clock comoving-slice
  exports — retiring the D6 input. Needed for χ_static's IR segment and any future
  T_dS-against-comoving-frequency use. NOT needed for the H-free refutation leg.
  Failure condition: the excised ω ≲ H segment enters as one more unconstrained
  subtraction constant; conditional-never-unconditional, direction preserved.
- **NOT needed:** anything for the four safe rows (no bridge changes their numbers); no
  repair/re-derivation of any banked number (all chart-invariant per the number-change
  test); no sourcing of physics inputs priced as inputs (τ₂, the staked B, etc.).

## Limits on the face

Ten recorded (full list in the .json), including: the covariant column records
exhibition, not possibility; the Ω-A number-change test is analytic, not a re-run
calc; marked physics judgments are load-bearing at named points; the diverges boolean
cannot express tendrils (the siren row carries one); AMBER dependence throughout the
statability columns; and everything the map cannot decide belongs to the owner (rung3's
tier; Ω-A/Ω-B booking; P1B; P6).

---

*Filed 2026-09-14. Per the owner's economy directive (2026-09-14: "We've built the
referee. Now let's make the referee shut up long enough to let the physicist work"),
this is the complete U1 record — no separate certification artifacts. U2 proceeds
directly on the specification above, with audit machinery used only to prevent
unsupported claims.*
