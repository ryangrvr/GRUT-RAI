# REALITY PROGRAM — NEXT-ACTION ANALYSIS

> **This is a prioritization artifact. It is NOT science, NOT a gate, and NOT a scientific claim.**
> Q3 remains **OPEN** (`program/QUESTION_LEDGER.md:163`). Q2-BRIDGE untouched. Register untouched
> (74 nodes, sha256 `beaeb84e8a6f8468`). Ledger delta 0. No physics computed, no observable built
> or sketched, no ledger status changed.

**Date:** 2026-09-08 · **Ranking key:** dependency value and information gain. **Cost is not a
ranking key** — an action that only tidies documentation ranks below a costlier one that converts
an unanswerable question into an answerable one.

**STANDING CAUTION, carried on the face of this document.** The prior decision analysis classified
the observable assembly **B — constructible in principle, requiring frontier expertise**. That is a
**RESOURCE classification, not a scientific validation.** It does **not** mean the observable is
mathematically well-defined. The eight-link chain still contains specification gaps, an
unreconciled charter/register contradiction, and two items no amount of assembly work can
discharge. Nothing below may be read as establishing the observable's existence.

---

## 1. THE THREE FINDINGS THAT DOMINATE THE RANKING

### 1.1 The zero-`derived` tier is a GATE, not a computational shortfall

`provenance/resident.py:230-234` implements: *"(2) tier contradiction: a RESULT tier resting on an
OPEN/assumed input"* — a claim in a result tier whose `depends_on` includes any node in an open
tier is flagged and capped. Every derived-pending or graduating node in this register has such a
dependency:

    rung3_single_pole   -> rung1_ontology_finite_memory   (assumed)
    mu_linear           -> p_tt_ansatz                     (assumed)
    eft_operator_basis  -> rung5_gr_limit                  (assumed)
    x_no_pin_theorem    -> eft_operator_basis              (to-derive)
    kk_static_transfer  -> x_no_pin_theorem                (derived-pending)

> ### ⛔ THIS SUBSECTION'S CONCLUSION IS **WITHDRAWN**. It was false three ways and it was an
> exculpatory reframe of the program's worst number — caught by the hostile refuter, which I had
> specifically tasked to attack it as such.
>
> **(a) The rule does not forbid anything.** `provenance/resident.py:208-210`: these flags
> *"**never BLOCK on their own** (a heuristic should not hard-block) -- they push the verdict to
> FLAG-FOR-FIREWALL so the adversarial screen + human decide."* Flagged is not capped.
> **(b) The register already contradicts it.** Two RESULT-tier nodes rest on an OPEN-tier input and
> are banked anyway — `rung1_inin_formalism` (shown) → `background_time_translation_flow` (assumed),
> and `rung2_kms_gate` (shown) → the same. Ten further RESULT-tier nodes have **no** open dependency.
> Clean result-tier entry has happened **twelve** times.
> **(c) The charter says the opposite.** `CHARTER.md:85-86`: *"`derived` requires a settling check…
> **Awaiting a computation ⇒ `derived-pending`, not `derived`**"* — a computation is precisely what
> graduates a node, and `mu_linear`'s own boundary names that computation.
> **(d) An equivocation.** `OPEN_TIERS` = {`to-derive`, `assumed`, `derived-pending`}, not
> {`assumed`}. I silently narrowed "OPEN/assumed input" to "assumption". Retiring a `to-derive`
> node **is** doing the computation.
>
> **What survives:** the dependency chains in the table above are real, and the `derived` tier is
> genuinely empty while `shown` holds 12. That is all. It is a fact about where graduation has
> stopped — not a proof that computation cannot cause it.

### 1.2 The entire K_R campaign supports nothing downstream

Machine-verified over all 74 nodes:

    kr_contract_retarded_tier4    tier=shown   out-degree = 0
    response_lorentz_covariance   tier=shown   out-degree = 0

The Tier-1 vertex (26,032 terms), the massless bath, the one-loop response, the retarded contract
kernel and the covariance discharge are real, executed, and currently **have zero downstream
register edges**.

> **INTERPRETATION WITHHELD (owner ruling, binding).** An earlier draft read this as "the graph's
> leverage is not where the effort went." **That inference is withdrawn.** Out-degree 0 is a
> dependency-graph fact and **does not by itself mean the work was scientifically unproductive.**
> Before it may be used to redirect any effort it must be classified as exactly one of:
> **(a)** scientifically justified — it genuinely supports nothing downstream yet, for a stated
> reason; **(b)** a bookkeeping/registration omission — downstream nodes exist that should list it
> in `depends_on` and do not; **(c)** superseded — the consumers were retired or re-scoped;
> **(d)** genuinely unresolved. **A scientific dead end and a dependency-graph disconnect are
> different things, and this document does not distinguish them.** That audit is open; no ranking
> here may lean on this fact until it returns.

### 1.3 Out-degree among unresolved nodes

*(This subsection previously read "Leverage sits on the ASSUMPTIONS, not the computations." That
framing rested on the withdrawn §1.1 conclusion and is **withdrawn with it** — `CHARTER.md:85-86`
makes a computation precisely what graduates a node. The table below is a graph fact with no
inference attached.)*

Out-degree among unresolved nodes (machine-counted; 43 of 74 nodes have out-degree 0, and 26 are
total isolates):

| node | tier | unblocks |
|---|---|---|
| `rung3_single_pole` | derived-pending | **4** |
| `p_tt_ansatz` | **assumed** | **3** |
| `background_time_translation_flow` | **assumed** | **3** |
| `rung7_wz` | to-derive | 2 |
| `rung1_ontology_finite_memory` | **assumed** | 2 |

Nothing else exceeds 2. Three of the top five carry tier `assumed`. **No inference is drawn from
that here**: §1.1's conclusion is withdrawn, so nothing licenses treating assumption-retirement as
privileged over computation.

---

## 2. CANDIDATE ACTIONS BY CLASS

Classes: **A** owner/specification ruling · **B** in-house computation/data · **C** first external
dispatch · **D** Q4 gate · **E** observable assembly · **F** defer.

| Action | Cls | Unblocks | Status change | New assum.? | Frontier? | Frontier tested? | Info gain | Fences |
|---|---|---|---|---|---|---|---|---|
| **First external dispatch** (narrow, §4) | **C** | 0 direct; up to 3–4 contingent | **SCIENTIFIC (contingent)** | No | Tests the label | **NEVER — 0 sent, ever** | **HIGH — resolves an untested resource claim** | Preserved |
| **Menu γ** — scheme-independence for the local slot | **B** | 1 (fires reopening cond. (ii)) | **SCIENTIFIC** | No | No | n/a | MEDIUM–HIGH; instrument exists | Preserved |
| **Retire an assumption** (p_tt_ansatz path) | B | 3 | **SCIENTIFIC** | No | Partly | No | MEDIUM — *downgraded; its HIGH rating rested on the withdrawn §1.1* | Preserved |
| Σ at O(k_ext²) | B | 1 | SCIENTIFIC | No | No | n/a | MEDIUM; removes 1 of 3 blockers on one consumer | Preserved |
| Resolve band tension (routing 13) | A/B | 2 | SCIENTIFIC-ish | No | No | n/a | MEDIUM | Preserved |
| The 9 specification repairs | **A** | mostly **0** | **BOOKKEEPING** (16 of 21 owner items) | No | No | n/a | LOW except where noted | Preserved |
| — except: define/strike "form-factor feature" | A | 0 direct | **SCIENTIFIC** | No | No | n/a | MEDIUM — decides whether a whole class of future work can count as reopening | Preserved |
| Q4 gate | **D** | 0 | — | — | — | — | **LOW — no gate file exists; in-degree 0, out-degree 0** | Preserved |
| Build the observable | **E** | 3 genuinely distinct | SCIENTIFIC | No | Yes | **Never tested** | Opens the **confirming** direction only | Preserved |
| Defer everything | **F** | 0 | — | — | — | — | Zero | Preserved |

**Removed from consideration on verification** (each was a plausible candidate that does not exist
as an available action): the μ(c₀)/slip calculation **already ran** (2026-08-02,
`calc/mu_slip_interior.py`); the low-ℓ TT-auto gate **already ran** and was A8-demoted, its repair
barred by a charter automatic-fail rule rather than by cost; **DESI DR3 does not exist until 2027**
and the valuable half — the sealed falsification threshold — was completed 2026-08-18; the w(z)
reconstruction needs a data interface the program states it does not have.

---

## 3. TOP THREE

### RANK 1 — **Send the first external dispatch** (class C)
The only action on the board that resolves an **unknown about the program's own capability
envelope**. Every other action operates *inside* an assumed envelope; this one measures it.
Nine dispatch-ready artifacts exist, **zero have ever been sent**, and the frontier label on the
two genuinely frontier routing items has therefore never been tested by anything except in-house
attempts. Informative in both directions: a reply establishes external capability is reachable; no
reply, or a refusal, establishes the boundary the program has assumed for months without evidence.

**This is NOT ranked on cheapness.** It is ranked because it converts an untested resource
assumption into a measured fact, and because the program's entire frontier triage currently rests
on that assumption.

### RANK 2 — **Execute menu γ** (class B)
The scheme-independence demonstration for the local slot: *"open and never executed"*
(`program/gates/Q3_LAMBDA_PHYSICAL_CONTENT_GATE.md:30`). The instrument already exists (the frozen
PV cross-check pattern), the program has demonstrated capability of this class, and it fires a
**named, externally-decidable reopening condition (ii)**. It is the strongest purely in-house
action that changes a scientific status rather than a document.

### RANK 3 — **Open the assumption-retirement path** (class B)
**The justification that stood here is withdrawn** — it read "the only class of action that can ever
produce the register's first `derived` entry", which §1.1 now refutes. What survives is the
out-degree fact alone: `p_tt_ansatz` (3 dependents) is
the natural first target — it has already been interrogated and returned **CHOSEN, not forced**, and
the record names the deliverable that would retire it (the un-projected linear-response object
decomposed without inserting P^TT). Ranked third on ordinary grounds. **The claim that "on leverage alone this is first" is
withdrawn** with §1.1; on the corrected picture it is one candidate among several.

---

## 4. THE DISPATCH SPECIFICATION

### 4.1 What must NOT be sent, and why

**Not the pole-vs-cut headline ask.** `DISPATCH_ONE_PAGE.md` reached RELEASED FOR DISPATCH on
2026-08-04 and has been **HELD since 2026-08-10** on a substantive concern: the question may be
ill-posed in the formalism its intended readers use — its central variable, ω conjugate to cosmic
time, is essentially absent from the corpus it was written for, because that corpus never reduces
Σ(x;x′) to a time difference. **A first dispatch that arrives ill-posed tests nothing about
external capability**; it only tests whether a stranger will answer a malformed question. The hold
should stand.

**Not the full technical brief, not the frozen package, not the superseded briefs.** Three are
marked do-not-dispatch; two contain no question at all; the rest inherit the hold.

### 4.2 The recommended first dispatch — the narrowest ready object

> ### ⛔ THE OBJECT SELECTED HERE IS **WITHDRAWN**, on two independent counts.
>
> **(1) It came from a brief that names its own replacement.**
> `SPECIALIST_BRIEF_2_alpha_carrier.md:3-5`: *"**STATUS: PARTIALLY SUPERSEDED** … the carrier half
> remains live but is now carried as **Rider A of `SPECIALIST_BRIEF_rung3_spine.md`** — **dispatch
> that, not this.**"* §4.1 above excludes superseded briefs and then this section exempted its own
> choice from that filter.
>
> **(2) It is NOT direction-independent, and the asymmetry favours GRUT.** I selected the
> **normalization** half — which `:8` marks as the stale half — and dropped the **carrier** half the
> brief marks LIVE. In register terms the outcome matrix is:
> * **negative** → restates `rung9b_bridge`, already banked `disposition: settled-negative`, with
>   **out-degree 0**. Nothing moves.
> * **positive** → is that node's *sole* reopen condition — *"FROZEN (Version I) — not actively
>   pursued, reopen ONLY on a genuinely new scalar→TT [intertwiner]"* — reviving a frozen no-go.
>
> **Only the confirming direction changes any program state.** That is exactly the property for
> which §2 disqualifies "build the observable." I booked the concession as a virtue in the next
> paragraph and did not notice it was the disqualifier.

**The corrected candidate is Rider A of `SPECIALIST_BRIEF_rung3_spine.md`** — the carrier question,
which the record marks LIVE and explicitly directs be dispatched instead. **Its
direction-independence has NOT been verified here** and must be, before it is sent. The
specification below is retained only as the *shape* a dispatch must have; its content is void.

    EXACT OBJECT            an intertwining map between the scalar-probe assembled response and
                            the TT (spin-2) channel, on the declared de Sitter background
    EXACT ASK               does such a map exist, and if so is it unique? A yes/no with a
                            construction, or a no with the obstruction named.
    INPUTS SUPPLIED         the declared background, the mode conventions, and the scalar-probe
                            assembly of the published literature. Nothing GRUT-specific is needed.
    EXPECTED OUTPUT         existence verdict + either the map or the obstruction
    ACCEPTANCE CRITERION    a stated verdict either way with its reasoning. NOT contingent on the
                            answer's direction.
    EXPLICITLY NOT ASKED    no endorsement of GRUT; no opinion on the framework; no assembled
                            observable; no pole-vs-cut verdict; no review of the program.

**The direction-independence argument that stood here is withdrawn** (see the box above): "its
positive outcome is written in the same terms as a frozen reopen condition already on the record"
was recorded as a virtue when it is the defect — it identifies the confirming direction as the only
one that moves anything.

**Two repairs are required before it could be sent** (both one-line, both mechanical): its question
section is a dangling placeholder pointing at fields that exist in no file, and its cited
obstruction cites a superseded coarse form. Neither is a scientific change.

**Transmission is an owner act.** This document specifies; it does not authorize, and nothing here
constitutes sending anything.

---

## 5. RECOMMENDATION

> ## **THE DISPATCH CLASS REMAINS RANK 1 — but the object is NOT yet selected.**
>
> The recommendation is: **verify direction-independence for Rider A of
> `SPECIALIST_BRIEF_rung3_spine.md`, and if it holds, send that.** §4.2's original object is
> withdrawn. No dispatch should be sent until an object is shown to change program state in **both**
> directions.

**The justification is not cost.** It is that the program's entire frontier triage — which items are
reserved, which are in-house, what "constructible with frontier expertise" is worth — rests on a
resource claim that has **never once been tested in the program's history**. Nine artifacts reached
dispatchable quality; zero were transmitted; and the label has been carried forward as though
established. One narrow, well-posed, direction-independent question resolves that, and it commits
the program to nothing.

**If Rider A also fails the direction-independence test**, or the owner declines the dispatch,
RANK 2 (menu γ) becomes the recommendation and stands on its own merits: in-house, demonstrated
capability, fires a named reopening condition.

**The surviving premise for RANK 1.** The refuter attacked "never tested" as false, citing four
loci that read as outside engagement. That attack **fails**: the register's sealed
authority-vocabulary annotation states that *"'specialist', 'referee', 'independent' and 'external'
denote in-house passes — separate AI sessions run by that same author"*, that **no outside human has
ever been contacted**, and that the misleading strings are *"ANNOTATED, NOT RENAMED"* — i.e. left in
place deliberately, with the annotation governing. The refuter cited exactly the strings the
annotation exists to correct. **The untested-resource-claim premise stands.**

**What this recommendation explicitly does not do.** It does not build the observable, does not
touch Q3's OPEN disposition, does not reopen Q2-BRIDGE, adds no assumption, and changes no register
status. It is a test of the program's resource envelope, not of GRUT.

---

## 6. WHAT WOULD CHANGE THIS RANKING

1. **A dispatch is sent and answered** — the frontier label becomes measured; the two frontier items
   are re-triaged on evidence, and RANK 3 likely rises above RANK 2.
2. **A dispatch is sent and ignored** — equally informative; the program should then treat
   FRONTIER-RESERVED as a hard boundary and concentrate entirely on in-house classes A/B.
3. **The owner reverses the `STATE.md:113` supersession** — `rung3` moves from settleable to gated
   and the observable's relative value rises.
4. **An assumption is retired** — the first `derived` entry becomes reachable for the first time,
   and §1.1's cap lifts for everything downstream of it.

---

## 7. DISCLOSURE

**Method.** Five record sweeps, each adversarially verified. **The verifiers found the ranking
sweep committing two of the biases this analysis was commissioned to avoid**: a top-ranked action's
unblock count inflated threefold (a transitive edge counted as direct, and an item counted that the
dispatch does not actually ask for), and a second action ranked explicitly on being *"cheapest for
the recipient"* — the precise inversion the ranking key prohibits. Both corrections are applied
above: unblock counts in §2 are verified-direct only, and §5's justification is stated in
information terms with cost excluded.

The verifiers also caught a **staleness defect**: a candidate re-pose rested on a "gapped tower of
quasinormal poles" reading that the program's own computation **retracted** nine days later (the
free retarded response is pole-free). That candidate is not carried here.

**Asymmetric strictness, corrected.** The ranking sweep demoted sixteen owner items to BOOKKEEPING
on a strict test and then exempted its own top two from that test. §2 applies the test uniformly —
which is why the dispatch's status change is marked **SCIENTIFIC (contingent)** rather than
SCIENTIFIC: deciding to transmit changes nothing known until a reply arrives, and the base rate of
replies in this program is zero because nothing has been sent.

**One owner item was missing from the enumeration** and is added: a numerical μ / Λ_R declaration,
which must be *"priced as a new register input"* — it belongs in the recommend-against cluster.

**Not carried:** `program/gates/Q3_PHYSICAL_SCALE_BRIDGE_GATE.md` is stamped FAILED AUDIT — NOT A
RESULT; it is referenced here only as an attempted-and-failed adjudication.
