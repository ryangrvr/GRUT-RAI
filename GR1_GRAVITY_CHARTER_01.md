# GR-1 — (K,N)_grav AS A HIERARCHY POINT; THE CLASS-4 GATE ATTACKED: OWNER RULING RECORD + CHARTER + PRE-REGISTRATION (frozen before evaluation)

**Date:** 2026-09-25 · **Authority:** GitHub Issue #2, owner comment
**5828202542**. **G-2 accepted at recorded strength** (spectral recovery
in-class; single-site degeneracy promoted to a structural interface
limit; the invariant-order ladder; machinery NULL-REDUNDANT; no
universal spacetime/metric theorem). **The gravity-hierarchy question
opens**, with the binding instruction: *do not silently upgrade the ω⁷
result — class-4 remains open until independently frozen, executed and
adjudicated; ask whether gravitational response/noise data can inhabit
the admissible influence hierarchy consistently with the recovered
geometry, and whether the ω⁷ channel is genuinely SELECTED by the
surviving per-branch counting structure rather than merely REPRODUCED
by an already-chosen occupancy.*

**Standing record consumed (cited, not re-derived):** the acoustic
two-phonon channel is CONDITIONALLY DERIVED (v3 instrument 20/20,
measured ω^7.008, kernel class t⁻⁸ under the propagator convention;
`CHERENKOV_RECORD_CORRECTION_01.md`) with three ledgered imports —
κ = 1/M_Pl, retained-sector structure, minimal-stress postulate. S-1's
selection law (additive increments: +2 per vertex momentum power,
+(d−1) phase space, +4 per order-2 symmetry cancellation), as modified
by P-3 (per-branch counting; min-dominance masking). **Fences:** no
Einstein equations or gravity field equations anywhere; ω⁷ never
promoted beyond its recorded status by this run; Λ_R, Matsubara, Π₀, U5
fenced; ℏ located-not-generated (gravity INHERITS the floor, never
generates it); type-III boundary map only; geometry consumed ONLY as
the G-2-recovered substrate; stationarity a property of the static
testbeds. Amplitudes stay free per S-1 PC-D.

## 1. THE LEGS (frozen)

- **L-B (branch table — elimination is structural):** (i) same-sign
  pair channel at zero net momentum: kinematically closed (q, q′ > 0
  with q + q′ = 0 impossible — verified by scan, no interior root);
  (ii) gapped counterfactual ω(k) = √(1 + k²): the pair root-finder
  finds NO root at ω ∈ {0.5, 1.0, 1.9} and finds one at ω = 2.5 —
  emptiness below threshold 2Ω exact (the true-emptiness fingerprint,
  reproduced fresh); (iii) exactly linear dispersion: the full vertex
  is IDENTICALLY zero (< 1e-14) — tracelessness kills the channel
  without curvature. Min-dominance then selects the sole surviving
  branch trivially.
- **L-X (the selection core — counterfactual-predictive counting on a
  FRESH instrument):** 1D counter-propagating pair channel, dispersion
  ω(k) = k − αk³ (α = 0.05), net momentum zero (q′ = −q), golden-rule
  J(ω) = Σ_roots |M|²/|dω_tot/dq| by exact Newton root-finding, slopes
  from log[J(0.2)/J(0.1)]/log 2. Vertex variants with predictions
  WRITTEN HERE, before any run: kinetic-only M = ω(q)², slope = base b;
  potential-only M = q², slope = b; non-minimal admixture
  M = ω(q)² − (1 + 0.3)q², slope = b (the cancellation is destroyed);
  FULL minimal-stress M = ω(q)² − q², slope = **b + 4** (the order-2
  cancellation leaves the α-residual −2αq⁴ + …); exactly-linear: 0.
  In this 1D zero-transverse convention b = 4. **Gates:** the three
  base-class slopes agree within ±0.2; full − base = +4 ± 0.2; linear
  < 1e-14; all slopes invariant under coupling rescale λ = 16 (exact)
  — κ is amplitude-only. **Frozen adjudication:** exponents tracking
  STRUCTURE (which symmetry cancellation is present), predicted in
  advance and confirmed per counterfactual ⇒ SELECTED-BY-STRUCTURE
  in-class, not reproduction; any counterfactual landing off its
  prediction ⇒ REPRODUCED-ONLY is declared.
- **L-D (dimension consistency with the RECOVERED geometry):** on the
  same substrates where G-2's interface machinery measured d̂ =
  1.000 and 2.942 (chain N = 61; lattice 13³, Neumann spectra), count
  low-frequency phonon PAIRS: N_pairs(W) with ω_i + ω_j ≤ W at the
  frozen windows W ∈ {0.4, 0.8} (1D) and {0.9, 1.8} (3D). **Gates:**
  log-slope = 2d within ±0.4 (1D) and ±0.6 (3D). The d entering the
  gravitational phase-space counting is the SAME d the interface
  recovers spectrally — the hierarchy point sits over the recovered
  geometric substrate, not over an inserted label.
- **L-H ((K,N)_grav as a hierarchy point):** discretize
  J(ω) = ω⁷e^{−ω} into 40 modes (ω uniform on [0.075, 6],
  g_i² = J(ω_i)Δω). (i) Vacuum floor: ν_i = ℏg_i²/2 saturated EXACTLY
  — by construction of the vacuum bath, labeled as such: gravity's
  vacuum point sits ON the P-2 floor, inheriting ℏ (located, never
  generated); thermal β = 2 strictly interior (margin measured > 0).
  (ii) The 4×4 operator Gram over {1, B(0), B(t₁), B(t₂)}
  (t = 1.3, 2.9) is PSD (min eigenvalue ≥ −1e-12); the ×1.5
  inflated-commutator tamper MUST be detected (halt-grade). (iii)
  λ-scaling: λJ admissible for λ ∈ {0.1, 16} (S-1 PC-D at the gravity
  point). (iv) Kernel-tail identity: quadrature of
  ∫ω⁷e^{−ω}sin(ωt)dω matches the closed form Im[Γ(8)(1−it)^{−8}] to
  1e-6 relative at t ∈ {5, 20, 60}; the measured large-t slope on
  [20, 80] is REPORTED ONLY — the sealed t⁻⁸ class lives in the v3
  propagator convention and no cross-convention gate is manufactured.
- **L-I (import adjudication — the class-4 ruling input, frozen
  mechanical):** κ = 1/M_Pl: DISCHARGED as amplitude-only if the
  L-X rescale gate passes. Minimal-stress: LOAD-BEARING if the
  non-minimal counterfactual loses the +4 (its measured slope = base
  class). Retained-sector: LOAD-BEARING if the gapped counterfactual
  is empty below threshold. **Frozen consequence rule: the class-4
  gate PASSES only if no import beyond locality + symmetry is
  load-bearing for the exponent. If minimal-stress and/or
  retained-sector are load-bearing (expected), the gate REMAINS OPEN
  with its residual obstruction named exactly: the exponent is
  selected by counting GIVEN the coupling class, and the coupling
  class is supplied.** The absolute exponent 7 stays graded as S-1
  left it — CONSISTENT-BY-MECHANISM at increment level (base-3 in the
  v3 convention + 4), never one-formula-derived here.

## 2. OUTCOME CLASSES (frozen mechanical)

**GRAV-HIERARCHY-ADMISSIBLE** (L-H: the ω⁷ point inhabits 𝔠_full with
floor/Gram/scaling verified) · **SELECTED-BY-STRUCTURE-IN-CLASS** (L-B
+ L-X: branch elimination structural; increments counterfactual-
predictive) · **REPRODUCED-ONLY** (any counterfactual off its written
prediction) · **CLASS-4-PASSED** (only per the L-I rule — no
load-bearing import) · **CLASS-4-OPEN** (expected; obstructions named)
· **DIMENSION-CONSISTENT** (L-D) · **FAILS-NULL** (any construction
fails). Components reported separately, never averaged.

## 3. CONTROLS

Halt-grade: the Gram tamper detector must fire; the linear-dispersion
zero and gapped emptiness are exact (a nonzero value halts as an
instrument bug); the kernel quadrature-vs-closed-form identity at
1e-6. Convention-laundering null carried from S-1: increments are
gated, absolute slopes in this instrument's convention are never
compared across conventions. All numbers frozen above; the v3
primaries are cited by hash, never re-run or edited.

## 4. DELIVERABLES AND STOP

1. `calc/gr1_gravity.py` (pure stdlib; emits `GR1_GRAVITY_RESULT.json`,
   sha-hashed).
2. `GR1_GRAVITY_VERDICT_01.md` + closing comment on Issue #2.
3. **HARD STOP at the GR-1 verdict** (owner: "hard stop after the next
   gravity verdict"). Geometry-selection remains a separate possible
   fork if gravity cannot close the hierarchy question.
