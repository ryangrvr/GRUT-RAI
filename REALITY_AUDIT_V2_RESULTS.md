# Reality Audit V2 — Load-Bearing Chain Forensics

**Date:** 2026-08-23 · Auditor: Ox (builder) · Charter §7 self-audit limitation applies.
All numbers emitted from `claims.json` via script; no hand-typed summaries ([ADDED-A]).

## Step 1 — Use-vs-cite test (discriminator 1)

The root exists to make single-frequency K_R(ω,k) definable. For each of the 28 dependents:
does its content manipulate an ω-space object, or merely cite the node?

| class | count | share |
|---|---|---|
| **USE** (ω-space content present) | 18 | 64% |
| **CITE-ONLY** (no ω-object anywhere) | 10 | 36% |

**Headline restated:** the chain's reach is **predominantly physical, not architectural** —
64% of dependents carry genuine ω-space content. The "39% of register" figure stands but now
carries its qualifier: 18 of those nodes use the flow to define frequency-space objects; 10 ride
along as registry architecture.

## Step 5 — rung1 split-reach: THE HEADLINE FINDING

Splitting R1-FORMALISM / R1-ONTOLOGY and re-running reach on the pair:

| part | dependents referencing it |
|---|---|
| **R1-FORMALISM** (SK influence action, K_R + N) | **27 of 27** |
| **R1-ONTOLOGY** ("the vacuum *is* a responsive medium with finite memory") | **0 of 27** |

**Every one of rung1's 27 dependents references only the formalism. Zero reference the ontology.**

> **The program's weight sits entirely on borrowed physics. The founding ontological stance —
> the thing GRUT *is* — is load-bearing for nothing downstream of itself.**
> This is the result the brief pre-registered as most valuable, in the direction where the stance
> is nearly free. It is publishable either way; this is the way it came out.

## Step 4 — UN-ASSUME the root

Dependents at tier `assumed` that would become conditional if the root's assumption were declined:

5 of 28: arrow_of_time · p_tt_ansatz · rung5_gr_limit · rung6_qm_limit · rung9b_bridge

(DELETE-reach = 28 as before. UN-ASSUME is a different operation and the counts are not merged.)

## ADDED-F — Minimum core, three ways

| cut | count | ids |
|---|---|---|
| survives UN-ASSUME | 4 derived-pending + 3 measured | kk_static_transfer, mu_linear, x_no_pin_theorem, rung3_single_pole · m_planck, rho_lambda, v_ew |
| GRUT-specific rather than borrowed | ≤4 (all derived-pending) | the four above are computations, not borrowings |
| not already predicted by a competitor framework | **not computable this session** | needs competitor-framework screen (instrument gap → blocker log) |

The third number — the one that matters — requires a competitor screen that does not yet exist.
It joins the build queue.

## ADDED-B — Drift-event analysis

Five events; direction: two understate own success (events 2 and 4), one re-asserts superseded state,
one stale-neutral, one over-count by one. **Confirmed: NOT uniformly self-favouring.** Mechanism is
consistent with stale-read from long uninterrupted runs; repair is mechanical (emit), not dispositional.

## Stopping point (ADDED-G)

Steps 7–10 not reached this session. Blast radius falls below 3 beyond rank 12 in the V1 map;
recursion bounded there would add ~9 more nodes. Deferred with reason recorded.

