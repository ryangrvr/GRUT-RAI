# CLOCK-MISMATCH — OWNER RULING 01

**Date:** 2026-09-25 · **Rules on:** `CLOCK_MISMATCH_VERDICT_01.md` (verdict (C),
battery 23/23, artifact sha `e3c757dd…`). Recorded from the owner's own words;
any misstatement is corrected by owner edit.

## 1. THE VERDICT STANDS AS (C) — AND IS NOT TO BE READ AS (B)

Owner, verbatim: *"don't quietly turn C into B."* The distinction is ruled
binding: **(B)** would mean the discrepancy was a pure coordinate artifact;
**(C)** means *the coordinate issue disappears, but the physical comparison
remains undefined because the required background-transport rule has not been
established.* The key correction, at the owner's precision:

> The supposed clock mismatch was **misidentified**. The unresolved problem is
> the **transport of a kernel derived on constant-H de Sitter onto the
> matter-containing FRW background in which τ₂ is defined.**

## 2. WHAT IS RECORDED (the five points, owner-specified)

1. **Killing time ↔ FRW cosmic time: no discrepancy on the shared comoving
   worldline.** T = t from the embedding; the old "two clocks" objection is
   **retired**.
2. **Within the de Sitter toy: unchanged.** The 6.12% / 4.32 / 6.68 control
   result remains valid.
3. **The cosmological comparison: unresolved.** It mixes an exact constant-H
   dS ladder/kernel with a τ₂ defined on an evolving matter+Λ FRW background.
4. **The missing object is a transport prescription**
   K_dS(t−t′; H) ⟶ K_FRW(t, t′; H(t), …). Until GRUT derives or independently
   fixes that map, neither the frozen-H₀ shortfall nor the high-z e-fold
   escape can be promoted into a physical contradiction.
5. **The ~367× temperature variation prices the e-fold escape:** it cannot be
   described as a single fixed-temperature Matsubara ladder transported
   backward through the entire interval.

## 3. THE ADVERSE STATEMENT IS DEMOTED TO A CONDITIONAL CALCULATION

Ruled form: *"If the constant-H₀ de Sitter kernel is naively applied to the
FRW background, the comparison gives a shortfall."* A legitimate conditional
result; **not a GRUT contradiction.** Register annotations implementing this
demotion and the two boundary-condition notes are DRAFTED for the bank gate at
`handover/REGISTER_EDITS_DRAFT_2026-09-25_clock.md` — **not applied**
(`claims.json` untouched; the 25-flag unreviewed backlog is not silently
grown).

## 4. PRIORITY CHANGE — THE TRANSPORT PROBLEM IS NEXT

The owner does **not** proceed to Π₀ now. `background_time_translation_flow`
sits upstream of: whether the Matsubara ladder survives on the evolving
background; how the memory kernel is defined in cosmology; whether the low-ω
K_R calculation has a legitimate stationary limit; and whether the old
cosmological adverse finding has any physical content.

**The chartered question, owner's exact framing:**

> Can GRUT derive a dS→FRW kernel transport rule from already admitted
> principles and inputs?
> If yes, derive it.
> If no, establish precisely what additional structure is required.
> If multiple inequivalent transport rules survive, that nonuniqueness itself
> becomes a structural result.

**Discipline clause (binding):** *"don't let 'this would be useful' become a
reason to assume the transport law exists."*

## 5. FENCES MAINTAINED

Λ_R, Matsubara-inheritance, Π₀, and U5 remain fenced until the transport
instrument's verdict is recorded and the owner selects again. The
prediction/uniqueness tree (`PREDICTION_UNIQUENESS_MAP_01.md`) is not
reopened by this ruling; the transport instrument is inserted upstream of it.
