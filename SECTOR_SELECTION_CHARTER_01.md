# S-1 — THE MISSING ARROW 𝔠 → PHYSICAL SECTOR: CHARTER + PRE-REGISTRATION (frozen before evaluation)

**Date:** 2026-09-25 · **Chartered by:** owner (`P2_S1_OWNER_RULING_01.md`
§4): given 𝔠, what additional structural principle selects the physical
trajectory through it? Candidate classes: **extremization / stability /
information / locality-compatibility**. The Gaussian cone is the FIXED
starting point; P-3 remains the explicit quantization test; criterion
unification is not the question. Register untouched; ledger 0; fenced
routes untouched; the ω⁷ result is consumed only as sector-occupancy
evidence (class-4 gate open), per the ruling.

## 1. THE TESTABLE FORM OF THE QUESTION

The recorded physical sectors occupy specific exponent classes of J(ω):
**s = 3** (matter/acoustic, rung3 record), **s = 5** (graviton loop, the
frozen T3/T4 flat kernel), **s = 7** (two-phonon graviton channel,
adjudicator v3, conditionally derived). The record also names a MECHANISM
per step: s = 5's "+2" is attributed to the two-derivative TT vertex
("contributing ω⁴ in |V|²", benchmark verdict); s = 7's "+4" over the ω³
base is attributed to the tracelessness cancellation (v3 counterfactual
controls: kinetic-only → ω³, potential-only → ω³, full → ω⁷). S-1 tests
whether these occupancies are GENERATED by a principle, at three levels:

- **PC-A (locality/compatibility = power counting):** in an independent,
  exactly solvable golden-rule battery, do exponent increments compose
  ADDITIVELY from structural features — vertex momentum powers, emitted-
  quantum number and dimension, symmetry cancellations? Hypotheses frozen:
  **H1**: +2 per additional momentum power in the vertex amplitude;
  **H2**: an additional emitted quantum shifts s by a d-dependent
  phase-space increment Δ_phase(d), measured (not presumed);
  **H3**: a symmetry-enforced amplitude cancellation of order m adds +2m
  (tracelessness: m = 2 → +4).
- **PC-B (mapping):** does the recorded ladder (3, 5, 7) map onto the
  battery's additive law USING ONLY the recorded mechanism attributions
  (vertex derivative counts; the cancellation controls)? Graded
  CONSISTENT-BY-MECHANISM at best — never "derived from one formula" —
  because the recorded sectors live in different conventions; the
  confrontation is at the increment level (+2, +4), where conventions
  cancel.
- **PC-C (extremization):** over a declared variation family (exponent
  s ∈ [1, 9] at fixed band-integrated weight on a declared ω-band), do
  the recorded s values extremize any of three declared functionals —
  F1 memory duration τ² = ∫K²t²dt/∫K²dt, F2 low-ω weight fraction,
  F3 fluctuation load ∫ν dω at the vacuum floor? **A null is first-class
  and expected to be reported as found**; directional-optimism applies
  hardest to a HIT (the flashy outcome).
- **PC-D (amplitude non-selection control):** within any exponent class,
  the overall coefficient λ spans the cone freely (λJ admissible for all
  λ > 0) — re-deriving the record's no-pin structure at cone level:
  whatever selects, it selects CLASSES, not amplitudes.

## 2. THE BATTERY (frozen; instrument's own declared convention)

Convention: J(ω) ≡ Σ_f |M_f|² δ(ω − ω_f) with mode normalization 1/√(2ω)
per quantum; exponents measured by log-slope over the declared window
ω ∈ [0.02, 0.2] (units of the band top), fit tolerance ±0.05; each case
also derived analytically where closed form exists (agreement gate 1%).

| case | structure | tests |
|---|---|---|
| B1 | 1 quantum, d = 3, vertex q⁰ vs q¹ vs q² | H1 (+2 per power, twice) |
| B2 | 2 quanta, d = 1, minimal vertex vs 1-derivative-per-leg | H1 in the pair channel |
| B3 | 2 quanta, d = 1 vs d = 3, minimal vertex | H2 (measure Δ_phase(d)) |
| B4 | 2 quanta, d = 1, stress-type vertex: kinetic-only / potential-only / full (near-cancelling) | H3 (independent reproduction of the +4 cancellation pattern in a model built fresh here, no v3 code) |

## 3. OUTCOME CLASSES (frozen, mechanical)

- **SELECTION-BY-CLASS:** the battery obeys the additive law (H1 within
  ±0.1 per increment; H3 pattern reproduced) AND PC-B maps the recorded
  ladder by recorded mechanisms AND PC-C returns null AND PC-D holds.
  Meaning: the missing arrow, as far as the tested classes reach, is
  **locality + symmetry power counting selecting the exponent CLASS**,
  with the point in the class (amplitude, state) sector-supplied.
- **SELECTION-BY-EXTREMUM:** any PC-C functional's extremum lands on a
  recorded s (within ±0.25) robustly across the declared band choices
  (three bands, all must agree) — reported with maximal suspicion flags.
- **COMPOSITION-FAILS:** the battery itself violates additivity — the
  counting hypothesis dies; report the violating case.
- **MAPPING-FAILS:** the battery is additive but the recorded ladder does
  not map by recorded mechanisms; the recorded occupancies stay
  unexplained; report the mismatch.
- Mixed outcomes are reported as their components; nothing is averaged.

## 4. CONTROLS AND FENCES

Analytic-vs-quadrature agreement gates (1%) on every closed-form case;
slope-window stability check (window halved → exponent shift < 0.05, else
the measurement is flagged unstable and excluded); B4 must reproduce the
kinetic-only ≈ potential-only ≈ base, full ≫ base pattern or the
cancellation leg halts (it is the one leg confronting adjudicator data —
built independently, no shared code). NO-CAMPAIGN fence: S-1 selects
nothing for the register, opens no sector calculation, and treats ω⁷
strictly as occupancy evidence. Λ_R/Matsubara/Π₀/U5 fenced; class-4 gate
unpassed; Cherenkov inheritance guard carried; the ℏ floor is consumed as
located-not-generated throughout.

## 5. DELIVERABLES AND STOP

1. `calc/sector_selection_s1.py` (pure stdlib; emits
   `SECTOR_SELECTION_S1_RESULT.json`, sha-hashed).
2. `SECTOR_SELECTION_VERDICT_01.md` — the outcome, the increment table,
   the PC-C null-or-hit with suspicion flags, and what the arrow
   𝔠 → sector now is at recorded strength.
3. **HARD STOP at the verdict** (waiting decision: the owner reads what
   kind of principle the arrow is — or is not — and directs P-3, the
   gravity-sector development, or the geometry leg).
