# GRUT Origin Point — the dependency web, and the well-posed origin question

**Date:** June 2026 (2026-06-15)
**Purpose:** step back from the trees to the *terrain* and locate GRUT's genuine origin
point — the minimal input from which the whole structure unfolds. Companion to
`theory/GRUT_TERRAIN.md`. Held to the same rule: an origin idea counts only if it moves a
quantity from *hosted/anchored* to *selected* — never a narrative that explains everything
and selects nothing.

---

## 1. The foundational dependency web (from the actual modules)

The cosmological spine is a short chain (`closure_protocol.py`, `anomaly.py`,
`hubble_from_first_principles.py`, `hubble_tension.py`):

```
  α_vac = 1/3            [AXIOM — a/c=1/3 is a real theorem GIVEN the identification
                          "IR carrier = one real conformal scalar"; mode-count is
                          selected (Target a, §6), the value 1/3 is not]
     │
     ▼
  S = 12π/α² = 108π                         [SELECTED — screening, from α]
  R = √(4/3) = 1.1547                       [SELECTED — Path G refractive index]
     │
  τ_Λ ≡ 1/H₀   ("input, not prediction")    [INPUT — one dimensionful scale]
     │
     ▼
  τ₀ = τ_Λ / S = 1/(108π·H₀)                [ANCHORED — = (1/S) × the one scale]
     │
     ▼
  H_inf = (2−R)/(S·τ₀) = (2−R)·H₀           [SELECTED ratio: H_inf/H₀ = 2−R]
  Ω_Λ = (H_inf/H₀)² = (2−R)² ≈ 0.714        [SELECTED — dimensionless, no H₀ needed;
                                             within a few % of Planck 0.689]
```

The decisive observation, made explicit:

> **Every *dimensionless* quantity in GRUT's spine is SELECTED** — `α=1/3`, `S=108π`,
> `R=√(4/3)`, the ratio `H_inf/H₀ = 2−R`, `Ω_Λ = (2−R)²`. **Exactly one *dimensionful*
> quantity is INPUT** — the scale `τ_Λ = 1/H₀` (the Hubble time = the S⁴ radius). And
> `τ₀` is "anchored" for one reason only: it is `(selected dimensionless 1/S) × (the one
> input scale)`. It carries the dimension.

---

## 2. The honest result: "select τ₀" is mis-posed

`τ₀` cannot be selected from dimensionless first principles, because **a theory of pure
numbers (topology, CTP combinatorics on S⁴) cannot produce a dimensionful scale.** S⁴
gives `108π`, `1/3`, `√(4/3)` — all dimensionless. One dimensionful anchor is
mathematically required to set "how big is the universe / how slow is the relaxation."

This is **not a defect** — it is universal. Every physical theory needs exactly one scale
input (the SM: the Planck mass, or `Λ_QCD` via dimensional transmutation; GR: `G`). So the
τ₀ worry dissolves: τ₀ is the scale-carrier, and no theory escapes setting one scale.

> **CORRECTED (after the Target-(a) attack, §6):** the first pass of this section claimed
> "zero dimensionless free parameters + one scale." That **over-claimed** — it assumed
> `α=1/3` was selected. It is not: `α=1/3` is a dimensionless **AXIOM** (GRUT's own
> `ALPHA_VAC_PROVENANCE.md` calls it "de facto an axiom"). The honest foundational claim is:
> **one dimensionless axiom (`α = 1/3`) + one scale (`τ_Λ = 1/H₀`).**

That is still a strong position — GRUT selects an extraordinary amount from one dimensionless
number plus one scale — but it is *not* parameter-free at the dimensionless level. Closing
the α axiom (§6) is what would make it so.

---

## 3. The well-posed origin questions (what the origin *can* select)

Reframing "select τ₀" into questions that are dimensionless and therefore in-scope for an
S⁴/CTP origin:

### (a) TRACTABLE — select `N = 3` (≡ `d`, the impedance dimension → `α = 1/d = 1/3`)
`α_vac = 1/3` currently rests on Duff a/c for **one** conformally-coupled scalar. *Why one?
Why `d = 3`?* `N` (generations) `= d` (impedance dimension) is **dimensionless** — the origin
*can* select it. If the S⁴ conformal-instability + CTP structure forces `d = 3` (one real
conformal mode in the relevant decomposition), then `α = 1/3` becomes fully origin-selected,
and the `A² = N−1` flavor pattern acquires its `N`. This is the most reachable origin target.

### (b) DEEP FRONTIER — the scale *hierarchy* (the only legitimate "scale" question)
The absolute scale is input, but its *ratio* to a microscopic scale is dimensionless and
hence selectable. The open ratios:
- `τ₀ / τ_micro ≈ 10³⁴` (the two-τ-scale gap, already flagged open);
- `H₀ / M_Planck ≈ 10⁻⁶¹` (the cosmological-constant / hierarchy problem).
Can GRUT's S⁴/CTP/**regularization (−1/12)** structure produce one of these ~enormous
dimensionless ratios? This is literally the Λ-problem, and it is where the −1/12 foundation
(finite vacuum response) should bear. Selecting a hierarchy ratio would move the *last* scale
from anchored to selected — the deepest possible closure. Hard; the right kind of hard.

### (c) THE FORWARD-BUILD SPINE (the cosmogony, done honestly)
GRUT already *has* an origin narrative made of derived pieces — it does **not** need a new
pre-big-bang story:
> Euclidean **S⁴** → conformal mode unstable (Gibbons–Hawking negative mode, the `−100` in
> `C_Cosmo`) → drives expansion → constitutive memory damps it → terminal velocity
> `H_inf = (2−R)/(S τ₀)` → de Sitter-like late universe.
The forward-build program ("build the universe from the origin") is to run this chain
**outward** and check, sector by sector, what each epoch *selects* vs *hosts* — the same
discipline, applied along the cosmic timeline. This is the spine for the "reality computer"
vision: a GRUT-RAI reality engine needs the dimensionless laws (selected) + one scale (input)
+ the forward chain (the cosmogony) — which is exactly this structure.

---

## 4. Success criterion (per the terrain rule)

The origin program succeeds only by moving a quantity **up** the ladder:

| Target | Current | Origin would make it |
|---|---|---|
| `N = 3` / `d = 3` (→ α) | hosted (one-scalar assumption) | **SELECTED** (S⁴/CTP forces d=3) |
| hierarchy ratio (cosmic/micro) | anchored (one scale input) | **SELECTED** (−1/12-regularized ratio) |
| the cosmogony chain | partly selected | a falsifiable forward-build |

The absolute scale `τ₀` itself stays input — and that is the *correct, honest* end state, not
a failure. Naming it so is itself progress: it converts an apparent gap ("τ₀ unexplained")
into a precise statement ("one scale input, like every theory").

---

## 5. Recommended next concrete step

Start with **(a)** — the `d = 3` selection — because it is dimensionless, tractable, and
load-bearing (it underwrites `α = 1/3`, hence `S`, `R`, the whole spine, *and* the flavor
`A² = N−1` pattern). Concretely: examine whether the S⁴ Weyl/conformal decomposition + the
CTP doubling forces exactly **one real conformal mode** (d = 3 in the a/c sense), or whether
"one scalar" is an assumption. If forced → α is origin-selected and N=3 is explained; if
assumed → we have located the next honest gap precisely, the same way Koide just was.

**What GRUT is, at the terrain level (corrected):** a **one-dimensionless-axiom** (`α=1/3`)
framework with one scale input, a derived cosmogony spine (S⁴ instability → terminal
velocity), a strong SELECTED column resting on the α axiom + one scale, and two origin
targets — cracking the α-value (§6) and the hierarchy ratio (the deep, −1/12 frontier).

---

## 6. Target (a) attacked — RESULT (June 2026): partial success

We attacked "does S⁴ topology + CTP doubling force exactly one real conformal mode (→ α=1/3)?"
GRUT's actual basis is `grut/foundation/conformal_mode_scalar.py`: `α_vac = a/c` of the "IR
carrier," **identified** with one real conformally-coupled scalar, for which `a/c = 1/3` is a
genuine theorem. So `α=1/3` = (real theorem) × (the identification). Splitting it:

**SELECTED (the win):** the **mode count is one.** On S⁴ the metric's conformal/trace sector
is exactly one real scalar `σ(x)`; CTP doubling `(σ₊,σ₋)→(σ_c,σ_q)` is density-matrix branch
bookkeeping — it adds no physical conformal mode and leaves the *ratio* a/c invariant. So
"exactly one real conformal mode" is **forced** by S⁴ + CTP. The "one" in "one conformal
scalar" is no longer an assumption.

**NOT SELECTED (the residual axiom):** the **value 1/3.** "One mode" does not give 1/3 by
itself. It requires two un-forced identifications:
  (i) that the conformal mode is a **2nd-order conformally-coupled scalar** (a/c=1/3) — but
      its actual gravitational dynamics is **4th-order Riegert/Paneitz**, whose a/c is not the
      2nd-order value; and
  (ii) that the impedance `α_vac` *equals* the trace-anomaly ratio `a/c` (posited, not derived).
The competing "α = 1/d, d=3" story is independently dead: `ALPHA_VAC_PROVENANCE.md` finds "no
mathematical bridge" from its KK sum (§H.3) to `α=1/d` (§H.4).

**Verdict:** Target (a) is a **partial success** — it *selects the mode count* and *precisely
localizes the residual axiom* (the two identifications turning one mode into a/c=1/3). `α=1/3`
stays a dimensionless axiom, consistent with GRUT's own provenance audit. This is a real
advance: it converts "α is an opaque axiom" into "α = one selected count + two named
identifications," and tells us exactly where the next derivation must bite — the **4th-order
Riegert a/c of the gravitational conformal mode**, and **why impedance = a/c**.

**Next, if pursued:** compute the actual a/c (or effective impedance) of the *4th-order* S⁴
conformal mode (the Riegert/Paneitz sector), and test whether it equals 1/3 or something else.
If 1/3 → α is selected (axiom closed). If not → the conformal-scalar identification is
falsified and α's value is genuinely free, a major honest negative. Either is decisive.

### RAN it (June 2026) — outcome allowed

Executed the decidable computation (`grut/foundation/conformal_mode_scalar.py`):

- **a/c = 1/3 for a single real 2nd-order conformally-coupled scalar is CONFIRMED and REAL.**
  The (a, c) coefficients GRUT uses are the genuine textbook central charges (in 1/360 units:
  scalar (1,3), Weyl fermion (11/2,9), vector (62,36)). The ratio is **distinctive**: scalar
  `1/3`, fermion `11/18`, vector `31/18`. So 1/3 is *not* a fudge — it is the unique signature
  of one real conformal scalar.
- **But closure FAILS at the named postulate.** GRUT's own module states it explicitly:
  *"Postulate: the gravitational conformal mode is the IR carrier… Under this identification,
  α_vac IS the trace-anomaly ratio a/c."* The conformal mode is 4th-order (Riegert/Paneitz);
  `a/c=1/3` is the **2nd-order** scalar's value. So `α=1/3` = (real 2nd-order theorem) ×
  (un-derived identification that the 4th-order mode's impedance equals the 2nd-order a/c).
- **Meta-finding:** GRUT's foundational *code was already honest* — it names the postulate as
  "the conditional that makes this a derivation." The over-claim ("α selected") was in the
  summary docs, now corrected.

**Verdict:** the α-axiom does **NOT** close. Outcome allowed: the value 1/3 is real and
distinctive (partial vindication), the gap is a single precisely-named postulate (IR carrier =
one 2nd-order conformal scalar), and the one decisive computation that would settle it — the
4th-order S⁴ Paneitz/Riegert IR impedance — is a curved-space-QFT calculation we will **not
fabricate**. It is named and left open. `α=1/3` stands as GRUT's one dimensionless axiom.
