# The rung-3 spectral-measure specification — what object, which channels, which chain

> **STATUS: NOTHING BANKED. NOT A CLAIM. NOT A RESULT.** A specification for the next
> calculation, written default-BROKEN per CHARTER §1.4. Commissioned by the owner 2026-08-21
> after `calc/worldline_reduction.py` produced a concrete adverse prediction at proxy scope.
> Before execution this file needs its own pre-registration decisions; before any result banks,
> adversarial pre-screen + overseer relay. No ledger net figure is typed here.

## 0. The question, narrowed by everything that came before

Not "can we justify J ∼ ω³", and not "find an exponent that gives the desired memory":

> **What is the spectral measure of the actual object whose response enters rung 3 —
> computed per channel — and does anything resembling the registered memory behavior
> emerge when the chain ρ(ω) → K_R(t) → late-time asymptotic → τ_eff is derived rather
> than assumed?**

The theory follows the kernel. No τ₀ target is stated anywhere downstream of this line.

## 1. Object identification (do this BEFORE computing anything)

Every candidate object gets a class label (A worldline / B′ homogeneous susceptibility /
B quasi-static / C full spacetime self-energy), per `RUNG3_BRIDGE_SCOPE.md` §2:

| candidate | class | status |
|---|---|---|
| free conformal-scalar geodesic kernel | A (reduced) | computed 2026-08-21 (`worldline_reduction.py`); PROXY ONLY |
| free TT-graviton geodesic two-point function | A, gravitational | **uncomputed — the nearest legitimate target** |
| assembled G_R^TT = 2 Im G_R^TT from Σ(x;x′) | C | uncomputed; walls (A)–(C) stand; dispatch territory |
| χ(ω) susceptibilities (rung4/rung7) | B′ | stationarity imported; not the keystone |
| ω→0 projector bookkeeping (mu_linear/ISW) | B | no spectral content needed |

**The next calculation's primary object is row 2**: the free TT-graviton geodesic two-point
function, reduced per D3a's licensed scope. It is class-A (computable in-house), genuinely
gravitational (TT-projected), and it is the minimal object that can confirm or refute whether
the proxy floor is a scalar artifact or survives the graviton channel.

## 2. Channel tracking (mandatory)

The reduction must be executed **separately for P^(2) and P^(0,s)** projections, never on a
scalar surrogate offered as a TT stand-in:

- transversality ≠ tracelessness (the register's own banked interrogation result): the two
  channels can carry different low-frequency structure;
- if the floor (or any memory-bearing structure) appears in only one channel, that is a
  first-class finding — it feeds directly into the u4/u5 constitutive-classification questions;
- the p_tt_ansatz fence applies: a TT-only presentation of results is a choice and must be
  labelled as one.

## 3. The kill-condition firewall (from the [R_wl, R_IR] ≠ 0 exhibit)

Any reduction executed under this spec must state, on its face:
1. the order of (IR regulation, spatial/worldline restriction, point-particle limiting);
2. whether the operations commute on the executed integral family (test exhibited, not
   asserted — the closed-form non-commutation of the screen is the template);
3. physical justification for the chosen order **selected independently of the desired memory
   behavior** — an ordering chosen because it preserves or destroys the mechanism is laundering.

## 4. The chain reconstruction (replaces the τ₀-first habit)

Once the per-channel spectral measure ρ_channel(ω) exists:
1. K_R,channel(t) = ∫ dω/(2π) e^{−iωt} ρ_channel(ω) — derive, do not parametrize;
2. late-time asymptotic of K_R(t): power law? exponential? constant tail? — classify;
3. τ_eff extracted from THAT asymptotic, with its clock named (keystone map §1);
4. only then: comparison against the registered single-pole/Markovian structure — accepting
   whatever dynamics results, including "no finite τ" and "no comparison possible".

Pre-registered outcome classes (framing only; instruments are owner-signed):
(a) reduction carries required memory behavior → real opening;
(b) reduction carries different memory structure → physics modification problem, priced;
(c) reduction loses the IR structure → worldline sector cannot establish cosmological memory;
(d) no consistent low-frequency memory structure → rung-3 mechanism fails as registered;
(e) answer depends on a new constitutive/microscopic input → price and relocate it explicitly,
    no hiding inside J(ω).

## 5. Guards

1. Nothing here banks; the proxy result remains class-A-scoped and relay-gated.
2. Forbidden phrasings per the owner's red list (screen record, firewalls §Firewalls).
3. The E6 fence rides on every use of the free ladder; the D3a license rides on every use of
   the words "worldline" and "cosmic time" together.
4. Class labels (A/B′/B/C) are mandatory in every kernel sentence from now on.
